# Evidence: T-04 - 迁移 ServerHost 并建立健康与能力接口

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/04-server-host-and-health.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、backend `main`
- **Base/implementation/candidate/result SHA：** root parent-before `9205b8b136c382aa82a3e5bf3c5bba0ae76ba487`；backend `7c1c4aa7510b064b94a61c5c1e0ad69b70c7880b` -> `c5d7905c7e8fafb735be3cb588072c4d5d419ddc`；candidate `not-applicable`；root result `9a5f4b1221a1f778bd02b3bd9040d7cbdb155f1e`
- **状态：** done

## 1. 实现摘要

- 新建生产 `cmd/go-admin` Cobra 入口，保留 `server -c/-a` 及 `migrate/version/config/app` 命令，新增 `auto/server/legacy` profile、文件根与 shutdown timeout；Makefile 默认和 SQLite build 均切到新入口，旧 `cmd/api` 继续作为兼容 shim。
- ServerHost 单独拥有 signal、TCP listener、直接 TLS、HTTP Serve/Shutdown、Application 生命周期与 profile 资源；迁移和依赖构建在 listener 前完成，bind/start/AfterStart 失败 fail closed，超时后 Host 返回且不合作的底层清理继续执行。
- 新增 `/health/live`、`/health/ready`、`/api/v1/runtime/capabilities`：live 不依赖外部资源，ready 同时检查 Application 和数据库/缓存，能力响应只有 host/version/布尔能力字段。
- Server profile 复用 T-03 PostgreSQL/Redis/FileStore/tenant adapters 并启动前迁移；legacy profile 保持现有 SQLite 等配置行为。`-a` 清单同步在 Module ready 后、HTTP 发布前执行。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`
- **Dispatch Packet/checkpoint：** Lead direct；root base `09b9bf5`，T-04 activation/parent-before `9205b8b`，backend base `7c1c4aa`
- **允许动作：** current workspace T-04 writable/shared paths、逐 Ticket 本地 commit 和 direct-parent checkpoint；无 worktree、push、PR、deploy、release 或生产迁移
- **返回：** backend `c5d7905`、root `9a5f4b1`；backend clean；14 个实现文件均位于 T-04 writable/shared paths
- **Lead 独立核对：** pass；重读固定 diff、旧 `cmd/api` 启动顺序、路径清单、真实进程日志、全量命令退出码与 root gitlink
- **只读 Agent findings：** 无；未派遣 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-plus/cmd/go-admin/**</Path>` | writable | 新生产 CLI、profile 装配、兼容 flags 与进程 E2E |
| `<Path>go-admin-plus/internal/host/server/**</Path>` | writable | listener/signal/TLS/shutdown/AfterStart 生命周期 |
| `<Path>go-admin-plus/internal/application/health/**</Path>` | writable | live/ready/capabilities 公共 HTTP 合同 |
| `<Path>go-admin-plus/Makefile</Path>` | shared:T-04 | 默认与 SQLite 构建指向新入口 |

- **read-only 修改：** 无；`cmd/api/**`、`internal/platform/**`、`test/characterization/**` 固定 diff 为零
- **未声明路径：** 无
- **生成文件/锁文件：** 无；临时 E2E 数据库、跨平台和本机构建产物已移入 macOS 废纸篓，测试服务与端口均已停止

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 / Host-neutral Application | ServerHost lifecycle、AfterStart、停止顺序 | Application ready 后发布，signal/timeout/失败均有界停止 | pass |
| AC-002 / API 与 CLI 兼容 | characterization、SQLite 真实进程、旧命令/flags | `/info` 与既有 `/api/v1` 回归绿色，`-c/-a` 及子命令保留 | pass |
| AC-013 / 运维语义与不泄密 | PostgreSQL/Redis 真实进程、HTTP tests | ready 正常 200；Redis 下线 503；live 保持 200；capabilities 无 DSN/path/token | pass |
| fail-closed / listener ownership | bind、未迁移 SQLite、启动 callback tests | 失败非零，不发布 Started，不遗留监听端口，资源只关闭一次 | pass |
| TLS / shutdown timeout | TLS pair tests、非合作 Close/race | TLS enable 缺文件启动前拒绝；deadline 返回后清理继续 | pass |
| 路径/result 一致 | Git 与 SpecDev 审计 | backend `c5d7905`、root `9a5f4b1`，无越界路径 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `go test ./... -count=1` | backend current-workspace / macOS arm64 | pass | 全量包、进程测试与 characterization 通过 |
| `go test -race ./internal/application ./internal/application/health ./internal/host/server ./cmd/go-admin -count=1` | backend current-workspace / macOS arm64 | pass | Application/Host/health/CLI 无 race |
| `go vet ./...`、`git diff --check`、scope audit | backend fixed diff | pass | 静态检查、whitespace 与 writable/shared path 合同通过 |
| `make build`、`make build-sqlite` | backend current-workspace / macOS arm64 | pass | 新入口生成 Mach-O arm64；SQLite CGO build 通过，仅第三方 `go-m1cpu` clang warning |
| `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build ./cmd/go-admin` | backend current-workspace | pass | 静态 ELF x86-64 |
| `GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build ./cmd/go-admin` | backend current-workspace | pass | PE32+ x86-64 |
| SQLite 真实 CLI/HTTP/signal E2E | 临时 SQLite，真实子进程 | pass | migrate、live/ready/capabilities、旧 `/info`、SIGINT 退出和端口释放 |
| 未迁移 SQLite 启动 E2E | 临时空数据库，真实子进程 | pass | 启动非零且未遗留 listener |
| Server profile E2E | PostgreSQL 17.11 + Redis 8.10，真实 ServerHost | pass | 自动迁移、旧 API、三运维接口、Redis 降级、SIGINT 与端口释放 |

- **失败后修复与重跑：** Redis 降级关闭暴露底层 Close 不响应 context；Host 改为 deadline 返回且后台继续清理，并以 race/high-count test 重跑。提交前审查又修正 `-a` 早于 Module 启动和 TLS enable 空文件两项兼容缺口，随后全量、race、vet 与交叉编译全部重跑通过。
- **未运行检查：** Windows/Linux 原生运行时和容器部署；本 Ticket 验证可构建性，Compose 与原生发行分别属于 T-10/T-12。
- **E2E：** required；Lead 在 current workspace 使用真实 PostgreSQL/Redis Server profile 和 SQLite legacy profile 执行，passed。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `7c1c4aa7510b064b94a61c5c1e0ad69b70c7880b..c5d7905c7e8fafb735be3cb588072c4d5d419ddc`
- **结果：** pass
- **Findings 与修正：** 修正不合作 Runtime Close 的 deadline、`-a` startup 顺序和 TLS enable 空配置；最终无 hard violation。

### 规范轴

- **固定输入与来源：** 同上固定 diff；Spec、T-04、Tickets Map、Goal Plan G3 与 AC-001/002/013
- **结果：** pass
- **Findings 与修正：** ServerHost ownership、ready 时序、依赖降级、旧 API/CLI、direct TLS、secret/path redaction 与 writable/shared ownership 均满足；Compose/desktop 保持范围外。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `9205b8b136c382aa82a3e5bf3c5bba0ae76ba487` |
| Implementation/source SHA | backend `c5d7905c7e8fafb735be3cb588072c4d5d419ddc` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | 全量 Go、race、vet、Makefile、Linux/Windows AMD64 build、SQLite/PostgreSQL/Redis process contracts passed |
| E2E disposition | required：真实 ServerHost、网络、依赖降级与 signal 边界 |
| E2E result | passed：PostgreSQL 17.11/Redis 8.10 reference profile 与 SQLite legacy profile |
| Parent result/re-read | result `9a5f4b1221a1f778bd02b3bd9040d7cbdb155f1e`；父 HEAD 包含 result，backend gitlink 为 `c5d7905` |

## 8. 偏差与决策

- **偏差：** 无；旧 `cmd/api` shim 保留到 T-13，Docker/desktop 未提前切换
- **记录：** 不适用
- **批准来源及影响：** 用户授权 current workspace 逐 Ticket 本地 commit；此前 T-03 push 授权不扩展到 T-04

## 9. 残余风险与交付定位

- **残余风险/已知限制：** legacy profile 继续承接非 PostgreSQL/Redis 配置直到 T-13；容器入口和原生 Windows 运行时尚未切换；不合作的资源清理在 Host deadline 后只能后台 best-effort 继续。
- **后续 Ticket：** T-06（尚未激活）
- **监控或回滚触发：** bind/start failure、ready 503、dependency close deadline、TLS 文件错误和 signal exit；可回滚 root `9a5f4b1` 与 backend `c5d7905`
- **Source commit：** backend `c5d7905c7e8fafb735be3cb588072c4d5d419ddc`
- **Parent result：** `9a5f4b1221a1f778bd02b3bd9040d7cbdb155f1e`
- **Source workspace：** current root/backend `main`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-04.md</Path>`
