# Evidence: T-07 - 抽离 App Core、Domain 并迁移稳定路由键

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/07-app-core-domains-and-routing.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、backend、frontend `main`
- **Base/implementation/candidate/result SHA：** root parent-before `7239a2b360cffb458467645759a73f13656c46a9`；backend `f11b8443b35c380c239718f97a0989baffd05d02` -> `8b3fc9279a0e3b9744f63e46bf5769e8307ca926`；frontend `4ea414d2c2750edae8ff6666897bc17e9a5c987a` -> `9922c08042d670c6bee60598b926a2f6354a5f7a` -> `8fe488e3c83206689a0c5888477e8f1b08b9d555`；candidate `not-applicable`；root checkpoint `b9c48432e5f4c793825d1063e2d070e87e394b1c`；root result `ea79b44adb2886699ed3ed63d2c5f44554e91165`
- **状态：** done

## 1. 实现摘要

- 前端建立 App Core 的 Domain registry、route resolver、Host ports、ApiClient bridge 和稳定不可用页；Admin Shell 只组合 system/jobs/demo/tools/monitor 五个公开 Domain barrel。
- 现有业务页面、API、composable 和共享 UI 从 App 物理迁入 `domains/**` 与 `packages/ui/**`，旧路径仅保留窄兼容 facade。Domain 可分别 type-check，自动扫描禁止 App 反向依赖、跨 Domain 深层 import、UI 业务 API 和 App 内残留业务页面。
- 菜单优先按显式 routeKey 白名单解析，仅在 routeKey 缺失时精确兼容旧 component；未知 routeKey fail closed，不再落入任意动态 import。20 个业务路由用真实 Chromium 验证 routeKey 优先、legacy fallback 和 unknown unavailable。
- 后端菜单模型与 append-only migration 新增稳定 routeKey，历史 component 保留；菜单树手工复制接缝同步携带 RouteKey，并验证三层树和更新 DTO 兼容。
- mocked E2E 公共 response helper 补齐 T-06 已锁定的 canonical `msg/data` envelope；测试用例、断言和业务 fixtures 未修改，生产 parser 未放宽。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`；未派遣 subagent
- **执行策略：** current workspace、implementation limit 1、direct-parent；无 source/candidate worktree
- **允许动作：** T-07 writable/shared paths、backend/frontend 本地 commit、root checkpoint/final result；无 push、PR、deploy、release 或生产迁移
- **返回：** backend `8b3fc92`；frontend 架构 `9922c08`、mock envelope `8fe488e`；root checkpoint `b9c4843`、最终 result `ea79b44`
- **Lead 独立核对：** 固定 diff、子仓库 gitlink、routeKey 迁移/树序列化、Domain 边界、136 mocked E2E、20 路由矩阵和 result 包含关系均通过

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-ui-plus/packages/app-core/**</Path>` | writable | Domain/route/host/client 基础合同与单测 |
| `<Path>go-admin-ui-plus/packages/ui/**</Path>` | writable | App 无关共享 UI/composable/utils |
| `<Path>go-admin-ui-plus/domains/**</Path>` | writable | 五个业务 Domain、页面/API/barrel/type-check |
| `<Path>go-admin-ui-plus/apps/admin/src/**</Path>` | writable | Shell 装配、Host ports、兼容 facades |
| `<Path>go-admin-ui-plus/tests/e2e/mocked/support/crud.ts</Path>` | writable | canonical mocked response envelope |
| `<Path>go-admin-plus/app/admin/models/sys_menu.go</Path>` | writable | 菜单 RouteKey 字段 |
| `<Path>go-admin-plus/app/admin/service/sys_menu.go</Path>` 与测试 | writable | 菜单树 RouteKey 保真与回归 |
| `<Path>go-admin-plus/cmd/migrate/migration/version/**</Path>` | shared:T-07 | append-only routeKey schema/data migration |

- **read-only 修改：** 无；generated contracts、ApiClient、OpenAPI、unit/E2E specs/assertions 和业务 fixtures 保持不变
- **未声明路径/manifest/lockfile：** 无；根 manifests 与 `pnpm-lock.yaml` 未变
- **局部路径修正：** 实现审查发现菜单树手工复制和严格 parser 所需 canonical mocked helper，均先同步进 T-07 路径合同；不改变 Spec、产品行为或测试断言

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-002 / API 与菜单兼容 | Go migration/service tests、全量 Go | routeKey expand，component 保留，三层菜单树不丢字段 | pass |
| AC-003 / Admin 行为等价 | 220 unit + 136 mocked E2E + production build | 登录、菜单、权限和全部既有 CRUD 回归绿色 | pass |
| AC-006 / 稳定安全路由 | App Core unit + 20-route Chromium matrix | routeKey 优先、legacy 缺失回退、unknown fail closed | pass |
| AC-018 / 单一前端基础合同 | boundary scan + package type-check | Domain/UI/App 方向和公开 barrel 可自动证明 | pass |
| 路径/result 一致 | Git 与 SpecDev 审计 | backend/frontend/root SHA 与 writable/shared ownership 一致 | pass |

## 5. Workspace Verification

- **验证环境：** `current-workspace` / macOS ARM64 / Node 22 / Chromium / Go toolchain

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| backend targeted migration/service tests、`go test -race` | pass | 旧最小 schema、已知/未知 route、显式 key、幂等、三层菜单树和 DTO 兼容通过 |
| backend `go test ./... -count=1`、`go vet ./...` | pass | 全量 Go 与 vet 通过 |
| frontend root Vitest、App Core Vitest | pass | Admin 27 files / 220 tests；App Core 2 files / 11 tests |
| Admin/root/UI/App Core/五个 Domain type-check | pass | system/jobs/demo/tools/monitor 可独立类型检查 |
| ESLint、dependency/boundary scan、`git diff --check` | pass | 0 error、29 个既有 warning；无非法依赖或 App 业务页面残留 |
| `CI=true pnpm install --frozen-lockfile` | pass | frozen install 通过，lockfile 无变化 |
| production build | pass | 3100 modules；仅既有 CSS pseudo-element warning |
| `verify-domain-navigation.mjs` | pass | Chromium 中 20/20 routeKey 页面通过；legacy fallback 与 unknown unavailable 通过 |
| Playwright `--project=mocked` | pass | 15 files / 136 tests；`.last-run.json` 为 `passed` 且 failedTests 为空 |

- **失败后修复与重跑：** 首次全量 mocked E2E 在 2 passed 后出现 3 failed/1 interrupted；原因是旧 CRUD mock helper 的成功 envelope 缺少 T-06 canonical `msg`，部分写操作还缺 `data`。生产严格 parser 与测试断言保持不变，只修公共 mock response helper；analytics 子集 6/6 后，全量 136/136 重跑通过。
- **未运行检查：** 无 T-07 计划内检查缺失；原生 Desktop E2E 属于 T-08/T-09。
- **E2E：** required，Lead 在 current workspace 执行，passed。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `f11b844..8b3fc92`；frontend `4ea414d..8fe488e`；root `7239a2b..ea79b44`
- **结果：** pass
- **Findings 与修正：** 修正菜单树手工复制遗漏 RouteKey、query-string bridge、隐式 App layout/store 依赖和 mocked canonical envelope；最终无 hard violation。

### 规范轴

- **来源：** Spec、T-07、Tickets Map、Goal Plan G5 与 AC-002/003/006/018
- **结果：** pass
- **Findings 与修正：** routeKey expand-migrate-contract、Domain public barrel、Host-independent UI、兼容字段保留、路径 ownership 和 required E2E 均满足；无公共合同偏差。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `7239a2b360cffb458467645759a73f13656c46a9` |
| Implementation/source SHA | backend `8b3fc9279a0e3b9744f63e46bf5769e8307ca926`；frontend `9922c08042d670c6bee60598b926a2f6354a5f7a` + `8fe488e3c83206689a0c5888477e8f1b08b9d555`；root result `ea79b44adb2886699ed3ed63d2c5f44554e91165` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | 全量 Go/vet、前端 unit/type/lint/frozen/build/boundary、20-route browser 与 136 mocked E2E passed |
| Parent result/re-read | 父 HEAD 包含 `ea79b44`，backend/frontend gitlinks 分别为 `8b3fc92`/`8fe488e` |

## 8. 偏差与决策

- **Spec/产品偏差：** 无
- **Ticket 局部合同修正：** 增加菜单树 service 接缝与唯一 mocked response helper；均为实现发现且已在 checkpoint/result 前投影到 Ticket
- **批准来源及影响：** 用户授权 current workspace 逐 Ticket本地 commit；此前 push 授权不扩展到 T-07

## 9. 残余风险与交付定位

- **残余风险/已知限制：** component fallback 与旧 App facade 按 expand-migrate-contract 保留到 T-13 零调用收缩；unknown routeKey 会稳定失败并记录诊断。T-06 记录的 jobs start/remove 既有未认证风险不在本 Ticket 改变。
- **后续 Ticket：** T-08（尚未激活）
- **监控或回滚触发：** unknown/duplicate routeKey、boundary scan、页面导航和 envelope protocol error；可回滚 root `ea79b44`、backend `8b3fc92`、frontend `8fe488e`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-07.md</Path>`

## 10. T-08 Gate 期间的覆盖纠正

- T-08 的远端前端 Verify（frontend `e40e884`，run `32836695681`）暴露出 `check-api-contract.mjs` 仍只扫描迁移前的 `apps/admin/src/views`，且三条 Options-API 白名单仍指向旧路径。该 run 的 lint、type-check、unit 与独立 E2E job 均通过，只有 API contract step 因旧路径失败。
- 深入核对确认 T-07 Evidence 中“API contract 全覆盖”的结论过强：迁入 `domains/*/src/pages` 的 `useTable` 页面没有被 checker 扫描。本节取代此前关于该 checker 覆盖完整性的表述，但不改变 T-07 已验证的路由、Domain 边界和产品行为结论。
- frontend `3bfc10f6d3c1d247f1b31b88220f648ac522d6a2` 将扫描根扩展到 Admin views 与所有 Domain pages，并把三条 Options-API 路径改为实际 Domain 路径；root `27e7f942c117737245c1e3f70fd1a74b971f25f4` 记录该后续纠正及 frontend gitlink。
- 纠正后本地 `pnpm test:ci` 通过：lint 0 error（25 个既有 warning）、type-check、28 files / 225 unit tests 与 API contract 全部通过；checker 实际覆盖 8 fixtures、15 migrated pages、3 Options-API pages 和 `types/admin.ts` declarations。独立 `rg` 枚举同样得到 15 个 `useTable` 页面。
- 原 T-07 result `ea79b44adb2886699ed3ed63d2c5f44554e91165` 仍是该 Ticket 的原始结果；本次为 Gate 回归中发现的测试覆盖缺陷修复，不涉及生产行为。当前父分支同时包含原 result 与纠正提交，纠正提交尚未 push。
