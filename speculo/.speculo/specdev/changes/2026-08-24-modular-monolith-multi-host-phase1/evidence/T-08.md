# Evidence T-08: Wails DesktopHost 垂直曳光弹

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/08-desktop-host-tracer.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / `main`
- **Base/source/candidate/result SHA：** root `daf714718d78a96baf891553a0115b969da60621` / `2cd0139304854ec2938e7d7447bc3e2153bd1aeb` / `not-applicable` / `2cd0139304854ec2938e7d7447bc3e2153bd1aeb`
- **状态：** `done` / `integrated`

## 1. 结论

- macOS ARM64 与 Windows AMD64 原生 Wails tracer 均完成嵌入 Admin 启动、bootstrap、登录、`getinfo` 和 demo product create/list/update/delete，并输出唯一 `GO_ADMIN_DESKTOP_E2E_PASS`。
- Windows 成功证据为 GitHub Actions run `32842185071`：backend `a51e7ddd76f871ce1539e11213d26f487437a9a0`，frontend `3bfc10f6d3c1d247f1b31b88220f648ac522d6a2`，Windows Server 2025 AMD64，Wails 2.15.0，WebView2 `151.0.4129.86`。
- 业务 CRUD 继续使用同一 `/api/v1` HTTP contract，无 Wails 业务 binding、CDN 或公网运行时依赖。T-08 开发 tracer Gate 关闭。
- Windows 10/11 正式安装、签名和 WebView2 缺失矩阵仍属于 T-12；本 Ticket 的 Server 2025 tracer 不替代发行 Gate。

### 实现摘要

- 新增 Wails 2 DesktopHost，嵌入同一 Admin dist，并在同进程通过 `127.0.0.1:0` 启动同一 Application/Gin API。
- bootstrap binding 只返回 loopback base URL、随机 launch token、版本与 capabilities；Desktop gateway 校验精确 Wails Origin 和 launch-token header。
- DesktopRuntime 使用原生 fetch，Web Runtime/Axios 保持不变；SQLite、memory Cache/Queue、local FileStore 和 fixed tenant 由 Desktop profile 装配。
- 真实 tracer 驱动修复了 seed cwd 依赖、认证 envelope、CORS、异步 `gin.Context`、WebKit custom-scheme transport及 Windows drive-letter SQLite URI。
- 手动 Windows workflow 固定 backend dispatch SHA 和显式 40 位 frontend SHA，以 `sqlite3,desktop_e2e` 原生构建；超时、非零退出、FAIL marker 或唯一 PASS marker 缺失均 fail closed，并始终上传 provenance/stdout/stderr。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`；未派遣 subagent。
- **策略：** current workspace、implementation limit 1、direct-parent；无 source/candidate worktree。
- **允许动作：** 用户授权逐 Ticket 本地 commit，并在 2026-08-25 显式授权三仓已审计提交 push 与 Windows Desktop Tracer dispatch；授权已消费至 backend `a51e7dd`、frontend `3bfc10f`、root `2cd0139` 和 run `32842185071`。
- **返回与核对：** Lead 重读 backend/frontend/root 提交、gitlink、路径 diff、本地测试和三次远程 run；成功 run 的 checkout SHA、环境、build、WebView2、HTTP CRUD 与 marker 一致。

## 3. 修改范围与路径所有权

| 项目 | SHA | 状态 |
|---|---|---|
| backend DesktopHost implementation | `e42f9a2bb62ba6a2bad2ea09b9d7de5eebf618f9` | committed、pushed |
| frontend DesktopRuntime implementation | `e40e88491bf026205aed70ada26d4eb4c574adeb` | committed、pushed |
| frontend API checker correction | `3bfc10f6d3c1d247f1b31b88220f648ac522d6a2` | committed、pushed；归属 T-07 correction |
| Windows workflow candidate | `dba1d945136c3d33a2df88c369f73055b56dca4f` | committed、pushed |
| Windows workflow non-blocking diagnostics | `b627059426f38e1058c94ec41ceb660f6547d7c8` | committed、pushed |
| Windows SQLite URI fix | `a51e7ddd76f871ce1539e11213d26f487437a9a0` | committed、pushed |
| root implementation result | `2cd0139304854ec2938e7d7447bc3e2153bd1aeb` | committed、pushed、direct-parent result |

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-plus/cmd/go-admin-desktop/**</Path>` | writable | Wails entry、assets、bindings、tagged tracer 与 PowerShell runner |
| `<Path>go-admin-plus/.github/workflows/desktop-windows-tracer.yml</Path>` | writable | 固定输入、最小权限、硬超时与诊断上传的 Windows native harness |
| `<Path>go-admin-plus/internal/host/desktop/**</Path>` | writable | lifecycle、loopback gateway、bootstrap 与 runtime integration |
| `<Path>go-admin-plus/internal/profile/desktop.go</Path>`、`desktop_dsn_test.go` | writable deviation | 将 `D:/...` 编码为 `file:///D:/...` 并覆盖 Unix/Windows DSN |
| `<Path>go-admin-plus/common/**</Path>`、`<Path>go-admin-plus/config/**</Path>`、migration init | writable | request context、auth/CORS envelope 与嵌入 seed 接缝 |
| `<Path>go-admin-plus/go.mod</Path>`、`<Path>go-admin-plus/go.sum</Path>` | shared:T-08 | 固定 Wails 2 依赖 |
| `<Path>go-admin-ui-plus/packages/runtime/src/desktop/**</Path>`、Admin utility/tests | writable | bootstrap runtime、native fetch 和 Desktop token storage |

- **read-only 修改：** 无。
- **未声明路径：** 无；Windows profile 偏差在修改前投影回 Ticket expected/writable paths。
- **生成文件：** Wails build、Admin dist、SQLite 数据和工具缓存均未提交。

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-004 / 双 Runtime 同一 API | bootstrap + ApiClient + real Application | macOS/Windows 原生登录和 CRUD marker；Web 回归 | pass |
| AC-007 / macOS ARM64 离线核心流程 | native Wails tracer | UI 登录及 getinfo/create/list/update/delete | tracer-pass；正式分发属 T-11 |
| AC-008 / Windows AMD64 离线核心流程 | native Wails/WebView2 tracer | run `32842185071` build/run/login/CRUD | tracer-pass；Windows 10/11 installer 属 T-12 |
| AC-017 / 断网核心流程 | embedded Admin + loopback Application | 两平台无 CDN/公网运行时依赖 | tracer-pass |
| Ticket 路径/集成合同 | Git/SpecDev audit | 三仓 SHA、gitlink、writable/shared owner | pass |

## 5. Workspace Verification

- **Verification workspace：** `current-workspace`；原生远程 runner 作为 required E2E 外部环境，所有 source/result 仍由 current workspace direct-parent 管理。

| 检查 | 环境 | 结果 | 摘要 |
|---|---|---|---|
| `go test ./... -count=1` | macOS ARM64 / backend `a51e7dd` | pass | Windows DSN 修复后的全量 Go 回归通过 |
| `go test -race ./internal/profile ./internal/host/desktop ./cmd/go-admin-desktop -count=1` | macOS ARM64 | pass | profile/host/cmd 受影响边界无 race |
| `go vet ./...` | macOS ARM64 | pass | 无 vet finding |
| 定向及 tagged tests | macOS ARM64 | pass | profile、host、cmd 与 `sqlite3,desktop_e2e` 组合通过 |
| Wails production/tagged build | macOS ARM64 + Windows AMD64 | pass | macOS 原生与 Windows cross/native 均完成 |
| macOS tagged tracer | macOS ARM64 native | pass | WebView 登录后完成 getinfo 与 demo CRUD，精确 PASS marker |
| `pnpm test:ci`、`pnpm build:prod` | macOS ARM64 / frontend `3bfc10f` | pass | lint/type、28 files/225 unit、API contract 与 production build |
| Playwright mocked | macOS ARM64 / Chromium | pass | 15 files / 136 tests |
| `actionlint v1.7.12` | workflow source | pass | workflow 语法、expression 和 action contract 通过 |
| Speculo implement validator | clean root `2cd0139` | pass | `0 error(s), 0 warning(s)` |

### Windows Native Runs

| Run | Backend / Frontend | 结果 | 发现与处置 |
|---|---|---|---|
| `32840548065` | `dba1d94` / `3bfc10f` | fail | 可选 registry probe 将 PowerShell step 置为失败；删除探针并将 diagnostics 保持 non-blocking |
| `32841039531` | `b627059` / `3bfc10f` | fail | 原生进程报 `invalid uri authority: D:`；以回归测试修正 Windows SQLite file URL |
| `32842185071` | `a51e7dd` / `3bfc10f` | pass | Windows AMD64 build、WebView2 environment、login/getinfo/demo CRUD、唯一 PASS marker 和 artifact 全部通过；6m14s |

成功 run 的日志明确包含：WebView2 environment created、`GET /api/v1/getinfo` 200、demo product POST/GET/PUT/DELETE 200、`GO_ADMIN_DESKTOP_E2E_PASS` 和 `Windows native desktop tracer passed.`。Artifact 仅保留 environment/provenance/stdout/stderr，不包含 launch token 或用户数据库。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `8b3fc92..a51e7dd`；frontend `8fe488e..3bfc10f`；root `a144076..2cd0139`。
- **结果：** pass；路径 diff、gitlink、tests、action contract 和远程 checkout provenance 一致。
- **修正：** seed/CORS/request-context/custom-scheme、workflow diagnostic exit code 和 Windows DSN 均由真实 tracer 暴露并补回最小回归测试；没有以 cross-build 或 workflow 文件存在替代 native run。

### 规范轴

- **来源：** Spec、T-08、Goal Plan G5 与 AC-004/007/008/017。
- **结果：** pass；同一 Application/Admin/API、loopback random port、token 不进 URL/日志、CRUD 不走 binding 和双平台原生 tracer 均满足。
- **边界：** run OS 是 Windows Server 2025 AMD64，足以关闭 T-08 native development tracer；Windows 10/11 clean-machine、installer、签名和 WebView2 missing behavior 继续 fail closed 地保留在 T-12。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `a14407693fe18bccb0e5ae0934a84b7d1c6e70aa` |
| Implementation/source SHA | backend `a51e7ddd76f871ce1539e11213d26f487437a9a0`；frontend `3bfc10f6d3c1d247f1b31b88220f648ac522d6a2`；root `2cd0139304854ec2938e7d7447bc3e2153bd1aeb` |
| Candidate | `not-applicable`（current/direct-parent） |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | full suite、race、vet、frontend regression、macOS tracer、Windows native tracer passed |
| E2E disposition/result | required；macOS ARM64 passed；Windows AMD64 run `32842185071` passed |
| Parent result/re-read | `result_sha=2cd0139304854ec2938e7d7447bc3e2153bd1aeb`；父 HEAD 包含全部 source commits，finalization 仅更新 SpecDev 工件 |

## 8. 偏差与决策

- **Spec/产品偏差：** 无。
- **Ticket 局部合同修正：** auth/seed/CORS/request-context、Wails cache、Windows workflow 和 Windows DSN 接缝均在写入前投影至 Ticket；Application/platform/ApiClient 保持只读。
- **授权：** current workspace 逐 Ticket commit 已授权；三仓 push 与 Windows workflow dispatch 的一次性授权已消费，不扩展至后续 push、PR、deploy、签名或发布。

## 9. 残余风险与交付定位

- **Full suite：** passed。
- **Required E2E：** passed（macOS ARM64 native + Windows AMD64 native）。
- **Integration/promotion：** passed / applied；root result `2cd0139304854ec2938e7d7447bc3e2153bd1aeb`。
- **Ticket：** `done`；允许按 Goal Plan 严格顺序进入 T-09。
- **残余风险：** Desktop durability、安全 hardening、单实例、升级恢复属于 T-09；macOS/Windows 正式签名安装与目标干净机矩阵属于 T-11/T-12。
- **Source workspace：** `current`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-08.md</Path>`。
