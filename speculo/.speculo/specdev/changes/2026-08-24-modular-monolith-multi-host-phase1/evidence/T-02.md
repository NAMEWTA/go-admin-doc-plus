# Evidence: T-02 - 建立 Host 无关 Application 生命周期内核

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/02-application-lifecycle-kernel.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、backend `main`
- **Base/implementation/candidate/result SHA：** root `9faf7e9154567780f8f8f876feacde84eaf6e753`；backend `8f506a0e6fca22969ab376af6d4c51a3efe517f0` -> `d413003a399515d702359c5655ff3ac67f0d2c86`；candidate `not-applicable`；root result `1ba844ad6efd708867274d933429ba19961196b0`
- **状态：** done

## 1. 实现摘要

- 新增仅依赖 `context`/`net/http` 的 Application 内核，Build 时显式接收 Config、Dependencies 和 ModuleSet，并暴露 Handler、Start、Stop、Migrations 与只读状态快照。
- 固定 Module ID、路由、迁移声明和生命周期合同；重复 ID、缺失 Handler、真实 Gin 路由冲突和 Module 启动失败均返回错误，部分启动失败与正常停止均按反序清理。
- 为 admin/demo/jobs/other 建立显式模块组合；queue 由可等待的 `Shutdown` 管理，jobs cron 由 Module 持有并可在 context deadline 内反序停止。
- 旧 `go-admin server -c` 改为 Host adapter：配置、HTTP listener 和 OS signal 留在 `cmd/api`，Application 不拥有 listener/signal/Wails；旧 `AppRouters` 兼容入口保留但不再驱动新 CLI。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`
- **Dispatch Packet/checkpoint：** Lead direct；root base `9faf7e9`，backend base `8f506a0`
- **允许动作：** current workspace T-02 writable paths、本地 implementation commit；无 worktree、push、PR、deploy 或 release
- **返回：** backend `d413003`、root `1ba844a`；backend/root clean；13 个实现文件均位于 T-02 writable paths
- **Lead 独立核对：** pass；重读 backend HEAD/parent、完整 path list、固定 diff、全量命令退出码和 root gitlink
- **只读 Agent findings：** 无；未派遣 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-plus/internal/application/**</Path>` | writable | Host 无关 Application/ModuleSet、状态机与生命周期测试 |
| `<Path>go-admin-plus/internal/modules/**</Path>` | writable | 显式业务模块顺序、Jobs route 组合与 runtime queue 生命周期 |
| `<Path>go-admin-plus/app/*/module.go</Path>` | writable | admin/demo/jobs/other 模块适配与可停止 cron ownership |
| `<Path>go-admin-plus/cmd/api/server.go</Path>` | shared:T-02 | 旧 CLI 改为 signal/listener Host adapter |

- **read-only 修改：** 无
- **未声明路径：** 无
- **生成文件/锁文件：** 无；`go-admin` build binary 为既有 ignore 输出，未提交

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 / Application 与 Host 分离 | AST 边界测试、CLI 集成 | Application 无 signal/listener/Wails；`cmd/api` 持有 listener/signal | pass |
| AC-002 / HTTP 与 CLI 兼容 | T-01 characterization | 真实临时 SQLite CLI 启动、登录、菜单、CRUD、任务、停止均通过 | pass |
| AC-011 / 生命周期有界 | Application unit/race、真实进程 cleanup | 顺序启动、反序停止、重复启停拒绝、部分失败清理、cron/queue Stop | pass |
| 缺失依赖/重复模块/路由冲突 | Build failure tests | 返回带 module 上下文的 error，不由 Application 退出进程 | pass |
| 路径与 implementation commit | Git path/parent/clean 审计 | backend 非空 commit `d413003`，无越界路径 | pass |
| required E2E | 真实 CLI process characterization | 临时端口/API 请求/SIGINT/有界 cleanup | pass |
| Ticket/Map/Evidence/root result 一致 | SpecDev 状态与根 checkpoint | result `1ba844a` 已写回；父 HEAD 包含 result，finalization 仅含 SpecDev 投影 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `go test ./internal/application -count=1` | backend current-workspace / macOS arm64 | pass | Build、状态、路由冲突、启停与失败清理 |
| `go test ./internal/application ./internal/modules ./app/jobs -count=1` | backend current-workspace / macOS arm64 | pass | 内核、显式模块顺序和 Jobs 编译接缝 |
| `go test ./test/characterization -count=1` | backend current-workspace / macOS arm64 | pass | 真实 CLI/API/SQLite/停止基线通过 |
| `go test -race ./internal/application ./internal/modules -count=1` | backend `d413003` / macOS arm64 | pass | 无 race；第三方 `go-m1cpu` 有既有 Clang VLA warning |
| `go vet ./internal/application ./internal/modules ./cmd/api` | backend current-workspace / macOS arm64 | pass | 无输出 |
| `go test ./... -count=1` | backend `d413003` / macOS arm64 | pass | 全量包与 characterization 通过 |
| `make build` | backend `d413003` / macOS arm64 | pass | CGO-disabled server binary 构建通过 |

- **失败后修复与重跑：** 首次真实模块编译发现 `jobs/router -> jobs/service -> jobs` 依赖环；将 cron lifecycle 留在 `app/jobs/module.go`，由 `internal/modules/jobs.go` 组合 route adapter 后通过。审查发现完整 `SysJob` 更新会扩大旧副作用，改为仅持有 `EntryId` 的更新对象后重跑通过。
- **受控反向验证：** 临时把 Stop 从反序改为正序，`TestApplicationStartsInOrderAndStopsInReverse` 以 `stop:admin,stop:jobs` 与预期相反而失败；恢复反序实现后定向测试通过。
- **未运行检查：** Windows/Linux runner；本 Ticket 不构建对应平台产物，平台原生验证归 T-10/T-12。前端套件未运行，因为 T-02 未修改前端合同，HTTP/UI 兼容由 T-01 真实后端 characterization 覆盖。
- **E2E：** required；Lead 在 current workspace 运行真实 CLI process characterization，passed。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `8f506a0e6fca22969ab376af6d4c51a3efe517f0..d413003a399515d702359c5655ff3ac67f0d2c86`
- **结果：** pass
- **Findings 与修正：** Jobs 根包导入 router 会形成循环依赖，改为 `internal/modules` 组合适配；cron entry 更新收窄为旧实现相同的单字段副作用；停止后清空只读状态中的 started modules。最终未发现 hard violation 或仍成立的判断性 smell。

### 规范轴

- **固定输入与来源：** 同上固定 diff；Spec、T-02、Tickets Map、Goal Plan G1/AC-001/AC-002/AC-011
- **结果：** pass
- **Findings 与修正：** 旧 `AppRouters`/init 兼容层按 T-13 收缩条件保留，但新 CLI 只使用显式 `modules.Default()`；Application 的 AST 测试锁定无 signal/listener/Wails。最终 IN/OUT、失败清理、路由冲突、HTTP/CLI 兼容与路径合同一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `9faf7e9154567780f8f8f876feacde84eaf6e753` |
| Implementation/source SHA | backend `d413003a399515d702359c5655ff3ac67f0d2c86` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | lifecycle/race/vet、全量 Go、build 与 T-01 characterization passed |
| E2E disposition | required：启动核心跨配置、路由和后台生命周期 |
| E2E result | passed：真实临时 SQLite CLI/API smoke 与 SIGINT cleanup |
| Parent result/re-read | result `1ba844ad6efd708867274d933429ba19961196b0`；父 HEAD 包含 result，backend gitlink 为 `d413003` |

## 8. 偏差与决策

- **偏差：** 无；未改变外部 API、CLI 参数或数据 schema
- **记录：** 不适用
- **批准来源及影响：** 用户授权 current workspace 逐 Ticket 本地 commit；本 Ticket 未 push、未使用 worktree、PR、deploy 或 release 动作

## 9. 残余风险与交付定位

- **残余风险/已知限制：** go-admin-core Runtime 仍为进程级兼容依赖；旧 router 内部的 init 列表和 `AppRouters` 兼容入口留待 T-13 零调用后收缩；本 Ticket 未在 Windows/Linux runner 复跑。
- **后续 Ticket：** T-02 已关闭；随后 T-05
- **监控或回滚触发：** Application 状态、启动阶段错误、shutdown timeout、route/HTTP characterization 漂移；可回滚 backend `d413003`
- **Source commit：** backend `d413003a399515d702359c5655ff3ac67f0d2c86`
- **Parent result：** `1ba844ad6efd708867274d933429ba19961196b0`
- **Source workspace：** current root/backend `main`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-02.md</Path>`
