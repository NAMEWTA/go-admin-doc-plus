# Evidence: T-05 - 将现有 Admin 等价迁入前端 workspace

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/05-admin-workspace-shell.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、frontend `main`
- **Base/implementation/candidate/result SHA：** root `cff2f0259f829ca3ad861dfd63d3c901082c5f5e`；frontend `105b364112d3dfd131bbace8a77b8ad124b4ce36` -> `89ae3fa35335f61651e18250acc0b0643daf4d8f`；candidate `not-applicable`；root result `c5147971092ec27c5d65ff70e07051a2d0cfcafb`
- **状态：** done

## 1. 实现摘要

- 建立 pnpm workspace，范围固定为 `apps/*`、`domains/*`、`packages/*`，本阶段只有一个真实应用 `apps/admin`，没有创建未来 App 空壳。
- 将根 `src`、`public` 和 `index.html` 以 421 个 100% 内容不变的 Git rename 迁入 Admin；源码导入、页面 URL、静态资源、环境变量来源和根 `dist` 输出保持兼容。
- 根脚本作为兼容门面通过 workspace filter 委托 `@go-admin/admin`；Vite、Vitest、TS、ESLint、API contract 和 Plop 路径统一指向新应用根。
- 新增私有 `@go-admin/config` 路径包，集中声明 repo/Admin/src/dist 位置，避免多份配置各自推导相对路径。
- 将包管理器声明对齐到实际 pnpm `11.1.3`，覆盖规则迁入 `pnpm-workspace.yaml`；仅放行 4 个既有依赖安装脚本，并锁定 Pinia 的 devtools peer 为基线 `6.6.4`，未引入运行时升级。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`
- **Dispatch Packet/checkpoint：** Lead direct；root base `cff2f02`，frontend base `105b364`
- **允许动作：** current workspace T-05 writable paths、implementation commit 和用户本轮明确授权的 push；无 worktree、PR、deploy 或 release
- **返回：** frontend `89ae3fa`、root `c514797`；frontend/root clean；436 个实现提交路径均位于 T-05 writable paths
- **Lead 独立核对：** pass；重读 frontend HEAD/parent、完整 path list、固定 diff、锁文件、安全覆盖、目录断言与全量命令退出码
- **只读 Agent findings：** 无；未派遣 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-ui-plus/apps/admin/**</Path>` | writable | 唯一 Admin 应用 manifest、入口、静态资源与 100% 内容不变源码迁移 |
| `<Path>go-admin-ui-plus/packages/config/**</Path>` | writable | 共享 repo/Admin/src/dist 路径合同 |
| `<Path>go-admin-ui-plus/src/**</Path>`、`public/**`、`index.html` | writable deviation | 机械移动旧根入口并消除双写路径 |
| `<Path>go-admin-ui-plus/package.json</Path>`、`pnpm-lock.yaml`、`pnpm-workspace.yaml` | shared:T-05 | workspace 委托、pnpm 11 可复现安装、安全覆盖与单锁文件 |
| `<Path>go-admin-ui-plus/{vite,vitest,eslint}.config.mjs</Path>`、`tsconfig.json`、`jsconfig.json` | writable | 新应用根、别名、测试覆盖与根 dist 兼容 |
| `<Path>go-admin-ui-plus/scripts/check-api-contract.mjs</Path>`、`plop-templates/**` | writable | 工具消费者迁移到 `apps/admin/src` |

- **read-only 修改：** 无；tests、Playwright config、CI workflow、Dockerfile 均未改
- **未声明路径：** 无
- **生成文件/锁文件：** 单一根 `pnpm-lock.yaml` 已更新；`dist`、Playwright 输出和本地 env 未提交

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-003 / Admin workspace 等价 | 根脚本、Vite build、Playwright | 登录、菜单、动态路由、缓存、CRUD 与页面 URL 136 项通过 | pass |
| AC-018 / 单一前端基础合同 | 目录/锁文件/import 扫描 | 只有 `apps/admin`、一个锁文件、无根 `src/public/index.html` | pass |
| 根命令兼容与退出码透传 | workspace filter | install/build/type/lint/unit/e2e 均从根运行；受控错误返回非零 | pass |
| 安装与依赖行为等价 | frozen install、lock 审计 | pnpm 11 非交互安装通过；安全覆盖生效；Pinia peer 保持 6.6.4 | pass |
| 路径与 implementation commit | Git path/parent/clean 审计 | frontend 非空 commit `89ae3fa`，无越界路径 | pass |
| Ticket/Map/Evidence/root result 一致 | SpecDev 状态与根 checkpoint | result `c514797` 已写回；父 HEAD 包含 result，finalization 仅含 SpecDev 投影 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `CI=true pnpm install --frozen-lockfile` | frontend current-workspace / macOS arm64 | pass | 3 workspace projects；4 项精确 allowBuilds；单锁文件 |
| `pnpm test:unit` | frontend `89ae3fa` / macOS arm64 | pass | 27 files、220 tests |
| `pnpm type-check` | frontend `89ae3fa` / macOS arm64 | pass | vue-tsc 与 Node tsconfig 无错误 |
| `pnpm lint` | frontend `89ae3fa` / macOS arm64 | pass | 0 errors；29 个既有 Vue warnings |
| `GO_ADMIN_PATH=../go-admin-plus pnpm check:api -- --require-models` | frontend/backend current-workspace | pass | 8 fixtures、15 migrated pages、3 Options pages、Admin declarations |
| `pnpm build:prod` | frontend `89ae3fa` / macOS arm64 | pass | Vite 8 production build，3063 modules，输出根 `dist` |
| `pnpm e2e` | frontend `89ae3fa` / mocked webServer | pass | 136/136 passed，3.5m |
| 目录/锁文件断言与 `git diff --check` | frontend fixed diff | pass | 一个 App、一个 lock、旧根入口不存在、无 whitespace error |

- **失败后修复与重跑：** pnpm major 声明与实际运行器语义错配曾使安全 overrides 被忽略并解析出旧依赖；将 workspace 工具链统一到 pnpm 11.1.3、配置精确 build-script allowlist 和 devtools peer 后，锁文件只含合规版本，frozen install 与全部门禁重跑通过。
- **受控反向验证：** 临时将 Vite `@` alias 指向不存在目录，根 `pnpm build:prod` 以 `ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL` 非零失败；恢复 alias 后生产构建通过。
- **未运行检查：** live-backend Playwright 与视觉截图项目；本 Ticket 的 required mocked E2E 已覆盖 T-01 定义的 Admin 行为基线，真实后端 API 基线由 T-01 characterization 保持。
- **E2E：** required；Lead 在 current workspace 运行 136 项 mocked Playwright，passed。

## 6. 双轴审查

### 标准轴

- **固定输入：** frontend `105b364112d3dfd131bbace8a77b8ad124b4ce36..89ae3fa35335f61651e18250acc0b0643daf4d8f`
- **结果：** pass
- **Findings 与修正：** 删除了为消除 peer warning 而临时加入的 `@vue/devtools-api@8` 直接依赖；改用 workspace override 保持基线 6.6.4。确认 421 个应用文件均为 100% rename，测试/CI/Dockerfile 未修改，最终无 hard violation 或仍成立的判断性 smell。

### 规范轴

- **固定输入与来源：** 同上固定 diff；Spec、T-05、Tickets Map、Goal Plan G1/AC-003/AC-018
- **结果：** pass
- **Findings 与修正：** 原 Ticket 将根 `src` 同时定义为只读和迁移目标，执行期按引用图修订 path owner，并把实际工具消费者加入 writable paths；未扩大到 ApiClient、Domain、OpenAPI 或视觉重设计。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `cff2f0259f829ca3ad861dfd63d3c901082c5f5e` |
| Implementation/source SHA | frontend `89ae3fa35335f61651e18250acc0b0643daf4d8f` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | frozen install、unit/type/lint/API contract/build、目录合同与 136 E2E passed |
| E2E disposition | required：目录迁移跨入口、静态资源、动态菜单和全部管理页面 |
| E2E result | passed：136/136 mocked Playwright，3.5m |
| Parent result/re-read | result `c5147971092ec27c5d65ff70e07051a2d0cfcafb`；父 HEAD 包含 result，frontend gitlink 为 `89ae3fa` |

## 8. 偏差与决策

- **偏差：** Ticket 局部 path owner 修订；根 `src/public/index.html` 从只读/遗漏调整为机械迁移可写范围，实际配置消费者同步纳入
- **记录：** T-05 locked decision、expected/writable/read-only paths 与 Tickets Map owner/status 已同步
- **批准来源及影响：** 用户授权 current workspace 逐 Ticket commit，并于本轮明确要求 commit/push；未使用 worktree、PR、deploy 或 release 动作

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 根 manifest 暂时保留应用依赖作为 tests/tooling 兼容门面，后续 T-07/T-13 收缩；Vite dev server 会把既有 Element Plus prop warnings 展开为巨量循环对象日志，但 136 项断言全部通过；旧 CSS `::input-placeholder` build warning 仍在基线中。
- **后续 Ticket：** T-05 已关闭；随后 T-03
- **监控或回滚触发：** frozen install、根命令退出码、Admin URL/资源 404、动态菜单/E2E 漂移；可回滚 frontend `89ae3fa`
- **Source commit：** frontend `89ae3fa35335f61651e18250acc0b0643daf4d8f`
- **Parent result：** `c5147971092ec27c5d65ff70e07051a2d0cfcafb`
- **Source workspace：** current root/frontend `main`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-05.md</Path>`
