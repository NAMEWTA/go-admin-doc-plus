# Evidence T-13: 汇合产品发布并收缩旧兼容契约

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/13-product-release-and-contract.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、backend、frontend `main`
- **Base/implementation/candidate/result SHA：** root base `3ef3a6cc713493e1a000dc021c861a4935a65ffb`；activation `b85bb91c5cb31f98568cb3eca613c305abcb9089`；implementation/result `2f01bae8ce3c0dd50271ce6d978b7084efa737e8`；candidate `not-applicable`
- **精确子仓库：** backend `1859e63b93f2a599020be1027b795a2153448e9a`；frontend `4899b3603d4b0085b51cf1d46117ef10af15dffd`
- **状态：** `done`

## 1. 实现摘要

- 根仓库新增 `Taskfile.yml`、产品级 GitHub workflow 和无第三方运行时依赖的 Node manifest 工具，统一校验 root/backend/frontend、产品版本、OpenAPI SHA-256、migration max version、三个原生 run 和 Actions artifact digest。
- 三个平台 workflow 将 `version/root/frontend/mode` 固定进 GitHub 可查询的 run title，并将 `0.1.0` 固定进 artifact 名；Linux manifest 分离产品 `version=0.1.0` 与不可变镜像 `build_version=<backend SHA>`。
- product manifest 明示 Linux 无签名、macOS ad-hoc/未公证、Windows 无 Authenticode，全部含 SHA256SUMS 与 SPDX JSON 合同；策略固定 `external_publish_authorized=false`、`production_deployment=false`、`global_security_disable=false`。
- 删除后端只写不读的 `AppRouters` 与三个 package-init 注册文件；删除前端 12 个零消费者 Domain API 转发 facade。自动扫描保留并列出仍有真实消费者的四个 API facade、default request facade、精确 component fallback 和 heuristic contract checker。
- 产品 Gate 只上传 14 天 Actions manifest artifact，没有创建 GitHub Release、外部 publish、部署、生产 migration、平台可信签名或全局安全绕过。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`；未派遣 subagent。
- **执行策略：** current workspace、implementation limit 1、direct-parent；无 worktree/candidate。
- **返回：** backend `1859e63`、frontend `4899b36`、root implementation `2f01bae`；三仓均已推送并与远端 `main` 对齐。
- **允许动作：** T-13 writable paths、每 Ticket commit/push、三平台 release Gate 与产品 Gate dispatch。
- **未授权动作：** GitHub Release、external publish、deploy、production migration、Developer ID/Authenticode/Store 或 source cleanup。
- **Lead 独立核对：** 固定 diff、Ticket 局部路径修正、gitlinks、workflow run title、run/artifact API、关键日志 marker、下载后的 product manifest 和三个仓库 clean/ancestor 状态全部通过。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>Taskfile.yml</Path>`、`<Path>.github/workflows/product-release.yml</Path>` | shared:T-13 | 精确来源 preflight、三平台 dispatch、产品合同/源码 Gate 与候选 manifest 上传 |
| `<Path>release/manifest/**</Path>` | shared:T-13 | manifest collect/verify、负向测试、兼容收缩扫描和维护说明 |
| `<Path>go-admin-plus/.github/workflows/release-linux.yml</Path>` | writable:T-13 correction | 语义产品版本、build version、签名状态、run title 与 artifact 名 |
| `<Path>go-admin-plus/.github/workflows/release-macos.yml</Path>`、`<Path>go-admin-plus/.github/workflows/release-windows.yml</Path>` | writable:T-13 correction | 可查询 provenance run title 和带版本 artifact 名 |
| `<Path>go-admin-plus/cmd/api/**</Path>` | writable | 删除零读取 `AppRouters` package-init 兼容注册 |
| `<Path>go-admin-ui-plus/apps/admin/src/api/**</Path>` | writable | 删除 12 个零消费者 Domain API 转发 facade |

- **read-only 修改：** 无；Compose、三平台 release 脚本/identity、OpenAPI、frontend packages/domains 与业务逻辑未改。
- **未声明路径：** 无；平台 workflow 元数据接缝在编辑前已投影进 Ticket writable paths。
- **生成文件：** product manifest 只存在于系统临时目录或 Actions artifact；未提交二进制、生成 bundle 或 lockfile。

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-002 | HTTP/模块兼容 | 全量 Go/characterization；`modules.Default()` 继续注册全部模块，旧 `AppRouters` 零读取后删除 | pass |
| AC-005 | OpenAPI 单一来源 | frontend checker + product preflight | 65 paths，generated zero drift；SHA-256 `7fefbe3bbe9e76ddefc8286e9993468d8b7a5b5ccf618c4f54820de09aecbc81` | pass |
| AC-006 | 稳定路由 | compatibility scan + unit/E2E | routeKey 路径保持；仍被 Shell/旧行消费的 exact component fallback 明确保留 | pass |
| AC-016 | 三类 release artifact | native runs + product manifest | 三类 `0.1.0` artifact、SHA256SUMS、SPDX、签名状态和 API archive digest 完整 | pass |
| AC-018 | 单一前端基础合同 | facade scan + frontend CI | 12 个零消费者 facade 删除；四个 active API facade 与 request/component/checker 残余有消费者清单 | pass |

## 5. Workspace Verification

- **Workspace verification mode：** `current-workspace`；本机 macOS ARM64 + GitHub Ubuntu 24.04/macOS 15 ARM64/Windows Server 2025 AMD64。

| 命令或步骤 | 环境 | 结果 | 摘要 |
|---|---|---|---|
| `go test ./... -count=1`、`go vet ./...` | backend current-workspace | pass | 全量 Go、characterization、migration、Server/Desktop Host 通过 |
| `pnpm test:ci`、`pnpm build:prod` | frontend current-workspace | pass | lint 0 error/25 既有 warning、type-check、28 files/225 tests、API contract 与 3101-module build |
| Node syntax/test/preflight/compatibility scan | root current-workspace | pass | 3 manifest tests；版本漂移和 external publish 授权负向测试 fail closed；兼容扫描 marker 通过 |
| Ruby YAML + `actionlint@v1.7.7` + `git diff --check` | current-workspace | pass | Taskfile 与四份 workflow 语法/语义通过 |
| Linux run `32880870190` | Ubuntu 24.04 AMD64 | pass | Compose empty volume/CRUD/persistence/failure/security、SBOM/checksum；`GO_ADMIN_LINUX_COMPOSE_E2E_PASS` |
| macOS run `32880876001` | macOS 15 ARM64 | pass | 两次 native tracer、DMG、ad-hoc/scoped self-use；`GO_ADMIN_MACOS_SELF_USE_INSTALL_PASS` |
| Windows run `32880882112` | Windows Server 2025 AMD64 | pass | 两次 native tracer、NSIS、断网 WebView2、install/upgrade/data；全部 Windows marker 通过 |
| Product run `32881713952` | Ubuntu 24.04 AMD64 | pass | 独立全量 Go、225 unit、OpenAPI/build、metadata collect/verify 与 artifact upload |

- **三平台 artifact：** Linux `9575831917` / `b06051f139c4ee5a18242ff85f8044c7079f4bc197e45a48e9fd507b82bf01d8`；macOS `9575815084` / `99df7007472318a0e72e7dab1039e32da94deee4db9405d6fa3a3f1b733987f3`；Windows `9575974057` / `e3580b04c7e56ec11f2baa2f3af63b6c088a7125ad60eedca0c19258a542e780`。
- **产品 artifact：** `9576144123`，name `go-admin-plus-0.1.0-product-manifest-32881713952-1`，archive digest `8c92552cb71f0c4ce4178deb7603910862c54c77e8dafc1fab64c7563b07282b`；下载后 manifest SHA-256 `b2baebaf01264f86d0f2b37182fc4348cddf9a10923d3b19fc4f2818aaa1d8ba` 并再次 verify pass。
- **失败后修复与重跑：** 手工错误 full SHA 被 preflight 拒绝；首次 `workflow_dispatch --ref <commit SHA>` 被 GitHub 422 拒绝且未创建 run。Task 修为 `--ref main` 并先验证 backend `origin/main == gitlink`，随后三平台与产品 Gate 全绿。
- **E2E：** required / passed；复用并重跑 T-10/11/12 的完整 Compose、native WebView、安装、升级和数据持久化 Gate。

## 6. 双轴审查

### 标准轴

- **固定输入：** root `3ef3a6c..2f01bae`；backend `dde21ea..1859e63`；frontend `7887d32..4899b36`。
- **结果：** pass。
- **Findings 与修正：** product collector 最初只能从 artifact API 证明 backend/run，不能证明 dispatch 的 root/frontend；在编辑前修正 Ticket 路径合同，再用 GitHub-authenticated run title 和版本化 artifact 名闭合来源链。Task dispatch 的 SHA ref 422 已以远端分支等值预检修正。

### 规范轴

- **固定输入与来源：** Spec AC-002/005/006/016/018、T-13、Goal Plan G8、DEV-001。
- **结果：** pass。
- **判定：** 同版本/三 git refs/OpenAPI/migration/digest/checksum/SBOM/signature 状态可从 product manifest 重建；只删除零消费者兼容层；active residual 有原因和消费者；没有外部分发、生产部署或全局安全绕过。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | activation 前 `3ef3a6cc713493e1a000dc021c861a4935a65ffb`；T-13 activation `b85bb91c5cb31f98568cb3eca613c305abcb9089` |
| Implementation/source SHA | root `2f01bae8ce3c0dd50271ce6d978b7084efa737e8`；backend `1859e63b93f2a599020be1027b795a2153448e9a`；frontend `4899b3603d4b0085b51cf1d46117ef10af15dffd` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无冲突 |
| Integration checks | 本地全量 source/contract/build + 三平台 native release Gate + root Product Gate 均 passed |
| E2E disposition/result | required / passed；runs `32880870190`、`32880876001`、`32880882112` |
| Parent result/re-read | root HEAD/result `2f01bae`，包含 implementation；gitlinks 精确为 backend `1859e63`、frontend `4899b36`；三仓 clean 且远端 main 对齐 |

## 8. 偏差与决策

- **DEV-001：** 用户批准 Phase 1 macOS/Windows `unsigned-self-use`；product manifest 记录 macOS ad-hoc/未公证与 Windows unsigned，签名和平台 reputation 延期。
- **Ticket 局部路径修正：** artifact API 不暴露 dispatch 输入，故在修改前将三个 platform workflow 的 provenance/version metadata seam 加入 T-13 writable paths；构建、验证和分发策略未改变。
- **兼容收缩决策：** `AppRouters` 和 12 个 facade 有静态零消费者证据后删除；仍被消费的 facade/request/component/heuristic checker 明确保留，不按时间强删。
- **批准边界：** commit/push/workflow dispatch 已授权；GitHub Release、外部 publish、deploy、production migration、签名、公证、Store 和 cleanup 未执行。

## 9. 残余风险与交付定位

- **active compatibility residual：** 四个 Admin API facade 分别由字典、配置、权限和资料页/单测消费；default request facade 由 login/user/ImageCropper 消费；component fallback 支持 Shell/无 routeKey 行；heuristic checker 覆盖 canonical OpenAPI 尚未描述的 Go model/frontend field。扫描器会在这些入口意外变成零消费者或被误删时失败。
- **平台限制：** Windows 原生 Gate 是 Windows Server 2025，不伪装为干净 Windows 10/11；后者仍是未来 external publish 前人工烟测。Smart App Control/受管策略阻断时报告 unsupported，不关闭全局安全能力。
- **保留期：** 四个 Actions artifact 到 2026-09-08；它们是可下载候选和 Evidence，不是长期 GitHub Release 渠道。
- **后续 Ticket：** 无；Phase 1 Goal Plan 完成。外部分发、生产部署、平台可信签名和后续兼容收缩需新授权/change。
- **监控或回滚触发：** run title/ref/version/digest 漂移、OpenAPI/migration drift、兼容扫描新增调用或任一平台 Gate 失败即阻止候选；代码可回滚 root `2f01bae`、backend `1859e63`、frontend `4899b36`。
- **Source commit / Parent result / workspace：** `2f01bae8ce3c0dd50271ce6d978b7084efa737e8` / 同 SHA / `current`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-13.md</Path>`
