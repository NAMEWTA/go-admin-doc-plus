# Evidence: T-01 - 冻结 API、UI、数据与启动行为基线

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/01-freeze-behavior-baseline.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、backend、frontend `main`
- **Base/implementation/candidate/result SHA：** execution start root `81106fb011a1797687050eb092bf1c8913ffe625`；独立 Speculo refresh `34524fd5d6ca17f39ae435eea997feaac340762c`；T-01 direct-parent root `34524fd5d6ca17f39ae435eea997feaac340762c` -> `e912bcdca930c87e69c3073b58d045e2617c1d1e`；backend `750537c0b8edde522f9c9dddc2bfcf64169689ea` -> `8f506a0e6fca22969ab376af6d4c51a3efe517f0`；frontend `1d1af04e3305faf3e7087fe4503b2fb132009663` -> `105b364112d3dfd131bbace8a77b8ad124b4ce36`；candidate `not-applicable`；root result `e912bcdca930c87e69c3073b58d045e2617c1d1e`
- **状态：** done

## 1. 实现摘要

- 使用临时 SQLite、随机 loopback 端口和临时配置构建并启动当前后端，固定 health、登录成功/失败、菜单、权限、任务查询、Product CRUD、进程中断和 cleanup 行为。
- 固定 `/api/v1` 关键路由及无 token 授权 envelope；KODO、OBS、OSS 默认上传测试改用本地 `httptest` contract，不再读取真实凭据、缺失文件或公网服务。
- 固定来源可追溯的 pre-phase-one SQLite SQL fixture、索引、迁移版本、合成数据和 SHA-256 `07810c8bed64f4dd1921d541402a695d76109049d6f8117f9d49deb19c61a81c`。
- 增加 Admin 登录、动态菜单、错误凭据、后端不可达、Product create/update/delete 的 mocked Playwright characterization；受限权限和任务行为继续由既有 app-shell/schedule E2E 覆盖。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`
- **Dispatch Packet/checkpoint：** Lead direct；execution start root `81106fb`，T-01 direct-parent root `34524fd`，backend `750537c`，frontend `1d1af04`
- **允许动作：** current workspace 中 T-01 writable paths、本地 implementation commits；用户最新明确授权本次 commit push；无 worktree、PR、deploy 或 release
- **返回：** backend `8f506a0`、frontend `105b364`、root `e912bcd`；三个 workspace clean，root checkpoint 已形成并推送
- **Lead 独立核对：** pass；重读两个 HEAD、父提交、三点 diff、路径列表、包含关系、clean 状态与命令退出码
- **只读 Agent findings：** 无；未派遣 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-plus/common/file_store/*_test.go</Path>` | writable | 三种对象存储的 hermetic upload contract |
| `<Path>go-admin-plus/common/file_store/testdata/**</Path>` | writable | 小型合成上传 fixture |
| `<Path>go-admin-plus/test/characterization/**</Path>` | writable | API、数据、启动/关闭和迁移前 fixture 基线 |
| `<Path>go-admin-ui-plus/tests/e2e/mocked/baseline-contract.spec.ts</Path>` | writable | Admin 核心正常/失败 UI 基线 |

- **read-only 修改：** 无
- **未声明路径：** 无
- **生成文件/锁文件：** 无；`dist/`、`test-results/` 和 backend build binary 均为既有 ignore 输出，未提交

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-002 | live 临时后端 + route/envelope characterization | 登录、菜单、授权拒绝、Product CRUD、任务查询、上传路由与进程停止 | pass |
| AC-003 | Admin mocked Playwright + 现有全量 E2E | 登录/菜单、受限权限、错误/断网恢复、CRUD、任务页面 | pass |
| 用户数据隔离与 cleanup | `t.TempDir`、随机 loopback、合成 fixture、进程完成信号 | 未读取或修改仓库用户数据库；临时进程和数据有界清理 | pass |
| 默认对象存储测试 hermetic | KODO/OBS/OSS 本地 fake | `go test ./common/file_store` 无公网、真实凭据或 skip | pass |
| 升级前 SQLite fixture | SQL import、schema/data 查询、固定摘要 | 迁移版本 `1786700000000`、Product 数据与 ownership/soft-delete 字段 | pass |
| 路径与 implementation commit | Git 三点 diff、`git diff --check`、parent/HEAD 核对 | backend/frontend 非空 commit；全部项目写入位于 writable paths | pass |
| required E2E | current-workspace `pnpm e2e` | `136 passed (3.4m)` | pass |
| Ticket/Map/Evidence/root result 一致 | SpecDev 状态与根 checkpoint | result `e912bcd` 已写回；父 HEAD 包含 result，finalization 仅含 SpecDev 投影 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `go test ./common/file_store ./test/characterization -count=1` | backend current-workspace / macOS arm64 | pass | storage 与 characterization 均通过 |
| 临时导入 `pre_phase1.sql` 后查询 schema/data | backend current-workspace / SQLite CLI | pass | 表、两个索引、迁移版本、Product code 和 `deleted_at=0` 可读 |
| `go test ./...` | backend `8f506a0` / macOS arm64 | pass | 默认全量不再依赖真实云端；characterization 通过 |
| `make build` | backend `8f506a0` / macOS arm64 | pass | CGO-disabled server binary 构建通过 |
| `pnpm exec playwright test tests/e2e/mocked/baseline-contract.spec.ts --project=mocked` | frontend `105b364` / macOS arm64 | pass | 4 passed |
| `pnpm test:unit` | frontend `105b364` / macOS arm64 | pass | 27 files、220 tests passed |
| `pnpm type-check` | frontend `105b364` / macOS arm64 | pass | `vue-tsc` 与 node TypeScript 通过 |
| `pnpm lint` | frontend `105b364` / macOS arm64 | pass | 0 errors、29 个既有 warnings |
| `pnpm build:prod` | frontend `105b364` / macOS arm64 | pass | 3063 modules；保留既有 CSS pseudo-element warning |
| `pnpm e2e` | frontend `105b364` / current-workspace | pass | required E2E：136 passed (3.4m) |

- **失败后修复与重跑：** Qiniu fake 初次缺少 SDK response protocol header、进程测试初次错误假设 `/info` envelope 与失败登录业务码、前端初次使用错误 captcha path/dialog label；均按当前公共行为修正后重跑通过。双轴审查进一步修正 SQLite schema/version 真实性与 early-exit cleanup 二次等待风险。
- **受控反向验证：** 临时把 Product route 预期改为 `/api/v1/demo-product-contract-break`，定向 Go test 以 missing route 失败；临时把 cookie 预期改为 `contract-break-token`，Playwright 以 expected/received token 不同失败。两处均用补丁恢复，随后定向测试分别通过，且 `git diff --exit-code` 证明两个子仓库 tree 回到 checkpoint。
- **未运行检查：** Windows/Linux 原生 runner 与 Web UI 对 live API 的组合项目；T-01 当前无对应 Playwright live project，live API 行为由真实临时进程测试覆盖，平台原生验证归后续 Ticket。
- **E2E：** required，Lead 在 current workspace 执行，passed。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `750537c...8f506a0`；frontend `1d1af04...105b364`
- **来源：** root/backend/frontend `AGENTS.md`、Go/Playwright/ESLint/TypeScript 配置、SpecDev code-review standards 与 Fowler smell baseline
- **结果：** pass
- **Findings 与修正：** readiness 前进程退出会先消费 `waitCh`，cleanup 可能二次等待；增加独立 `doneCh` 和有界 cleanup 等待后通过。最终未发现 hard violation 或仍成立的判断性 smell。

### 规范轴

- **固定输入与来源：** 同上固定 diff；Spec、T-01、Tickets Map、Goal Plan G0/AC-002/AC-003
- **结果：** pass
- **Findings 与修正：** 初版 migration fixture 的 NULL 约束和版本值未精确匹配现有 GORM schema；改为实际表/索引/版本并通过临时 SQLite import。最终路径、IN/OUT、正常/失败行为和 Gate 要求一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `34524fd5d6ca17f39ae435eea997feaac340762c`；先存 Speculo refresh 已在独立 commit 处置 |
| Implementation/source SHA | backend `8f506a0e6fca22969ab376af6d4c51a3efe517f0`；frontend `105b364112d3dfd131bbace8a77b8ad124b4ce36` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | G0 全量 Go/unit/type/lint/build 与 required E2E passed |
| E2E disposition | required：T-01 定义全 change 行为基线 |
| E2E result | passed：136 mocked Admin E2E；live 后端进程 characterization passed |
| Parent result/re-read | result `e912bcdca930c87e69c3073b58d045e2617c1d1e`；父 HEAD 包含 result，backend/frontend gitlink 分别为 `8f506a0`/`105b364` |

## 8. 偏差与决策

- **偏差：** 无；未改变生产 API、数据或用户可见行为
- **记录：** 不适用
- **批准来源及影响：** 用户授权 current workspace 逐 Ticket提交，并在 2026-08-25 最新请求中明确要求本次 commit push；未使用 worktree、PR、deploy 或 release 动作

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 全量 E2E 存在既有 Element Plus/Vite console warning；本 Ticket 未在 Windows/Linux runner 复跑。
- **后续 Ticket：** G0 已关闭；进入 T-02
- **监控或回滚触发：** 任一固定 route/envelope/UI/fixture 或默认 hermetic test 漂移即阻断后续 Gate；可分别回滚两个子仓库 implementation commit
- **Source commits：** backend `8f506a0e6fca22969ab376af6d4c51a3efe517f0`；frontend `105b364112d3dfd131bbace8a77b8ad124b4ce36`
- **Parent result：** `e912bcdca930c87e69c3073b58d045e2617c1d1e`
- **Source workspace：** current root/backend/frontend `main`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-01.md</Path>`
