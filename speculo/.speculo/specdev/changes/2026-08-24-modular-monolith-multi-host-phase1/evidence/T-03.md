# Evidence: T-03 - 建立 Server/Desktop 基础设施适配与可恢复迁移

- **Change：** `2026-08-24-modular-monolith-multi-host-phase1`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/ticket/03-platform-adapters-and-migrations.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / root、backend `main`
- **Base/implementation/candidate/result SHA：** root `bb367669d621ead495ac63061c387b3ae45aeca5`；backend `d413003a399515d702359c5655ff3ac67f0d2c86` -> `7c1c4aa7510b064b94a61c5c1e0ad69b70c7880b`；candidate `not-applicable`；root result `fa5dc13efdf2af797b447961ce1997636b1f0cfe`
- **状态：** done

## 1. 实现摘要

- 建立 Server/Desktop profile：Server 使用 PostgreSQL、Redis、持久 FileStore 和可信 Host tenant resolver；Desktop 使用单连接 SQLite、内存 Cache/Queue、AppData FileStore 与固定 `local` 租户。
- 建立路径受限 FileStore、反序且只关闭一次的依赖聚合器，以及可被 context 取消的 Queue/Jobs 生命周期；HTTP Job 的请求与退避同样响应取消。
- 迁移 runner 以有序、去重、事务化 ledger 执行同一批不可变版本；Desktop 迁移前备份数据库/WAL/SHM，备份或迁移失败均 fail closed 并保留恢复材料。
- 针对 PostgreSQL driver 1.6.2 的 `DROP INDEX CURRENT_SCHEMA().name` DDL 缺陷，在 runner 内对对应已发布版本加入窄兼容 migrator，未修改历史 migration 文件。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `root`
- **Dispatch Packet/checkpoint：** Lead direct；root base `bb36766`，backend base `d413003`
- **允许动作：** current workspace T-03 writable paths、implementation commit，以及用户本轮明确授权的 push；无 worktree、PR、deploy、release 或生产迁移
- **返回：** backend `7c1c4aa`、root `fa5dc13`；backend clean；23 个实现文件均位于 T-03 writable/shared paths
- **Lead 独立核对：** pass；重读固定 diff、历史 migration 零 diff、路径清单、真实 PostgreSQL 日志、全量命令退出码和 root gitlink
- **只读 Agent findings：** 无；未派遣 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>go-admin-plus/internal/platform/**</Path>` | writable | FileStore、依赖集合与反序关闭合同 |
| `<Path>go-admin-plus/internal/profile/**</Path>` | writable | Server/Desktop profile、SQLite 选项与备份恢复 |
| `<Path>go-admin-plus/internal/tenant/**</Path>` | writable | 固定租户与可信 Host 解析 |
| `<Path>go-admin-plus/app/jobs/**</Path>` | writable | context 生命周期、动态任务与 HTTP 取消 |
| `<Path>go-admin-plus/cmd/migrate/**</Path>` | shared:T-03 | 事务 runner、CLI 错误传播、双方言 fixture 与 PostgreSQL DDL 兼容 |

- **read-only 修改：** 无；`cmd/migrate/migration/version/**` 固定 diff 为零
- **未声明路径：** 无
- **生成文件/锁文件：** 无；临时 PostgreSQL 构建、数据与日志已移入 macOS 废纸篓，未提交

## 4. 验收与合同映射

| Contract / Acceptance | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-010 / 双方言迁移与恢复 | SQLite fixture、真实 PostgreSQL 17 | 首次升级、二次幂等、事务 rollback、备份失败/迁移失败数据保留 | pass |
| AC-011 / 生命周期有界 | Queue/Jobs/HTTP context tests、race | cancel 后 worker/cron/in-flight HTTP 退出，无永久阻塞 | pass |
| AC-015 / FileStore 隔离 | LocalFileStore contract | Put/Open/Delete、覆盖恢复、遍历与 symlink escape 拒绝 | pass |
| AC-017 / 离线基础设施 | Desktop profile/network scan | SQLite、内存 Cache/Queue、固定租户和本地文件不依赖公网 | pass |
| Server profile contract | PostgreSQL 17 + Redis 8.10 integration | migration、cache、queue、files、tenant 与 close 全链通过 | pass |
| 路径/历史版本/result 一致 | Git 与 SpecDev 审计 | backend `7c1c4aa`、root `fa5dc13`，无历史 migration 或越界路径 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `CGO_ENABLED=1 go test ./... -count=1` | backend current-workspace / macOS arm64 | pass | 全量包与 characterization 通过 |
| `CGO_ENABLED=1 go test -race ./internal/platform ./internal/profile ./internal/tenant ./app/jobs ./cmd/migrate/migration ./cmd/migrate -count=1` | backend current-workspace / macOS arm64 | pass | 受影响包无 race |
| `go vet ./...`、`CGO_ENABLED=1 go build ./...` | backend current-workspace / macOS arm64 | pass | 静态检查与本机构建通过 |
| `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build ./...` | backend current-workspace | pass | Linux AMD64 无 CGO build 通过 |
| `GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build ./...` | backend current-workspace | pass | Windows AMD64 无 CGO build 通过 |
| 环境门控 `TestServerProfileAndPublishedMigrationsOnPostgres` | 临时 PostgreSQL 17.11 + Redis 8.10 | pass | 首次/二次 migration、Cache/Queue/File/Tenant/Close 通过 |
| `git diff --exit-code 37ef739^ -- cmd/migrate/migration/version`、path audit、`git diff --check` | backend fixed diff | pass | 已发布版本未变、无越界路径或 whitespace error |

- **失败后修复与重跑：** 真实 PostgreSQL 首次暴露 driver 生成非法 `DROP INDEX CURRENT_SCHEMA().index`；runner 对唯一受影响的不可变版本注入窄 migrator 兼容，保留原始 Dialector 给早期历史迁移，随后定向集成与全部门禁重跑通过。
- **未运行检查：** Windows/Linux 原生运行时与安装包；本 Ticket 只验证可构建性，原生发行验收分别属于 T-10/T-12。
- **E2E：** required；Lead 在 current workspace 使用真实 PostgreSQL/Redis 与本地 SQLite upgrade/recovery fixture 执行，passed。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `d413003a399515d702359c5655ff3ac67f0d2c86..7c1c4aa7510b064b94a61c5c1e0ad69b70c7880b`
- **结果：** pass
- **Findings 与修正：** 修正旧 Jobs 永久阻塞与动态任务 context 丢失、文件覆盖恢复、Server 建连 fail-closed/secret redaction，以及 PostgreSQL DropIndex schema DDL；最终无 hard violation。

### 规范轴

- **固定输入与来源：** 同上固定 diff；Spec、T-03、Tickets Map、Goal Plan G2 与 AC-010/011/015/017
- **结果：** pass
- **Findings 与修正：** Server/Desktop adapter、迁移追加性、备份恢复、网络边界、路径隔离、关闭错误聚合与 writable/shared ownership 均满足；历史版本保持字节不变。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | root `bb367669d621ead495ac63061c387b3ae45aeca5` |
| Implementation/source SHA | backend `7c1c4aa7510b064b94a61c5c1e0ad69b70c7880b` |
| Candidate branch/workspace | current；candidate `not-applicable` |
| Method/conflicts | direct-parent；无代码冲突 |
| Integration checks | 全量 Go、race、vet、本机 build、Linux/Windows AMD64 build、SQLite/PostgreSQL/Redis contracts passed |
| E2E disposition | required：跨数据库、文件、队列和后台生命周期 |
| E2E result | passed：真实 PostgreSQL 17/Redis 与 SQLite upgrade/recovery fixture |
| Parent result/re-read | result `fa5dc13efdf2af797b447961ce1997636b1f0cfe`；父 HEAD 包含 result，backend gitlink 为 `7c1c4aa` |

## 8. 偏差与决策

- **偏差：** 无；未改变业务 HTTP、已发布 migration 或真实用户数据
- **记录：** 不适用
- **批准来源及影响：** 用户授权 current workspace 逐 Ticket commit，并在本轮明确要求 commit push；不扩展到 PR、deploy、release 或生产迁移

## 9. 残余风险与交付定位

- **残余风险/已知限制：** PostgreSQL 兼容层与 driver 1.6.2 的已知 DDL 形态绑定，未来升级 driver 后应由依赖维护 Ticket 删除；原生 Windows/Linux runtime 仍由后续交付 Ticket 验证。
- **后续 Ticket：** T-04（尚未激活）
- **监控或回滚触发：** migration version/result、备份保留、adapter init、queue/jobs shutdown timeout；可回滚 root `fa5dc13` 与 backend `7c1c4aa`
- **Source commit：** backend `7c1c4aa7510b064b94a61c5c1e0ad69b70c7880b`
- **Parent result：** `fa5dc13efdf2af797b447961ce1997636b1f0cfe`
- **Source workspace：** current root/backend `main`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-modular-monolith-multi-host-phase1/evidence/T-03.md</Path>`
