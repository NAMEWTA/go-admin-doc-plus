# Evidence: T-17 - Current Full-Stack Generator Scaffold

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/17-generator-current-scaffold.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `eb27e0a295975d6514e1424ec4d7672490b32cd6` / `b4ff4a068625a9d216d4fbab27c63c738d2f82d3` / `b4ff4a068625a9d216d4fbab27c63c738d2f82d3`
- **Status:** done

## 1. 实现摘要

Generator 现在稳定生成 modular OpenAPI、Go model/repository/service/HTTP/permissions、SQLite/PostgreSQL migrations 与测试、headless domain、Web Domain controller/client/tests，以及使用 AppPage、QueryBar、TableToolbar、Pagination、EmptyState、StatusTag、FormDialog、FormGrid 的当前列表/表单页面。生成 transport 仍由 canonical contract generator 产生，preview/write、allowlist、冲突拒绝和原子写入语义保持不变。

生成的 HTTP client 只保留正则 allowlist 后的 trace reference；controller 对 stale read、权限失败和不可用投影进行 fencing。旧 `.generated-records`、`.editor`、手写分页、tenant、Redis、JWT、common/utils 与 provider-specific IAM adapter 均有负向扫描。

## 2. Lead Dispatch And Candidate Return

Source worktree 从 `main@ace37852eb50b399e6b482ef84682771ab6e9c5f` 启动，吸收 Lead 治理提交后形成 source `eb27e0a`。最新冻结父提交为 `c2bf56c82f015341a2b3bb45df8cf680b315ae01`；无冲突 merge 产生 candidate `b4ff4a0`，tree `e80fffca93cfb0f047901fdea6dae8acdeacf610`，验证后 fast-forward 晋升为 result。

## 3. 修改范围与路径所有权

实现改动限定 Generator templates/renderer/compile gate/tests、contract CLI Windows wrapper/tests 与 architecture checks。DEV-17-001 只把本地 CLI 路径规范化为合法 `file:///` URL。DEV-17-002 只让 Node 22 在 Windows 安全启动 `.cmd`，拒绝 shell metacharacter 参数；lint/generate 参数、输出和路径守卫未改变。Demo、共享 UI、业务模块、canonical contract 与生成物均未修改。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| 完整垂直树 | 24 个 base files + 5 个 canonical generated transport/client files；路径清单精确测试 | pass |
| 双方言与数据层 | SQLite/PostgreSQL migration/provider/service tests；真实 PG 17.11 marker | pass |
| 公共合同 | fragment lint、generate、generate --check、Go/TS transports | pass |
| 当前前端 | domain/Web Domain typecheck/tests、共享组件页面、workspace check、双 App build | pass |
| 权限与安全 | capability registration、read/write/delete fencing、安全 trace reference、禁止旧模式扫描 | pass |
| 原子写入 | path/conflict/mutation/root replacement/reparse root/cleanup tests | pass |

## 5. Workspace Verification

以下检查分别在 `source-worktree` 与独立 `parent-candidate` 执行；`current-workspace` 只负责治理记录和验证通过后的 main fast-forward。

| Check | Result |
|---|---|
| canonical isolated generator | pass，532.715s；fresh fixture |
| real PostgreSQL | `speculo_t17_generator` at `127.0.0.1:55432`；marker pass |
| generated Go/domain/Web Domain | test/build/typecheck pass |
| generated workspace and dual App | check:workspace + build pass |
| contract CLI | 7/7 pass；含 Windows Node 22 shim 回归与负向合同 |
| architecture checks | 19/19 + repository check pass |
| generator non-canonical full package | pass；atomic/conflict/security tests included |
| SpecDev validator | 0 error / 0 warning |

Windows 不能创建普通 symlink 的测试账户使用无提权 directory junction 验证 reparse root 拒绝；gate 内替换已打开根目录时 Windows OS 在替换动作处拒绝，writer 返回 gate failure 且 staging 清空。Unix 仍断言替换成功后 inode 锚定返回 invalid，两条路径都不发布输出。

### Candidate Verification And Attempts

Candidate attempt 1 在未显式准备 candidate `node_modules` 时，canonical transport 的 2 分钟产品超时耗尽并返回非零；未调整超时或记录为通过。执行 `pnpm install --frozen-lockfile --ignore-scripts` 后，attempt 2 原样运行并通过：canonical fixture 539.539s、真实 PG marker、生成 Go/TS/Vue tests/build、生产 WorkspaceCompileGate、non-canonical generator 全包、19/19 architecture 和 SpecDev 0/0。

## 6. 双轴审查

- **代码轴：** 完整文件树、双方言、OpenAPI/transport、权限、当前 UI、trace allowlist、stale fencing、原子冲突/清理和 Windows 工具解析均无未解决 finding。
- **规范轴：** preview/write 语义未变；不生成 tenant/Redis/JWT/compat 聚合；canonical contracts、生成物、Demo、共享 UI 和产品 composition 保持所有权边界；真实浏览器流程仍归 T-19。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `c2bf56c82f015341a2b3bb45df8cf680b315ae01` |
| Source | `eb27e0a295975d6514e1424ec4d7672490b32cd6` |
| Candidate/result | `b4ff4a068625a9d216d4fbab27c63c738d2f82d3` |
| Tree | `e80fffca93cfb0f047901fdea6dae8acdeacf610` |
| Method/conflicts | merge-commit / none |
| Attempts | 2；attempt 1 prerequisite failure，attempt 2 pass |
| Promotion | main fast-forward applied |
| E2E | not-required；真实浏览器 Generator preview/write 归 T-19 |

## 8. 偏差与决策

DEV-17-001 只修复 Windows 本地 contract CLI 的标准 file URL；DEV-17-002 只修复 Node 22 `.cmd` 启动并拒绝 shell metacharacter。两项均由 `USER-DECISION:all-approved` 覆盖，没有修改产品超时、合同输出、路径守卫或外部行为。candidate attempt 1 的前置依赖失败被保留为非零，frozen install 后才执行并记录 attempt 2 通过。

## 9. 残余风险与交付定位

Generator 明确输出 scaffold，不自动修改 product registry 或 App Shell composition；这是 `new-business-module` 当前合同，新增模块仍需显式产品组合。T-18 必须把真实 PostgreSQL、安全供应链和新架构检查设为 required CI；T-19 必须在真实浏览器验证 Generator preview/gate fail/reconfigure/write。source、candidate、失败 attempt 工作区与保护 stash 全部保留。
