# Evidence: T-10 - Desktop Native First Setup And Session Conversion

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/10-desktop-first-setup.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `d7e9dc9e34878fddd7333c365443dbca4df9a104` / `f4a1d481821c3dab100f7c60d6a377eabbeacac3` / `f4a1d481821c3dab100f7c60d6a377eabbeacac3`
- **Status:** done

## 1. 实现摘要

产品组合新增仅受 loopback control token 保护的 `POST /__desktop/first-setup` 私有路由。Go 处理器以数据库事实区分 `required`、`login-required` 与不一致状态，严格限制 content type、JSON 字段、正文大小和执行时限，并复用现有 IAM Bootstrap 与 Session service。Bootstrap 已提交但 Session 建立失败时保留管理员，返回普通登录恢复状态；重启后数据库事实阻止重复 Bootstrap。

Rust 新增两条专用 Tauri command。密码和原始 Session/CSRF 只在 native command 与 Rust vault 短暂存在并执行清零；写入 Stronghold 后必须立即验证 identity 与 navigation manifest，失败时 best-effort logout 并清空 vault。WebView 只得到严格校验后的公开 Profile、setup state 与完成/恢复信号，不经通用 business bridge，也不写浏览器持久存储。Desktop App 的 setup gate 覆盖加载、首次表单、部分成功恢复、不可用重试和工作区切换。

## 2. Lead Dispatch And Candidate Return

Source worktree 从治理父提交 `4a607834` 返回 `d7e9dc9`。Lead 在冻结的同一父提交上无冲突构建 merge candidate `f4a1d48`；source 与 candidate tree 均为 `3b49657a0b76043a601557a17b2e8008e2a414ae`。父分支随后 fast-forward 到该 result。

## 3. 修改范围与路径所有权

产品 diff 限定为 15 个已批准路径：Go Desktop setup composition/tests、Rust first-setup/proxy/main/build/capability，以及 Desktop production/native-e2e App 与 setup client/gate。Tauri autogenerated permission 由 build manifest 生成并校验，继续遵循既有 ignore 策略，未强制纳入 Git。公开 Bootstrap endpoint、客户端持久 setup flag、WebView password/Session/CSRF 投影和通用 Desktop setup bridge 扫描均为零。

G0 保护对象 `39480546c2a2e2ff386a176f4278c6183a0e868c` 已 apply 到 source 且未 drop；`canonical_release_directory`、`canonical_paths_equal`、Windows extended-path normalization 与对应测试逐段核对均保留。continuation stash `f593f53b2850063f415c9cd521ab6aaa8a99c510` 同样保持可达。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-004 | 空库 state、一次性 Bootstrap、首 Session、二次提交 conflict | pass |
| AC-005 | Session 注入失败保留管理员；重启后进入 `login-required` | pass |
| AC-031 | production Desktop SQLite 私有路由与 native setup gate | pass |
| Secret boundary | password/raw Session/CSRF 只在 native/Rust；公开 Profile 才进入 WebView | pass |
| Fail closed | 严格/限长输入、数据库不一致、identity/workspace 验证失败均拒绝完成 | pass |

## 5. Workspace Verification

以下检查在 `source-worktree` 和 Lead-owned `parent-candidate` 按适用范围执行；`current-workspace` 只负责 result 晋升与治理记录。

| Check | Result |
|---|---|
| pnpm frozen install、lint、complete typecheck | pass |
| full Vitest | 37 files / 227 tests pass |
| workspace boundary | 10/10 pass |
| Web + Desktop production build | pass；Desktop production verifier pass |
| native harness Vite compile | success；唯一临时输出目录已删除，不构成 required E2E |
| Rust fmt/default/native tests | pass；25/25 与 26/26 |
| Rust clippy | pass；all targets/features，`-D warnings` |
| Rust/Tauri custom-protocol no-bundle build | source 与 candidate pass |
| Go focused tests、vet、CGO-disabled cmd build | pass |
| architecture/governance/Speculo validator | pass；validator `0 error / 0 warning` |
| setup persistence/log/secret scan | zero findings |

Candidate 默认的超长 `.integration/.../target` 路径触发 Windows linker `LNK1104`，改用已验证的较短 `CARGO_TARGET_DIR` 后同一 candidate 的 Rust tests、clippy 与 Tauri no-bundle 全部通过；这是输出路径限制，不是源代码差异。candidate sidecar SHA-256 为 `395BE15C98DEB122FEDC116D2FB0E0ECF38F8E7FC65F6CF88A2C35E4D55A6C48`。

完整 Go/Node suite 仍复现父分支已有 Windows 基线：sidecar pipe readiness EOF、Desktop temp/root、Generator file URL/符号链接权限、SQLite UNC、Desktop backup、Files `% literal.txt`，以及 Node 43/44 中由 T-20 所有的 Windows/macOS artifact path；T-10 focused product tests 均通过。Go race 在当前 shell 无法启动，因为 PATH 中没有 `gcc`；未把该环境限制记为通过。

## 6. 双轴审查

- **代码轴：** setup 状态机、bounded decode、一次性提交、partial success、vault commit/clear、identity/workspace verification、command allowlist 与敏感值清零无未解决 finding。
- **规范轴：** 后端数据库仍是 setup 事实源；不存在公开初始化后门；失败不回滚已提交管理员；WebView 不持有高权限秘密；真实 macOS native window/Keychain/restart 验证继续由 T-20 承担。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `4a60783454d94a7de84de0628ea1d9aa4ef5d4c7` |
| Source | `d7e9dc9e34878fddd7333c365443dbca4df9a104` |
| Candidate/result | `f4a1d481821c3dab100f7c60d6a377eabbeacac3` |
| Tree | `3b49657a0b76043a601557a17b2e8008e2a414ae` |
| Method/conflicts | merge-commit / none |
| Promotion | main fast-forward applied |

## 8. 偏差与决策

DEV-10-001 开放完成 setup 所需的现有 Desktop composition/private-route、production App、Rust proxy、Tauri build/capability/autogenerated permission 路径。批准没有改变安全合同：setup 只能处于 loopback control-token 边界内，秘密不得进入 WebView 持久状态，不得新增公开 Bootstrap API 或客户端事实旗标。

## 9. 残余风险与交付定位

本 Ticket 的确定性 native seam 已完成，E2E not-required。T-20 必须在真实 macOS Tauri/Keychain/native window 环境证明首次设置、重启和恢复；T-14/T-15/T-16/T-19 仍是其其余依赖。source/candidate worktree、旧候选和两份保护 stash 全部保留，未执行清理。
