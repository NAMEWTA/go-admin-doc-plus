# Evidence: T-16 - Tools Pages UI

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/16-tools-pages-ui.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `40babcc42d4e589ea90e13514d6e12af441045f2` / `6b9fe9c136ca6cee1c3d9746eb80d1a2c3f2083d` / `6b9fe9c136ca6cee1c3d9746eb80d1a2c3f2083d`
- **Status:** done

## 1. 实现摘要

Files、Generator 与 Demo 页面迁移到 T-11 的 AppPage、QueryBar、TableToolbar、Pagination、EmptyState、StatusTag 与 FormDialog 工作台组件。Files 对 content、账号 quota 与全局 capacity 进行 typed 分类；quota/capacity 只停止上传，列表、下载、删除和刷新继续可用，且不推断或展示其他账号与全局精确容量。

Generator 明确呈现 source/configure/preview/complete 步骤和必需门禁状态。gate 失败保留纯文本 preview 与安全参考编号，不隐式重复 write；用户可返回配置并重新 preview。Demo 保持权限门控、标准查询/排序/分页、批量删除和显式共享表单对话框。

## 2. Lead Dispatch And Candidate Return

Source worktree 从 T-16 启动提交 `f070eaa` 返回实现提交 `40babcc`。DEV-16-001 治理提交 `8a421e3` 是 Lead-owned candidate 的直接父；无冲突 merge 产生 candidate `6b9fe9c`，并仅快进晋升为 result。candidate tree 为 `ed1b58243c56dd0ef4941e1540943c5e3ddd9b6b`。

## 3. 修改范围与路径所有权

产品改动限定 Files/Generator/Demo domain 与 Web Domain 的 controller、transport、component 和 tests。DEV-16-001 只更新 shell visual contract 中 Demo 共享 FormDialog 识别及 Generator preview 成功逻辑的源码排版断言。App Shell、generated clients、共享 UI、依赖 manifest/lockfile 与 generator templates 均未修改。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-014~016 | 现有 manifest 单一路由、共享 Web/Desktop composition 与双宿主 build | pass |
| AC-017~018 | T-11 共享工作台组件、稳定表格滚动、responsive constraints 与 visual contracts | pass |
| AC-027~029 | quota/capacity typed 分类；容量保护只禁上传；下载/删除和 projection 保持 | pass |
| AC-038 | 三领域 transport/controller/UI、Problem trace 与完整 workspace 回归 | pass |

## 5. Workspace Verification

以下 required 检查分别在 `source-worktree` 与独立 `parent-candidate` 执行；`current-workspace` 负责候选晋升和治理记录：

| Check | Result |
|---|---|
| pnpm 11.1.3 frozen install | pass |
| Files domain/Web Domain | 3 domain；20 Web Domain tests pass |
| Generator domain/Web Domain | 2 domain；5 Web Domain tests pass |
| Demo domain/Web Domain | 2 domain；25 Web Domain tests pass |
| full Vitest | 41 files / 256 tests pass |
| workspace lint + complete typecheck | pass |
| workspace boundary | 10/10 pass |
| Web + Desktop production build | pass；Desktop production assets pass |
| diff/path checks | 19 paths，全部属于 ticket writable paths 或 DEV-16-001；`git diff --check` pass |
| legacy page scan | 三目标页面的旧 dialog/search/upload/grid class 为零 |

完整 Node shell 基线在主分支既有 43/44：唯一失败是 Windows 执行只接受 POSIX `/repository/...` 的 macOS Desktop artifact-path assertion，实际为 `D:\repository\...`。该跨平台修复与真实 native 验证仍由 T-20 负责，没有把非零结果记为通过。

## 6. 双轴审查

- **代码轴：** quota/capacity/content 分类、upload-only fencing、release actions、preview/write 分离、gate recovery、raw preview 文本渲染、trace allowlist、CRUD projection 和权限失效无未解决 finding。
- **规范轴：** 未展示后端未提供的容量精确值；preview 不等于 write；generated wire、共享 UI 和 App Shell 保持只读；真实上传/生成/CRUD 浏览器证明继续归 T-19。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `8a421e3ffaf662ae51307a2c9d9fc5ea90b1361c` |
| Source | `40babcc42d4e589ea90e13514d6e12af441045f2` |
| Candidate/result | `6b9fe9c136ca6cee1c3d9746eb80d1a2c3f2083d` |
| Tree | `ed1b58243c56dd0ef4941e1540943c5e3ddd9b6b` |
| Method/conflicts | merge-commit / none |
| Promotion | main fast-forward applied |

## 8. 偏差与决策

DEV-16-001 没有保留旧 `management-dialog-backdrop` 标记，而是让既有 visual contract 识别 T-11 共享 FormDialog；同时把 Generator 单行源码断言拆为两个语义等价断言。其他 visual contract 与测试未变。

## 9. 残余风险与交付定位

T-17 必须以本 result 的真实 Generator/UI/权限形态冻结当前全垂直脚手架。T-19 必须更新旧 Files/Demo/Generator 直接挂载 driver 的 class selectors，并在 SQLite/PostgreSQL 真实后端验证 quota/capacity、download/delete、gate fail/reconfigure/write 与 CRUD；T-20 负责已知 Windows 路径测试归属及真实 macOS Tauri 证明。source、candidate、旧工作树与保护 stash 全部保留。
