# Evidence: T-05 - 账号 Tombstone 与文件处置生命周期

- **Change：** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/05-account-deletion-lifecycle.md</Path>`
- **Lead：** `codex-root`
- **Workspace/branch：** `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-05` / `speculo/2026-08-29-productization-and-ui-reconstruction/T-05`
- **Base/source/candidate/result SHA：** `f4ab97365c43f40328d8d120108f410a9c0869f4` / `7bb3432787b9d48c23d9a65457fa8d0897cb1968` / `f3dcfc1092e8652274c2ea9e6ee9b6e460299685` / `f3dcfc1092e8652274c2ea9e6ee9b6e460299685`
- **状态：** done

## 1. 实现摘要

新增 IAM 账号删除状态机、可靠 Outbox 事件和 Files 幂等消费者。账号由 active/disabled 进入 deletion-pending 时立即撤销 Session 并推进 generation；transfer 要求有效目标，purge 要求显式确认；queued 可取消，claimed 后只允许前向完成或重试。Files 在自身事务边界内完成 owner 转移、容量计数调整或物理文件清理，IAM 在消费完成后匿名化个人字段并保留稳定 `audit_ref`。最后启用系统管理员、已删除账号、恢复阻断和目标账号有效性均由数据库与用例共同约束。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct (`codex-root`)
- **返回：** source commit `7bb3432787b9d48c23d9a65457fa8d0897cb1968`；T-09 migration composition checkpoint `5b09c2b66a9ae6ddbec78c40973034314333ef8f`
- **Lead 核对：** source-worktree、候选、路径合同、双方言迁移、真实 PostgreSQL、race、故障恢复和父分支包含关系均已复核
- **E2E：** not-required；管理交互与真实浏览器文件流程归 T-14/T-19

## 3. 修改范围与路径所有权

- IAM 新增 `administration/deletion.go` 及定向 SQLite/PostgreSQL 测试，并新增双方言 `0060-account-lifecycle` migration，版本为全局唯一 `7120000000000`。
- Files 新增 `account_lifecycle.go`、定向故障/幂等/PostgreSQL 测试及 Files-owned inbox migration，版本为全局唯一 `7520000000000`。
- Files migration 路径位于 Ticket 已批准的 `account_lifecycle*` wildcard；IAM 仅通过公开 port 与 versioned event 协作，不导入 Files 私有实现或写 Files 私表。
- T-09 owner 在独立 composition workspace 中将 IAM/Files 两个 provider 加入产品 registry；公共 OpenAPI、transport、旧硬删除收缩和 UI 均未由 T-05 修改，分别保留给 T-08/T-14。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-013 | pending 时 Session revoke、generation advance、recovery block；取消仅限 claim 前 | pass |
| AC-023 | 最后启用系统管理员、无效/自 transfer、deleted target 均稳定拒绝 | pass |
| AC-024 | transfer 按稳定锁序更新 Files owner、quota/counter 与生命周期 inbox | pass |
| AC-025 | purge 显式确认，部分物理删除失败后保持可重试且最终幂等清理 | pass |
| AC-026 | consumer 完成后匿名化，稳定 `audit_ref` 保留，已删除账号不可恢复 | pass |

## 5. Workspace Verification

| 命令或检查 | 环境 | 结果 |
|---|---|---|
| targeted IAM/Files lifecycle、outbox、migration tests；关键用例 `-count=20` | source-worktree / Windows / SQLite | pass |
| `go test ./internal/modules/iam/... ./internal/modules/files/... ./internal/app/product -count=1` | source + candidate | pass |
| changed-scope `go test -race ... -count=1` | source + candidate / WinLibs GCC 16.1 | pass |
| IAM claim/cancel 20 轮竞争与 Files transfer/purge contract | source + candidate / isolated PostgreSQL 17.11 | pass |
| `go vet ./...`、`task architecture:check`、`go mod tidy` | source + candidate | pass；tidy 无 diff |
| `go test ./... -count=1` 与 `go test -tags sqlite ./... -count=1` | candidate | executed；新增生命周期、migration、product 包 pass；失败集合与 source/父基线一致 |

全量失败被明确归因到既有 Windows sidecar/Desktop、Generator URL/symlink、SQLite UNC/backup，以及 T-08-owned 旧稳定 Session HTTP 断言和既有 Files SQLite contract；候选未新增失败。该归因不把失败命令伪装为绿色，T-18~21 required gates 仍保持不变。

## 6. 双轴审查

- **代码轴：** `f4ab97365c43f40328d8d120108f410a9c0869f4..7bb3432787b9d48c23d9a65457fa8d0897cb1968` 的事务边界、账号锁顺序、最后管理员不变量、Outbox 原子性、Files 幂等恢复、匿名化与 migration ordering 均通过；无未解决代码 finding。
- **规范轴：** AC-013、AC-023~026 的 SQLite、真实 PostgreSQL、竞态和部分 purge 故障证据通过；HTTP/UI/E2E 依赖保持在 T-08/T-14/T-19。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `ee2208680bc6ae69296dbd1c4855820ac3f36d76` |
| Source SHA | `7bb3432787b9d48c23d9a65457fa8d0897cb1968` |
| Candidate workspace | `specdev-worktree/.integration/2026-08-29-productization-and-ui-reconstruction/T-05` |
| Candidate SHA/tree | `f3dcfc1092e8652274c2ea9e6ee9b6e460299685` / `98bcd636ff4d75e989006b05a55ae67a1ee52a7b` |
| Method/conflicts | merge-commit / 无冲突 |
| Promotion | passed；`main` fast-forward 到 `f3dcfc1092e8652274c2ea9e6ee9b6e460299685` |

## 8. 偏差与决策

T-05 的 Files inbox migration 位于已批准的 `account_lifecycle*` writable wildcard。产品 registry 属于 T-09，因此按照已批准的 DEV-09-001，由 T-09 owner 在 composition workspace 提交 `5b09c2b66a9ae6ddbec78c40973034314333ef8f`，只注册两个新 migration provider 并把产品矩阵从 14 更新为 16；该 checkpoint 随 T-05 候选验证，不改变 T-09 尚未关闭的运行时职责。

## 9. 残余风险与交付定位

source、composition 与 candidate worktree 均保留且 clean，不执行 branch/worktree/stash 清理。T-08 仍须原子发布 HTTP/OpenAPI 并移除旧硬删除与 batch-delete；T-09 仍须接线 worker/运行拓扑；T-14/T-19 负责管理 UI 与真实文件工作流。T-05 result 为 `f3dcfc1092e8652274c2ea9e6ee9b6e460299685`。
