# Evidence: T-20 - Required Desktop Native E2E

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/20-desktop-native-e2e.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `841573cfeab758c80efc079fe594550e8a49ad92` / `ed58fc7184e5542a834af75d9a560c02bcff62b7` / pending
- **Status:** in_progress

## 1. 实现摘要

Desktop native runner 现在是 required/non-skip：缺少 `GO_ADMIN_DESKTOP_NATIVE_E2E=1`、非 macOS、任一 phase 失败或缺少最终精确 marker 均非零。artifact path 按目标平台使用确定性的 POSIX/Windows path API，并根据 macOS `arm64/x64` 动态选择 sidecar triple。production verifier 对相关 Tauri config/capability 做精确键集合与值校验，包括唯一隐藏 `main-window`、`960x640` 最小尺寸、禁用 devtools、无 remote origin、单一 sidecar、精确 13 项 command capability allowlist 与 production byte marker；反向测试会拒绝远程来源、权限增长、可见启动窗口或 devtools。

产品 App Shell 组合既有 theme controller，提供 system/light/dark 三态 Lucide 图标控制并持久化偏好。真实 Web Shell E2E 在 SQLite/PostgreSQL 及 desktop/mobile 视口选择 dark mode，并在既有 deep-link reload 后核对根 theme token 与选中状态。native test build 使用隔离 Tauri identifier/WebKit data store，验证 dark mode 跨 Stronghold/SQLite restart 恢复，并仅清理精确 theme/run marker；该原生行为仍需 macOS 实跑确认。

真实窗口路线新增两类空库首次设置：正常 setup 后以 Stronghold Session 重启；以及在临时 Stronghold snapshot/父目录注入只读故障，使管理员已提交而 vault 写入失败，观察恢复 UI、恢复临时权限并以合法隔离 Keychain account 重启登录。窗口 frame 在 setup 与 workspace 要求至少 `960x640`，全部表单通过聚焦原生 AX submit button 后发送 Enter 键提交。既有 migration failure、登录、权限/数据范围、Session revoke/relogin、单实例、CRUD、SQLite 停机验证、Stronghold restart、logout restart、loopback、production marker、进程/Keychain/临时目录清理继续保留。

## 2. Lead Dispatch And Candidate Return

Source 从 `main@22bbbe6385e6ec52e1df2ce6650cdc8e6409e126` 启动，初始 checkpoint 为 `151bc21c42e46201418d3e42d00b8d48bfaf4679`；Context 修正 checkpoint 为 `53663e6b65c7445026017e2c6058d43e792179de`；确定性 recovery fault checkpoint 为 `526f7f0e62574efdbc564f2cf7e753d93c752390`；固定枚举诊断 checkpoint 为 `892e27a98710dba45717a01bdb602ef49143fa4c`；恢复转换 phase checkpoint 为 `841573cfeab758c80efc079fe594550e8a49ad92`。规范 candidate 为 `ed58fc7184e5542a834af75d9a560c02bcff62b7`、tree `0a47b32c65612a18b718cac67936dedb447381d9`，包含最新治理父 `main@48f744d` 与最终 source。

第一次 merge 命令误在 main worktree 运行，形成 `4870670`；`8e8855b` 随即以前向 revert 恢复主线文件树。候选快进到该父提交后显式 reapply，未重写历史、未删除 checkpoint、未把误操作标为晋升。

## 3. 修改范围与路径所有权

基础改动限定 T-20 writable paths：`go-admin-plus-ui/tests/e2e/desktop/**` 与 `go-admin-plus-ui/apps/admin-desktop/scripts/verify-build.mjs`。DEV-20-002 精确开放 `packages/app-shell/src/product/ProductWorkspace.vue`、App Shell manifest、对应 lock importer 与 test-only native App；DEV-20-003/004 只开放 Web Shell driver 的 lazy route DOM 等待与主题重载验证；DEV-20-006 只开放 `main.rs` 的 `native-e2e` Context data-store 注入。production Context/config、Desktop entry、sidecar、Session API 和业务页面均未修改。

## 4. 验收与合同映射

| Contract | Candidate evidence | State |
|---|---|---|
| AC-004 / AC-005 | empty setup、vault failure recovery、restart phases | prepared; macOS pending |
| AC-016~017 | Web product shell/viewport 实跑通过；native route/workspace、AX keyboard submit、960x640 window frame phases | Web pass；native prepared/macOS pending |
| AC-018 | Web dark mode 在 SQLite/PostgreSQL、desktop/mobile 选择并跨 deep-link reload 恢复；native isolated store/restart phase | Web pass；native prepared/macOS pending |
| AC-031 | SQLite-only host、loopback、single-instance、vault、精确 production capability allowlist | portable configuration checks pass; macOS pending |
| AC-036 | required flag、non-skip、exact success marker | portable negative checks pass; macOS pending |

## 5. Workspace Verification

`source-worktree` 只完成非原生便携检查，其 native E2E 不得作为 Gate；`parent-candidate` 已完成下列 Windows 可执行回归。真实 macOS command 为 not-run/pending。

| Check | Result |
|---|---|
| frozen install | pass；25-project workspace，201 packages reused，无 lock drift |
| Desktop runner self-tests | pass；20/20，0 skip |
| full Node shell | pass；47/47，旧 T-20 两项红灯清零并覆盖精确配置增长负例 |
| Vitest | pass；41/41 files，256/256 tests |
| typecheck / lint | pass |
| real Web Shell E2E | pass；`WEB_SHELL_E2E_PASS profiles=sqlite,postgres viewports=desktop,mobile`；含 dark-mode deep-link reload 恢复、reduced-motion、横向溢出与非空截图 |
| Go product + fixture | pass |
| Rust fmt/tests/clippy | pass；27/27，all features/all targets，warnings denied；新增 Context 行为测试精确核对唯一 `main` 窗口和 `[u8; 16]` data store |
| Tauri production-like build | pass；release no-bundle + `custom-protocol`，短 target path |
| production assets/config | pass；Web dist、host、sidecar 均无 native test marker；identifier、CSP、窗口、bundle、sidecar 与 13 项 capability allowlist 精确匹配；remote origin、权限增长、可见窗口和 devtools 反向样例被拒绝 |
| host SHA-256 | `926486F45089A1125CC0163CC6EEA320FF8F926F843644F473FE425052BFF2E7` |
| sidecar SHA-256 | `4594804E7A99F4C55DA1C7B557BB3DDF714936EC2CD8E5461FE0C299B26F7346` |
| non-mac required run | expected nonzero；`desktop native E2E requires the macOS native runner` |
| macOS required run | pending / failed；最新 Actions `33504927079` 在 macOS 15.7.7 arm64、AX enabled 上通过 sidecar/UI/cargo release prebuild 并启动完整 runner；“管理员已创建”恢复态等待已通过，随后 restore/click/login 三步仍共用 `first-setup-recovery-state`，未产生 pass marker |

第一次 Tauri CLI 命令漏传 `custom-protocol`，被 release compile guard 非零拒绝；补齐产品要求的 feature 后同一候选通过。首次失败保留为命令选择红灯，没有重标为通过。

## 6. 双轴审查

- **代码轴：** required/non-skip、目标架构、临时故障注入、精确 marker、production byte scan、bounded diagnostics、host/sidecar/Keychain/workspace cleanup 均有静态或便携反向验证。
- **规范轴：** 产品代码与安全边界未变；故障只作用于随机临时目录和测试 Keychain namespace；Windows 结果没有冒充真实 macOS 证明。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `48f744d8aaf734b7a6cc28c7e79895d55ea31d3b` |
| Source | `841573cfeab758c80efc079fe594550e8a49ad92` |
| Candidate | `ed58fc7184e5542a834af75d9a560c02bcff62b7` |
| Tree | `0a47b32c65612a18b718cac67936dedb447381d9` |
| Containment | latest governance parent `48f744d` 与 source `841573c` 均为 candidate ancestor；普通 merge，无历史重写 |
| Method/conflicts | audited merge/revert/reapply history / none |
| Portable verification | passed |
| Native E2E / result / promotion | pending / null / not applied |

## 8. 偏差与决策

DEV-20-001 记录误合并与前向恢复：main 的 `4870670` 由 `8e8855b` 立即撤销文件树，candidate lineage 从 `e6a83fc -> 2066c7d -> 84a3b8e` 延续到初始 `2bce131`，在包含 frozen parent 后重应用实现并合入 source；DEV-20-006 correction 再以普通 merge 延续到 `24bf45f`，未重写历史。DEV-20-002/003/004 分别精确授权主题产品组合、Web lazy-route DOM 等待和 Web 主题重载实证。首次真实 Web 红灯为 PostgreSQL/mobile 用户搜索尚未可用，修正为等待同一稳定 lazy route DOM 后通过，没有增加 retry、sleep、skip 或放宽断言。该过程使用既有 local candidate authorization；除已记录的隔离 probe branch 外没有其他 remote write，也没有部署、发布、生产迁移或清理。

## 9. 残余风险与交付定位

Lead 对当前可见执行通道完成审计：仓库 Actions API 返回 0 个 self-hosted runner；当前 Windows host 没有 Hyper-V、VirtualBox 或 VMware macOS VM；现有 SSH profiles 没有可用的无交互 macOS 会话。DEV-20-005 因此授权从规范 candidate 派生 workflow-only probe，在 hosted macOS 上实测当前环境；该 probe 不进入 source/规范 candidate，不使用 release environment、production secrets 或 artifact upload。

| Hosted attempt | Probe/run | Environment | First honest result |
|---|---|---|---|
| 1 | `b442acf` / Actions `33495120070` | macOS 15.7.7 arm64；`System Events` UI elements enabled=`true` | fail；全新 Go module cache 与 sidecar builder 的 `GOPROXY=off` 合同不匹配，首个 phase 为 `native-sidecar-build`；尚未进入窗口行为 |
| 2 | `2fd7026` / Actions `33495429463` | macOS 15.7.7 arm64；AX enabled；锁定 Go modules 已预取 | fail；sidecar/native UI build 已越过，首个 phase 为 `native-host-build`；runner 的受控诊断隐藏 compiler/linker 内容，故最后一次 attempt 先用同参数可见 cargo prebuild 取得精确诊断或预热成功后继续完整 Gate |
| 3 | `f090da4` / Actions `33496830623` | macOS 15.7.7 arm64；AX enabled；同参数 sidecar/UI/cargo prebuild 可见 | fail；sidecar 与 native UI build 通过，cargo 在 `tauri::generate_context!()` 报 `expected [u8; 16], found Vec<u8>`；锁定 `tauri-utils 2.9.3` 对 `data_store_identifier: Option<[u8; 16]>` 使用 `opt_vec_lit`，尚未启动原生窗口 |
| 4 / DEV-20-006 attempt 1 | `c44421a` / Actions `33500422333`，job `99832226086` | macOS 15.7.7 arm64；AX enabled；修正后 sidecar/UI/cargo release prebuild 通过 | fail；完整 native runner 已启动，首个行为红灯为 `first-setup-recovery-state`：故障注入后的提交动作完成，但 90 秒内未观测到“管理员已创建”恢复状态；无 skip、无 pass marker，DEV-20-006 尚余 2 次逐个归因 attempt |
| 5 / DEV-20-006 attempt 2 | `e5a9496` / Actions `33502511066`，job `99838877690` | macOS 15.7.7 arm64；AX enabled；确定性 path collision 候选的 sidecar/UI/cargo release prebuild 通过 | fail；完整 native runner 再次启动，11:34:26Z 至 11:41:02Z 的 required gate 首个行为红灯仍被折叠为 `first-setup-recovery-state`；无 skip、无 pass marker。Stronghold 保存目标已是目录冲突，但 runner 未区分超时后的 setup/workspace/login/unavailable/unknown 页面，DEV-20-006 尚余 1 次逐个归因 attempt |
| 6 / DEV-20-006 attempt 3 | `ce8e524` / Actions `33504927079`，job `99846682939` | macOS 15.7.7 arm64；AX enabled；固定枚举 candidate 的 sidecar/UI/cargo release prebuild 通过 | fail；required gate 从 12:01:50Z 运行至 12:07:47Z，仍输出无后缀 `first-setup-recovery-state`。新 classifier 只会在“管理员已创建”等待失败时返回后缀，因此该等待已通过；第一诚实红灯位于随后 restore snapshot、点击“进入登录”或 30 秒登录页等待，但三步未分 phase。无 skip、无 pass marker；DEV-20-006 的 3 次 attempt 已用尽 |

Windows candidate 上的同 feature/config release build通过 schema/feature 编译阶段后命中既有长工作树 `LNK1104` linker path 基线；该平台结果不能归因 macOS host-build 红灯，也未被记为 native pass。

DEV-20-005 与 DEV-20-006 各自的 3 次授权 attempt 已全部用尽且均保留为红灯。DEV-20-006 的 feature-only Context 修复已稳定越过编译；第三次执行又证明确定性 snapshot path collision 后真实 UI 到达“管理员已创建”恢复态。无后缀失败不是该等待再次超时，而是 runner 在恢复 snapshot、点击继续和等待登录页三步沿用了旧 phase。DEV-20-007 因此只开放这三步的固定 phase 归因与 self-test，最多 3 次逐个归因的同一 hosted macOS probe execution；尚未产生 pass marker。

唯一未关闭 Gate 是完整真实 macOS native execution。下一 candidate 只在既有恢复 snapshot、点击继续、等待登录页之前设置 `first-setup-recovery-restore`、`first-setup-recovery-continue`、`first-setup-recovery-login` 固定 phase；不打印输入值、秘密、路径或任意 UI 内容，不增加 retry、sleep、skip，不改变 30/90 秒上限和产品行为。下一次仍要求精确输出 `DESKTOP_NATIVE_E2E_PASS runtime=tauri-native profile=sqlite skipped=0`，并核对窗口 frame、键盘提交、主题重启恢复及无残留窗口、sidecar、测试 Keychain 与临时目录。该结果通过前，T-20、G7、T-21 和整体 change 均不得完成或晋升。
