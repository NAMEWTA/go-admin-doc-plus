# Evidence: T-20 - Required Desktop Native E2E

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/20-desktop-native-e2e.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `4a34b13e3f1698fa7c03f15078d241119322933f` / `84a3b8e93742d5e64e141bfa0c9eaf5f8d8eb773` / pending
- **Status:** in_progress

## 1. 实现摘要

Desktop native runner 现在是 required/non-skip：缺少 `GO_ADMIN_DESKTOP_NATIVE_E2E=1`、非 macOS、任一 phase 失败或缺少最终精确 marker 均非零。artifact path 按目标平台使用确定性的 POSIX/Windows path API，并根据 macOS `arm64/x64` 动态选择 sidecar triple。production verifier 结构化校验唯一隐藏 `main-window`、`960x640` 最小尺寸、单一 sidecar、精确 13 项 command capability allowlist 与 production byte marker；新增反向测试会拒绝权限增长或可见启动窗口。

真实窗口路线新增两类空库首次设置：正常 setup 后以 Stronghold Session 重启；以及在临时 Stronghold snapshot/父目录注入只读故障，使管理员已提交而 vault 写入失败，观察恢复 UI、恢复临时权限并以合法隔离 Keychain account 重启登录。窗口 frame 在 setup 与 workspace 要求至少 `960x640`，全部表单通过聚焦原生 AX submit button 后发送 Enter 键提交。既有 migration failure、登录、权限/数据范围、Session revoke/relogin、单实例、CRUD、SQLite 停机验证、Stronghold restart、logout restart、loopback、production marker、进程/Keychain/临时目录清理继续保留。

## 2. Lead Dispatch And Candidate Return

Source 从 `main@22bbbe6385e6ec52e1df2ce6650cdc8e6409e126` 返回最终 checkpoint `4a34b13e3f1698fa7c03f15078d241119322933f`。规范 candidate 为 `84a3b8e93742d5e64e141bfa0c9eaf5f8d8eb773`、tree `1ec7ed2f03a2ed88d4a53f12a1c2c3cc1b8d53f3`，包含 frozen parent `main@a1b54c12380cd5afbc30039b5a35151e0524fae5` 与 source。

第一次 merge 命令误在 main worktree 运行，形成 `4870670`；`8e8855b` 随即以前向 revert 恢复主线文件树。候选快进到该父提交后显式 reapply，未重写历史、未删除 checkpoint、未把误操作标为晋升。

## 3. 修改范围与路径所有权

修改严格限定 T-20 writable paths：`go-admin-plus-ui/tests/e2e/desktop/**` 与 `go-admin-plus-ui/apps/admin-desktop/scripts/verify-build.mjs`。产品 `main.rs`、Tauri product code、WebView、sidecar 和业务页面均只读；页面静态断言只对齐当前原生 label 语义，没有修改产品可访问性合同。

## 4. 验收与合同映射

| Contract | Candidate evidence | State |
|---|---|---|
| AC-004 / AC-005 | empty setup、vault failure recovery、restart phases | prepared; macOS pending |
| AC-016~018 | native route/workspace、AX keyboard submit、960x640 window frame phases | prepared; macOS pending |
| AC-031 | SQLite-only host、loopback、single-instance、vault、精确 production capability allowlist | portable configuration checks pass; macOS pending |
| AC-036 | required flag、non-skip、exact success marker | portable negative checks pass; macOS pending |

## 5. Workspace Verification

`source-worktree` 只完成非原生便携检查，其 native E2E 不得作为 Gate；`parent-candidate` 已完成下列 Windows 可执行回归。真实 macOS command 为 not-run/pending。

| Check | Result |
|---|---|
| frozen install | pass；25-project workspace，201 packages reused，无 lock drift |
| Desktop runner self-tests | pass；19/19，0 skip |
| full Node shell | pass；46/46，旧 T-20 两项红灯清零 |
| Vitest | pass；41/41 files，256/256 tests |
| typecheck / lint | pass |
| Go product + fixture | pass |
| Rust fmt/tests/clippy | pass；26/26，all features/all targets，warnings denied |
| Tauri production-like build | pass；release no-bundle + `custom-protocol`，短 target path |
| production assets/config | pass；Web dist、host、sidecar 均无 native test marker；identifier、窗口、sidecar 与 13 项 capability allowlist 精确匹配；权限增长和可见窗口反向样例被拒绝 |
| host SHA-256 | `3223A7CAC6878AA305DE39469BF37752E18E1065B4128AC84A1DDF129CCE953B` |
| sidecar SHA-256 | `4594804E7A99F4C55DA1C7B557BB3DDF714936EC2CD8E5461FE0C299B26F7346` |
| non-mac required run | expected nonzero；`desktop native E2E requires the macOS native runner` |
| macOS required run | pending / not-run；本 Windows host 无 Tauri/Keychain/native-window 环境 |

第一次 Tauri CLI 命令漏传 `custom-protocol`，被 release compile guard 非零拒绝；补齐产品要求的 feature 后同一候选通过。首次失败保留为命令选择红灯，没有重标为通过。

## 6. 双轴审查

- **代码轴：** required/non-skip、目标架构、临时故障注入、精确 marker、production byte scan、bounded diagnostics、host/sidecar/Keychain/workspace cleanup 均有静态或便携反向验证。
- **规范轴：** 产品代码与安全边界未变；故障只作用于随机临时目录和测试 Keychain namespace；Windows 结果没有冒充真实 macOS 证明。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `a1b54c12380cd5afbc30039b5a35151e0524fae5` |
| Source | `4a34b13e3f1698fa7c03f15078d241119322933f` |
| Candidate | `84a3b8e93742d5e64e141bfa0c9eaf5f8d8eb773` |
| Tree | `1ec7ed2f03a2ed88d4a53f12a1c2c3cc1b8d53f3` |
| Containment | frozen parent `a1b54c1` 与 source 均为 candidate ancestor；后续 main 治理提交不改变候选产品树 |
| Method/conflicts | audited merge/revert/reapply history / none |
| Portable verification | passed |
| Native E2E / result / promotion | pending / null / not applied |

## 8. 偏差与决策

DEV-20-001 记录误合并与前向恢复：main 的 `4870670` 由 `8e8855b` 立即撤销文件树，candidate lineage `e6a83fc -> 2066c7d -> 84a3b8e` 在包含 frozen parent 后重应用实现并合入最终 source。该过程使用既有 local candidate authorization；没有 history rewrite、remote write、部署、发布、生产迁移或清理。

## 9. 残余风险与交付定位

唯一未关闭 Gate 是真实 macOS native execution。下一恢复点是在 macOS、已授予终端 Accessibility 权限的 `parent-candidate@84a3b8e` 执行 `GO_ADMIN_DESKTOP_NATIVE_E2E=1 node go-admin-plus-ui/tests/e2e/desktop/run.mjs`，要求最终精确输出 `DESKTOP_NATIVE_E2E_PASS runtime=tauri-native profile=sqlite skipped=0`，并核对窗口 frame、键盘提交及无残留窗口、sidecar、测试 Keychain 与临时目录。该结果通过前，T-20、G7、T-21 和整体 change 均不得完成或晋升。
