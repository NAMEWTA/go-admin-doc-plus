# Evidence: T-08 - 汇合公共 OpenAPI 合同与 HTTP Adapters

- **Change：** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/08-public-contract-integration.md</Path>`
- **Lead：** `codex-root`
- **Workspace/branch：** `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-08` / `speculo/2026-08-29-productization-and-ui-reconstruction/T-08`
- **Base/source/candidate/result SHA：** `03b6426434b7ddea085ea1ecd0589de1bb2de444` / pending / pending / pending
- **状态：** in_progress

## 1. 实现摘要

执行中；已建立 canonical OpenAPI 结构化红灯并发布 Session heartbeat/renew、账号组织与删除生命周期、角色五态数据范围、Files 容量 Problem，生成 Go/TypeScript transport 并接入指定 HTTP adapter。

## 2. Lead Dispatch And Candidate Return

Lead direct (`codex-root`)；source、candidate 与 result 待验证后补录。E2E disposition 为 not-required，真实浏览器与 native 验证归 T-19/T-20。

## 3. 修改范围与路径所有权

canonical/generated/HTTP adapter 由 T-08 所有。DEV-08-001 临时把 IAM administration consumer contract 和 Web Domain consumer 纳入本 Ticket，只进行 breaking wire consumer 迁移，不提前实施 T-14 页面重构。

## 4. 验收与合同映射

执行中，完成后补录 AC-009~013、AC-019~029、AC-038 的命令与结果。

## 5. Workspace Verification

执行中；当前结构化 OpenAPI contract 与定向 IAM/Files Go tests 已通过，完整矩阵待补录。

## 6. 双轴审查

执行中，source commit 后补录代码轴与规范轴结果。

## 7. Integration Verification

candidate 尚未建立。

## 8. 偏差与决策

**DEV-08-001（ticket path ownership，approved）：** breaking OpenAPI 生成后，`<Path>go-admin-plus-ui/packages/web-domains/iam/src/administration/web-administration-client.ts</Path>` 仍调用已删除的单删与 batch-delete 路径，旧 controller 类型也只接受 `all/self`。`pnpm --dir go-admin-plus-ui typecheck` 以三个相关 TypeScript 错误失败；若等待 T-14，T-08 -> T-09 -> T-12 -> T-14 的 DAG 会形成无法关闭的 consumer pause。选项为回退 breaking contract、允许不可编译候选、或由 T-08 原子迁移最小 consumer；前两项违反零兼容与 Gate，因此采用第三项。用户 `2026-09-01T01:36:14+08:00` 以“都批准”批准；授权路径、限制与协调结果已写回 Ticket，最终处理结果待 source/candidate 验证后补录。

**DEV-08-002（ticket + migration/release seam，approved）：** T-03 只保存 `csrf_hash`，但 T-08 必须在不写 Session 的 `current` 中返回稳定 family CSRF；hash 无法恢复 plaintext，`AuthorizeRequest` 也因此返回空 CSRF，导致管理 HTTP 回归稳定失败。回退 rotate-on-read 违反 DEC-007，省略 header 违反公共合同，因此采用前向 schema 修正：新 Session 保存可返回的 CSRF 值，旧活动 Session 统一撤销并重新登录。用户 `2026-09-01T01:57:08+08:00` 以“都批准”批准；授权路径、零兼容恢复和 Gate 已写回 Ticket，最终双方言与候选结果待补录。

**DEV-08-003（ticket regression ownership，approved）：** 全量双方言 Go 回归确认前向 CSRF migration 的固定总数断言仍为 16，且 Audit 真实 IAM adapter 仍要求只读请求轮换凭据。用户已批准全部必要偏差；T-08 仅更新对应两个测试文件为 17 个 migration 与稳定 CSRF，不改产品或 Audit 实现。

## 9. 残余风险与交付定位

T-14 仍拥有完整 IAM/Organization route、页面、表单和不可逆交互；T-08 不以最小 consumer 迁移替代其验收。source/candidate worktree 和 stash 均保留，不执行清理。
