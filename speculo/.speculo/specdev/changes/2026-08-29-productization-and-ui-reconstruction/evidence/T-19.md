# Evidence: T-19 - Required Web Browser E2E

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/19-web-browser-e2e.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `df8e88b4dbd35807164d6c02dcedf7d62375da48` / `84ac0ca12c82668f2e308b5c4b9c033e2193b9b7` / `84ac0ca12c82668f2e308b5c4b9c033e2193b9b7`
- **Status:** done

## 1. 实现摘要

新增统一 required Web runner，顺序执行 Product Shell、IAM Session、IAM Administration、Organization、Audit、Settings、Scheduler、Demo、Files 与 Generator 共 10 个 suite。runner 要求真实 Chromium、显式 disposable PostgreSQL URL、Generator PostgreSQL 工具目录和 required opt-in；任一 skip、缺少精确 pass marker、子进程非零或超时均失败。

Product Shell 使用真实产品组合验证 desktop/mobile viewport、深链、history、授权导航和 403；Session 使用双标签验证稳定 CSRF、显式 renew 与 revoke；业务 suite 使用真实 HTTPS host、生产 handler/service、SQLite/PostgreSQL 隔离数据库和真实页面路由完成权限、CRUD、审计、调度、设置、文件生命周期与 Generator 写入。所有 runner 统一有界等待、脱敏诊断和子进程清理。

## 2. Lead Dispatch And Candidate Return

Source worktree 从 `main@399daa86eb96656d933370f0f21adbe51d04594c` 启动，最终 source 为 `df8e88b4dbd35807164d6c02dcedf7d62375da48`。Lead 从冻结父提交 `e50295fa607d69e2a32b835c046205a32966c14c` 建立六个无冲突 candidate checkpoint；前五个分别保留首个诚实失败，第六个 candidate/result `84ac0ca12c82668f2e308b5c4b9c033e2193b9b7`、tree `c1030174f634c353aa15c51fe5f9ace9fa69a60a` 完成全部回归与 required E2E。通过后以 `--ff-only` 晋升 main。

最终规范 candidate branch/workspace 为 `speculo/integration/2026-08-29-productization-and-ui-reconstruction/T-19` 与 `specdev-worktree/.integration/2026-08-29-productization-and-ui-reconstruction/T-19`；五个历史红灯 checkpoint 继续保留为 attempt worktree/branch，不做清理。

## 3. 修改范围与路径所有权

实现限定于 Web E2E runner/tests、产品浏览器 harness 和 ticket 已授权测试接缝。DEV-19-001~010 由 `USER-DECISION:all-approved` 覆盖，只包含一个 ProductWorkspace 瞬态授权快照修正、当前 fixture migration/service 组合和一条过期 route 视觉合同修正；未放宽产品 API、迁移内容、授权规则、Generator 输出合同或 required/non-skip 语义。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-011 / AC-013 | 真实双标签 CSRF、显式 renew、稳定 Session 与 revoke | pass |
| AC-014 / AC-015 | 产品深链、route-derived view 与 history | pass |
| AC-016~018 | 真实 Web 产品壳、desktop/mobile viewport 与可操作 UI | pass |
| AC-036 | required 真实 Chromium、SQLite/PostgreSQL、0 skip | pass |
| AC-038 | 当前 generated client、生产 handler 与 UI workflow | pass |

## 5. Workspace Verification

`source-worktree` 完成 suite 独立调试和非 E2E 检查，其 E2E 不得作为 Gate；独立 `parent-candidate` 完成冻结安装、回归与最终 required E2E；`current-workspace` 只负责治理记录、资源泄漏复核和 main fast-forward。

| Check | Result |
|---|---|
| frozen install | pass；25-project workspace，201 packages reused，无 lock drift |
| targeted Go | pass；10 个 T-19 harness/package target |
| lint/typecheck | pass；完整 workspace 与全部 E2E TypeScript config |
| Vitest | pass；41/41 files，256/256 tests |
| T-19 Node runner tests | pass；14/14 |
| production build | pass；Web/Desktop build 与 Desktop production asset verifier |
| required Web E2E | pass；`REQUIRED_WEB_E2E_PASS suites=10 profiles=20 skipped=0` |
| Generator runtime | pass；`GENERATOR_E2E_PASS profiles=sqlite,postgres generated_postgres_runtime=pass` |
| cleanup verification | pass；无 T-19 子进程，`speculo_t19_web` 无非系统 schema |
| SpecDev validator | pass；0 error / 0 warning |

完整 Node shell matrix 在 Windows 为 42/44；两个失败均由 T-20 Desktop 接管：旧 Demo SKU accessibility label 断言，以及用 Windows path semantics 执行 macOS artifact path 断言。它们没有被记为 T-19 pass。

### Attempts And Red Evidence

| Attempt | Candidate | First honest failure |
|---|---|---|
| 1 | `176eadea` | IAM Administration 仍挂载旧 tabs/deletion flow |
| 2 | `d4576071` | Organization 仍挂载旧 tabs 而非产品路由 |
| 3 | `dac3c3d3` | Audit 仍期待已删除的 rotate-on-read Session 行为 |
| 4 | `caddf671` | Settings fixture 使用 Windows 不兼容 build entry |
| 5 | `22a0e098` / tree `fa1aabcc` | 最终回归暴露过期 paused-tab 视觉合同 |
| 6 | `84ac0ca1` / tree `c1030174` | green；10 suites、20 profiles、0 skip |

六次 attempt 超过原计划上限 4；每次都在首个真实红灯处停止，未重写历史结果。`USER-DECISION:all-approved` 授权把本 ticket 上限扩到 6 并保留全部 checkpoint。所有 worktree、branch、artifact、stash 与诊断目录均保留。

## 6. 双轴审查

- **代码轴：** required/non-skip、精确 pass marker、双方言隔离、真实路由/权限/CRUD、双标签 Session、Generator generated PostgreSQL runtime、超时和清理均有正反验证。
- **规范轴：** 产品 API 与安全不变量未放宽；偏差严格限定 fixture/测试接缝；Web gate 没有替代 native macOS gate。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `e50295fa607d69e2a32b835c046205a32966c14c` |
| Source | `df8e88b4dbd35807164d6c02dcedf7d62375da48` |
| Candidate/result | `84ac0ca12c82668f2e308b5c4b9c033e2193b9b7` |
| Tree | `c1030174f634c353aa15c51fe5f9ace9fa69a60a` |
| Method/conflicts | merge-commit / none |
| Attempts | 6 candidate checkpoints；前五个保留红灯，最终 checkpoint 完整通过 |
| Containment | parent 与 source 均为 candidate ancestor |
| Promotion | main fast-forward applied |
| E2E | required；10 suites、20 profiles、0 skip |

## 8. 偏差与决策

DEV-19-001~010 与 ticket-local 六次 attempt 上限由 `USER-DECISION:all-approved` 覆盖。前者只补齐现行 fixture/服务组合和过期测试合同；后者只允许继续建立诚实 candidate checkpoint，不允许跳过、降级或把失败重标为通过。远程 push、部署、发布、生产迁移和清理均未授权。

## 9. 残余风险与交付定位

T-19 Web gate 已完成。真实 macOS Tauri/Keychain/native-window 证据仍唯一归 T-20，不能由 Windows Web、Rust 单测或静态检查替代。完整 Node shell 的两个 T-20-owned 红灯将由下一 ticket 修正并重新验证。
