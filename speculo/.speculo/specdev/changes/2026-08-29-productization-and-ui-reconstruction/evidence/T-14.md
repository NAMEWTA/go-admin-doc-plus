# Evidence: T-14 - IAM and Organization Administration UI

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/14-iam-organization-ui.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `1ceefc1ef67f762cde9e662217aa5bde60994ddf` / `49c690cdfbcad27e681f4e7575f8ed7245448c88` / `49c690cdfbcad27e681f4e7575f8ed7245448c88`
- **Status:** done

## 1. 实现摘要

IAM 与 Organization 页面现在只由精确二级 route 派生视图，旧的页内平行 tabs 和硬编码默认页已删除。用户编辑增加一个主部门和多个岗位的 typed mutation；角色编辑支持 all、self、organization、organization-tree、custom 五种范围，并要求 custom 非空部门集合。

账号删除使用显式 transfer/purge 流程。transfer 排除当前账号作为接收目标；purge 需要页面确认与 destructive command 确认；启动结果、刷新状态和 queued 取消均由 controller 建模。claimed 后前端不再承诺取消，权限撤销或重新登录会清除删除投影，404/403/validation/conflict 映射为稳定反馈。

## 2. Lead Dispatch And Candidate Return

Source worktree 从治理提交 `78094f7` 返回实现提交 `1ceefc1`。DEV-14-001 治理提交 `f52c5cb` 是 Lead-owned candidate 的直接父；无冲突 merge 产生 candidate `49c690c`，并仅快进晋升为 result。candidate tree 为 `69a5a66f57c341108e20514d6710ad09b83ad292`。

## 3. 修改范围与路径所有权

产品改动限定 IAM administration domain/controller/component/tests、Organization route-derived component/tests，以及 DEV-14-001 明确允许的两个 Web Domain `package.json` 和对应 lockfile importer。App Shell、route manifest、生成 OpenAPI client、共享 UI、依赖版本和其他 lock importer 均未修改。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-014~016 | 五个业务子页面精确 route mapping；unknown/trailing-slash 不落默认页；Web/Desktop build | pass |
| AC-017~018 | 既有 token/management dialog/compact shell visual contracts 与 mobile boundary | pass |
| AC-019~022 | 五种 role scope、custom 非空集合、主部门与岗位 DTO 校验及 dedicated endpoint | pass |
| AC-023~026 | transfer/purge、二次确认、queued cancel、claimed refusal、刷新与权限撤销清理 | pass |
| AC-038 | IAM/Organization package、shell regression、typecheck 与双宿主 build | pass |

## 5. Workspace Verification

以下 required 检查分别在 `source-worktree` 与独立 `parent-candidate` 执行；`current-workspace` 负责候选晋升、基线归因和治理记录：

| Check | Result |
|---|---|
| pnpm 11.1.3 frozen install | pass |
| IAM domain/Web Domain | 15 domain tests；39 Web Domain tests；typecheck pass |
| Organization domain/Web Domain | 3 domain tests；11 Web Domain tests；typecheck pass |
| full Vitest | 39 files / 239 tests pass |
| workspace lint + complete typecheck | pass |
| workspace boundary | 10/10 pass |
| Web + Desktop production build | pass；Desktop production assets pass |
| diff/path checks | 16 product paths，全部属于 ticket writable paths 或 DEV-14-001；`git diff --check` pass |
| full Node shell suite | main 与 candidate 均 43/44；唯一失败为既有 T-20 Windows/macOS artifact-path assertion |

完整 `pnpm test` 的非零结果没有被记为通过：Node 用例期望 POSIX `/repository/...`，Windows 实际为 `D:\\repository\\...`；该失败在 T-14 改动前的主工作区独立复现。跨平台修复与真实 native 验证仍由 T-20 负责。

## 6. 双轴审查

- **代码轴：** route 精确匹配、输入去重/空值、mutation busy、projection repair、删除查询竞态、权限失效清理、404 分类和 secret 清理无未解决 finding。
- **规范轴：** 页面可见性不替代后端权限；generated wire 保持只读；purge 和 claim 边界没有被 UI 弱化；真实 API/route/删除浏览器证明继续归 T-19。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `f52c5cb8d2debadcf1f77b9113d1d4f2b7c66eef` |
| Source | `1ceefc1ef67f762cde9e662217aa5bde60994ddf` |
| Candidate/result | `49c690cdfbcad27e681f4e7575f8ed7245448c88` |
| Tree | `69a5a66f57c341108e20514d6710ad09b83ad292` |
| Method/conflicts | merge-commit / none |
| Promotion | main fast-forward applied |

## 8. 偏差与决策

DEV-14-001 仅为直接导入 `useRoute` 的两个 Web Domain 声明现有 catalog `vue-router` runtime dependency。lockfile 只新增两个目标 importer 的投影，无版本变化或其他 importer churn。

## 9. 残余风险与交付定位

T-19 必须在 SQLite/PostgreSQL 真实后端验证 route、权限变化、组织归属、五种 scope 与 transfer/purge/claim 浏览器流程并保存 trace/screenshot。T-20 负责 Windows 路径测试修正归属与真实 macOS Tauri viewport/native 证明。source、candidate、旧工作树与保护 stash 全部保留。
