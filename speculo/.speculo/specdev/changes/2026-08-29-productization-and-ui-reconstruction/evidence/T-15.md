# Evidence: T-15 - Operations Pages UI

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/15-operations-pages-ui.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `e567989e1fee295181c0aff23d14ff4a3837ac46` / `d727faaf259fc0fcf2d91bc5c4b54b8ff9a87c40` / `d727faaf259fc0fcf2d91bc5c4b54b8ff9a87c40`
- **Status:** done

## 1. 实现摘要

Settings 与 Scheduler 页面现在由精确二级 route 派生视图；unknown 与 trailing-slash 路径不会静默落入默认页。旧页面内平行 tab 状态已经删除，route 变化会加载当前投影并关闭失效表单。Audit 保持单一路由及只读/清理语义。

三个领域的 Problem 映射可保留经过格式和长度约束的 trace ID，并只向页面显示安全参考编号；raw detail 不进入 UI。controller 会在新操作、成功及本地失败时清除陈旧 reference，且不改变无 trace 时既有错误序列化。

## 2. Lead Dispatch And Candidate Return

Source worktree 从治理基线 `d15bcfb` 返回实现提交 `e567989`。DEV-15-001 治理提交后的 `2777238` 是 Lead-owned candidate 的直接父；无冲突 merge 产生 candidate `d727faa`，并仅快进晋升为 result。candidate tree 为 `87cf96ede609c9d36f7a72c6cd78f168340b9748`。

## 3. 修改范围与路径所有权

产品改动限定 Audit/Settings/Scheduler domain 与 Web Domain 的 controller、transport、component 和 tests，以及 DEV-15-001 明确允许的 Settings/Scheduler `package.json` 与对应 lockfile importer。App Shell、route manifest、generated clients、共享 UI、依赖版本和其他 lock importer 均未修改。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-014~016 | Settings/Scheduler 精确 route mapping；unknown/trailing-slash 不落默认页；双宿主 build | pass |
| AC-017~018 | 既有紧凑工作台、shared UI 与 mobile shell boundary 回归 | pass |
| AC-038 | 三领域 controller/transport/Problem 语义及完整 workspace 回归 | pass |

## 5. Workspace Verification

以下 required 检查分别在 `source-worktree` 与独立 `parent-candidate` 执行；`current-workspace` 负责候选晋升和治理记录：

| Check | Result |
|---|---|
| pnpm 11.1.3 frozen install | pass |
| Audit domain/Web Domain | domain 1；Web Domain 12 tests pass |
| Settings domain/Web Domain | domain 2；Web Domain 17 tests pass |
| Scheduler domain/Web Domain | domain 2；Web Domain 10 tests pass |
| full Vitest | 41 files / 248 tests pass |
| workspace lint + complete typecheck | pass |
| workspace boundary | 10/10 pass |
| Web + Desktop production build | pass；Desktop production assets pass |
| diff/path checks | 25 product paths，全部属于 ticket writable paths 或 DEV-15-001；`git diff --check` pass |

完整 Node shell 基线在主分支既有 43/44：唯一失败是 Windows 执行只接受 POSIX `/repository/...` 的 macOS Desktop artifact-path assertion，实际为 `D:\repository\...`。该跨平台修复与真实 native 验证仍由 T-20 负责，没有把非零结果记为通过。

## 6. 双轴审查

- **代码轴：** route 精确匹配、watch 加载、陈旧表单关闭、trace allowlist、raw detail 丢弃与 controller reference 生命周期无未解决 finding。
- **规范轴：** route 是页面唯一事实；Audit 与 runtime logs 语义继续分离；generated wire 与 App Shell 保持只读；真实 route/API 浏览器证明继续归 T-19。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `2777238e56edebf161961ba4507292150363af5e` |
| Source | `e567989e1fee295181c0aff23d14ff4a3837ac46` |
| Candidate/result | `d727faaf259fc0fcf2d91bc5c4b54b8ff9a87c40` |
| Tree | `87cf96ede609c9d36f7a72c6cd78f168340b9748` |
| Method/conflicts | merge-commit / none |
| Promotion | main fast-forward applied |

## 8. 偏差与决策

DEV-15-001 仅为直接导入 `useRoute` 的 Settings/Scheduler Web Domain 声明现有 catalog `vue-router` runtime dependency。lockfile 只新增两个目标 importer 的投影，无版本变化或其他 importer churn。

## 9. 残余风险与交付定位

T-19 必须用真实 router 与 SQLite/PostgreSQL API 验证 Audit/Settings/Scheduler deep-link、history、mutation 与 Problem reference；旧的直接 mount browser driver 必须迁移为 router harness。T-20 负责已知 Windows 路径测试归属与真实 macOS Tauri viewport/native 证明。source、candidate、旧工作树与保护 stash 全部保留。
