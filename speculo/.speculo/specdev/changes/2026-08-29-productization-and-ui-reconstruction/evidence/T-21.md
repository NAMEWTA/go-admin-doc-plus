# Evidence: T-21 - 文档收敛与三 Profile Clean-room

- **Change：** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/21-release-docs-clean-room.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/goal-plan.md</Path>`
- **Lead：** `codex-root`
- **Workspace/branch：** `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-21` / `speculo/2026-08-29-productization-and-ui-reconstruction/T-21`
- **Base/source/candidate/result SHA：** `67c09ac3daf58d6830da8fe72bd3672bc57b7040` / `c888fffec8ee2c2c5f21b7588128a7b3f9faa66c` / `b1821e0fe8a0f7fba1333113b951ee10e0011bff` / `b1821e0fe8a0f7fba1333113b951ee10e0011bff`
- **状态：** in_progress；completion audit reopened

## 1. 实现摘要

当前文档统一描述 Server SQLite、Server PostgreSQL 与 Desktop SQLite 的 migrate、bootstrap/首次设置、Doctor、serve/worker、登录、管理、重启、备份和恢复流程；示例只传 secret/DSN 文件引用，不包含固定凭据或 argv secret。PostgreSQL 迁移由离线命令或 Compose 一次性服务独占，API 与 worker 不隐式迁移。release policy 把 three-profile clean-room、`not-required` 的本地签名/公证结论和禁止本地发布写成可执行反向合同。

兼容扫描与 Generator fixture 具备 Windows 路径语义，根 typecheck 包含 Web Shell E2E，Files outbox 测试使用确定性时钟。最终审查还修正 sidecar builder 的 host/target 路径混淆：环境路径始终按构建主机解释，`GOOS/GOARCH` 才按目标解释，并由 POSIX host 交叉构建 Windows 目标的单测锁定。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead；未使用 subagent。
- **Dispatch Packet/checkpoint：** source 从 `67c09ac` 建立；实现 checkpoint 依次为 `1b5b7c1`、`3391294`、`408d9d2`、`06c5c58`、`b040a8d`、`c888fff`。
- **允许动作：** T-21 worktree changes、local source commit、Lead-owned candidate merge 与本地 parent fast-forward；不含 push、publish、deploy、production migration、archive 或 cleanup。
- **返回：** source `c888fff`，tracked clean；实际路径均属于 Ticket writable paths 或 DEV-21-001~005 的精确临时授权。
- **Lead 独立核对：** pass；读取完整 diff、路径列表、提交链、候选 tree、ancestor、clean status、release manifest 与 required runner 输出。
- **只读 Agent findings：** 未使用其他 agent。Lead 的标准轴发现 sidecar builder 按 target 解释 host path；`c888fff` 修正后复审通过。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>README.md</Path>`、`<Path>docs/**</Path>`、`<Path>database/README.md</Path>`、`<Path>deploy/**</Path>` | writable/shared:T-21 | 当前三 profile、运维、恢复与部署顺序 |
| `<Path>release/**</Path>`、`<Path>scripts/release/**</Path>` | writable/shared:T-21 | release policy、sidecar host path 与本地 rehearsal |
| `<Path>scripts/quality/docs-check*.mjs</Path>` | writable:T-21 | 文档正反合同与链接检查 |
| `<Path>scripts/quality/compatibility-zero*.mjs</Path>` | DEV-21-001/002 | Windows 路径归一化与两个精确负例 |
| `<Path>go-admin-plus-ui/package.json</Path>` | DEV-21-003 | 根 typecheck 纳入 Web Shell tsconfig |
| `<Path>scripts/contracts/modules.test.mjs</Path>` | DEV-21-004 | 跨平台绝对 fixture root |
| `<Path>go-admin-plus/internal/modules/files/account_lifecycle_test.go</Path>` | DEV-21-005 | outbox 测试注入既有固定时钟 |

- **read-only 修改：** 无未授权修改。
- **未声明路径：** 无。
- **生成文件/锁文件：** 无 tracked 生成或 lock drift；sidecar、release manifest 与 clean-room 输出均为 ignored disposable artifacts。

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-030 | PostgreSQL migration ownership | 空库显式 migrate 后独立 API/worker；Doctor/readiness | pass |
| AC-031 | three runtime profiles | SQLite/PG clean-room 与 T-20 native empty setup/restart | pass |
| AC-032 | structured logging | `runtime_ready`、request 与 `runtime_stopped` 含 profile/role/database | pass |
| AC-033 | Doctor/readiness | SQLite 与 PG 各 9 项检查 healthy；live/ready 200 | pass |
| AC-034 | current Generator | 29/29 contract tests，`CONTRACT_GENERATE_CHECK_PASS` | pass |
| AC-035 | real PostgreSQL | PostgreSQL 17.11，`REQUIRED_POSTGRES_PASS executed=18 skipped=0` | pass |
| AC-036 | real Web/native E2E | `REQUIRED_WEB_E2E_PASS suites=10 profiles=20 skipped=0`；T-20 native exact marker | pass |
| AC-037 | vulnerability/security | 复用 T-18 required supply-chain/security Evidence；T-21 release policy 反向测试 16/16 | pass |
| AC-038 | API/UI compatibility | contract lint 29/29、API client 7/7、Vitest 256、Node 49、Web 双 profile | pass |
| AC-039 | clean-room/docs scan | 三 profile、docs/compatibility/release policy 与 restart rehearsal | pass |

## 5. Workspace Verification

`source-worktree` 只运行非 E2E 定向检查；required E2E 不得由 source-worktree 标记为 pass。`parent-candidate` 负责集成、真实数据库/浏览器、clean-room 与 release rehearsal；Desktop 复用未改变原生产品字节的 T-20 required macOS Evidence。

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| governance / architecture / compatibility / docs / Task contract | Windows parent-candidate | pass | `GOVERNANCE_CHECK_PASS`、`ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、`DOCS_CHECK_PASS`、`TASK_CONTRACT_PASS` |
| contract lint / generate | Windows parent-candidate | pass | 29/29、0 fail/skip；API client 7/7；`CONTRACT_GENERATE_CHECK_PASS` |
| pnpm frozen install/typecheck/lint/test/workspace/build | Windows parent-candidate | pass | 201/201 packages；Web Shell included；Vitest 41 files/256 tests；Node 49/49；workspace 10/10；Desktop production assets pass |
| Go targeted/vet | Windows parent-candidate | pass | `go vet ./...`；cmd/product/host/Files/outbox targeted packages；日期修正测试 `-count=20` |
| Rust tests/clippy | Windows parent-candidate，`CARGO_TARGET_DIR=D:\t20cargo` | pass | 27/27 all features/targets；warnings denied |
| PostgreSQL required runner | PostgreSQL 17.11 disposable DB | pass | `REQUIRED_POSTGRES_PASS executed=18 skipped=0` |
| Web required runner | Chrome，SQLite + PostgreSQL | pass | 10 suites / 20 profiles；`skipped=0` |
| sidecar builder / release policy | Windows parent-candidate | pass | host sidecar built；3/3 builder tests；16/16 policy tests；4/4 preflight tests |
| `task release VERSION=0.1.0` | Windows parent-candidate | pass | root `b1821e0`，OpenAPI SHA `88fa89f...942b0`，migration `8100000000000`；无 publish |
| SpecDev validator | clean parent/candidate | pass | `0 error(s), 0 warning(s)` |

宽泛根 `task test` 在 Windows 运行约 9 分钟后诚实非零。Files 的日期相关首红已由 DEV-21-005 修正并以精确测试 20 次及相关包复验通过；其余输出均为既有 Windows baseline：desktop-sidecar parent-pipe、desktop control boundary/instance drain、SQLite UNC URI、Desktop backup unsafe-root 两项及 symlink privilege。该命令未被写成 pass，也没有通过 skip、retry 或 allow-failure 掩盖；最终候选使用定向 Go、真实 PostgreSQL/Web、Rust 与 native Gate 覆盖受影响产品边界。

完成记录提交后在 main 根重跑 `task governance:check` 非零：扫描器把根目录下按授权边界保留的 42 个 source/candidate worktree 中的 `.github`、`.editorconfig` 等列为 nested governance assets。扫描器没有 Git-worktree 感知或排除参数，清理这些 worktree 未获授权。精确 result candidate 的同一门禁已返回 `GOVERNANCE_CHECK_PASS`；result 到关闭提交只改变扫描器主动 prune 的 `<Path>speculo/**</Path>`，因此该 post-close 环境结果不重标为产品失败，也不伪造为 main-root pass。

### Clean-room rehearsal

- **Server SQLite：** ignored root `<Path>.artifacts/t21-cleanroom/server-sqlite/</Path>`；空库 migrate `applied=17 current_version=8100000000000`，bootstrap 成功。首次 Doctor 因预期 files root 尚不存在返回 degraded，创建运行时需要的目录后 9/9 healthy。首次与同根重启均为 ready/login/current/users 200、`total=1`；两次停止均输出 `runtime_stopped`，最终端口/进程为 0。首次漏写 `/api` 得到 404，PowerShell 自动 cookie 因 Secure 语义得到 401，均保留为操作性首红；使用不输出 cookie 的内存 HttpClient 后通过。
- **Server PostgreSQL：** disposable DB `speculo_t21_cleanroom_pg_20260902_01`；DSN 仅从 ignored file 引用。migrate `applied=17 current_version=8100000000000`，bootstrap 与 Doctor 9/9 healthy。API/worker 分进程首次与重启均 ready/login/users 200、`total=1`；每次停止两个角色都输出 `runtime_stopped`，最终端口/进程为 0。首次脚本引用错误 secret 文件名而中止，改用实际 ignored 文件名后从头通过；没有输出 secret。
- **Desktop SQLite：** T-21 Ticket 明确允许复用 T-19/T-20 Evidence。`3ab6ee5..b1821e0` 对 Desktop/native 产品路径为空 diff；T-20 在 macOS 15.7.7 arm64、AX enabled 的 Actions `33558345476` / job `100024584867` 完成空状态首次设置、vault failure recovery、登录、权限、CRUD、SQLite/Stronghold restart 与清理，精确输出 `DESKTOP_NATIVE_E2E_PASS runtime=tauri-native profile=sqlite skipped=0`。

- **失败后修复与重跑：** compatibility、architecture、Generator、Files clock 与 sidecar host path 首红均有不可变 checkpoint；修正后对应完整门禁通过。sidecar 首次修正测试为 15/16，原因是旧 POSIX fixture 未声明 host path；显式声明后 16/16 与真实 host build 通过。
- **未运行检查：** 签名与公证 `not-required`；publish、deploy、production migration、archive、source/worktree/stash cleanup 未授权且未运行。
- **E2E：** required；PostgreSQL、Web、两种 Server clean-room 在 parent-candidate 运行，Desktop 使用原生产品字节未变的真实 macOS required Evidence。

## 6. 双轴审查

### 标准轴

- **固定输入：** `67c09ac3daf58d6830da8fe72bd3672bc57b7040...b1821e0fe8a0f7fba1333113b951ee10e0011bff`；commit log 与三点 diff。
- **来源：** `<Path>AGENTS.md</Path>`、`<Path>go-admin-plus/AGENTS.md</Path>`、`<Path>.editorconfig</Path>`、工具配置与 Fowler smell baseline。
- **结果：** pass。
- **Findings 与修正：** 一项 host/target path medium finding；`c888fff` 使 buildEnvironment 默认使用 host path API，并增加 POSIX-host Windows-target 与 Windows-host fixtures。无剩余 finding。

### 规范轴

- **固定输入与来源：** 同一固定 diff；Spec、T-21、Goal Plan、AC-030~039、DEV-21-001~005。
- **结果：** pass。
- **Findings 与修正：** 无；新增 review 修正仍在 `<Path>release/**</Path>` writable scope，不改变产品/API/migration/security 合同。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `082c496775f5deb18531620f78bd1f11babeb748` |
| Source SHA | `c888fffec8ee2c2c5f21b7588128a7b3f9faa66c` |
| Candidate branch/workspace | `speculo/integration/2026-08-29-productization-and-ui-reconstruction/T-21` / `specdev-worktree/.integration/2026-08-29-productization-and-ui-reconstruction/T-21` |
| Candidate/result/tree | `b1821e0fe8a0f7fba1333113b951ee10e0011bff` / same / `e4548effed47fdb75e94d9c34e0e2ef9ff3fd366` |
| Method/conflicts | source 与治理父普通 merge；无冲突；main `--ff-only` |
| Integration checks | full portable gates、required PG/Web、three-profile clean-room、release rehearsal、双轴 review 与 validator passed |
| E2E disposition/result | required / passed；所有 required marker `skipped=0` |
| Parent result/re-read | main 精确 fast-forward 到 result；`082c496` 与 `c888fff` 均为 result ancestor，result tree 与 clean status 已重读 |

## 8. 偏差与决策

- **偏差：** DEV-21-001~005，均由用户“都批准”授权并严格限于 Ticket 所记路径与行为；source checkpoints 分别为 `3391294`、`3391294`、`408d9d2`、`06c5c58`、`b040a8d`。
- **记录：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/21-release-docs-clean-room.md</Path>`。
- **批准来源及影响：** 只修复 Windows 扫描/fixture、Web Shell typecheck 覆盖和确定性测试时钟；无 skip、兼容层、产品迁移或安全边界扩张。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** Windows 宽泛 `task test` 的既有 OS baseline 仍非零，已逐项归属且未冒充通过；正式跨平台分发仍需受保护 workflow 的签名、公证与安装验证。
- **后续 Ticket：** 无；G8 与本 change 完成。Archive、publish、deploy、production migration 与 cleanup 仍需独立授权。
- **监控或回滚触发：** 发布前复核 source SHA、migration version、schema/readiness 与签名 Evidence；候选失败不发布，数据恢复只使用同一时点完整备份和向前迁移。
- **Source commit：** `c888fffec8ee2c2c5f21b7588128a7b3f9faa66c`
- **Parent result：** `b1821e0fe8a0f7fba1333113b951ee10e0011bff`
- **Source workspace：** `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-21`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/evidence/T-21.md</Path>`
