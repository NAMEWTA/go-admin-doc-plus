# Evidence: T-12 - Router-Driven Workspace Shell

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/12-router-workspace-shell.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `cfff08d31f1f89dfa6f1605847409ba817c07313` / `edcdf14930e0d45b4cf61f64f891fdc5e7f883cc` / `edcdf14930e0d45b4cf61f64f891fdc5e7f883cc`
- **Status:** done

## 1. 实现摘要

产品 manifest 现声明稳定 name、module、path、permission、title、icon、order 与编译期 component loader。统一 router factory 为 Web 安装 HTML5 history、为 Desktop 安装 hash history；数据库 navigation 只与编译 manifest 和 capability grants 取交集，多余 component 字段被 adapter 清除。ProductWorkspace 使用 `useRoute/useRouter/RouterView` 派生菜单、标签、面包屑、标题与页面，不再维护 `window.history/popstate/path ref` 平行状态。

## 2. Lead Dispatch And Candidate Return

Lead direct source worktree 返回 `cfff08d`。冻结父提交 `5560174` 上无冲突合并得到 `edcdf14`。E2E not-required；真实浏览器 refresh/history 和 native viewport 归 T-19/T-20。

## 3. 修改范围与路径所有权

改动限定 App Shell product 源、双 App entry、指定 shell tests，以及 DEV-12-001 临时开放的 workspace boundary 单一断言。未修改 adapters、领域页面、Rust host 或 dependency allowlist。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-014~016 | deep link、history back、403/404/unavailable、Web/Desktop history | pass |
| AC-017 | route-derived menu/tags/breadcrumb/title、mobile drawer、compiled loader | pass |
| AC-018 | 继承 T-11 theme token/controller 基础 | foundation only；产品级可选主题与持久化实证归 T-20 |
| Security | navigation/component payload 清除、permission + compiled route intersection | pass |

## 5. Workspace Verification

以下检查在 `source-worktree` 与 Lead-owned parent candidate 执行；`current-workspace` 仅晋升 result 与提交状态。

| Check | Result |
|---|---|
| pnpm 11.1.3 frozen install | pass |
| targeted router/manifest/runtime tests | 3 files / 20 tests pass |
| full Vitest | 37 files / 212 tests pass |
| workspace lint + typecheck | pass |
| Web + Desktop production build | pass；Desktop production assets pass |
| Node shell/boundary suite | 43/44 pass；唯一失败为既有 T-20 macOS artifact path |
| old `window.history/popstate/path ref` scan | zero |

## 6. 双轴审查

- **代码轴：** manifest validation、lazy loader、router guards、history adapters、dynamic controller props 与 app bootstrap 无未解决 finding。
- **规范轴：** 一个 route truth、数据库不能加载组件、403/404 区分、adapter failure 可恢复且脱敏；领域内部 UI 与 Session protocol 未提前实现。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `5560174be39c99f89dd9f81b463e26cddba71529` |
| Source | `cfff08d31f1f89dfa6f1605847409ba817c07313` |
| Candidate/result | `edcdf14930e0d45b4cf61f64f891fdc5e7f883cc` |
| Tree | `ddd4ece9602ca72ef1e2783086c4494a2b8c760f` |
| Method/conflicts | merge-commit / none |
| Promotion | main fast-forward applied |

## 8. 偏差与决策

DEV-12-001 只把 workspace boundary 的静态 `FilesPage` 断言迁移为编译期 Files loader、RouterView 与 route-derived Platform Port props；双 App platform ownership、adapter dependency allowlist 和 Desktop command allowlist 均继续通过。用户“都批准”提供授权。

## 9. 残余风险与交付定位

T-13 负责稳定 CSRF 与跨标签/native Session client；T-14~16 替换 loader 指向页面内部实现；T-19/T-20 验收真实 browser/native history 与视口。source/candidate、旧候选和保护 stash 全部保留。
