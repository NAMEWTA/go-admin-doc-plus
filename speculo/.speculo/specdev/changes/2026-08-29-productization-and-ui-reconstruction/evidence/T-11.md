# Evidence: T-11 - UI Design System

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/11-ui-design-system.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `a5f80acf95482c038aa8e6b3b4e9f138ab7a9451` / `4193229f364a0042c6345a2917c98ea9eb775f79` / `4193229f364a0042c6345a2917c98ea9eb775f79`
- **Status:** done

## 1. 实现摘要

引入 Element Plus、Lucide、Sass、happy-dom 与 Vue Test Utils，建立 light/dark/compact 语义 token、主题入口和共享管理组件 runtime/type exports。列表、搜索、表格、分页、状态、对话框、表单与通知组件具备稳定 ARIA、键盘/focus、reduced-motion、长文本及空/错/禁用状态合同；旧宽泛模拟样式被主题实现替换，仅保留最小 reset。

## 2. Lead Dispatch And Candidate Return

T-11 source worktree 返回 `a5f80ac`。Lead 在 T-09 后的冻结父提交 `6ea8b9f` 上无冲突合并为 `a95da8e`，并在 parent candidate 修复锁文件的 Node 22.19 工具链不兼容，得到最终 result `4193229`。E2E not-required；真实页面视口归 T-19/T-20。

## 3. 修改范围与路径所有权

改动限定 workspace manifests/lock、`packages/ui`、App Shell UI dependency 与两份 shell visual tests。未修改领域页面、路由或宿主业务流程。候选锁修复仍位于 T-11 独占 lock/catalog 路径。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-017 | 语义 token、light/dark/compact、responsive/long-text contracts | pass |
| AC-018 | shared list/form/dialog/status components、ARIA、keyboard/focus/reduced-motion | pass |

## 5. Workspace Verification

以下检查在 `source-worktree` 与 Lead-owned parent candidate 执行；`current-workspace` 只晋升已验证 result 并提交状态。

| Check | Result |
|---|---|
| `corepack pnpm@11.1.3 --dir go-admin-plus-ui install --frozen-lockfile` | pass |
| workspace lint | pass |
| workspace typecheck | pass |
| Vitest full shell suite | 37 files / 206 tests pass |
| targeted list/visual contract | 2 files / 33 tests pass |
| Web + Desktop production build | pass；`DESKTOP_PRODUCTION_ASSETS_PASS` |
| full `pnpm test` Node portion | executed；43/44 pass，唯一失败为既有 Windows 上 macOS artifact path 断言 |

初始 source lock 使用 `@vue/test-utils 2.5.0`，其 `nopt@10.0.1` 要求 Node 22.22.2，而仓库环境为 22.19.0。候选将测试工具固定为 2.4.6，并由 pnpm 11.1.3 重建 lock；严格 frozen install 随后通过。未放宽 engine policy。

## 6. 双轴审查

- **代码轴：** theme API、component runtime/types、依赖图、CSS reset 与 lock diff 无未解决 finding。
- **规范轴：** AC-017/018 的主题、状态、可访问性和响应式基础完整；页面组合继续由 T-12、T-14~16 所有。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `6ea8b9fde3d9958adcda32c2125798386e7cbbce` |
| Source | `a5f80acf95482c038aa8e6b3b4e9f138ab7a9451` |
| Merge checkpoint | `a95da8e4d8749fdc89468151dde1ad7b32a6daee` |
| Candidate/result | `4193229f364a0042c6345a2917c98ea9eb775f79` |
| Tree | `91aa051b0cd0a716cd9a583735b7d7b72a903aab` |
| Method/conflicts | merge-commit + candidate fix / none |
| Promotion | main fast-forward applied |

## 8. 偏差与决策

没有规范级偏差。候选锁修复是通过 frozen install Gate 所必需的 T-11 范围修正。T-20-owned Windows/Darwin path 断言未越权修改，完整失败被如实保留。

## 9. 残余风险与交付定位

T-12 负责在双宿主 Shell 使用本组件系统，T-14~16 负责领域页面，T-19/T-20 负责真实浏览器/native 视觉与交互。所有 source/candidate worktree、旧候选和保护 stash 保留，未清理。
