# Evidence: T-06 - 文件配额与磁盘容量治理

- **Change：** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/06-file-capacity-governance.md</Path>`
- **Lead：** `codex-root`
- **Workspace/branch：** `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-06` / `speculo/2026-08-29-productization-and-ui-reconstruction/T-06`
- **Base/source/candidate/result SHA：** `46ee8862801085bc8df175f0089d91372fcda415` / `ddc2e6725646a640deba7d5b7981a9a146b5aae7` / `d7bfb162ae35ae4e467f66eaffdcb483e5224952` / `null`
- **状态：** blocked

## 1. 实现摘要

新增有限默认 CapacityPolicy、每次上传调用的 CapacityProbe、账号与全局两级原子计数器、上传前预留、声明/实际大小漂移拒绝、删除释放、过期预留与 pending/deleting 对账。上传顺序固定为鉴权预检、磁盘探针、原子 reserve、stage、最终鉴权并提交，未授权请求不会读取正文或探测磁盘。单次 Reconcile 受配置批量上限约束。

SQLite/PostgreSQL `0020` migration 使用全局唯一版本 `7510000000000`，对全部既有对象 backfill 账号/全局计数并创建可回收预留表。Windows 上不支持的目录 fsync 使用文件 Sync 与不可替换 hard-link 边界，符号链接专项测试仅在宿主具备创建能力时执行。

## 2. 路径与架构核对

- 修改仅位于 Files service/repository/storage、直接测试、`quota*`、`0020-capacity/**` 与 `test/files/dialect_capacity*` 授权路径。
- 未修改只读 OpenAPI、HTTP transport、typed config 或产品 migration registry。
- 默认策略始终有限；T-09 仍需将受验证 profile 配置和真实宿主磁盘探针接入产品 composition。
- T-08 仍负责把稳定容量错误投影到公共 Problem/OpenAPI，T-16/T-19 负责 UI 与真实浏览器上传链路。

## 3. 验收映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-027 | 两级原子条件更新、并发账号上限、对象数/字节数、删除释放，关键用例 `-count=20` | partial：SQLite pass，真实 PostgreSQL 缺失 |
| AC-028 | 低字节/比例水位在读取正文前稳定拒绝，未授权请求不探测；下载/删除路径不调用探针 | partial：模块 pass，真实宿主 probe wiring 归 T-09 |
| AC-029 | stage/publish 既有恢复、过期预留批量回收、pending/deleting 释放与幂等重试 | pass（SQLite）；PG 并发恢复待真实环境 |

## 4. Workspace 与 Candidate 验证

| 命令 | 环境 | 结果 |
|---|---|---|
| `go test ./internal/modules/files/... -count=1` | source + candidate / Windows / SQLite | pass |
| `go test ./test/files -run 'TestFilesCapacity' -count=1` | source + candidate / SQLite 双 Profile | pass；PostgreSQL 明确 skip |
| 容量关键用例与双方言测试 `-count=20` | source / Windows | pass |
| `go vet ./internal/modules/files/... ./test/files/...` | source | pass |
| `go vet ./...` | candidate | pass |
| `go test ./... -count=1` | candidate | fail：Files 新模块与 migration pass；T-09-owned 产品/旧 dialect registry 未注册 0020；另有既有 Windows sidecar、desktop、Generator、UNC 与 backup 失败 |
| `go test -race ./internal/modules/files/... ./test/files/... -count=1` | candidate | blocked：默认 CGO disabled；启用后缺少 `gcc` |
| `go test ./test/files -run TestFilesCapacityPostgresDialectContract -count=1 -v` | candidate | blocked：`GO_ADMIN_TEST_POSTGRES_DISPOSABLE_DSN` 未配置，测试明确 skip |

- **前置基线：** source 创建后，既有 Files 测试已因 Windows 目录 fsync/符号链接和既有组合问题失败；目录 fsync 可移植性在授权 storage 路径内修正，产品 registry 未越界修改。
- **E2E：** not-required；真实页面和上传工作流归 T-16/T-19。

## 5. 双轴审查

- **代码轴：** `46ee8862801085bc8df175f0089d91372fcda415..ddc2e6725646a640deba7d5b7981a9a146b5aae7` 路径合同、事务回滚、全局后账号的锁顺序、计数非负、错误脱敏与 bounded reconciliation 通过；无未解决 T-06 代码 finding。
- **规范轴：** AC-027~029 的模块行为与 SQLite 证据成立；真实 PG/race、产品 registry 和真实宿主 probe 尚未闭合，不能判定 Ticket 完成。

## 6. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `b54b5039dc6a284a656406a96d0874a5fef51347` |
| Source SHA | `ddc2e6725646a640deba7d5b7981a9a146b5aae7` |
| Candidate workspace | `specdev-worktree/.integration/2026-08-29-productization-and-ui-reconstruction/T-06` |
| Candidate SHA/tree | `d7bfb162ae35ae4e467f66eaffdcb483e5224952` / `731ac7722cde32e9f156a3a96e3d48609125ddb2` |
| Method/conflicts | merge-commit / 无冲突 |
| Promotion | failed；`main` 保持 `b54b5039dc6a284a656406a96d0874a5fef51347` |

## 7. 恢复定位

源与候选 worktree 均保留且 clean。T-09 接入 `0020-capacity` provider、有限 profile 配置和真实磁盘探针后，从最新 `main` 重建 candidate；在支持 CGO race 且提供 disposable PostgreSQL DSN 的 runner 重跑所有 required checks。成功前 `result_sha` 保持 `null`，不执行 source/candidate cleanup。
