# Evidence: T-18 - Required PostgreSQL And Supply-Chain CI Gates

- **Change:** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/18-postgres-security-ci-gates.md</Path>`
- **Owner:** `codex-root`
- **Source/candidate/result:** `c28d447c0595474cb4a29d9fa8313e318d6d0485` / `3fce364f67d9a468238e710e635ff77886c3a1d0` / `3fce364f67d9a468238e710e635ff77886c3a1d0`
- **Status:** done

## 1. 实现摘要

CI 新增 required PostgreSQL 与 security/supply-chain jobs。PostgreSQL runner 要求显式 require flag 和 disposable DSN，顺序执行 18 个双方言/并发/迁移套件，并拒绝缺环境、零执行、skip 或失败。Security runner 固定 govulncheck、pnpm production audit、cargo-audit、cargo-deny、gitleaks、Syft 与 generate drift；gitleaks 扫描 tracked Git history，Syft 生成 CycloneDX SBOM，扫描结果与 SBOM 都有机器可判定的非空/零 finding 校验。

PostgreSQL 17.11 service、Gitleaks v8.30.1 与 Syft v1.50.0 均在 workflow 固定 image digest；upload-artifact 固定 commit。Rust deny policy 拒绝 wildcard、未知 registry/git source；secret allowlist 只覆盖三个可核验的历史/fixture 值及一个精确脚本行，没有宽泛测试或源码目录排除。

## 2. Lead Dispatch And Candidate Return

Source worktree 从 `main@083f1bddebe6cdf88edc0ea6bccee918d7e64674` 启动。DEV-18-001/002 治理提交进入 source 后形成 `c28d447`。冻结父提交为 `b634db0ef6894d6e7b7ba6ca52980547c4e05a8d`；三次无冲突 merge 依次合入主实现、tracked-history secret hardening 与 SBOM source-boundary 修正，最终 candidate `3fce364`、tree `3f0e11b8be7cb1fa657dcaa3b6c8bd2e99d92dc4`。Lead 完成最终真实门禁后以 `--ff-only` 晋升 main。

## 3. 修改范围与路径所有权

实现限定于 CI workflow、required runners/tests、security policies、PostgreSQL schema prepare helper 和 architecture assertions。DEV-18-001 仅让既有 Files 双方言 contract fixture 组合当前 `0020-capacity` provider；DEV-18-002 仅更新 Cargo.lock 的 plist/quick-xml 解析以消除两项可达 advisory。两项均由 `USER-DECISION:all-approved` 覆盖，未修改产品 API、业务实现、Cargo.toml 或直接依赖约束。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| required PostgreSQL/non-skip | PostgreSQL 17.11，18 个 suite 全执行、0 skip；缺环境/skip/zero/fail 反向测试 | pass |
| vulnerability/dependency policy | govulncheck、pnpm audit、cargo audit/deny 固定版本真实执行 | pass |
| secret/SBOM/generate | tracked-history Gitleaks 0 finding；CycloneDX 1060 components；generate drift 通过 | pass |
| integration integrity | frozen candidate、action syntax、architecture、Rust/Node tests 与 SpecDev validator | pass |

## 5. Workspace Verification

以下检查分别在 `source-worktree` 与独立 `parent-candidate` 执行；`current-workspace` 只负责治理记录、最终真实 PostgreSQL 复验和通过后的 main fast-forward。

| Check | Result |
|---|---|
| real PostgreSQL 17.11 | pass；最终 candidate 输出 `REQUIRED_POSTGRES_PASS executed=18 skipped=0` |
| runner/policy/architecture tests | pass；Node 29/29，repository architecture check pass |
| negative gates | pass；缺 PG require、缺 security require、skip/zero/missing/fail 与空/有 finding artifact 均非零 |
| govulncheck | pass；0 个 called vulnerability；2 个 required-module finding 均不可达 |
| pnpm production audit | pass；无已知漏洞 |
| cargo audit/deny | pass；advisory 处置后 audit exit 0，deny bans/sources exit 0；19 项允许的 GTK/glib maintenance warning 被保留 |
| Gitleaks v8.30.1 | pass；456 commits，0 finding |
| Syft v1.50.0 CycloneDX | pass；`components=1060`，SBOM 1,318,135 bytes |
| generate/action syntax | pass；generate drift pass，actionlint v1.7.12 pass |
| Rust tests | pass；source 25/25；candidate 使用短 `CARGO_TARGET_DIR` 后 25/25 |
| SpecDev validator | pass；0 error / 0 warning |

本机没有 Docker，也未获远程 push/CI 写入授权，因此未声称 GitHub-hosted workflow run；固定 digest/Actions 语法由 actionlint 检查，扫描器以相同固定版本在 candidate 本机真实执行。未跟踪的 redacted Gitleaks/SBOM 报告保留在 candidate `artifacts/`，未清理且未进入产品提交。

### Attempts And Red Evidence

真实 PG runner 首次暴露共享 public schema 状态，改为每套件唯一 schema；随后暴露 product migration 自建数据库缺 schema，改为该套件自隔离；第三次暴露 Files fixture 未组合 capacity migration，DEV-18-001 精确修复后 source 与最终 candidate 均 18/18。诊断期临时输出只用于确认 `files_capacity_counters` 缺失，随即移除，未进入 commit。

Security 首次 cargo audit 发现 quick-xml 两项 high advisory，DEV-18-002 后通过。首次无 Git 边界 secret scan 报告 34 项，其中 28 项来自 Rust target、6 项为已知 fixture/history；由此改为 tracked-history 扫描与窄 allowlist，最终 0 finding。首个 Syft 范围包含生成 target，终止后收敛为源码/manifest/lock/node_modules 并只排除 `.git`、`artifacts` 与生成 Rust target。

Source Rust 首次因 sidecar binary 前置缺失而非零，构建 host sidecar 后 25/25。Candidate 在长 integration 路径两次出现 Windows linker `LNK1104`；进程/锁检查无残留后，以短临时 target 路径原样重跑并通过 25/25。所有这些红灯均未记录为 pass。

## 6. 双轴审查

- **代码轴：** required/non-skip、DSN 脱敏、suite schema 隔离、固定工具、窄 secret allowlist、SBOM 非空和 generate drift 均有正反验证。
- **规范轴：** 产品代码与公共 API 未变；两个偏差严格限定 fixture/lockfile；Web/native E2E 没有被静态或本机扫描冒充。

## 7. Integration Verification

| Item | Value |
|---|---|
| Parent before | `b634db0ef6894d6e7b7ba6ca52980547c4e05a8d` |
| Source | `c28d447c0595474cb4a29d9fa8313e318d6d0485` |
| Candidate/result | `3fce364f67d9a468238e710e635ff77886c3a1d0` |
| Tree | `3f0e11b8be7cb1fa657dcaa3b6c8bd2e99d92dc4` |
| Method/conflicts | merge-commit / none |
| Attempts | 3 candidate merge checkpoints；最终 checkpoint 完整通过 |
| Promotion | main fast-forward applied |
| E2E | not-required；真实 Web/native 分别归 T-19/T-20 |

## 8. 偏差与决策

DEV-18-001/002 由 `USER-DECISION:all-approved` 覆盖。前者只补齐测试 fixture 的现行 capacity migration provider；后者只更新 lockfile advisory resolution。没有扩大产品路径、放宽 required 语义或修改直接依赖合同。Docker/GitHub-hosted CI 未运行的事实明确保留，不以 actionlint 或本机等价执行替代其声明。

## 9. 残余风险与交付定位

残余风险是 GitHub Actions service/container 组合尚未在远程 runner 实际执行；当前本机以 PostgreSQL 17.11 和固定版本扫描器完成等价行为验证。远程写入不在本授权内，T-19/T-20/T-21 的 required release evidence 仍需继续建立。
