# Evidence: T-04 - 组织归属与五种数据范围

- **Change：** `2026-08-29-productization-and-ui-reconstruction`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-29-productization-and-ui-reconstruction/ticket/04-organization-data-scopes.md</Path>`
- **Lead：** `codex-root`
- **Workspace/branch：** `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-04` / `speculo/2026-08-29-productization-and-ui-reconstruction/T-04`
- **Base/source/candidate/result SHA：** `41445bb0aa21516acbfdfd00656ac04ce41149c0` / `dca203c199a274c479d8c1ab3acbdc01850eb0d8` / `1961d45c43955d4c9d244c7c5273dc96439ed9b6` / `null`
- **状态：** blocked

## 1. 实现摘要

新增五种数据范围解析器、IAM 账号组织归属和角色范围管理用例、Organization 事务投影，以及 SQLite/PostgreSQL 同版本 `0050` migration。只有实际授予目标 Permission Code 的启用角色参与并集；空主部门、空 custom、失效引用均 fail closed。账号主部门、岗位和角色 custom 部门在同一事务内校验，外键删除冲突保持稳定拒绝。

## 2. 修改范围与架构核对

- IAM 新增 `authorization/scope.go` 与 `administration/data_scope.go`。
- Organization 新增最小事务投影，仅由 IAM 通过公开 port 消费；未保留跨模块私表查询。
- `0050-data-scope` 使用全局唯一 migration version `7110000000000`，位于 Organization `7100000000000` 之后以满足外键依赖。
- 双方言 provider、迁移测试、五种范围、组织树移动、禁用/无权角色、岗位一致性和删除引用测试均已加入。
- 公共 OpenAPI、transport 和 UI 未修改，分别仍由 T-08/T-14 所有。

## 3. 验收映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-019 | all/self/organization/organization-tree/custom 与实际授权角色并集测试 | partial：SQLite pass，真实 PostgreSQL 环境缺失 |
| AC-020 | 主部门/岗位管理与组织树移动后下一请求重新解析 | pass |
| AC-021 | `ResolvedScope.Allows`、空范围、失效引用和外键冲突 fail closed | pass |
| AC-022 | capability grant 与 scope resolution 分离，禁用/无权角色不参与 | pass |

## 4. Workspace 与 Candidate 验证

| 命令 | 环境 | 结果 |
|---|---|---|
| `go test ./internal/modules/iam/... ./internal/modules/organization/... ./internal/application ./test/iam/authorization/... ./test/organization/... -count=1` | source + candidate / Windows / SQLite | pass |
| 关键数据范围与管理用例 `-count=20` | source / Windows / SQLite | pass |
| `go vet ./...` | source + candidate | pass |
| `go test ./... -count=1` | candidate | fail：T-04 相关 IAM/Organization/migration/authorization 包均 pass；失败集中在既有 Windows sidecar、desktop、Files、Generator、UNC 和 symlink 用例 |
| `go test -race ... -count=1` | candidate | blocked：默认 `CGO_ENABLED=0`；启用后缺少 `gcc` |
| `go test ./test/organization -run TestOrganizationPostgresDialectContract -count=1 -v` | candidate | blocked：`GO_ADMIN_TEST_POSTGRES_DISPOSABLE_DSN` 未配置，测试明确 skip |

- **E2E：** not-required；管理 UI 与浏览器直接越权流程由 T-14/T-19 负责。
- **未运行检查：** 真实 PostgreSQL 数据范围等价和 race。恢复条件为可丢弃 PostgreSQL DSN 与带 C 编译器的 CGO runner。

## 5. 双轴审查

- **代码轴：** `41445bb0aa21516acbfdfd00656ac04ce41149c0..dca203c199a274c479d8c1ab3acbdc01850eb0d8` 路径合同、事务边界、锁顺序、fail-closed 行为和 migration ordering 通过；无未解决代码 finding。
- **规范轴：** AC-019~022 的模块实现与 SQLite 证据通过；required race/真实 PostgreSQL 证据 blocked，不能判定 Ticket 完成。

## 6. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `0f3275141687263797a6d09a438e8bc4da4bb1da` |
| Source SHA | `dca203c199a274c479d8c1ab3acbdc01850eb0d8` |
| Candidate workspace | `specdev-worktree/.integration/2026-08-29-productization-and-ui-reconstruction/T-04` |
| Candidate SHA/tree | `1961d45c43955d4c9d244c7c5273dc96439ed9b6` / `26d423007e953ddb5373d58f9e10600b424b450c` |
| Method/conflicts | merge-commit / 无冲突 |
| Promotion | failed；`main` 保持 `0f3275141687263797a6d09a438e8bc4da4bb1da` |

## 7. 恢复定位

### DEV-03-001 Ownership Split

DEV-09-001 联合候选发现 Authorization disposable fixture 未应用 T-03 的 0040 provider。`test/iam/authorization/**` 已由 T-04 拥有，因此没有扩大 T-03 到该目录；T-04 在联合候选派生的 composition workspace `specdev-worktree/2026-08-29-productization-and-ui-reconstruction/T-04-COMPOSITION` 以 `c35237ca60a1f99518a65fe03bfcc2d62b591cc4` 仅为 `administration_test.go` 与 `browser_harness_test.go` 增加 provider。登录缺表失败消失，剩余 rotate-on-read Header 红灯归 T-08 wire contract。该 checkpoint 不改变 T-04 生产实现或宣称 result。

源 worktree 与候选 worktree 均保留且 clean；不执行 branch/worktree 清理。恢复后从最新 `main` 重建候选，使用可丢弃 PostgreSQL 和支持 CGO race 的 runner 重跑全部 required gate，成功前 `result_sha` 保持 `null`。
