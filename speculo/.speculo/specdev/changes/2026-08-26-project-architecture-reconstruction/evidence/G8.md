# Evidence: G8 - Unified System E2E（进行中）

- **Gate：** `G8 Unified System E2E`
- **Lead：** `codex-root`
- **状态：** attempt 1/2/3/4 均已失效；R01 至 R04 修复实现均已通过独立候选的完整非 E2E 门禁并进入 `main`；按用户 2026-08-28 最新指令暂停 E2E，G8 保持未完成
- **授权边界：** 本地实现提交与候选集成已授权；远端发布、生产签名、公证、受保护 runner 和 source/candidate cleanup 未授权

## 1. Attempt 1

- **Base/candidate：** `b72af2cbcc715cb1476dcf6856f5948b18910d43`
- **Branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-1`
- **环境：** macOS；Google Chrome 151；disposable PostgreSQL 17.11；真实 SQLite 临时数据库

在同一最终系统候选上，IAM Session、IAM Administration、Audit、Demo 和 Organization 的 SQLite/PostgreSQL browser E2E 均通过。Audit 首次调用因 PostgreSQL URL 缺 URL host 而在 harness 参数解析前失败，改用同一 socket/port 且带 `localhost` 的等价 DSN 后从零重跑并通过；该项属于运行参数纠正，不是产品通过的替代。

Settings 场景依次暴露三个真实产品缺陷和一个 harness 前置错误：分类切换在投影完成前发布新 category，导致 UI 表单仍写旧 category；后端拒绝敏感值后页面仍把该值保留在 DOM；不同 Web domain 各自缓存后端每请求滚动的 CSRF，跨模块读取后下一模块 mutation 会被旧令牌拒绝；字典 stale-revision fixture 在 revision 1 上减一只形成非法 revision 0，而不是冲突。attempt 1 因此整体失效，未继续 Scheduler、Files、Generator、reliable-runtime、Desktop native 和平台发行组，也不作为部分 G8 完成依据。

## 2. G8-R01 Settings 修复

- **Source branch/workspace：** `speculo/2026-08-26-project-architecture-reconstruction/G8-R01-settings` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/G8-R01-settings`
- **Source commit/tree：** `76efe495616470bb985952e130f3253cd5e1745d` / `8b09abdfed5e330bac3dbacf6fa627249f0e8b59`
- **Parent before：** `b72af2cbcc715cb1476dcf6856f5948b18910d43`
- **Candidate/result/tree：** `45ba95b1e2022aba8831ad5b4333f39a7de62b87` / `45ba95b1e2022aba8831ad5b4333f39a7de62b87` / `8b09abdfed5e330bac3dbacf6fa627249f0e8b59`
- **Candidate branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-R01-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-R01-attempt-1`
- **Promotion：** `main` 已以 `--ff-only` 提升到 result；source/candidate 均按未授权 cleanup 合同保留

修复令 category/dictionary 选择在对应投影成功后原子提交，选择期间阻断 mutation，失败回滚；UI tab 成功后同步空表单 category，敏感值 validation 失败后清空 intent。Browser adapter 新增单一 session fetch，串行化同源 `/api/` 请求，以最新响应 CSRF 覆盖或清除各 domain 调用方旧值，并禁止跨 origin 携带该状态；Web runtime、IAM 和全部业务客户端由同一 fetch 实例组合。Settings harness 的 stale conflict 前置改为先形成 revision 2，再提交 revision 1。

### Verification

| Check | 结果 |
|---|---|
| Frontend lint/typecheck | pass；25 workspaces，Settings fixture 额外 typecheck pass |
| Frontend unit/boundary/build | pass；27 files / 136 tests，7 workspace boundaries，Web/Desktop Vite build |
| Shared CSRF regression | pass；跨 domain 最新令牌覆盖、全局请求串行、跨 origin 隔离、CSRF rejection 后清空 |
| Settings Go | pass；`./internal/modules/settings/...` 与 `./test/settings/...` |
| Root gates | `GOVERNANCE_CHECK_PASS`、`ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、`DOCS_CHECK_PASS`、`TASK_CONTRACT_PASS`，quality tests 2 / 2 |
| Path/tree/clean | 11 个修复路径；source 与 candidate tree 相同；diff-check 与 clean tree pass |

修复 source 上的 SQLite/PostgreSQL Settings browser 场景均返回 `SETTINGS_E2E_PASS`，只用于证明修复方向和防止无效候选进入父分支。按 Goal Plan，source E2E 不计入 required G8；attempt 2 必须在新最终候选从 IAM Session 开始完整重跑所有组。

## 3. Attempt 2

- **Base/candidate：** `fe156d7579cd4d8be98eaef6db3229b50d44aeff`
- **Branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-2` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-2`

attempt 2 从最新父分支洁净创建，但 IAM Session PostgreSQL browser harness 连续失败。诊断证明 harness 先用 `pgx.ParseConfig` 修改 `RuntimeParams["search_path"]`，再调用不会序列化该新增 runtime parameter 的 `ConnString()`；migration 与 seed 因而落入 `public`，第二次固定账号 seed 发生冲突。该候选违反 disposable schema isolation，整体失效且没有继续后续组；临时诊断已撤销，候选按 cleanup 未授权合同原样保留。被污染的原始 disposable `postgres` 数据库保留为诊断证据，不把其任何结果计入 G8。

## 4. G8-R02 PostgreSQL isolation 与 Scheduler 修复

- **Source branch/workspace：** `speculo/2026-08-26-project-architecture-reconstruction/G8-R02-postgres-isolation` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/G8-R02-postgres-isolation`
- **Source commit/tree：** `f8a664e142c244224ef18bdfed21a7f6a1e79df2` / `1f5dc8a0aff57c05e24eeb3214bc2ae7292f7156`
- **Parent before：** `fe156d7579cd4d8be98eaef6db3229b50d44aeff`
- **Candidate/result/tree：** `51c6dec898983261f1633925762d1e0f5784e27d` / `51c6dec898983261f1633925762d1e0f5784e27d` / `1f5dc8a0aff57c05e24eeb3214bc2ae7292f7156`
- **Candidate branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-R02-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-R02-attempt-1`
- **Promotion：** 候选创建命令的工作目录错误令同一 source 先形成本地 `main@c2aae797540f615ac478c5253b0ed2a385b7c2da`；正式 candidate 全绿后以第二父节点进入 `main@e92a1aecdf1c0c20fc735afeda53440aba6c89b1`，两次提交 tree 均为 `1f5dc8a0aff57c05e24eeb3214bc2ae7292f7156`，未改写历史、未推送远端

四个 PostgreSQL harness 不再依赖未被 `ConnString()` 保留的运行时 map，而是在验证 URL scheme/host 和专属随机 schema grammar 后，将 URL-safe `search_path` 写入查询参数并保留 socket、port、SSL 与 application name 等已有参数；对应 regression tests 覆盖保留与 fail-closed 输入。Scheduler browser 客户端改为复用浏览器 adapter 的单一 rolling-CSRF transport；Scheduler 页面首次 load 后直接从 controller 成功快照初始化 task type 与参数，避免 Vue revision 尚未提交时读取旧 computed projection。

### Verification

| Check | 结果 |
|---|---|
| 双数据库真实场景 | IAM Session browser SQLite/PostgreSQL pass；Scheduler browser SQLite/PostgreSQL pass；Scheduler PostgreSQL runtime pass；IAM PostgreSQL concurrency pass |
| PostgreSQL isolation | 新 disposable database 的 `public` table count 与临时 schema count 在各次 repaired run 后均为 0 |
| Go full matrix | normal、SQLite tag、race、vet、四个 CGO0 command build、`go mod tidy` 零漂移全部 pass；三轮 generator 均禁用缓存真实运行 |
| Frontend full matrix | lint/typecheck pass；27 files / 136 tests；7 workspace boundaries；Web/Desktop build pass |
| Tauri 2 Rust host | host sidecar build、19 tests、clippy deny warnings、locked custom-protocol release check pass |
| Contract/root gates | OpenAPI lint 与 28 tests、API client 7 tests、generate-check、governance、architecture、compatibility-zero、docs、Task contract 全部 pass |
| Path/tree/clean | 7 个修复路径；source/candidate tree 相同；diff-check 与 clean candidate pass |

source 上的双方言 browser 与 PostgreSQL isolation 结果只证明修复方向；按 Goal Plan 不累计到 required G8。attempt 3 必须使用包含正式 candidate 父节点的最新 `main`，从 IAM Session 开始完整重跑。

## 5. Attempt 3

- **Base/candidate：** `bb26303f70717235e7f57c6c47e9ffdf66040882`
- **Branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-3` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-3`
- **数据库：** clean disposable database `go_admin_g8_attempt3`；各 PostgreSQL harness 使用独立临时 schema，`public` 与临时 schema 均保持零残留

attempt 3 从 IAM Session 开始完整重跑。IAM Session、IAM Administration、Audit、Demo、Organization、Settings、Scheduler 的 SQLite/PostgreSQL browser 场景均通过；Files 场景随后失败。诊断确认三类问题：Files 的 Session/Administration/Files 客户端未共享 rolling-CSRF transport；batch-delete fixture 直接修改 controller selection，绕过 Vue checkbox 响应式状态，导致实际删除按钮仍禁用；权限撤销和 Session revoke fixture 直接调用 controller refresh，绕过页面 revision 与 `sessionRequired` 事件链。候选因此整体失效，未继续 Generator、reliable-runtime、Desktop native 和平台发行组；之前通过的组不累计为 G8 完成证据。

## 6. G8-R03 Files 与 Generator 修复

- **Source branch/workspace：** `speculo/2026-08-26-project-architecture-reconstruction/G8-R03-files-generator` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/G8-R03-files-generator`
- **Source commit/tree：** `614355dce9450e88c0a5758c3909fd20999f6e12` / `74ca87f723e99857bda4731ed0089c2f588b1690`
- **Parent before：** `bb26303f70717235e7f57c6c47e9ffdf66040882`
- **Candidate/result/tree：** `d41691a6bbf8e0246e5cf2b82bfa86873716a9b4` / `d41691a6bbf8e0246e5cf2b82bfa86873716a9b4` / `74ca87f723e99857bda4731ed0089c2f588b1690`
- **Candidate branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-R03-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-R03-attempt-1`
- **Promotion：** `main` 已以 `--ff-only` 提升到 result；source/candidate 均按未授权 cleanup 合同保留

Files 与 Generator browser fixture 统一复用 Browser adapter 的 session fetch。Files batch-delete 改为驱动真实 row checkbox 并等待按钮响应式启用；权限和 Session 撤销改为提交真实搜索表单，使页面 revision、失败投影清理和重新登录事件完整运行。

Generator 修复同时闭合随后由独立 source E2E 暴露的生成链问题：SQLite source fixture 使用标准 UUID/BOOLEAN metadata；service sort switch 对 primary key 去重；生成的 SQLite/PostgreSQL 测试只在搜索 fixture 实际使用时导入 `fmt`/`sort`；PostgreSQL `generator_configs.actor_account_id` 改为与权威 IAM `TEXT` identity 一致，并新增双方言 provider regression。

### Verification

| Check | 结果 |
|---|---|
| 修复方向 E2E | Files SQLite/PostgreSQL 返回 `FILES_E2E_PASS`；Generator SQLite/PostgreSQL browser、真实 generate/compile gate 与 PostgreSQL runtime 返回 `GENERATOR_E2E_PASS` |
| Go full matrix | normal、SQLite tag、race、vet、四个 CGO0 command build、`go mod tidy` 零漂移全部 pass；三轮 Generator 分别约 393/349/357 秒并禁用测试缓存 |
| Frontend full matrix | frozen install、lint/typecheck pass；27 files / 136 tests；7 workspace boundaries；Web/Desktop build pass |
| Tauri 2 与发行策略 | host sidecar build、19 Rust tests、clippy deny warnings、locked custom-protocol release check、16 项三平台发行 policy tests pass |
| Contract/root gates | OpenAPI lint、28 contract tests、API client typecheck 与 7 tests、generate-check、governance、architecture、compatibility-zero、docs、Task contract 全部 pass |
| Path/tree/clean | 8 个修复路径；source/candidate tree 相同；diff-check 与 clean candidate pass |

source 上的 Files/Generator 双 profile 结果只证明修复方向并防止无效修复进入父分支，按 Goal Plan 不累计到 required G8。

## 7. Attempt 4

- **Base/candidate：** `eb5de678c253c563343d31bba289840464eec60c`
- **Branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-4` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-4`
- **数据库：** clean disposable database `go_admin_g8_attempt4`；browser/fault 结束后 `public` table 与临时 schema 均为 0

attempt 4 从最新实现结果重新执行全部 browser module 的 SQLite/PostgreSQL 场景以及 PostgreSQL reliable-runtime fault，均通过。进入 Desktop native 时，runner 在预检发现当前架构对应的 `go-admin-plus/test/desktop/fixture` 不存在，无法建立上一迁移版本、失败恢复和持久化验证所需的真实 SQLite fixture；该候选因此整体失效。此前 browser/fault pass 不累计为 G8 完成证据，候选按未授权 cleanup 合同保留。

## 8. G8-R04 Desktop native 实现修复

- **Source branch/workspace：** `speculo/2026-08-26-project-architecture-reconstruction/G8-R04-desktop-fixture` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/G8-R04-desktop-fixture`
- **Source commits：** `d28e5531666afb54163f036180ed8f2adf6df6e0`、`f49aa4c3e3f3ef5a46c63c57d03f4efa7a568914`
- **Source final tree：** `80d7603f521d420835f1cff2ee74900e791d7525`
- **Parent before：** `eb5de678c253c563343d31bba289840464eec60c`
- **Attempt 1 candidate/tree：** `23e722938de27990f6e0f7d5df564d747207233a` / `a0c4d47d99dff823ee545595ea982c1eddbda6de`
- **Attempt 2 candidate/result/tree：** `8f817bf014e464e5c8d4411851cadc0cee293d48` / `8f817bf014e464e5c8d4411851cadc0cee293d48` / `80d7603f521d420835f1cff2ee74900e791d7525`
- **Attempt 2 branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-R04-attempt-2` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-R04-attempt-2`
- **Promotion：** `main` 已以 `--ff-only` 提升到 Attempt 2 result；source/candidate 均保留

R04 恢复当前架构的 Desktop SQLite fixture、迁移失败备份、真实 API proxy 路径、Stronghold 测试身份隔离、权限/数据范围/Session revoke、单实例、CRUD/重启/退出和生产制品污染检查。宿主等待异步 sidecar proxy 就绪；前端在中央 Session 失效时切回登录；原生 accessibility runner 使用有界命令、精确 AX role/name 与唯一删除动作。诊断解析进一步拆为可单测的 error-only 提取，正常 unauthenticated/vault 状态不再覆盖真实失败 phase。

Attempt 1 的完整非 E2E 门禁通过，但原生候选运行在登录阶段失败；Attempt 2 为诊断增量候选，用户随后明确要求停止 E2E、先完成全部编码和构建，因此正在运行的原生场景被终止且不计为 pass。终止后已清理测试 Keychain、宿主和 sidecar，重建 production sidecar/UI/Tauri host，并确认生产制品不含 native test control。

### Verification

| Check | 结果 |
|---|---|
| Go unchanged subtree | Attempt 1 normal、SQLite tag、race、vet、四个 CGO0 command build、module verify、tidy 零漂移全部 pass；Attempt 2 backend subtree 与 Attempt 1 content-identical |
| Frontend full non-E2E | Attempt 2 frozen install、lint/typecheck、27 files / 136 tests、12 desktop runner tests、7 workspace boundaries、Web/Desktop production build pass；native runner 未启用并明确 skip |
| Tauri 2 build/static | production sidecar build、Rust default 20 tests、native feature 21 tests、strict clippy、locked custom-protocol release build pass |
| Contract/root gates | OpenAPI lint、28 contract tests、API client 7 tests、generate-check、governance、architecture、compatibility-zero、docs、Task contract、release policy 全部 pass |
| Production/resource cleanup | test Keychain service 清空；生产 Keychain account 不存在；无遗留 host/sidecar；生产二进制与 dist 对 test-control marker 零命中 |
| Candidate/path/clean | Attempt 2 仅比 Attempt 1 多 3 个诊断 runner 文件；candidate clean；source/candidate final tree 一致 |

## 9. Remaining Gate（暂停）

按用户 2026-08-28 最新指令，不再继续 browser、fault、Desktop native 或发行 E2E；当前先以 `main@8f817bf014e464e5c8d4411851cadc0cee293d48` 作为编码和全部非 E2E 构建完成点。该指令是暂停而非豁免：恢复 G8 时必须从当时最新 `main` 新建唯一最终系统候选，从头执行三 profile、全部 browser module、PostgreSQL reliable-runtime fault、Desktop native 和获授权的平台发行场景。Linux Docker、Windows native/AuthentiCode、macOS 生产签名/公证/受保护安装仍只能使用真实 runner 与授权；G8 未闭合前，相关 Ticket 与 change 继续保持 `implemented-pending-final-e2e` / `in_progress`。
