# Evidence: G8 - Unified System E2E（进行中）

- **Gate：** `G8 Unified System E2E`
- **Lead：** `codex-root`
- **状态：** attempt 1/2 已失效；Settings 与 PostgreSQL isolation/Scheduler 修复均已通过独立候选集成；attempt 3 待从最新 `main` 完整重建和从头重跑
- **授权边界：** 本地实现提交与候选集成已授权；远端发布、生产签名、公证、受保护 runner 和 source/candidate cleanup 未授权

## 1. Attempt 1

- **Base/candidate：** `b72af2cbcc715cb1476dcf6856f5948b18910d43`
- **Branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-1`
- **环境：** macOS；Google Chrome 151；disposable PostgreSQL 17.11；真实 SQLite 临时数据库

在同一最终系统候选上，IAM Session、IAM Administration、Audit、Demo 和 Organization 的 SQLite/PostgreSQL browser E2E 均通过。Audit 首次调用因 PostgreSQL URL 缺 URL host 而在 harness 参数解析前失败，改用同一 socket/port 且带 `localhost` 的等价 DSN 后从零重跑并通过；该项属于运行参数纠正，不是产品通过的替代。

Settings 场景依次暴露三个真实产品缺陷和一个 harness 前置错误：分类切换在投影完成前发布新 category，导致 UI 表单仍写旧 category；后端拒绝敏感值后页面仍把该值保留在 DOM；不同 Web domain 各自缓存后端每请求滚动的 CSRF，跨模块读取后下一模块 mutation 会被旧令牌拒绝；字典 stale-revision fixture 在 revision 1 上减一只形成非法 revision 0，而不是冲突。attempt 1 因此整体失效，未继续 Scheduler、Files、Generator、reliable-runtime、Desktop native 和平台发行组，也不作为部分 G8 完成依据。

## 2. G8-R01 Settings 修复

- **Source branch/workspace：** `speculo/2026-08-26-project-architecture-reconstruction/G8-R01-settings` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/G8-R01-settings`
- **Source commit/tree：** `76efe495616470bb985952e130f3253cd5e1745d` / `8b09abdfed5e330bac3dbacf6fa627249f0e8b59`
- **Parent before：** `b72af2cbcc715cb1476dcf6856f5948b18910d43`
- **Candidate/result/tree：** `45ba95b1e2022aba8831ad5b4333f39a7de62b87` / `45ba95b1e2022aba8831ad5b4333f39a7de62b87` / `8b09abdfed5e330bac3dbacf6fa627249f0e8b59`
- **Candidate branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-R01-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-R01-attempt-1`
- **Promotion：** `main` 已以 `--ff-only` 提升到 result；source/candidate 均按未授权 cleanup 合同保留

修复令 category/dictionary 选择在对应投影成功后原子提交，选择期间阻断 mutation，失败回滚；UI tab 成功后同步空表单 category，敏感值 validation 失败后清空 intent。Browser adapter 新增单一 session fetch，串行化同源 `/api/` 请求，以最新响应 CSRF 覆盖或清除各 domain 调用方旧值，并禁止跨 origin 携带该状态；Web runtime、IAM 和全部业务客户端由同一 fetch 实例组合。Settings harness 的 stale conflict 前置改为先形成 revision 2，再提交 revision 1。

### Verification

| Check | 结果 |
|---|---|
| Frontend lint/typecheck | pass；25 workspaces，Settings fixture 额外 typecheck pass |
| Frontend unit/boundary/build | pass；27 files / 136 tests，7 workspace boundaries，Web/Desktop Vite build |
| Shared CSRF regression | pass；跨 domain 最新令牌覆盖、全局请求串行、跨 origin 隔离、CSRF rejection 后清空 |
| Settings Go | pass；`./internal/modules/settings/...` 与 `./test/settings/...` |
| Root gates | `GOVERNANCE_CHECK_PASS`、`ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、`DOCS_CHECK_PASS`、`TASK_CONTRACT_PASS`，quality tests 2 / 2 |
| Path/tree/clean | 11 个修复路径；source 与 candidate tree 相同；diff-check 与 clean tree pass |

修复 source 上的 SQLite/PostgreSQL Settings browser 场景均返回 `SETTINGS_E2E_PASS`，只用于证明修复方向和防止无效候选进入父分支。按 Goal Plan，source E2E 不计入 required G8；attempt 2 必须在新最终候选从 IAM Session 开始完整重跑所有组。

## 3. Attempt 2

- **Base/candidate：** `fe156d7579cd4d8be98eaef6db3229b50d44aeff`
- **Branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-2` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-attempt-2`

attempt 2 从最新父分支洁净创建，但 IAM Session PostgreSQL browser harness 连续失败。诊断证明 harness 先用 `pgx.ParseConfig` 修改 `RuntimeParams["search_path"]`，再调用不会序列化该新增 runtime parameter 的 `ConnString()`；migration 与 seed 因而落入 `public`，第二次固定账号 seed 发生冲突。该候选违反 disposable schema isolation，整体失效且没有继续后续组；临时诊断已撤销，候选按 cleanup 未授权合同原样保留。被污染的原始 disposable `postgres` 数据库保留为诊断证据，不把其任何结果计入 G8。

## 4. G8-R02 PostgreSQL isolation 与 Scheduler 修复

- **Source branch/workspace：** `speculo/2026-08-26-project-architecture-reconstruction/G8-R02-postgres-isolation` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/G8-R02-postgres-isolation`
- **Source commit/tree：** `f8a664e142c244224ef18bdfed21a7f6a1e79df2` / `1f5dc8a0aff57c05e24eeb3214bc2ae7292f7156`
- **Parent before：** `fe156d7579cd4d8be98eaef6db3229b50d44aeff`
- **Candidate/result/tree：** `51c6dec898983261f1633925762d1e0f5784e27d` / `51c6dec898983261f1633925762d1e0f5784e27d` / `1f5dc8a0aff57c05e24eeb3214bc2ae7292f7156`
- **Candidate branch/workspace：** `speculo-candidate/2026-08-26-project-architecture-reconstruction/G8-R02-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/G8-R02-attempt-1`
- **Promotion：** 候选创建命令的工作目录错误令同一 source 先形成本地 `main@c2aae797540f615ac478c5253b0ed2a385b7c2da`；正式 candidate 全绿后以第二父节点进入 `main@e92a1aecdf1c0c20fc735afeda53440aba6c89b1`，两次提交 tree 均为 `1f5dc8a0aff57c05e24eeb3214bc2ae7292f7156`，未改写历史、未推送远端

四个 PostgreSQL harness 不再依赖未被 `ConnString()` 保留的运行时 map，而是在验证 URL scheme/host 和专属随机 schema grammar 后，将 URL-safe `search_path` 写入查询参数并保留 socket、port、SSL 与 application name 等已有参数；对应 regression tests 覆盖保留与 fail-closed 输入。Scheduler browser 客户端改为复用浏览器 adapter 的单一 rolling-CSRF transport；Scheduler 页面首次 load 后直接从 controller 成功快照初始化 task type 与参数，避免 Vue revision 尚未提交时读取旧 computed projection。

### Verification

| Check | 结果 |
|---|---|
| 双数据库真实场景 | IAM Session browser SQLite/PostgreSQL pass；Scheduler browser SQLite/PostgreSQL pass；Scheduler PostgreSQL runtime pass；IAM PostgreSQL concurrency pass |
| PostgreSQL isolation | 新 disposable database 的 `public` table count 与临时 schema count 在各次 repaired run 后均为 0 |
| Go full matrix | normal、SQLite tag、race、vet、四个 CGO0 command build、`go mod tidy` 零漂移全部 pass；三轮 generator 均禁用缓存真实运行 |
| Frontend full matrix | lint/typecheck pass；27 files / 136 tests；7 workspace boundaries；Web/Desktop build pass |
| Tauri 2 Rust host | host sidecar build、19 tests、clippy deny warnings、locked custom-protocol release check pass |
| Contract/root gates | OpenAPI lint 与 28 tests、API client 7 tests、generate-check、governance、architecture、compatibility-zero、docs、Task contract 全部 pass |
| Path/tree/clean | 7 个修复路径；source/candidate tree 相同；diff-check 与 clean candidate pass |

source 上的双方言 browser 与 PostgreSQL isolation 结果只证明修复方向；按 Goal Plan 不累计到 required G8。attempt 3 必须使用包含正式 candidate 父节点的最新 `main`，从 IAM Session 开始完整重跑。

## 5. Remaining Gate

从包含 `e92a1aecdf1c0c20fc735afeda53440aba6c89b1` 的最新 `main` 创建唯一 G8 attempt 3，重新执行全部 browser module、PostgreSQL reliable-runtime fault、Desktop native 和当前授权允许的本地发行场景。Linux Docker、Windows native/AuthentiCode、macOS 生产签名/公证/受保护安装只能记录真实 runner/授权阻塞，不得模拟为通过；在 required Gate 未闭合前，T-10/T-12/T-13/T-15 至 T-21 与 change 保持进行中。
