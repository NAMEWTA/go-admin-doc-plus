# Evidence: T-10 - Settings 参数与字典垂直切片（进行中）

- **Ticket：** `T-10`
- **Implementation owner：** `codex-t10-settings`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-10` / `speculo/2026-08-26-project-architecture-reconstruction/T-10`
- **Base/source/tree：** `cc2ae75c0f2374f7b57c171cc1e819955e1da9fd` / `a630c50fd0f8f22208564a807accfb325ca34f6a` / `f36adb6b6e6243fd15d1867b586086939597d536`
- **状态：** implementation integrated；parent-candidate 全部非 E2E Gate 通过并提升，E2E 延后到最终统一系统候选

## 1. Source 实现

Source 交付双方言 Settings migration、参数/字典/options API、同事务 IAM authorization、Module Capability 注册、稳定错误、无 tenant schema、domain/web-domain、Vue 管理页和 env-gated browser harness。source worktree clean，38 个路径均位于 T-10/T10-D01 授权范围。

## 2. 独立审查 Findings

1. `security.go` 的 canonical marker 只覆盖少数复合词，`api_key`、`database.dsn`、`client.credential`、`runtime.log_level`、`server.listen_address` 等明显 runtime/secret key 可绕过，违反 Settings 与 T-03 runtime config 的硬边界。
2. `SettingsPage.vue` 在 `saveSetting` 返回 `failed` 时仍重置表单，会丢失用户输入和冲突修复上下文；其他写操作已经只在 `completed` 后重置。
3. controller 在分类/字典列表加载成功前提交当前选择；加载失败后，后续成功请求可能把上一选择的稳定 rows 暴露到新选择下。切换必须失败回滚，页面 tab 也只能在加载成功后提交。
4. 两个 Settings package 已声明真实依赖和 package-local checks，但根 `verify` 未运行其 typecheck/tests，两个预建 lock importer 仍为空。`T10-D02` 只授权这三处串行接入。

## 3. Parent Candidate 与 Findings 闭合

- **Parent before：** `71108aea07acef377157480c27844096debb6283`
- **Source：** `a630c50fd0f8f22208564a807accfb325ca34f6a`（tree `f36adb6b6e6243fd15d1867b586086939597d536`）
- **Candidate/result：** `030eb0b57fd47a4a5381f80a24cd3dafaeb35310`（tree `8cdd579f60f3018be864f55fb54cfa8b9fb8ab3f`）
- **Candidate locator：** `speculo/integration/2026-08-26-project-architecture-reconstruction/T-10` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-10`
- **Method/attempt：** merge candidate + fast-forward promotion / attempt 1
- **Parent containment：** `main` 已快进到同一 result SHA

候选把 key 与普通文本的敏感检测分离：runtime/database/server/session 等保留 namespace、secret/token/credential/DSN 等精确 key part 以及 API key/connection string/listen address 等复合 canonical key 全部拒绝，同时以 `shipping.tokenized_label`、`business.logistics_level` 等普通业务文本负向回归防止泛化误杀。保存失败不再清空表单；分类和字典选择只在投影加载成功后提交，失败按请求序号回滚。根 Settings typecheck、Vitest 和两个 lock importer 按 `T10-D02` 精确接入。最终 path gate 另清理 source SQLite migration 的尾部空行。

## 4. Candidate Verification

| Check | 结果 |
|---|---|
| pnpm frozen/root verify/build | pass；18 个 test files / 103 tests，Desktop runner 6 / 6 unit、boundary 6 / 6，Web/Desktop production build |
| Go normal/SQLite full | pass；Generator 真实闭环分别约 353 秒 / 309 秒 |
| Settings focused race | pass；module 与 browser harness package |
| Go vet、CGO=0 cmd build、module verify | pass |
| OpenAPI/API client | lint pass；contract 28 / 28；generate check pass；API client 7 / 7 |
| governance/Task contract | `GOVERNANCE_CHECK_PASS`；固定 Go Task v3.45.4 精确返回 `TASK_CONTRACT_PASS` |
| SpecDev/path/clean | Goal Plan validator 0 error / 3 serialization warnings；41 个路径全部属于 T-10/T10-D01/T10-D02；diff-check 与 clean tree pass |

三个 warning 只来自 T-10/T-15/T-16 历史 frontmatter 的共享根路径组合；Goal Plan/Map 已明确 T-16 -> T-15 -> T-10 的实际串行交接，不存在并发写入。临时 Task 工具已可恢复地移入 `/Users/wta/.Trash/go-admin-task-tool.5HB7vK`。

## 5. Remaining Gate

T-10 实现结果已集成并可解除 T-17 的 T-10 实现依赖，但 Ticket、Map 保持 `in_progress` / `implemented-pending-final-e2e`。SQLite/PostgreSQL Settings API/UI、授权、options、引用冲突、运行 secret 拒绝与产品 composition 将在全部 T-01 至 T-21 实现集成后的唯一最终系统候选统一执行。

## 6. G8 Repair

G8 attempt 1 在真实双数据库 Settings browser 场景发现 category 投影提交、敏感 intent 清理和跨 domain 滚动 CSRF 三项缺陷，候选已判失效。修复 source `76efe495616470bb985952e130f3253cd5e1745d` 经独立 candidate/result `45ba95b1e2022aba8831ad5b4333f39a7de62b87`（tree `8b09abdfed5e330bac3dbacf6fa627249f0e8b59`）通过 25-workspace lint/typecheck、27 files / 136 tests、7 boundaries、双 build、Settings Go 与根治理门禁后进入 `main`。完整诊断、运行环境、保留 locator 和 attempt 2 恢复条件见 `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/evidence/G8.md</Path>`；Ticket 仍保持 `in_progress`，不得以修复 source 上的 E2E 关闭。

## 2. Lead Dispatch And Candidate Return

Lead 已接收 Settings source checkpoint、审查修复与 candidate/result；最终 G8 修复进入 `main` 后从干净系统候选复验。

## 3. 修改范围与路径所有权

修改限于 T-10 授权的 Settings 合同、后端模块、前端 Domain/Web Domain、测试及获批共享接入路径；路径审计无越权。

## 4. 验收与合同映射

`AC-015` 与 `AC-035` 由本文件既有实现矩阵、Settings 双 profile browser 结果及 G8 Evidence 第 29 节共同闭合。

## 5. Workspace Verification

source-worktree 只执行并通过非 E2E 单元、双方言、race、lint、typecheck、合同和构建门禁；E2E not-run 于 source-worktree，最终场景只取 G8 candidate 结果。

## 6. 双轴审查

标准轴与规范轴发现的 secret intent、category projection、CSRF 和表单保留问题均已修复并回归，完成态零未关闭 finding。

## 7. Integration Verification

记录的 candidate/result 与后续 G8 修复均已进入 `main`；最终候选 Settings SQLite/PostgreSQL 场景通过且清理完成。

## 8. 偏差与决策

共享路径偏差均已批准并按 Goal Plan 串行；无未批准 deviation 或 blocker。

## 9. 残余风险与交付定位

个人自用完成范围内无 required 残余门禁；历史 attempt 与修复 locator 保留在本文件及 G8 Evidence。
