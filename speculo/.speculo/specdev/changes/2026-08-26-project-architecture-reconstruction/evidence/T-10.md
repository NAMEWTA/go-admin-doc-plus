# Evidence: T-10 - Settings 参数与字典垂直切片（源码审查完成）

- **Ticket：** `T-10`
- **Implementation owner：** `codex-t10-settings`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-10` / `speculo/2026-08-26-project-architecture-reconstruction/T-10`
- **Base/source/tree：** `cc2ae75c0f2374f7b57c171cc1e819955e1da9fd` / `a630c50fd0f8f22208564a807accfb325ca34f6a` / `f36adb6b6e6243fd15d1867b586086939597d536`
- **状态：** source review；candidate fixes 与 parent integration pending，E2E 延后到最终统一系统候选

## 1. Source 实现

Source 交付双方言 Settings migration、参数/字典/options API、同事务 IAM authorization、Module Capability 注册、稳定错误、无 tenant schema、domain/web-domain、Vue 管理页和 env-gated browser harness。source worktree clean，38 个路径均位于 T-10/T10-D01 授权范围。

## 2. 独立审查 Findings

1. `security.go` 的 canonical marker 只覆盖少数复合词，`api_key`、`database.dsn`、`client.credential`、`runtime.log_level`、`server.listen_address` 等明显 runtime/secret key 可绕过，违反 Settings 与 T-03 runtime config 的硬边界。
2. `SettingsPage.vue` 在 `saveSetting` 返回 `failed` 时仍重置表单，会丢失用户输入和冲突修复上下文；其他写操作已经只在 `completed` 后重置。
3. controller 在分类/字典列表加载成功前提交当前选择；加载失败后，后续成功请求可能把上一选择的稳定 rows 暴露到新选择下。切换必须失败回滚，页面 tab 也只能在加载成功后提交。
4. 两个 Settings package 已声明真实依赖和 package-local checks，但根 `verify` 未运行其 typecheck/tests，两个预建 lock importer 仍为空。`T10-D02` 只授权这三处串行接入。

## 3. Remaining Gate

Parent candidate 必须闭合上述 findings，运行双方言/SQLite、race、vet、Go build/module、frozen pnpm/root verify/build、合同/API client、governance、Task contract、SpecDev、path 与 clean-tree 全部门禁。浏览器及跨 profile 场景只登记到最终统一系统 E2E；本 Evidence 不代表实现已集成或 Ticket 已完成。
