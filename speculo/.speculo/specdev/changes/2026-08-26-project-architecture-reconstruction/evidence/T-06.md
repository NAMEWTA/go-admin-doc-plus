# Evidence: T-06 - 安全登录、Session 与个人账户闭环

- **Ticket：** `T-06`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-06` / `speculo/2026-08-26-project-architecture-reconstruction/T-06`
- **Base/source/candidate/result：** `825a1c63fd3afd748e7da7efea10004569dc484c` / `529a6b5ce008390d71930249f1bf806f887d2de7` / `529a6b5ce008390d71930249f1bf806f887d2de7` / `529a6b5ce008390d71930249f1bf806f887d2de7`
- **状态：** done

## 1. 实现摘要

新增 Argon2id 账户凭据、进程级有界密码工作预算、hash-only 数据库 Session、account generation fence、固定 account -> session 锁序、idle/absolute timeout、rotation/revoke、`__Host-*` Secure HttpOnly SameSite=Strict Cookie、CSRF、资料与密码闭环。OpenAPI 3.1 fragment、Go transport、headless IAM domain、Web adapter/controller 和登录/账户页面使用新 Workspace 公共导出；不接受旧 JWT、refresh token、Casbin 或旧密码 hash。

三 profile 暴露同一类型化 Session policy，并在 `time.Duration` 乘法前校验 file/env/CLI 的整数边界和关系。数据库 cancellation/deadline sentinel 在 Query/Scan/Exec/RowsAffected 路径保持不变，其他错误脱敏；账户创建只把 PostgreSQL `23505` 与 SQLite `1555/2067` 映射为 conflict。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex-t06-iam`
- **初始 base：** `825a1c63fd3afd748e7da7efea10004569dc484c`
- **最终返回：** clean source `529a6b5ce008390d71930249f1bf806f887d2de7`（tree `6fbff592ba2ff4a7ab37f26c4b91654fe09007fe`）；全部修改位于 T-06 writable paths；未在 source 声称 required E2E。
- **Lead 核对：** source commit/tree、完整审查范围 `b44f90952497a753454eb7a1256d920d40accfb8..529a6b5ce008390d71930249f1bf806f887d2de7`、路径、generated owner manifest、clean 状态和默认 skip harness 均通过。

## 3. 修改范围与路径所有权

| 路径 | 所有权与用途 |
|---|---|
| `<Path>contracts/openapi/modules/iam-session.yaml</Path>` | T-06；Session/account 公共合同 |
| `<Path>go-admin-plus/internal/modules/iam/{account,session,migrations/0010-session-*}/**</Path>` | T-06；凭据、Session、HTTP、迁移、并发回归与生成 transport |
| `<Path>go-admin-plus/internal/platform/config/**</Path>` 与三 profile schema | T06-D02；类型化 Session policy |
| `<Path>go-admin-plus/test/iam/session/**</Path>` | T-06；API、安全、双方言与 tracked HTTPS host |
| `<Path>go-admin-plus-ui/packages/{domains,web-domains}/iam/**</Path>` | T-06；headless controller、Web client、页面与单测 |
| `<Path>go-admin-plus-ui/tests/e2e/iam/session/**</Path>` | T-06；Chromium CDP 双 profile required runner |
| 前端 manifest、lockfile 与 root test config | T06-D01/T06-D02；公开 export、依赖和标准门禁 |

未修改 kernel、公共 Database、App Shell core、Desktop host 或 IAM administration 路径。

## 4. 验收与合同映射

| 合同 | 证据 | 结果 |
|---|---|---|
| AC-008 | 真实 Chromium 检查 Cookie 属性、HttpOnly 不可读、正确/缺失/篡改 CSRF 和无状态变化 | pass |
| AC-010 | 双方言 rotation replacement、旧 token 401、idle/absolute timeout、logout 与 generation fence | pass |
| AC-012 | 资料持久化、logout 500 保持身份可重试、密码修改撤销、旧/新密码结果与 Argon2id | pass |
| AC-025 | 统一 credential failure、双方言 unique/internal 分类、context sentinel、无 SQL/secret 详情 | pass |
| AC-036 | 实际 Web session client/controller 在同源 HTTPS 浏览器流程中进入 authenticated/error/unauthenticated 状态 | pass |

## 5. Workspace Verification

source-worktree 只运行非 E2E 检查，不得且未声明 required E2E 通过。下表的完整门禁与真实浏览器/数据库证据均由 Lead 在固定 parent-candidate 执行。

| Check | 结果 |
|---|---|
| Go 普通、`-tags sqlite3`、全量 race、vet、build、module verify | pass；IAM account/session 与 browser harness 均进入矩阵 |
| 新 Workspace frozen install、peer/list、verify | pass；25 packages、5 files/37 tests、6 boundary、typecheck/build |
| 合同串行 suite、lint、generate check、API client | pass；28/28、lint pass、无 drift、7/7 |
| governance、前后端 path contract、SpecDev goal-plan、diff/clean-tree | pass；SpecDev 0 error / 0 warning |
| PostgreSQL generation/revoke concurrency | pass；真实 PostgreSQL 17.11，旧与 replacement token 均被 fence |
| Chromium 双 profile required E2E | pass；Chrome for Testing 151，真实 SQLite 临时文件与 PostgreSQL 隔离 schema |
| 资源清理 | pass；两类随机 schema 数量均为 0，PostgreSQL 停止，临时目录删除，测试端口与 child process 零残留 |

当前机器没有 Go Task CLI，未安装系统工具；Task 对应的底层命令已直接运行。首次直接调用 path-contract 未传其自测环境而按设计失败，该调用作废；按 task-contract 声明的环境重跑后通过。

## 6. 双轴审查

- **最终固定输入：** `b44f90952497a753454eb7a1256d920d40accfb8..529a6b5ce008390d71930249f1bf806f887d2de7`
- **标准轴：** pass，0 findings。
- **规范轴：** pass，0 findings。
- **修正历史：** 依次作废 Argon2 无界工作、rotation/revoke 竞态、局部轮换、logout/CSRF 状态错误、配置溢出、SQL sentinel 丢失、缺少 tracked E2E、DSN 过宽传播、无界 child/schema 清理和数据库故障误报 conflict 的 checkpoint；最终全部闭合。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `f4cd993f49bd7930b1d128ce00d128bbda58eb71` |
| Source SHA | `529a6b5ce008390d71930249f1bf806f887d2de7` |
| Candidate branch/workspace | `speculo/integration/2026-08-26-project-architecture-reconstruction/T-06` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-06` |
| Method/conflicts/attempt | fast-forward；无冲突；attempt 1 |
| Candidate/result/tree | `529a6b5ce008390d71930249f1bf806f887d2de7` / `529a6b5ce008390d71930249f1bf806f887d2de7` / `6fbff592ba2ff4a7ab37f26c4b91654fe09007fe` |
| E2E disposition/result | required / pass |
| Parent promotion | `main` 从精确 frozen parent 以 `--ff-only` 提升；父/source ancestry 与最终 HEAD 已核对 |

Lead 首次创建 candidate 后误在根 workspace 执行 fast-forward，导致 parent 提前指向 source、candidate 留在旧 parent。发现后立即作废所有旧 parent 检查，将 clean `main` 精确恢复至 `f4cd993`，在独立 candidate 正确 fast-forward 后从 frozen install 起重跑全部 Go、前端、合同、治理和 required E2E。事故未改变 commit/tree、产品代码或最终验收合同，且没有把旧 parent 结果计入通过证据。

## 8. 偏差与决策

`T06-D01` 仅开放 IAM package manifests 与 Workspace lock；`T06-D02` 仅开放三 profile Session config/schema、root UI test entry 和 tracked E2E 路径。其余审查收紧均位于原 writable paths，不产生新产品偏差。用户 `Q1A/Q2A` 已授权 worktree、implementation commit、Lead local candidate integration 和 parent update；未操作远端、生产、发布或 source cleanup。

## 9. 残余风险与交付定位

- T-07 继续拥有 IAM 用户、角色、permission code 和 data scope；T-16 继续拥有 Desktop Stronghold proxy；T-17 负责完整产品组合复核。
- 本 Ticket 的 HTTPS host 与控制端点只存在于 Go `_test.go` 和前端 E2E 路径，不进入产品 kernel。
- source/candidate worktree 按当前未授权 cleanup 合同保留。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/evidence/T-06.md</Path>`
