# Evidence: T-17 - 产品组合与三 Profile 全链路汇合（进行中）

- **Ticket：** `T-17`
- **Lead / implementation owner：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-17` / `speculo/2026-08-26-project-architecture-reconstruction/T-17`
- **Base：** `0cf98457a3eb68b636a45cd17dcc62b24d01d4e1`
- **状态：** implementation integrated；parent-candidate 全部非 E2E Gate 通过并提升，三 Profile/双 App E2E 按用户最新决定延后到最终统一系统候选

## 1. 激活前事实

T-09/T-10/T-11/T-12/T-13/T-15/T-16 的 implementation result 均已包含于 `main`。现有后端只有通用 Application/Host 基座和仍指向旧 `modules.Default()` 的宿主链；现有 Web 是 runtime 占位壳，Desktop 只组合 Demo。Browser runtime Permission Code parser 仍使用冒号格式，与 IAM 实际点分格式不一致；Desktop bridge 仅允许 Demo；根 product registry、全迁移 provider 集合、完整 capability 注册、Files typed root、共享 worker group 和完整双 App manifest 尚不存在。

IAM-owned Organization Projection Port 与 Organization adapter 已存在，但 IAM 用户/角色合同没有部门或岗位归属字段，也没有任何可调用的消费语义。T-17 不以仅构造未调用 adapter 伪造闭环；stage 1 先建立真实 product registry 失败基线，再决定最小跨模块 amendment。

## 2. Stage 1 边界

`T17-D01` 只在原 T-17 独占 product/cmd/test 路径之外开放 app-shell package manifest 的 product subpath export 和 package-local checks。Apps、Browser/Desktop adapters、Rust bridge、根 package/Vitest/lock、模块实现和合同工具仍只读。任何新增共享路径必须先记录具体失败、owner、允许 diff 和零漂移项。

## 3. Remaining Gate

尚未形成 source checkpoint。必须先实现稳定模块顺序、全迁移 providers、完整 capability/route/worker graph、三 profile 配置映射、纯产品 manifest 和负向 composition tests；随后根据测试证明的实际缺口串行开放 stage 2，运行全部非 E2E Gate并形成 parent-candidate。Ticket、Map 保持 `in_progress`。

## 4. Stage 1 迁移失败基线与 T17-D02

前端共享 manifest 已通过 package-local TypeScript 与 3 项 Vitest。后端 product registry 首次组合十个 provider 时，`go test ./internal/app/product` 在 SQLite/PostgreSQL 两行均返回 `migration violates forward atomic policy`。只读扫描把唯一违规定位到 Generator 双方言 `6400000000000_generator_configs.sql`：两者均在合法 `+goose Up` 后保留 `+goose Down` 和 `DROP TABLE generator_configs`，而平台 runner 明确拒绝 `Down`。

Lead 批准 `T17-D02`：只删除这两份 SQL 的 Down directive 与 DROP statement，不改变 Up schema、版本、provider、Generator 业务实现或其他共享路径。修复后必须重跑双方言 Compose、真实 SQLite 全迁移/能力注册、Generator 聚焦测试和 T-17 路径检查。

## 5. Stage 1 Checkpoint 与 T17-D03

Stage-1 source checkpoint 为 `92a0ff8a56226fd317c20e71e1665822c9226595`（tree `50965ffd689dfe1a3e79bd3468ff07af8d1ca4a5`；核心 composition commit `9c98ba4caa000de56e8f613b88bf9fc7f1abe2e4`）。它包含 49 路径 product OpenAPI、十 provider 双方言组合、真实 SQLite 全迁移与 13 菜单、八模块 HTTP runtime、共享 Scheduler/Outbox lease、完整资源回收、新 Server command、Web/Desktop 共享 manifest，以及 Browser shell identity/navigation 投影。产品 Go 普通/race/vet/build、Generator 全包 348.6 秒、app-shell TypeScript/3 tests、Redocly lint/bundle 与真实 Server SQLite readiness/capabilities/401 smoke 均通过。

只读 App/adapter/native preflight 证明 Web 仍为占位工作台且 Browser 使用错误冒号 Permission pattern；Desktop App、adapter、Rust contract/proxy 与 sidecar 仍只支持 Demo，双方均未消费共享 product workspace。Lead 批准 `T17-D03` 进入 stage 2，只开放产品接线所需精确共享路径和对应 importer/check，不重开模块业务实现；完整 native/Web E2E 仍延后最终统一候选。

Stage-2 首轮聚合测试在新的 Browser 点分 parser 上暴露 3 个旧 fixture 失败，并确认 platform `PermissionCode` 本身仍声明冒号格式。Lead 批准 `T17-D04`，只允许把该类型与 `<Path>go-admin-plus-ui/tests/shell/web-runtime.spec.ts</Path>` 的权限值替换为真实的点分 `module.resource.action`；不接受双格式 parser 或 compatibility alias。

随后完整 TypeScript aggregate gate 证明 `<Path>go-admin-plus-ui/tests/shell/app-shell.spec.ts</Path>` 也是 platform Permission 类型的直接编译消费者，其 5 个冒号字面量无法通过。`T17-D05` 只开放这些 fixture 字面量的点分替换，不改变 Shell 行为或断言。

## 6. Stage 2 Source Checkpoint

最终 clean source checkpoint 为 `e3abed759ac47f8f154c4638aaeab9eac9a41b7d`（tree `77fe889cbc8fb19286200786df9d312fbc708ba6`）。双 App 现在只选择 host adapter 并消费同一个 `ProductWorkspace`；共享 workspace 组合 IAM、Audit、Organization、Settings、Generator、Scheduler、Demo、Files 全部 controller/page。Desktop sidecar 直接消费 `product.Build`，Rust 使用精确产品 method/path allowlist，Stronghold 继续独占 Session/CSRF，并为 Files 提供有界 multipart/base64 bridge；IAM 三个 password 输入仅在精确 allowlist 中放行且发送后清零。

已通过：25-workspace 完整 TypeScript、132 项 Vitest、7 项 architecture boundary、lint、Web/Desktop production build、Rust 14 tests/clippy/check、Go full normal/vet/build（Generator tracked-skeleton 真实 gate 363.6 秒）、product/sidecar race、合同 lint/generate-check、governance 与 `TASK_CONTRACT_PASS`。source 完整 Go race 随后以 Generator 391.4 秒通过；统一 Web/native/profile E2E 继续延后 G8。

## 7. Parent Candidate 与实现提升

- **Parent before：** `ab01846437680619fd55070c52d0bd7d366a582a`
- **Source：** `e3abed759ac47f8f154c4638aaeab9eac9a41b7d`（tree `77fe889cbc8fb19286200786df9d312fbc708ba6`）
- **Candidate/result：** `04d7bdfe5aa26583c4101dd4e7dfbe8c67a864e8`（tree `779f538fbae8e983caa2fd67b55821e798ad8f2d`）
- **Method/attempt：** source 四个实现 commit cherry-pick 到 frozen parent candidate；attempt 1；`main` 以 `--ff-only` 提升
- **Retained locators：** `speculo/2026-08-26-project-architecture-reconstruction/T-17` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-17`；`speculo-candidate/2026-08-26-project-architecture-reconstruction/T-17-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/T-17-attempt-1`

候选从 frozen parent 完整通过 Go normal/race/vet/build，其中完整 race 的 Generator tracked-skeleton gate 为 347.8 秒；25-workspace frozen lint/typecheck、26 files / 132 Vitest、7 architecture boundary、Web/Desktop production build；Rust 14 tests、clippy 与 check；正式 sidecar resource build；通用合同 lint/generate-check、product Redocly lint、governance、`TASK_CONTRACT_PASS`、34-path writable/diff/clean-tree 与 SpecDev validator。validator 为 0 error；10 个 warning 均为既有历史 ownership 投影，不是本候选新增越界。

主线已精确包含 candidate/result，候选 worktree clean。T-17 实现结果可解除 T-18/T-19/T-20 的实现依赖，但 Ticket 与 Map 保持 `in_progress` / `implemented-pending-final-e2e`；三 Profile、Web/Desktop、权限、文件系统、可靠运行时和完整业务工作流只在 T-21 后的唯一最终系统候选统一执行。

## 2. Lead Dispatch And Candidate Return

Lead 已接收两阶段产品 composition source 与 candidate/result，并在最终 G8 候选统一验证 Web、Desktop、权限、文件与可靠运行时。

## 3. 修改范围与路径所有权

修改限于 product composition、三 profile manifest/OpenAPI、双 App 注入和获批共享注册路径；模块内部实现保持各自 owner。

## 4. 验收与合同映射

T-17 所列 `AC-003/004/011/021/022/024/025/028/034/035/036` 由完整 composition 非 E2E、双数据库 browser 和 native Desktop 结果闭合。

## 5. Workspace Verification

source-worktree 只执行并通过非 E2E 三 profile composition、合同、Go/pnpm/Rust 与 production build；E2E not-run 于 source-worktree，最终结果来自 G8 candidate。

## 6. 双轴审查

标准轴与规范轴闭合 migration baseline、IAM capability、data scope、adapter ownership 与 manifest 一致性；完成态零未关闭 finding。

## 7. Integration Verification

candidate/result `04d7bdfe5aa26583c4101dd4e7dfbe8c67a864e8` 及后续架构修复已进入 `main`，最终本地产品矩阵通过。

## 8. 偏差与决策

T17-D02 至 D04 均经批准并按 shared owner 串行；无未批准 deviation 或 blocker。

## 9. 残余风险与交付定位

个人自用完成范围内无 required 残余门禁；三平台保护动作 disposition 由 T-18/T-19/T-20 和 G8 Evidence 记录。
