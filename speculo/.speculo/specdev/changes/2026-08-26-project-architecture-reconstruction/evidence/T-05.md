# Evidence: T-05 - pnpm Workspace、App Shell 与交互基座

- **Change:** `2026-08-26-project-architecture-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/ticket/05-frontend-workspace-shell.md</Path>`
- **Lead / implementation owner:** `codex-root` / `codex-t05-frontend`
- **Base/source/candidate/result:** `27a186cbda7e77c403b3c64074260a10e924ee92` / `a648baf851d25861ac04eef688b8856fcf439b71` / `d5d10dda68fb6b49850c7442bf036b195ba1d202` / `d5d10dda68fb6b49850c7442bf036b195ba1d202`
- **Candidate tree:** `0511250c439a3233d9217fb41de598dfdc17dadb`
- **Parent before:** `c2260191c86942f859088d15b9a3ba9af96c975b`
- **Status:** done

## 1. 实现摘要

建立 24 个 package 的 pnpm Workspace、Admin Web 宿主、App Shell 状态机、Browser Runtime Adapter、Platform Port、无头 list/mutation controller 及 domain/web-domain 包骨架。Shell 对 authenticated、unauthenticated、unauthorized、not-found 和 adapter-failed 提供稳定状态；列表与写操作固定 stale request、重复提交、写入成功后刷新失败和删除后刷新失败语义。

## 2. Lead Dispatch And Candidate Return

Lead 从固定 base 将 T-05 派发给 `codex-t05-frontend`，source owner 只执行非 E2E 验证；required browser E2E、candidate integration 和 parent update 由 Lead 保留。owner 依次返回 `dd9c816ae8e0cddeb5160a6480a476459bf59384`、`89683cc1e7defdf5cfcc5564e62a43e6296787c0`、`5b7b4f43298a6b28ab55190baca07e05ffa50911`，最终针对首次 candidate 的移动端视觉 finding 返回 clean source `a648baf851d25861ac04eef688b8856fcf439b71`。

## 3. 修改范围与路径所有权

46 个 source 文件位于 T-05 frontmatter 的新前端 Workspace、Admin Web、adapter manifest/browser source、app-shell core、platform、ui、domain/web-domain manifest 和 shell test 路径。`T05-D01` 精确授权 app-shell manifest；`T05-D02` 精确授权 browser adapter 与 desktop manifest，Desktop adapter source 仍归 T-16。Lead 另在 T-01 根治理所有权内补齐新前端及递归 workspace `node_modules` 忽略规则，未修改旧前端、产品 composition 或 Tauri source。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-035 | list 搜索/分页、stale request、表单校验、确认取消、duplicate busy、一次写入、写入/删除成功后 refresh-failed 分阶段结果 | pass |
| AC-036 | authenticated、unauthenticated redirect、unauthorized、not-found、adapter-failed/retry；identity credential key 精确白名单 | pass |
| Workspace boundary | 24 packages、公开 exports、无 cycle/deep import、headless domain 不依赖 Vue、App 不拥有业务/transport | pass |
| Responsive UI | Chromium `1440x1000` 与 `390x844`；无横向溢出，移动导航高 `71px <= 120px`，无内容遮挡 | pass |

## 5. Workspace Verification

source-worktree 运行 frozen install、lint、typecheck、22 项 Vitest、6 项 workspace boundary 和 production build，并保持 clean；不得且未声明 required E2E 通过。Lead 在 parent-candidate/current-workspace 重跑同一套前端门禁，并补合同生成、根治理、SpecDev 校验、diff/clean-tree 与真实 Chromium E2E。

| Check | Result |
|---|---|
| `pnpm install --frozen-lockfile` | pass；24 个 workspace projects，lockfile 无漂移 |
| `pnpm verify` | pass；lint 0 error、typecheck、22 tests、6 boundary tests、Vite production build |
| `node scripts/contracts/cli.mjs generate --check` | pass；`CONTRACT_GENERATE_CHECK_PASS` |
| `sh scripts/go-admin-plus/governance-check.sh` | pass；`GOVERNANCE_CHECK_PASS` |
| SpecDev goal-plan validator / self-check | pass；均为 0 error / 0 warning |
| `git diff --check`、ancestor/tree/status | pass；source 与 parent-before 均为 candidate ancestor，candidate clean |

本机没有 Go Task 可执行文件，`task-contract.sh` 按设计返回 “Go Task is required”；因此没有伪造该命令通过。T-05 使用其底层受管脚本与前端 package scripts 完成等价实际门禁。`implement` 终态 validator 还会要求 T-06 至 T-21 的 workspace records，当前中间态按合同使用 goal-plan stage 校验。

## 6. 双轴审查

规范轴在 `T05-D02` 后 pass。标准轴首轮发现 1 high、2 medium、1 low：post-write refresh failure 被误报写失败、异步导航乱序覆盖、identity 未知 credential key 未 fail closed、stale list error 语义不稳定；修正后固定范围 `27a186c..5b7b4f4` 双轴均 pass、0 findings。移动 CSS 修正后的最终范围 `27a186c..a648baf` 再次双轴审查，均 pass、0 findings。

## 7. Integration Verification

首次 candidate `f2bbd5211eada20b8ddc3762cba680bc7944ceae` 的功能断言通过，但 Lead 截图发现 390px 导航占据近半屏，required visual E2E 判定失败，未晋升并清理。第二次 candidate 在父分支治理修正后刷新，最终固定为 `d5d10dda68fb6b49850c7442bf036b195ba1d202`（tree `0511250c439a3233d9217fb41de598dfdc17dadb`）；全部门禁和 Chromium E2E 通过后，`main` 从 `c2260191c86942f859088d15b9a3ba9af96c975b` 以 `--ff-only` 晋升到同一 result。

浏览器最终摘要为：6 次 identity 请求；mutation write 1 次、duplicate=`busy`、completed=`submitted`、post-write refresh failure=`refresh-failed`；remove 1 次且 post-remove refresh failure=`refresh-failed`；移动端 `noOverflow=true`、`navigationHeight=71`。Lead 人工检查六张 desktop/mobile 截图，未见溢出、遮挡或错误构图。

## 8. 偏差与决策

`T05-D01`、`T05-D02` 均为已批准且精确限界的 Ticket 偏差，不改变 Spec 行为。`T05-D03` 由 T02-D04 全 Workspace 回归触发：已规划 API client 使硬编码 package 总数失效，Lead 批准只修正 T-05 自有 boundary test 为必需集合与开放扩展检查；新的 source/candidate/result 记录如下。首次视觉失败按 required E2E Gate 阻断而非降级验收；第二次 attempt 才允许 promotion。用户 `Q1A/Q2A` 授权本地 candidate lifecycle、implementation commit 与 parent update；未执行远端、生产、正式发布或 source cleanup。

### T05-D03 Correction Result

- **Base/source/candidate/result:** `12171f0a715b725ca5ddc5c48fd3393a5341afdb` / `832aede2541ac8dc0ee02c587c59d709889b5aad` / `832aede2541ac8dc0ee02c587c59d709889b5aad` / `832aede2541ac8dc0ee02c587c59d709889b5aad`
- **Candidate tree:** `6cf2e4f63211a2b934881a495e176878c4b2a1eb`; canonical candidate branch/workspace `speculo/integration/2026-08-26-project-architecture-reconstruction/T-05` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-05`; method `fast-forward`; attempt 1; no conflicts.
- **Path/review:** only `<Path>go-admin-plus-ui/tests/shell/workspace-boundary.test.mjs</Path>` changed; standard and conformance axes both pass with 0 findings.
- **Verification:** frozen install/peers, `pnpm verify`, Go normal/SQLite/vet/mod verify, canonical lint/generate-check, governance, SpecDev, diff/clean-tree all pass. Parallel contract suite exposed the pre-existing T02-D04-owned global temp assertion; the same 23-test suite passed with concurrency 1, and T02-D04 source replaces it with an isolated temp root before its own result.
- **E2E:** prior real-browser pass remains applicable because the correction changes only a Node boundary test; `git diff d5d10dda..832aede -- go-admin-plus-ui` excluding tests is empty, so runtime/UI/style assets are byte-identical.

## 9. 残余风险与交付定位

T-05 只建立可运行前端壳和共享交互基座，真实 IAM/模块页面由 T-06 至 T-15 填充，Desktop Runtime Adapter/Tauri 宿主由 T-16 实现，双 App 产品组合由 T-17 完成。截图和浏览器 harness 属 candidate transient artifact，结果已固化到本 Evidence；source branch/worktree按未授权 cleanup 合同保留。

## 10. T05-D04 视觉合同修正

最终编码审计把重构前基线 `888900751ab2b04d2db5fa8d5e6a6b811599ce93` 的 layout、Navbar、Sidebar、TagsView、design tokens 与登录页逐项对照当前 `ProductWorkspace`。审计确认当前壳曾退化为白色简易侧栏和单层顶栏，未满足用户“页面 UI/CSS 设计与之前保持一致，删减功能只在原样基础上删减”的执行约束。Lead 批准 `T05-D04`，并在当前 pnpm package 边界内直接修正，不恢复旧路由、store、import、验证码或已删除业务能力。

- **Base/source/result:** `22d1d65394462bb90bb88366e2244b9ed8715ce2` / `4cd5c506d1317e961a57cb58e509c2ad5b2c0259` / `4cd5c506d1317e961a57cb58e509c2ad5b2c0259`
- **Result tree:** `cb1e7d3ed4f469948e1308e0d62d5be4f2c7fc63`
- **实现：** `<Path>packages/ui/src/admin-theme.css</Path>` 成为 Web/Desktop 共用设计令牌和管理页面视觉层；`ProductWorkspace` 恢复 `210px/54px` 深色可折叠侧栏、`50px` 顶栏、面包屑、`40px` 已访问页签、账户菜单和移动抽屉；登录页恢复终端舞台、Go Gopher 与安静表单区，并使用当前根任务与 Tauri 2 文案。
- **防回退：** 新增 3 项源码视觉合同断言，并把 workspace 边界从旧横向移动导航更新为共享 UI 依赖和 off-canvas drawer 合同。
- **非 E2E 验证：** frozen install、lint、全 workspace typecheck、28 files / 139 tests、7 workspace boundaries、Web/Desktop production build、governance、architecture、compatibility-zero、docs、CI 固定 `go-task v3.48.0` 的 `TASK_CONTRACT_PASS` 和 2 项 quality tests 全部通过；`git diff --check` 通过。
- **E2E disposition:** 用户明确要求先完成构建与编码且暂停 E2E，因此本修正没有运行浏览器、Desktop native 或视觉截图 E2E；既有 G8 pending 状态不变，不能据此声明最终视觉验收通过。

## 11. T05-D05 管理页面视觉合同补全

对 `T05-D04` 结果继续逐页检查后，确认首次修正只足以证明 Shell 与登录页构图，仍不足以证明 retained management pages 延续重构前的页面语言和紧凑后台样式。IAM Administration 仍为英文，路由菜单和多个业务页残留英文文案；各页面 scoped CSS 还用 `#176b54`、`#17202a`、`#d7dce2` 等临时颜色覆盖共享 theme；Shell 内容区仍为 `20px`，搜索、表格操作、编辑区和分页也没有形成统一源码合同。Lead 因此登记 `T05-D05`，只补足展示层和相应静态测试，不改变 controller、API、Permission Code、字段名、`data-testid` 或删减后的功能集合。

- **Base/source/result:** `7a14cd01e2426aca929c8a1baea2506774436b48` / `f6f852cf715df2d0c26bb583ecac10639f578b30` / `f6f852cf715df2d0c26bb583ecac10639f578b30`
- **Result tree:** `44c0a3e5a1a89791669c47d3d85d41d9209e73ba`
- **实现：** retained product routes 与 9 个管理页面改为中文；IAM 用户/角色/菜单管理的状态、查询、表格、编辑和权限分配文案完整本地化；所有业务页的临时 scoped palette 改为共享 design tokens；Shell 外层恢复 `12px` spacing；共享 theme 固定页面 header、查询区、文本式表格操作、危险操作、编辑分隔带、fieldsets、横向表格容器和右对齐分页；登录页 Web 地址修正为实际 Vite `5173`。
- **防回退：** visual contract 从 3 项增至 4 项，新增中文路由/页面、旧 palette 零命中、共享布局 selector、`12px` spacing 和 `5173` 端口断言；Generator 既有源码文案断言同步到当前中文确认文本。
- **Frontend gates:** Oxlint、全 workspace typecheck、28 files / 140 tests、7 workspace boundaries、Web/Desktop production Vite build 全部通过。
- **Backend/root gates:** Go normal 与 SQLite 全量测试通过，Generator 分别实测 `393.255s` / `403.215s`；Go vet、SQLite/PostgreSQL Server build、四个 `CGO_ENABLED=0` command build、OpenAPI lint、28/28 contract tests、API client typecheck 与 7/7 tests、generate-check、governance、architecture、compatibility-zero、docs、固定 Go Task `v3.48.0` 的 `TASK_CONTRACT_PASS`、16/16 release policy tests 全部通过。
- **Tauri 2 gates:** production host sidecar build、Rust 20/20 tests、Clippy `-D warnings`、locked release `custom-protocol` check 全部通过；生产 sidecar 构建未启用 native E2E feature。
- **环境说明:** 首次 Task 合同聚合命令只通过 Volta shim 启动后在 child PATH 找不到 pnpm；第二次最小 PATH 又遗漏 goenv Go 实体路径。二者均为环境命令失败且不计 pass；使用 Node `22.22.3`、pnpm `11.1.3`、Go `1.26.7` 和 Go Task `v3.48.0` 实体路径后从受影响合同阶段完整重跑并通过，仓库无生成漂移。
- **E2E disposition:** 遵从用户暂停 E2E 的最新指令，本轮未运行 browser、fault、Desktop native、视觉截图或平台安装/签名 E2E。G8 继续 pending，本证据只证明编码、静态、单元、合同和构建完成，不声称最终视觉或系统验收已完成。

## 12. T05-D06 管理交互合同修正

逐页对照既有后台交互后，确认 `T05-D05` 虽恢复了中文和紧凑视觉，但 IAM、Organization、Demo、Settings 与 Scheduler 的新增/编辑表单仍永久占据页面，而不是既有的工具栏入口与弹窗工作流。同步审计还发现暂停的 browser/native 驱动仍依赖已删除的英文可见标签，Desktop 未登录边界无法命中当前登录页，Scheduler 的页面 settle helper 丢弃 command result，导致成功保存后弹窗无法关闭。Lead 登记 `T05-D06`，在现有无头 controller、Permission 和双宿主边界内修正，不引入 Element Plus 或任何兼容层。

- **Base/source/result:** `8165fffae1e659b732e563fe7a10b46dd56089bf` / `89f70304c8227d7401d7791b7f7c64e0f1cd2e84` / `89f70304c8227d7401d7791b7f7c64e0f1cd2e84`
- **Result tree:** `1cf6552b289ee3112638ee2facc59c594566e447`
- **实现：** 共享 theme 增加响应式管理弹窗合同；IAM 用户/角色/菜单、Organization 部门/岗位、Demo 产品、Settings 参数/字典/字典项和 Scheduler 任务定义均改为显式新增入口与创建/编辑弹窗；取消、遮罩关闭和切换页签会清空未提交状态，用户密码不跨弹窗保留；Scheduler settle helper 返回稳定 command result 并新增单测；Audit 与 Scheduler 的用户可见枚举完整中文化。
- **Harness 同步：** IAM、Organization、Scheduler、Audit、Demo、Files、Settings browser driver 与 Desktop native runner 改用当前中文页签、按钮、状态、告警及可访问名称；Desktop 未登录边界改为当前登录副标题；driver 保持暂停，只做静态编译与 Node 单测。
- **防回退：** visual contract 从 4 项增至 6 项，新增共享弹窗结构、五个 retained CRUD 页面显式 open control/`role=dialog`/零常驻 `.editor`，以及 browser/native driver 当前中文标签和旧标签零命中断言。
- **Frontend gates:** `pnpm lint`、全 workspace `pnpm typecheck`、28 files / 143 tests、7 workspace boundaries、7 个已修改 browser driver 的独立 `vue-tsc`、Desktop/diagnostics 19/19 Node tests、Web/Desktop production Vite build 和 `git diff --check` 全部通过。
- **E2E disposition:** 遵从用户“先完成所有构建、编码，不要 E2E”的最新指令，本轮没有运行 browser、fault、Desktop native、视觉截图或平台 E2E。G8 与各 Ticket 的最终 E2E 保持 pending；本结果只证明实现、静态契约、单元测试、类型检查和双端构建通过。

## 13. T05-D07 精确尺寸与样式所有权修正

第四轮审计不再只检查 Shell 区域和 token 是否存在，而是逐条读取重构前基线 `888900751ab2b04d2db5fa8d5e6a6b811599ce93` 的 `sidebar.scss`、`TagsView.vue`、`PageContainer.vue` 与当前九个 retained domain page 的最终 scoped CSS。审计确认旧侧栏菜单标题/项目可见高度为 `50px`，旧 TagsView 可见高度为 `34px`；`T05-D04` 却把页签误固化为 `40px`，当前侧栏条目也只有 `40/42px`。同时 Administration、Audit、Organization、Scheduler 和 Account 的 scoped CSS 仍以 `24px` padding、宽度上限和 `40px` controls 覆盖共享紧凑管理面，其他页面继续覆盖共享 table/button/alert 样式。Lead 登记 `T05-D07`，只纠正展示层所有权和静态合同，不修改任何产品行为。

- **Base/implementation/result:** `9458c99424883b1e5bf6777add2c763d48710e37` / `a38c7cf3b4f4e756393f487a6cc0f45a2e0449e2` / `a38c7cf3b4f4e756393f487a6cc0f45a2e0449e2`
- **Result tree:** `f510a297cb412c9c9b8b29f828f3dfbe63493414`
- **实现:** Shell 恢复 `50px` sidebar group/item、`34px` TagsView 与 `84px` header stack content viewport；共享 theme 补齐旧基线仍适用于当前原生 CSS 的 info/elevated/inverse/light-sidebar token；九个 retained page 删除会覆盖共享页面 padding、width、control height、table collapse/padding 和 alert palette 的 scoped 规则，只保留领域特有布局。
- **防回退:** visual contract 从 6 项增至 7 项，以 selector-scoped 正则固定 navbar/sidebar/TagsView/content 精确尺寸，并逐页拒绝 page-surface `max-width/padding`、`40px` base controls 和 collapsed table 回流。
- **Frontend gates:** pnpm `11.1.3` 下 Oxlint deny warnings、全部 workspace typecheck、九个暂停 E2E driver 的纯 TypeScript/Vue 编译、28 files / 144 tests、7 workspace boundaries、Web/Desktop production Vite build 和 `git diff --check` 全部通过。
- **Root gates:** governance、architecture、compatibility-zero、docs 与 2 项 quality regression 全部通过；后端、OpenAPI、Rust、发行脚本和依赖清单零变更。
- **E2E disposition:** 按用户要求没有运行浏览器、视觉截图、fault、Desktop native 或平台发行 E2E。该结果闭合可由源码与构建证明的 UI/CSS 偏差，但最终渲染验收仍属于暂停的 G8。
