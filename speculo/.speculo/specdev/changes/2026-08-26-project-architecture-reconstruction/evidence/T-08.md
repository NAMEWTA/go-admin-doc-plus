# Evidence: T-08 - 无 Redis 可靠运行时

- **Ticket：** `T-08`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-08` / `speculo/2026-08-26-project-architecture-reconstruction/T-08`
- **Base/source/candidate/result：** `05d619d1c4c90378a0138c630fa1aa3bcfa0f942` / `212f0fb785ea92480a0185fd0cacf592d844d969` / `503ea063092d9ee595c8adcb59308edca691171b` / `503ea063092d9ee595c8adcb59308edca691171b`
- **状态：** done

## 1. 实现摘要

新增双方言 Transactional Outbox、固定 PostgreSQL advisory lease、SQLite 单实例接缝、结构化幂等 consumer mutation、claim fencing/retry/receipt、脱敏 observer 和有界可禁用本地缓存。事件 topic 使用显式 payload schema 和非敏感 BusinessKey；重复 JSON member 被递归拒绝，只持久化 canonical payload，任意 SQL、公开 Claim/Deliver/Retry、Redis 和缓存事实源均不存在。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex-t08-reliability`
- **初始 base：** `05d619d1c4c90378a0138c630fa1aa3bcfa0f942`
- **最终返回：** clean source `212f0fb785ea92480a0185fd0cacf592d844d969`；21 个授权路径；无 `go.mod/go.sum` 变化；未在 source 声称 required E2E。
- **Lead 核对：** source commit、路径、双轴固定输入、默认 skip harness、candidate ancestry/tree 和 clean 状态均通过。

## 3. 修改范围与路径所有权

| 路径 | 所有权与用途 |
|---|---|
| `<Path>go-admin-plus/internal/platform/outbox/**</Path>` | T-08；事件 schema、store、dispatcher、mutation 与故障回归 |
| `<Path>go-admin-plus/internal/platform/coordination/**</Path>` | T-08；固定 advisory lease 与失锁分类 |
| `<Path>go-admin-plus/internal/platform/localcache/**</Path>` | T-08；有界 TTL/LRU、禁用、clear generation 与聚合指标 |
| `<Path>go-admin-plus/internal/platform/migrations/reliable-runtime/**</Path>` | T-08；PostgreSQL/SQLite forward-only schema |
| `<Path>go-admin-plus/test/reliable-runtime/**</Path>` | T-08；原子、恢复、隐私、race 和跨进程 PostgreSQL E2E |

未修改 kernel、Database、公共 migration runner、依赖清单或任何 Ticket 外路径。

## 4. 验收与合同映射

| 合同 | 证据 | 结果 |
|---|---|---|
| AC-018 | 状态+event 同事务；唯一 claim token/expiry；receipt 幂等；真实 PG backend termination、回滚、takeover、retry/replay | pass |
| AC-034 | 缓存 disabled/clear/expiry/generation/race；Redis diff 与源码扫描零命中 | pass |
| 事件隐私 | topic schema、closed string labels、canonical JSON、duplicate-key 和敏感 BusinessKey 负向测试 | pass |
| 运营信号 | pending age、claim duration、attempt/retry、active executor/lost lock；无 topic/key/payload/error | pass |
| SQLite 单实例 | 复用 T-04 Database process lock；candidate 全量含真实 native cross-process lock 回归 | pass |

## 5. Workspace Verification

| Check | 结果 |
|---|---|
| Go 普通、`-tags sqlite3`、全量 race、vet、build、module verify | pass |
| governance、SpecDev goal-plan、diff/clean-tree | pass；0 error / 0 warning |
| 合同串行 suite、lint、generate check、API client | pass；28/28、7/7、无 drift |
| 新 Workspace frozen install、peer、verify | pass；22 unit、6 boundary、typecheck/build |
| PostgreSQL 18.3 required E2E | pass；graceful exclusion/takeover + 多 OS 进程 backend termination/recovery/replay |

首次合同命令使用 Node 默认文件并发，在空 UI virtual store 内触发两个 pnpm 安装并产生 `chmod ENOENT`；该编排结果作废。Go 门禁完成后先串行 frozen install，再以 `--test-concurrency=1` 重跑合同，28/28 全绿。此 incident 未改产品文件或 candidate commit。

## 6. 双轴审查

- **最终固定输入：** `05d619d1c4c90378a0138c630fa1aa3bcfa0f942..212f0fb785ea92480a0185fd0cacf592d844d969`
- **标准轴：** pass，0 findings。
- **规范轴：** pass，0 findings。
- **修正历史：** 依次作废任意 SQL/可绕过 Store、lease 伪造、字段名敏感猜测、缺失指标、raw/duplicate payload、敏感 enum 成员和不可复现 PG E2E checkpoint；最终以结构化 mutation、真实 Lease、canonical schema 和受跟踪的跨进程故障 harness 全部闭合。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `08a6535f63ec5da8824f69d1c19fc851afa4f998` |
| Source SHA | `212f0fb785ea92480a0185fd0cacf592d844d969` |
| Candidate branch/workspace | `speculo/integration/2026-08-26-project-architecture-reconstruction/T-08` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-08` |
| Method/conflicts/attempt | merge-commit；无冲突；attempt 1 |
| Candidate/result/tree | `503ea063092d9ee595c8adcb59308edca691171b` / `503ea063092d9ee595c8adcb59308edca691171b` / `44fd724d0764ca3843714b6ede8c9de8ab902dd6` |
| E2E disposition/result | required / pass |
| Parent promotion | `main` 从精确 frozen parent 以 `--ff-only` 提升；父/source ancestry 与最终 HEAD 已核对 |

## 8. 偏差与决策

无 Spec/ADR/Ticket 级产品偏差。审查收紧均位于 T-08 原有路径和安全合同内。用户 `Q1A/Q2A` 已授权 worktree、implementation commit、Lead local candidate integration 和 parent update；未操作远端、生产、正式发布或 source cleanup。

## 9. 残余风险与交付定位

- 具体 Audit/Scheduler consumer 由 T-11/T-12 提供，T-17 组合产品 worker lifecycle，T-21 执行最终 Redis/旧 runtime 零命中收缩。
- PostgreSQL E2E 使用 disposable 数据库并清理动态表、trigger、function、outbox/receipt；临时启动 runner 已停止并删除，受跟踪 harness 保留可复现性。
- source/candidate worktree 按当前未授权 cleanup 合同保留。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/evidence/T-08.md</Path>`
