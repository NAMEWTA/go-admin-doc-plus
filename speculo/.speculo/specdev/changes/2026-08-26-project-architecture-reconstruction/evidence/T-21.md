# T-21 Evidence: 原子切换、质量门禁与旧体系归零

## 1. 激活与冻结基线

- **Base：** `5fb6f86dfd52dcd7aad93ab7380b9089e14c02b9`
- **Source branch/worktree：** `speculo/2026-08-26-project-architecture-reconstruction/T-21` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-21`
- **Owner：** implementation/integration 均为 `codex-root`
- **前置结果：** T-17 `04d7bdfe5aa26583c4101dd4e7dfbe8c67a864e8`、T-18 `37f989c7c64afb17c05bdeb35818c34416e753ca`、T-19 `f18d765246aad076f92c5ae8fda2732283a4186a`、T-20 `10aecc19dab8cb2a80c0fc8e16af6ecab2208d10` 均已包含于 frozen base。
- **E2E disposition：** source 与 parent-candidate 只完成原子收缩和全部非 E2E Gate；统一三 profile、双 App、模块/browser/native/发行 E2E 在实现进入 `main` 后由唯一最终系统候选执行。

### 冻结删除 inventory

1. `go-admin-plus/.dockerignore`
2. `go-admin-plus/.gitignore`
3. `go-admin-plus/Dockerfile`
4. `go-admin-plus/Makefile`
5. `go-admin-plus/cmd/go-admin-desktop/.gitignore`
6. `go-admin-plus/docker-compose.yml`
7. `go-admin-plus/scripts/Dockerfile`
8. `go-admin-ui-plus/.dockerignore`
9. `go-admin-ui-plus/.editorconfig`
10. `go-admin-ui-plus/.gitignore`
11. `go-admin-ui-plus/.husky/pre-commit`
12. `go-admin-ui-plus/Dockerfile`

删除仅在 T-21 source worktree 内逐项执行；不删除 Speculo 历史、Git 元数据、任何 source/candidate workspace 或 Ticket 可写范围外资产。零兼容 RED 基线、全仓引用分类、实际删除清单和 Gate 结果将在后续章节追加。

## 2. RED 盘点与 T21-D01

正式入口 `go list -deps ./cmd/go-admin-plus ./cmd/desktop-sidecar` 对 `go-admin/app`、`go-admin/common`、`go-admin/api` 与 `go-admin/internal/tenant` 为零依赖，只消费新 product/module/platform 结构。但全仓测试会继续编译未被入口消费的 expand 期包：`internal/modules` 根兼容集合、`internal/profile`、旧 Desktop host、root platform adapters、旧 migrate/cobra command 与 characterization/desktop tests 均引用待删除目录。根 `Taskfile` 管理脚本仍构建旧 root main 并使用 `sqlite3` tag；CI、Windows tracer、聚合 release manifest 和当前文档仍硬编码旧前端、Wails 与 unsigned-self-use。

Lead 据此批准 `T21-D01`，只开放 Ticket frontmatter 列出的精确遗留 scaffolding 与根治理接缝。正式 product/module/platform 子包、三个正式 command、前端新 Workspace 和 T18-T20 平台资产继续只读。此 amendment 是闭合 AC-002/AC-029/AC-033 所必需，不引入兼容层或产品行为扩张。

首次 compile RED 证明 `internal/application`、`internal/contracts` 和 `localcache` 是新产品/验收的真实依赖，`cache/observability` 也是已交付平台能力；它们已从删除 index 精确恢复。`T21-D02` 仅追加开放 4 个前端管理脚本、2 个 Linux dockerignore 和当前配置 README，修正不存在的 Desktop package filter 与旧目录引用，不修改产品源码。

`T21-D03` 进一步将 3 个仍消费旧 workspace 的前端公共脚本和 2 个 Tauri 嵌套 `.gitignore` 纳入根治理收口。对应忽略规则迁移到根后删除嵌套文件；新前端产品源码保持只读。

## 3. Source Checkpoint

- **Source commit：** `c6c669b720b6be7448de4a00a82940caf0a1abd0`
- **Source tree：** `04f5639e9f2bf284a139555a6b5e4f091da82724`
- **固定审查范围：** `5fb6f86dfd52dcd7aad93ab7380b9089e14c02b9..c6c669b720b6be7448de4a00a82940caf0a1abd0`
- **变更规模：** 959 files changed，925 insertions，88,946 deletions；旧前端、旧后端分层、Wails、租户、旧配置/SQL/命令/治理资产完成原子收缩。
- **路径结果：** 变更全部位于 `T21-D01` 至 `T21-D03` 批准的 writable/shared paths；source worktree clean。

该 checkpoint 只保留目标架构：后端以 `cmd/go-admin-plus`、`cmd/desktop-sidecar`、`cmd/config-check`、`cmd/migrate` 和 `internal/{app,application,contracts,modules,platform}` 为正式结构；前端仅保留 `go-admin-plus-ui` pnpm monorepo 与 Tauri 2 Desktop。新的 migrate command 只接受 Server SQLite/PostgreSQL，明确拒绝 Desktop profile，并通过真实 SQLite 首次迁移与二次幂等测试。根 `.github`、`.gitignore`、Task、scripts、deploy/database/release、CI/CodeQL/release workflow 和当前文档成为唯一治理面。

### Source Gate

- 架构、零兼容、文档、根治理与 Task 合同：`ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、`DOCS_CHECK_PASS`、`GOVERNANCE_CHECK_PASS`、`TASK_CONTRACT_PASS`。
- Go：normal、SQLite、full race、`go vet ./...`、CGO-disabled 四命令构建、`go mod tidy` 零差异全部通过；Generator 真实工作区生成/编译在三套全量测试中均通过。
- Frontend：25 workspaces，lint、typecheck、26 files / 132 tests、7 workspace boundary、Web build、Desktop Vite build 全部通过。
- OpenAPI：lint、28/28 tests、generate-check、API client typecheck 与 7/7 tests 全部通过。
- Tauri/Rust：按 sidecar-before-cargo 准备 native sidecar 后，19/19 tests、strict clippy、release custom-protocol check 全部通过。
- 发行策略：manifest/quality tests 6/6、Linux/macOS/Windows policy 16/16、`release:verify` 和产品 preflight 全部通过；profile/version/OpenAPI/migration identity 一致。
- 根功能入口：`task migrate PROFILE=server-sqlite` 实际应用 10 个迁移并到达 `8100000000000`；`task build TARGET=all` 与 `task package TARGET=server` 通过。
- YAML/JSON、shell/Node syntax、`git diff --check`、module tidy 与 source clean-tree 全部通过。

### 明确未宣称通过的边界

- parent-candidate integration 尚未执行，本节只证明 immutable source checkpoint。
- 三 profile、双 App、模块/browser/native/reliable-runtime 与三平台受保护发行 E2E 仍按 Goal Plan 延后到 T-21 实现进入 `main` 后的唯一 G8 最终系统候选；本地 source Gate 不替代受保护 runner、签名、公证、原生安装和远端发行授权。

## 4. Parent Candidate 与实现提升

- **Parent before：** `e1b218c6ca517811ce7874b843fce176d0d44443`
- **Candidate/result：** `abce91f9331340311855c4966063a8529629711d`（tree `45e839fa4363d32ea1aaaa8dc7a72f4603dcfaf4`）
- **Method/attempt：** final source 合入最新 parent candidate；无冲突 merge-commit attempt 1；`main` 以 `--ff-only` 提升
- **Retained locators：** source `speculo/2026-08-26-project-architecture-reconstruction/T-21` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-21`；candidate `speculo-candidate/2026-08-26-project-architecture-reconstruction/T-21-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/T-21-attempt-1`

attempt 1 从 clean candidate 通过 architecture/compatibility/docs/governance/Task contract、release policy 16/16 与 Speculo 0-error Gate；Go normal、SQLite、race 的 Generator 真实编译分别为 375.348 秒、359.483 秒、344.809 秒，随后 vet、CGO-disabled 四命令构建与 tidy 零差异通过；25-workspace frontend lint/typecheck、26 files / 132 tests、7 boundaries 和 Web/Desktop build 通过；OpenAPI 28/28、generate-check 与 API client 7/7 通过；native sidecar-before-cargo、Rust 19/19、strict clippy 与 release/custom-protocol check 通过；实际 SQLite 迁移 10 项、全目标构建、Server package、0.1.0 release preflight、YAML/JSON/shell/Node syntax、path/diff/clean-tree 全部通过。

候选第一次通过绝对路径调用 Go Task，导致 `task:contract` 的子进程无法从 `PATH` 发现 Task v3.48.0；将同一固定二进制加入候选进程 `PATH` 后，该 Gate 与后续 `release:verify` 从零重跑并通过，未修改源码或 candidate commit。辅助语法检查最初误用 `sh -n` 解析声明为 Bash 且使用 process substitution 的 macOS 签名脚本，并假定了不存在的 JSON manifest 文件名；按 shebang 和实际 identity/Tauri JSON 路径修正后全部通过，这两项命令选择错误均未记为产品通过。

`main` 快进后，已删除的嵌套 `.gitignore` 暴露了主工作区原先隐藏的 755MB 旧 Wails、旧 UI node_modules/dist/env、旧 command/binary、local config 与 upload 运行产物。它们不属于 candidate commit，已逐项移动到根忽略的可恢复隔离区 `.artifacts/t21-main-legacy-leftovers/`，未删除任何 source/candidate worktree；实际产品路径再次通过 clean、compatibility-zero、architecture 与 docs Gate。

T-21 实现现已进入 `main`，G7 原子收缩完成。Ticket 与 Map 继续保持 `in_progress` / `implemented-pending-final-e2e`：三 profile、双 App、全模块/browser/native/reliable-runtime 场景以及 Linux 双架构、macOS 签名/公证、Windows Authenticode/原生安装信任仍须由唯一 G8 最终系统候选和对应受保护 runner 执行；远端发布、生产签名、公证和凭据使用未获授权，也未运行或声明通过。

## 5. T21-D04：项目 Agent Skills 收口

最终文档审计发现 `.agents/skills/new-business-module/SKILL.md` 与 `.agents/skills/new-list-page/SKILL.md` 仍把后续开发引向已经删除的后端分层、前端目录、权限种子、迁移和页面状态模式，并且 `compatibility-zero` 与 `docs-check` 均未覆盖项目 Skill。Lead 据此批准 `T21-D04`，将两份 Skill 重写为当前双方言 Migration、OpenAPI strict transport、IAM capability registry、pnpm headless Domain/Web Domain、共享 list controller、管理弹窗和双 App 产品组合工作流；不恢复任何已删除功能或兼容路径。

- **Implementation commit：** `95663eb35d8554f5f66c9b8fdc965e9914943d50`
- **Implementation tree：** `23427ee92cec831ae1371fcc646fa30dcfc8f460`
- **变更范围：** 两份项目 Skill、`compatibility-zero` 及其测试、`docs-check`，共 5 个文件；产品源码零变更。
- **Skill 验证：** 两份 Skill 均通过 `quick_validate.py`，并由 `package_skill.py` 成功打包到系统临时目录；仓库未产生 `.skill` 制品。
- **后置语义修正：** `2906c2c459834fc7b6d5a9b9a5250960875f9811`（tree `3963687ed022eee614e38da382101e45bbce1639`）删除列表页 Skill 仍残留的 UI 框架耦合描述；重新 quick-validate、临时打包与全部静态治理门禁通过。
- **治理验证：** `COMPATIBILITY_ZERO_PASS`、兼容性回归 `1/1`、`DOCS_CHECK_PASS`、`ARCHITECTURE_CHECK_PASS`、`scripts/quality/*.test.mjs` `2/2`、`git diff --check` 均通过。
- **Task 启动器说明：** 当前 shell 未安装 `task`，因此三个根任务未由 CLI 包装器启动；已直接执行 Taskfile 中对应的唯一命令并全部通过，未安装系统工具或把缺失启动器记为产品失败。
- **E2E disposition：** 按用户暂停指令未运行 browser、fault、native 或发行 E2E；本修正不触及运行时代码，G8 继续保持 pending。

## 6. T21-D05：Compose Git 规则归根

全仓 tracked-file 审计发现 T-18 的 Compose runtime 与 secrets 目录仍各自保留 `.gitignore`，而根治理检查只扫描后端和前端源码树，因此无法兑现单一根 Git 治理。修正先扩展扫描并取得只命中这两个文件的有效 RED，再将其规则迁移到根并删除嵌套文件。

- **Implementation commit：** `14670cf65e48a30e2462eff335c7987839342333`
- **Implementation tree：** `9b7967862b7c4737086decd39ef502109f898c67`
- **RED：** 修正后的全仓治理扫描只报告 `deploy/compose/runtime/.gitignore` 与 `deploy/compose/runtime/secrets/.gitignore`，退出码为 1。
- **GREEN：** `GOVERNANCE_CHECK_PASS`；根 `.gitignore` 对普通 runtime 文件、`postgres_password` 和 `database_dsn` 的 `git check-ignore` 均命中对应根规则。
- **后置覆盖修正：** `2bab149ce72aa26cca85e1543581088d2c115570`（tree `8682419e27e5f065b333dc15dde9c1528dc1bda6`）移除 `.agents` 剪枝，使项目 Skill 也受全仓根治理扫描；治理及三项静态门禁重新通过。
- **回归：** `ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、`DOCS_CHECK_PASS`、quality tests `2/2`、shell syntax 与 `git diff --check` 全部通过。
- **范围：** 只修改根 `.gitignore`、既有 governance check，并删除两个 T-18 已冻结后的嵌套治理文件；Compose 配置、secret 内容、产品源码和发行逻辑零变更。
- **E2E disposition：** 按用户暂停指令未运行 E2E；G8 继续保持 pending。

## 7. T21-D06：AC-029 全仓零兼容扫描

完成审计证明旧扫描只覆盖少量入口目录，未达到 Spec 的全仓 allowlist 合同，也没有检测 JWT、refresh token、AutoMigrate 与旧 ORM。实现改为扫描仓库全部已知文本源码、配置、SQL、脚本、Rust/Vue 与锁文件；Speculo 历史、Git 数据、依赖和构建产物被剪枝，含 NUL 的二进制被安全跳过。

- **Implementation commit：** `e93e74ec0779538a6a1dcc1b820054d5942cebc6`
- **Implementation tree：** `7db38de323b5dc1e4bc074cd7d77a6263d4f6bcd`
- **Allowlist：** 19 个精确文件仅允许各自的许可证归属、安全拒绝、负向测试或发行策略类别；文件未获准的其他禁止类别仍失败。
- **失败探针：** 临时 Go 产品源码中的 GORM/JWT、Rust 源码中的 refresh token、Agent Skill 中的 Casbin 和已删除路径均被检测；许可的架构负向测试通过，无扩展名含 NUL 二进制被跳过。
- **回归：** `COMPATIBILITY_ZERO_PASS`、定向测试 `1/1`、quality tests `2/2`、governance/architecture/docs、Node syntax 与 `git diff --check` 全部通过。
- **E2E disposition：** 未运行 E2E；该修正只强化静态门禁，G8 继续保持 pending。

## 8. 当前 HEAD 完整非 E2E 编码与构建复验

在 `main@d38cf4c67db04d3ffe47e53090b43db9f1f79d11`（tree `4e920beb63478cf4147a423be7c035bddf164a7a`）上，以 CI 固定 Go Task `v3.48.0` 和 workspace 固定 pnpm `11.1.3` 从根公开任务重新验证。工具只安装/链接到系统临时目录，未写入仓库。

- **根任务：** governance、architecture、compatibility-zero、docs 与 `task:contract` 全部通过；后者补齐了先前本机缺少 Task 启动器的证据缺口。
- **Lint/typecheck：** `task lint` 通过，包括 Go 静态检查、Oxlint `--deny-warnings`、全部 Domain/Web Domain/App/driver TypeScript 与 Vue typecheck。
- **Go tests：** `task test` 的 normal 与 SQLite 两套全量测试通过；Generator 真实生成/编译分别耗时约 `375s` 与 `379s`。
- **Frontend tests/build：** `28` 个测试文件、`143` 项测试和 workspace boundary `7/7` 通过；`task build TARGET=all` 完成 Server/sidecar、Web 与 Desktop production Vite build。
- **Contract/release：** OpenAPI lint、合同测试 `28/28`、API client `7/7`、`generate:check` 无漂移、release policy `16/16` 全部通过。
- **Tauri 2：** 默认 Rust tests `20/20`、`cargo clippy --locked --all-targets -- -D warnings` 与 `cargo check --locked --release --features custom-protocol` 通过；未启用 `native-e2e` feature，未启动原生宿主。
- **作废环境尝试：** 第一次 Task 子进程因 Volta 移除 shim 路径而找不到 pnpm；第二次临时 Corepack 选择 pnpm `11.24.0`，被项目强制的 `11.1.3` 合同正确拒绝。两次部分结果均作废，最终矩阵使用 pnpm `11.1.3` 实体路径从头执行并通过，未放宽版本检查。
- **E2E disposition：** 按用户要求未运行 browser、fault、native 或三平台安装/发行 E2E；因此 G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持未完成。

## 9. T21-D07：规范 Go Module Path 与旧路径零命中

最终仓库审计把 CONTEXT/ADR 的“旧 module path 零命中”与当前 `go.mod`、全包 import graph 和 `compatibility-zero` 实现逐项对照，确认后端仍声明重构前的 `module go-admin`，151 个源码/测试/fixture/Generator 模板文件仍消费 `go-admin/internal/...`，而兼容扫描既不读取 `.mod`，也不拒绝该历史路径。Lead 登记 `T21-D07`，直接迁移为当前唯一远端仓库与后端子目录对应的 `github.com/NAMEWTA/go-admin-plus/go-admin-plus`；不提供 replace、别名、双 module 或兼容 import。

- **Base/implementation/result：** `4968ca5a8e2c715fe2b13c9efab7b3e85e276e32` / `200829ae39ba2f0aa9e101399467c86db02ff57f` / `200829ae39ba2f0aa9e101399467c86db02ff57f`
- **Result tree：** `1056edcd6b8cc5178f06bde44120031419fe478e`
- **范围与机械性：** 157 个文件只包含 `go.mod` module 声明、全部当前 Go/Generator import 路径、架构文档，以及 architecture/compatibility gate 与负向回归；逐行 diff 审计证明 Go 文件除旧/新 import 字符串外零业务变更，`go.sum` 零漂移。
- **Go matrix：** canonical `go list -m`/`go list ./...`、normal 与 `-tags sqlite` 全量测试、全量 race、双 profile vet/build、四个正式命令 `CGO_ENABLED=0` build、module verify/tidy 全部通过；三轮 Generator 真实生成/编译分别为 `400.569s`、`394.076s`、race `358.377s`。
- **Contract/frontend：** OpenAPI lint、合同测试 `28/28`、API client `7/7`、deterministic generate-check、workspace lint/typecheck、29 files / 149 tests、7/7 boundaries、Web/Desktop production Vite build 全部通过。
- **Root/Desktop/release：** 固定 Task `v3.48.0` 的 governance、architecture、compatibility-zero、docs、Task contract 与 `task build TARGET=all` 通过；当前平台 production sidecar、Tauri 2 Rust default `20/20`、native-feature unit `21/21`、all-target/all-feature strict clippy、custom-protocol release check/build、三平台 release policy `16/16` 通过。
- **零兼容与制品：** `.mod` 已纳入全仓扫描；负向测试同时拒绝历史 module 声明和 import，活动仓库旧路径零命中；production sidecar、release host 与 Desktop dist 对 test-control markers 零命中。
- **E2E disposition：** 未启动 browser、PostgreSQL fault、Desktop native、视觉截图、安装、签名、公证或平台发行 E2E；本地 native-feature 仅运行 Rust 单元测试。G8 继续按用户要求暂停。

## 10. T21-D08：规范前端 Workspace Package Scope

最终跨语言命名审计确认根 workspace 已声明 `@go-admin-plus/workspace`，但 24 个内部 App/Adapter/Domain/Web Domain/Platform/UI/API package、全部 workspace 依赖和源码 import、pnpm lock importer、Task/CI/发行 filter、OpenAPI client 生成器及 Go Generator 模板仍使用历史 `@go-admin/*` scope。该状态既不符合产品重命名，也会让后续 Generator 持续创建旧名包。Lead 登记 `T21-D08`，执行一次无别名、无双 scope、无兼容映射的原子迁移。

- **Base/implementation/result：** `dd1f6ba1e227a039ccaba3364278ae94b6223cfd` / `a342c99e1f38db504de4909c3a69c7913dd31374` / `a342c99e1f38db504de4909c3a69c7913dd31374`
- **Result tree：** `ebc6c5d1349eca682fc9e174bccd94baacb94612`
- **范围：** 130 个文件统一迁移到 `@go-admin-plus/*`，覆盖 25-project pnpm workspace、lock、全部消费者、暂停的 driver 编译输入、Generator/OpenAPI 模板、根任务、Linux/macOS/Windows 交付脚本及 workflow；UI/CSS、业务行为、API、Permission 和删减能力零变更。
- **治理加固：** workspace boundary 强制每个内部 manifest 使用规范 scope；architecture 固定根 workspace 名；compatibility-zero 拒绝历史 scope；两套负向测试证明旧根名或旧 package scope 会失败。活动仓库扫描只剩精确负向 fixture，未保留 alias、双 scope 或兼容依赖。
- **生成入口修正：** 审查发现 `<Path>scripts/go-admin-plus-ui/generate.sh</Path>` 原本过滤不存在的 contracts workspace package；现改为委托唯一 `<Path>scripts/contracts/generate.sh</Path>`，实际写入生成与随后 deterministic check 分别报告 `CONTRACT_GENERATE_PASS`、`CONTRACT_GENERATE_CHECK_PASS`。
- **Frontend/contract：** pnpm `11.1.3` frozen install 识别全部 25 projects；workspace lint、全 typecheck、29 files / 149 tests、7/7 boundaries、Web/Desktop production build通过；OpenAPI generation check、28/28 contract tests、API client typecheck 与 7/7 tests通过。
- **Go/Generator：** normal 与 SQLite 全量测试通过，Generator 真实生成后编译分别为 `406.665s` 与 `394.727s`；双方言 `go vet`/`go build` 及 `go mod verify` 通过。
- **Root/Desktop/release：** governance、architecture、compatibility-zero、docs、Task contract、`task build TARGET=all PROFILE=server-sqlite` 与 release policy `16/16` 通过；production sidecar 构建，Tauri 2 Rust default `20/20`、native-feature unit `21/21`、all-target/all-feature strict clippy、custom-protocol release check/build 全部通过。
- **E2E disposition：** 按用户要求没有执行 browser、fault、Desktop native、视觉截图、安装、签名、公证或平台发行 E2E；`native-e2e` 仅作为 Rust feature 运行单元测试。G8、相关 Ticket 和 change 继续保持 pending/in progress。

## 11. T21-D09：闭合根 Web/Desktop Package 产物

逐项非 E2E 完成审计不再只读取 Task 合同，而是以隔离 artifacts 目录实际执行三个本地 package target。Web RED 证明 Vite 把 Admin Web 写入系统临时目录，脚本随后从不存在的 workspace 根 `dist` 打 tar 并以 `Cannot stat` 失败；Desktop RED 证明 production sidecar 和 Vite build 成功后，release Rust build 因脚本漏传 `custom-protocol` 被源码保护拒绝。补传 feature 后又暴露通用 package 会继续进入 Finder 型 unsigned DMG，而正式设计已规定 DMG 只能在 `.app` 签名后由受保护 macOS workflow 单独生成。

- **Base/implementation/result：** `82d7831201779bc8ee82c7d19cdd59eaea4b37cb` / `15e97506c982a06dbc8454923762f5b42bd3bb99` / `15e97506c982a06dbc8454923762f5b42bd3bb99`
- **Result tree：** `578035dd681947e0915e10e1feeb093910cb6cfa`
- **Web package：** 只构建 `@go-admin-plus/admin-web`，显式写入 artifacts-owned `package-staging/web/dist`，以临时 tar 原子替换 `go-admin-web.tar.gz`；含空格 artifacts 路径下真实命令通过，归档精确包含当前 `dist/index.html`、CSS 和 JS，不再构建无关 Desktop 或读取源码树残留。
- **Desktop package：** 以 `GOHOSTOS` 选择 macOS `app` 或 Windows `nsis`，其他宿主明确拒绝；Tauri build 固定启用 `custom-protocol`，本机实际生成 `Go Admin Plus.app`。主程序与 production sidecar 均为 `arm64` Mach-O，`Info.plist` 有效，test-control marker 零命中。
- **Manifest clean-tree：** 接受 Tauri 2 CLI 规范的 `tauri-build = { version = "=2.6.3", features = [] }` 等价声明，避免每次 Desktop package 自动改写 tracked manifest；Cargo metadata locked check 通过。
- **三类本地产物：** 含空格 artifacts 路径下 Server package 生成当前 `darwin-arm64` Mach-O；Web tar 与 macOS Desktop App 均由根 `task package` 真实生成。Universal DMG、Windows Authenticode NSIS 和安装验证仍属于受保护发行 Gate，不由本地产物替代。
- **回归：** frontend lint、全 typecheck、29 files / 149 tests、更新后的 workspace/package boundary `8/8`、Task contract、release policy `16/16`、governance/architecture/compatibility/docs、Shell syntax、Cargo metadata、diff check 全部通过。
- **清理：** 第一次 DMG 诊断留下的单一 53MB `rw.*.dmg` 已移动到 macOS 废纸篓，可恢复；成功 `.app` 保留为本地构建证据，没有挂载测试磁盘或遗留进程。
- **E2E disposition：** 未启动 Web、fault、Desktop native、安装、签名、公证或发行 E2E；G8、十个 pending Ticket 与 change 状态不变。

## 12. T21-D10：闭合 Web 开发连接与命令面过滤器合同

最终命令和文档逐项审计确认开发指南声明 Web 会连接本地 `127.0.0.1:8080` Server，生成客户端也统一使用相对 `/api`，但 Admin Web Vite dev server 没有代理；因此 `task dev TARGET=web` 会把 API 请求留在 `5173`。同一审计还确认 Task、CI、脚本和发行配置虽然已迁移到规范 package scope，却没有自动拒绝未来引用不存在 workspace package 的 filter。Lead 登记 `T21-D10`，只闭合开发和治理接缝。

- **Base/implementation/result：** `95d45bad3dd276a90605641160a0c47a3833f1b4` / `f5d55ba47190604f836d84e3a19528ceb8c21346` / `f5d55ba47190604f836d84e3a19528ceb8c21346`
- **Result tree：** `6010068b3c082e42cde894cc4e1a6f9d62f762c8`
- **有效 RED：** 先加载真实 `<Path>go-admin-plus-ui/apps/admin-web/vite.config.ts</Path>` 断言 `/api` target；修正前 target 缺失，定向 workspace boundary 失败。实现增加固定 `http://127.0.0.1:8080` target 且不 rewrite，保留浏览器 same-origin Cookie/CSRF 行为。
- **命令面治理：** boundary 从 25 个实际 manifest 建立 package name 集合，扫描根 Taskfile、`.github`、`scripts`、`release` 与 `deploy` 的 `@go-admin-plus/*` 引用；独立清单审计报告 `workspacePackages: 25`、`unknown: []`，更新后的 boundary `10/10` 通过。
- **当前文档：** 根 README、开发指南和发行指南明确区分 `task build`、Server/Web/Desktop 本地 package 输出、macOS App/Windows NSIS 宿主限制，以及受保护 Universal DMG/Authenticode/signing Gate；不承诺 Linux Desktop 或本地已签名发行。
- **非 E2E 回归：** frontend lint、全 typecheck、29 files / 149 tests、10/10 boundaries、Web/Desktop production build、真实 Vite config loader、根 governance/architecture/compatibility/docs/Task contract、release policy `16/16` 与含空格 artifacts 路径的真实 Web package/tar 内容检查全部通过。
- **范围与环境：** 5 个实现文件只涉及 Vite dev proxy、workspace 回归和文档，UI/CSS、API、Permission、业务逻辑与已删功能零变化。审计时 `5173` 已被用户进程占用，Lead 未终止或复用该进程，也未运行 browser/server runtime、fault、Desktop native、安装、签名、公证或平台发行 E2E；G8 继续暂停。

## 13. T21-D11：闭合 SpecDev 当前验证命令与 Generator 独立 Workspace

继续完成审计发现 `<Path>{roots.state}/specdev/config.json</Path>` 仍以已删除的旧前端目录和 Make/旧私有构建入口作为恢复验证命令。该文件是 Speculo refresh 会三方合并并在每次 I-implement 恢复时读取的当前运行时配置，不属于可保留的历史 Evidence。迁移后首次真实执行新 `task test` 又证明 D09/D10 的根 package/command 断言被放入 Generator 会独立复制的 workspace suite：临时生成树正确不含根 Taskfile 和 package script，测试却以 ENOENT 失败。Lead 登记 `T21-D11` 并按所有权重新分层，而非在临时树跳过断言。

- **Base/implementation/result：** `7d078a528c403e6574822e932ca68f7eecc07f57` / `6e55fd64c3a8acd8c91e6d342da46ab1dc0b372d` / `6e55fd64c3a8acd8c91e6d342da46ab1dc0b372d`
- **Result tree：** `def28ab80f802f3cc27dcbf7d2eeff0f19ceadd5`
- **Runtime command plane：** SpecDev `verification.test/lint/build` 分别固定为 `task test`、`task lint`、`task build TARGET=all PROFILE=server-sqlite`，typecheck 固定为 `pnpm --dir go-admin-plus-ui typecheck`；architecture gate 精确读取当前 config 并拒绝任一漂移。
- **职责分层：** 25-manifest command filter 扫描和 Web/Desktop local package 合同迁入根 architecture gate，继续覆盖 Task、CI、scripts、release、deploy、Web artifacts staging、macOS App、Windows NSIS 与 `custom-protocol`；Generator 可复制的 workspace suite 只保留 8 项真正自包含的 package/export/adapter/headless/Vite/mobile 边界。
- **有效 RED：** 新 SpecDev command 负向测试在检查器缺失时失败；第一次完整 `task test` 的 normal Generator 在 `190.97s` 后因临时树缺少根 `Taskfile.yml` 与 `<Path>scripts/go-admin-plus-ui/package.sh</Path>` 失败，其他包通过但未记作整体绿色。
- **完整 GREEN：** 从头重跑 `task test` 退出 0，normal/SQLite Generator 真实生成与编译分别为 `351.677s`/`364.702s`，全部双方言 Go 包、frontend 29 files / 149 tests 与独立 workspace `8/8` 通过；配置原样执行的全 typecheck、`task lint` 和 `task build TARGET=all PROFILE=server-sqlite` 也全部通过。
- **治理回归：** architecture 负向/当前仓库 `6/6`、全部 quality tests `7/7`、`ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、Node syntax、JSON、diff check 全部通过；旧名称 fixture 运行时拼接而不获得 allowlist，活动仓库继续零命中。
- **范围与 E2E：** 4 个实现文件只涉及当前 SpecDev config、architecture gate/tests 和 workspace 测试职责；UI/CSS、业务源码、API、Permission、schema、发行行为与已删能力零变化。未运行 browser、fault、Desktop native、安装、签名、公证或平台发行 E2E；G8 继续暂停。

## 14. T21-D12：闭合 Workspace 验证覆盖盲区

最终完成性审计以 25 个实际 manifest、全部 package spec、全部 test project 和 Node test 文件反向核对根命令，确认“全 typecheck/test”仍由不完整手工清单定义：21 个声明 `typecheck` 的 package 中漏掉 API Client、Audit Domain 与 Audit Web Domain；10 份 shell/E2E driver `tsconfig.json` 中只编译 4 份；中央 Vitest 漏掉 API Client、Audit、IAM Administration 与 Audit fixture 共 8 个 unit spec；根 `task test` 只另跑 workspace boundary，没有执行 harness diagnostics、process lifecycle、runner support 与 Desktop runner 的 29 项 Node 单元测试。Lead 登记 `T21-D12`，只修复当前验证命令面和防回退门禁。

- **Base/implementation/result：** `f794a3e757049ae73bdb4982d7dec6cf98758c95` / `11e358f0381612a327337ba94b86c30572b81c04` / `11e358f0381612a327337ba94b86c30572b81c04`
- **Result tree：** `4d2a04376df5ad794744cb2cf210735ad3df5320`
- **有效 RED：** architecture 负向测试分别证明手工 package typecheck、手工 Vitest package 清单、遗漏 harness spec、遗漏 test project 与遗漏 Node test discovery 均不会被旧门禁拒绝；首次扩展 Vitest 真实发现 36 个文件后，API Client 生成测试因中央配置未继承 package-local `globals` 合同失败，修正后才进入绿色。无效的一次产品集合审计引号错误和两次超出输出窗口的 OpenAPI 尝试均未计入通过证据，随后以可判定退出码重跑。
- **Typecheck 合同：** 根命令先以 `pnpm --recursive --if-present typecheck` 执行全部 21 个声明检查的 package，再保留 Browser Files 专用检查，并用 `vue-tsc` 编译 shell 与全部 9 个 E2E driver project；architecture gate 枚举当前 `tests/**/tsconfig.json`，逐项拒绝根命令遗漏。
- **测试合同：** Vitest 以 `packages/**/*.spec.ts` 和 `tests/e2e/**/*.spec.ts` 集合发现替代模块手工清单，最终 `37` files / `191` tests 通过。新增 `<Path>go-admin-plus-ui/tests/shell/node-tests.mjs</Path>` 递归发现 `tests/**/*.test.mjs`，在真实仓库执行 37 项 Node 单元/静态测试，在 Generator 独立 skeleton 中仍只依赖被复制的 `tests/shell`；根 frontend test script 不再重复单独运行 workspace boundary。
- **完整 GREEN：** CI 固定 Task `v3.48.0` 下 `task lint`、`task test`、`task build TARGET=all PROFILE=server-sqlite` 退出 0；normal/SQLite Generator 真实生成与编译分别为 `377.530s`/`365.516s`，双方言 Go 全包通过。Web/Desktop production Vite build、architecture `11/11`、quality `12/12`、governance、compatibility-zero、docs、Task contract、release policy `16/16`、OpenAPI lint/generate-check、product Go test与后端/Domain/Web Domain/OpenAPI/UI 八模块集合一致性均通过。
- **双轴审查与范围：** 标准轴确认发现器有界于 `tests`、排序确定、失败/信号向上传播且不吞错；规范轴确认 6 个实现文件只涉及 frontend scripts/test config、自包含 runner 与 architecture gate/tests。UI/CSS、产品运行时、API、Permission、数据库 schema、迁移、依赖锁和发行行为零变化。
- **E2E disposition：** `tests/e2e` 下本轮只运行 `.spec.ts`/`.test.mjs` 单元测试和 `tsconfig` 静态编译；没有执行任何场景 `run.mjs`，没有启动 browser、Server fixture、PostgreSQL fault、Desktop native host、安装、签名、公证或平台发行 E2E。G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持 pending/in progress。

## 15. T21-D13：闭合 Tauri 原生构建入口

最终构建完成性审计不再把 Vite Desktop assets 等同于 Desktop App。当前 `<Path>scripts/go-admin-plus-ui/build.sh</Path>` 的 `desktop` 只调用 Admin Desktop 的 Vite `build`，`all` 只调用聚合 Vite build；根脚本另只编译 Server 主命令。由此，公开 `task build TARGET=all PROFILE=server-sqlite` 实际没有构建 Go sidecar 或 Rust/Tauri host，却在当前文档与既有 Evidence 中被描述为完整 Server/sidecar/Web/Desktop build。Lead 登记 `T21-D13`，把原生可编译性归还给 `build`，同时保持 bundle 和受保护发行职责不变。

- **Base/implementation/result：** `4ae448301b08e0593ea85988f8903b1141b78fc2` / `8d85159badcd5d4531bc6f17d4a29783fa2c24e4` / `8d85159badcd5d4531bc6f17d4a29783fa2c24e4`
- **Result tree：** `2b3f3f1e2978311baac1e97c881bc30cec9dae48`
- **有效 RED：** 新 architecture 负向用例使用 Vite-only build fixture，旧检查器不能报告 sidecar、Tauri host、aggregate wiring 或 direct Desktop wiring 缺失；该用例先以 `11/12` 失败。实现后 architecture `12/12` 通过，并额外拒绝重新调用只构建 WebView assets 的 `pnpm build:prod`。
- **构建职责：** `build_web` 只构建 Admin Web；`build_desktop` 要求 Node/Go/Cargo，先以唯一 `<Path>release/shared/sidecar/build.mjs</Path> --host` 原子生成当前批准 triple 的 Go sidecar，再执行 Admin Desktop `tauri build --features custom-protocol --no-bundle`。`all` 与 `desktop` 都必须调用该原生路径；`.app`/DMG/NSIS、签名和公证仍只属于 local package 或受保护 release workflow。
- **真实制品：** CI 固定 Task `v3.48.0` 的 `task build TARGET=all PROFILE=server-sqlite` 退出 0，完成 Web production build，并生成当前时间戳的 arm64 Mach-O Server（`32455266` bytes）、sidecar（`22018418` bytes）和 Tauri host（`7077184` bytes）；Tauri release 编译耗时 `45.47s`。目标 bundle 目录在本次构建时间后零新增文件，三个 production binary 对 `/__desktop/test-control`、`desktop_native_e2e` 和 `GO_ADMIN_DESKTOP_NATIVE_E2E` 零命中。
- **非 E2E Gate：** `task lint`（Go 静态检查、Oxlint、完整 package/test-project typecheck）、governance、architecture、compatibility-zero、docs、Task contract 与 release policy `16/16` 全部通过；全部 quality tests 为 `13/13`，Shell syntax、diff check 与 clean-tree 通过。紧邻 base 的 `T21-D12` 已在未改变的产品源码上通过双方言完整 `task test`（normal/SQLite Generator `377.530s`/`365.516s`、37 files/191 Vitest、37 Node tests）；本修正只修改 build orchestration、architecture regression 与两份当前文档，因此没有用重复 Generator 测试替代真实原生 build。
- **双轴审查与范围：** 标准轴确认 sidecar 构建失败、缺失 Cargo、Tauri build 失败与信号均由 `set -eu` 向根 Task 传播，`--no-bundle` 不进入安装器；规范轴确认 5 个文件只涉及 build/docs/architecture，未修改 UI/CSS、产品源码、API、Permission、schema、迁移、依赖、package 或 protected release 实现。Desktop 继续只支持 macOS 与 Windows，文档明确包含 Desktop 的 `all` 也受该宿主限制。
- **E2E disposition：** 没有启动刚构建的 Tauri host 或 sidecar，没有执行 browser、fault、native tracer、安装、签名、公证或平台发行 E2E；仅编译 production binaries。G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持 pending/in progress。

## 16. T21-D14：让 Desktop CI 链接真实 Tauri Host

本地公开 build 已在 D13 闭合，但最终 CI 对照发现 `<Path>.github/workflows/ci.yml</Path>` 的 macOS `desktop-rust` job 仍没有安装 pnpm workspace，并以 `cargo check --locked --release --features custom-protocol` 结束。该路径虽然能类型检查 Rust release graph，却不会运行 Tauri CLI、不会触发 `beforeBuildCommand` 的 WebView production build，也不会证明 release executable 可以完成链接。Lead 登记 `T21-D14`，让 CI 与当前本地原生 build 共享同一完成定义。

- **Base/implementation/result：** `8e5a8ad3beab5c28258d31c21cda08823b6930e0` / `2ac8e3fa1020caa38384d8888b2d85fa21f07ff4` / `2ac8e3fa1020caa38384d8888b2d85fa21f07ff4`
- **Result tree：** `4d383fb94bfb5d3919d1953b4cd9d09f23155317`
- **有效 RED：** 新 architecture fixture 只提供既有 `cargo check` Desktop job；旧门禁无法报告 frozen workspace install、host sidecar staging 或 Tauri release link 缺失，测试以 `12/13` 失败。实现后定向架构测试 `13/13`、全部 quality tests `14/14`，且检查只读取 `desktop-rust` job，其他 job 的相同命令不能误满足合同。
- **CI 合同：** Desktop job 固定 pnpm `11.1.3` 并启用 lockfile cache，执行 `pnpm --dir go-admin-plus-ui install --frozen-lockfile`；随后构建当前 host sidecar，以显式 Cargo manifest 运行 `20` 个 locked Rust tests 和 strict Clippy，最后执行 Admin Desktop `tauri build --features custom-protocol --no-bundle`。普通 CI 不生成安装 bundle，不消费签名/公证凭据，也不发布制品。
- **真实等价构建：** 本机按 CI 顺序执行 frozen install、`release/shared/sidecar/build.mjs --host`、Cargo test、Clippy 与完全相同的 pnpm Tauri 命令，全部退出 0。sidecar builder 报告 `aarch64-apple-darwin` 且 `nativeE2E=false`；Tauri 先完成 117 modules 的 production WebView build，再于 `45.49s` 完成 release link，生成 `<Path>go-admin-plus-ui/apps/admin-desktop/src-tauri/target/release/go-admin-plus-desktop</Path>`。
- **非 E2E Gate：** 固定 Task `v3.48.0` 的 `task lint` 退出 0，覆盖 Go vet、Oxlint、全部 workspace package 与 10 个测试工程 typecheck；governance、architecture、compatibility-zero、docs、Task contract 和 release policy `16/16` 全部通过。YAML parse、Shell syntax、`git diff --check` 与 clean implementation checkpoint 通过。
- **无效验证分类：** 最初使用 `go run` 的临时 Task 令子脚本找不到 Task/pnpm；随后几次人工最小 PATH 分别遗漏 Volta Node 实体或 `goenv` 本体，另一次让 Volta Node shim 从子进程 PATH 移除 pnpm shim。这些均为本机工具发现失败，不计作产品或 Gate 结果；最终以临时固定 Task、固定 Node `22.22.3` 实体路径和其余完整环境从头重跑 lint 并通过，未修改或放宽仓库合同。
- **双轴审查与范围：** 标准轴确认 install、sidecar、test、Clippy、WebView build 和 release link 任一步失败都会令 job 失败，显式 manifest 不依赖脆弱的目录往返；规范轴确认 3 个实现文件只涉及 CI 与 architecture gate/tests，未修改 UI/CSS、产品源码、API、Permission、schema、迁移、依赖版本、package 或 protected release 实现。紧邻 base 的 D13 已完成真实根全目标构建，D12 已在未变产品源码上完成双方言全量测试，本修正用真实 CI 等价原生构建验证新增风险。
- **E2E disposition：** 没有启动 Tauri host 或 sidecar，没有运行 browser、fault、native tracer、安装、签名、公证或平台发行 E2E。G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持 pending/in progress。

## 17. T21-D15：闭合 Volta Node 子进程的 pnpm 命令面

D14 验证期间，为取得有效 Task 结果曾把固定 Node `22.22.3` 实体目录前置于 PATH；后续从未经修饰的当前开发环境重跑时，确认该做法掩盖了公共命令缺陷。当前 pnpm 由 Volta 安装，但 Volta shim 启动实际 Node 后会从 Node 子进程 PATH 移除自己的 shim 目录；根 Task 经 `<Path>scripts/go-admin-plus/run-script.mjs</Path>` 再启动 shell 时，六个前端脚本因此无法发现已安装的 pnpm。Lead 登记 `T21-D15`，要求公开任务在该真实环境中直接成立。

- **Base/implementation/result：** `303565aa4ccc3d0fa0a48b38147351ab1cf9cc76` / `7e21956d33567cc49e148b0789c9781d5d556843` / `7e21956d33567cc49e148b0789c9781d5d556843`
- **Result tree：** `7bebf7898cfbf4ef3b5531f2da82f12cbf645e2f`
- **有效 RED：** 真实 Node runner 内 `pnpm --version` 返回 `ENOENT`，而同一 shim 绝对路径返回 `11.1.3`；新增 helper tests 因 `run_pnpm/exec_pnpm` 缺失以 `0/3`、127 失败，architecture 负向 fixture 也证明旧门禁接受脚本绕过 resolver。第一版 `corepack pnpm` 能执行 Oxlint，但随后的 workspace `type-check -> pnpm typecheck` 在内层再次报 `pnpm: command not found`，所以该轮 root lint 整体失败且未计作通过。最后补充的“双工具缺失”诊断先得到只提 Corepack 的不完整错误，再收敛为精确合同。
- **解析合同：** `<Path>scripts/go-admin-plus-ui/common.sh</Path>` 优先使用 PATH 已提供的 pnpm；仅缺失时要求 Corepack，在根忽略的 `.artifacts/tool-shims/corepack/` 生成标准 pnpm shim并导出 PATH，使外层命令和任意嵌套 package script 都解析 workspace `packageManager` 固定的 `pnpm@11.1.3`。`run_pnpm` 保持普通顺序执行，`exec_pnpm` 保持信号和退出码；pnpm/Corepack 均缺失时确定性失败。
- **命令面收口：** build、dev、lint-staged、lint、package、test 六个根前端脚本全部使用同一 resolver；architecture gate 逐个拒绝重新直接调用 pnpm，并同步 Web staging/Tauri build 精确合同。根 `task:contract` 固定执行 5 项 resolver tests，覆盖 Corepack fallback、嵌套 pnpm、直接 pnpm 优先、exec 状态 `23` 和缺失工具错误。README/开发指南同步 pnpm/Corepack 安装与忽略 shim 位置。
- **真实 GREEN：** 只把 CI 固定 Task `v3.48.0` 临时加入原始 PATH，不前置 Node 实体或 pnpm shim，从根执行 `task lint`、`task task:contract`、frontend test、`task build TARGET=all PROFILE=server-sqlite` 和 `task package TARGET=web PROFILE=server-sqlite` 全部退出 0。结果包含完整 workspace typecheck、`37` files / `191` Vitest、`37` Node tests、Server/Web/`aarch64-apple-darwin` sidecar 和 Tauri host；Tauri release link 为 `1m06s`。Web package 为 `75117` bytes，tar 内精确包含 `dist/index.html` 与当前 CSS/JS assets，证明 `GO_ADMIN_BUILD_DIR` 经过 shell function 仍正确传播。
- **非 E2E Gate：** helper + quality tests 合计 `20/20`，governance、architecture、compatibility-zero、docs、Task contract、release policy `16/16`、Shell syntax、diff check 与 ignored-artifact 检查全部通过。Go 产品源码和 test wrapper 未修改；D12 已在当前未变 Go 产品上完成 normal/SQLite 全量 Generator 测试，本轮实际运行 Go vet、Server build、sidecar build和所有受影响 frontend tests/build，不用重复完整 Generator 代替命令面风险验证。
- **双轴审查与范围：** 标准轴确认 shim 仅写入根已忽略 artifacts、含空格绝对 artifacts 路径继续由既有 resolver 处理、直接 pnpm 无额外写入、Corepack/生成/嵌套/命令失败均向上传播；规范轴确认 13 个实现文件只涉及根前端命令、测试、architecture gate 与当前开发文档，未修改 UI/CSS、业务源码、API、Permission、schema、迁移、依赖锁、package 内容或 protected release 实现。该路径是当前工具发现修复，不保留旧项目命令或兼容模式。
- **E2E disposition：** 没有启动 dev Server、Web 浏览器、Tauri host 或 sidecar；没有运行 browser、fault、native tracer、安装、签名、公证或平台发行 E2E。G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持 pending/in progress。

## 18. T21-D16/D17：固定工具链并闭合后端 Generator 冷启动

D15 解决前端 shell 后，从完全未经修饰的 Volta 环境执行根全量测试，进一步暴露后端 Go Generator 会启动 Node 合同生成器，而该进程同样无法发现 pnpm。仓库文档和门禁也没有完整声明 Go Task、Node CI baseline、Rust MSRV/CI baseline，无法从全新环境重建此前验证条件。Lead 将工具链声明和跨语言命令依赖作为两个独立决策收口。

- **Base/implementation/result：** `b97f2e20f30b0690bdcd98f69aa77e87207e365e` / `e62519958ed83bf42bd0bc76cd3848c0023ff9c0` / `e62519958ed83bf42bd0bc76cd3848c0023ff9c0`
- **Result tree：** `ae7a83851a3682757ae2790ab28324cc73bb274b`
- **有效 RED：** 首个工具链负向 fixture 证明旧 architecture gate 不拒绝浮动 Task 和不完整开发文档，以 `14/15` 失败。固定文档后，真实原始 Volta PATH 的 `task test` 在 Go Generator 报 `contract: pnpm is required: spawnSync pnpm ENOENT`；新增 resolver/CI 回归随后分别以 helper `3/7` 和 architecture `15/16` 失败，明确证明后端未继承 shim、漂移 direct pnpm 未拒绝且 backend CI 缺少跨语言安装。另实测仓库根的无版本 `corepack pnpm --version` 为 `10.34.5`，不能作为固定 Workspace 合同。
- **D16 工具链合同：** 根命令要求精确 Go Task `3.48.0`；Go 最低 `1.26.5`、Node 最低 22、pnpm 精确 `11.1.3`、Desktop Rust 最低 `1.88` 继续由原生 manifests 管理，文档另记录 CI 的 Go `1.26.5`、Node `22.22.3` 与 Rust `1.96.0` 验证基线。architecture gate 同时拒绝 CI 安装、Task 自检、README 和开发指南漂移。
- **D17 解析合同：** 新的共享 `<Path>scripts/go-admin-plus/pnpm.sh</Path>` 只接受精确 direct pnpm；缺失时原子生成 POSIX/Windows 标准 shim，并显式执行 `corepack pnpm@11.1.3`。前后端 common、Server dev/test、OpenAPI verify/generate-check 共用该 resolver；后端 CI 固定 Node/pnpm 并 frozen install Generator 所依赖的 frontend workspace，60 分钟 timeout 覆盖三轮生成器矩阵。
- **完整 GREEN：** 临时安装的 Task `v3.48.0` 在原始 Volta PATH 下通过 Task contract `7/7`。从原失败点完整重跑 `task test` 退出 0：normal/SQLite Generator 真实生成与编译分别为 `577.567s`/`406.933s`，双方言 Go 全包、frontend `37` files / `191` Vitest 与 `37` Node tests 全部通过。随后 `task lint`、contract lint（`28/28` + API client `7/7`）、generate-check、release policy `16/16`、quality `17/17`、五项根治理全部通过。
- **构建证据：** `task build TARGET=all PROFILE=server-sqlite` 退出 0，完成 Server、Web production、`aarch64-apple-darwin` sidecar 和 Tauri production WebView/host；Tauri release link 为 `46.56s`，目标 executable 为 `<Path>go-admin-plus-ui/apps/admin-desktop/src-tauri/target/release/go-admin-plus-desktop</Path>`。YAML parse、Shell syntax、JSON、diff check、精确 Corepack `11.1.3` 探针均通过。
- **双轴审查与范围：** 标准轴确认 shim 原子替换、含空格 artifacts 根、direct/Corepack 版本错误、缺失工具、nested package、Generator、Task 与 CI 任一步失败均确定性向上传播，未发现安全、并发或退出码吞错。规范轴确认 14 个实现文件只涉及工具链 resolver、根命令、CI、质量回归和开发文档；UI/CSS、业务源码、API、Permission、schema、迁移、依赖锁、package 内容和 protected release 行为零变化，也没有引入旧命令或版本兼容层。
- **E2E disposition：** 本轮只执行源码测试、静态/单元回归与 production 编译；没有执行任何 `run.mjs` 场景，没有启动 browser、fault、Desktop native fixture、安装、签名、公证或平台发行 E2E。G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持 pending/in progress。

## 19. T21-D18：闭合双宿主数据范围与 Generator Node 并发解析

最终从 AC-011/AC-022 的 retained controller 反向追踪运行时身份，确认 Desktop 已携带 `dataScope`，但 Web product runtime、共享平台类型和 browser adapter 均未保留该字段，Shell 还会把缺失范围默认为 `all`。这不会绕过后端授权，却会让 `self` 用户在 Web 端被投影成全部范围并看到随后由后端拒绝的控制项。修正后的真实完整 Go Gate 又暴露独立构建缺口：并发包测试中 Volta shim 两次在约 20 秒后以 OS error 35 拒绝启动 Node，隔离生成器则能完成 339.45 秒的真实生成与编译。

- **Base/implementation/result：** `8ca17e4d8b8d32ffa266540ebbaa20a70ee4b743` / `09a3619774294f6178850d52e3054a5b5f42dbe8` / `09a3619774294f6178850d52e3054a5b5f42dbe8`
- **Result tree：** `618947a990d1b0a4f09b9c8322fc90a230467c00`
- **有效 RED：** 真实 SQLite product runtime 建立 `self` role/account/session 后，`/api/runtime/identity` 返回空数据范围；browser exact-shape 测试同时证明合法 `dataScope` 被拒绝、缺失字段反而被接受，共 `4` 项失败。前端修正后第一次和第二次完整 Go 测试都只在 Generator 报 Volta `Resource temporarily unavailable (os error 35)`，而隔离 `TestCanonicalRendererInvokesLintAndGeneration` 以 `339.45s` 通过，确定为并发 shim 解析缺口而非生成结果错误。
- **运行时实现：** authenticated identity 直接发出 IAM manifest 的 `self|all`；共享 `RuntimeIdentity` 将其设为必填，browser adapter exact parser 拒绝缺失、空值和非法值，Desktop 复用同一类型，Product Shell 直接赋值且不再默认放大。unauthenticated identity、Cookie/CSRF、Permission code、navigation 与 UI/CSS 均未改变。
- **工具链实现：** Generator 识别 Volta 安装元数据并解析到真实 Node runtime，拒绝空值、`.`、`..` 和跨路径版本；临时 Workspace PATH 同时前置真实 Node 与精确 pnpm 目录。非 Volta Node、CI setup-node 和 Windows 保持既有路径，任何缺失、不可执行或子进程失败继续确定性向上传播。
- **完整 GREEN：** 定向 product/IAM/adapter/Shell 测试通过；frontend lint 与全部 package/test-project typecheck 通过，frontend `37` files / `192` Vitest、`37` Node 单元/静态测试通过。双方言完整 Go 脚本退出 0，normal/SQLite Generator 分别为 `366.924s`/`364.831s`；Go vet 与差异检查通过。
- **构建与根 Gate：** Web production `113` modules、Desktop WebView `117` modules、当前 `aarch64-apple-darwin` sidecar 与 Tauri custom-protocol release host 构建通过，Tauri link 为 `47.34s`。固定 Task `3.48.0` 的 governance、architecture、compatibility-zero、docs、Task contract `7/7`、OpenAPI contract `28/28`、API client `7/7`、generate-check、quality `17/17` 和 release policy `16/16` 全部通过且无生成漂移。
- **双轴审查与范围：** 标准轴确认范围只来自已认证 manifest、exact parser 不接受扩展或降级、Shell 无默认提权，Volta 元数据不能路径跳转且普通工具链不受影响；规范轴确认 12 个实现文件只闭合 AC-011/AC-022 双宿主投影和完整构建可复现性，没有修改公共模块 OpenAPI、schema、migration、依赖锁、UI/CSS、已删功能或 protected release。
- **E2E disposition：** 按用户明确要求没有执行任何场景 `run.mjs`，没有启动 browser、fault、Desktop native fixture、安装、签名、公证或平台发行 E2E。G8、十个 pending Ticket 与 change 继续保持 pending/in progress。

## 20. T21-D19：Desktop Production 构建隔离原生测试控件

最终 production bundle 审计不再只搜索可见按钮文本，而是检查实际 CSS/JS 字节。审计确认旧 Desktop App 虽以 `VITE_GO_ADMIN_NATIVE_E2E` 包裹测试 UI，Vite production JavaScript 已摇除条件分支，但 scoped `.native-e2e` CSS 仍进入 `dist`；既有 restore check 只搜索 `E2E scope self`，因此此前“production artifact 无测试控件”的证据覆盖不足。

- **Base/implementation/result：** `4ba72eadd7ef118df3c126d6ae5b7bbf8330fd8f` / `9002a383c669ec591d2cfc384924addaed2b6821` / `9002a383c669ec591d2cfc384924addaed2b6821`
- **Result tree：** `53cea92dcce8f9d1940cc031b23efda4a30273ca`
- **有效 RED：** 新增静态合同先断言 production `App.vue` 不得包含环境开关、test-control route、`native-e2e`、confirm override 或测试按钮，并要求独立测试入口存在；修正前分别因 production App 仍含全部控件、独立入口不存在而 `2` 项失败，其余 `11` 项通过。
- **构建图隔离：** production `<Path>go-admin-plus-ui/apps/admin-desktop/src/App.vue</Path>` 只保留 Desktop runtime/fetch/session 与 `ProductWorkspace` 组合；原测试 UI、边界探针、自动确认和 CSS 原样迁至 `<Path>go-admin-plus-ui/apps/admin-desktop/src/native-e2e/App.vue</Path>`。`main.ts` 使用受类型声明约束的虚拟入口，Vite 仅在精确 `--mode native-e2e` 时选择测试 App，默认及 Tauri `beforeBuildCommand` 始终选择 production App；不保留旧环境变量兼容分支。
- **制品拒绝：** 新 `<Path>go-admin-plus-ui/apps/admin-desktop/scripts/verify-production.mjs</Path>` 拒绝无效/空目录、符号链接、非普通目录项，并逐字节扫描 `/__desktop/test-control`、`native-e2e` 和三类测试控制文案。Desktop package `build` 在每次 Vite production build 后强制执行 verifier；native runner 的 production restore 复用同一实现后再扫描 sidecar/host binary，避免两份不一致规则。
- **前端 GREEN：** Desktop 定向 typecheck 与 `run.test.mjs` `14/14` 通过；完整 workspace Oxlint `--deny-warnings`、递归 package 与十个 test-project typecheck 通过；Vitest `37` files / `192` tests、Node unit/static `39/39` 通过。没有执行任何场景 runner。
- **生产构建：** Desktop Vite production build 转换 `116` modules 并报告 `DESKTOP_PRODUCTION_ASSETS_PASS`，独立 `rg -a` 对全部 marker 零命中。根全目标 build 完成 Server、Admin Web `113` modules、`aarch64-apple-darwin` production sidecar 与 Tauri 2 custom-protocol release host，Rust release link 为 `46.68s`；Tauri 前置 production build 再次通过 verifier。
- **合同与治理：** 临时固定 Go Task `3.48.0` 下 governance、architecture、compatibility-zero、docs、Task contract `7/7`、quality `17/17`、release policy `16/16`、OpenAPI contract `28/28`、API client `7/7` 与 deterministic generate-check 全部通过。临时 Task 工具随后移入 macOS 废纸篓，仓库未写入工具制品。
- **范围与复用证据：** 本提交只改变九个 Desktop build/test-harness 文件；产品可见 UI/CSS 仍由同一 `ProductWorkspace` 与共享 theme 提供，唯一从 production 产物删除的是不可见测试 CSS/控制代码。后端源码未改变，本轮没有重跑 D18 已通过的双方言完整 Go 测试，只以当前 Server/sidecar production build 验证跨语言构建。
- **E2E disposition：** 遵循用户暂停要求，没有执行 `tests/e2e/**/run.mjs`，没有启动 browser、fault、Desktop native fixture、安装、签名、公证或平台发行 E2E。G8、十个 `implemented-pending-final-e2e` Ticket 与 change 继续保持 pending/in progress。

## 21. T21-D20：统一 WebView、Sidecar 与 Host 的 Production Marker 合同

D19 证明 production WebView 当前干净，但后续覆盖审计把独立测试入口的全部文案、fixture、环境变量、Go build tag、Rust feature 与 route 和 verifier 逐项对照，确认 verifier 只枚举五项选定字符串；native restore 对 sidecar/host 又保留一份只搜索 route 的实现。因此新增测试控件或重命名 class/route 时，生产二进制证据可能继续绿色。

- **Base/implementation/result：** `2661de5c1e020f416d02e5b67e81fb1beed8c42c` / `9cb4002532ff869eed01dc3f57fb23477a4574be` / `9cb4002532ff869eed01dc3f57fb23477a4574be`
- **Result tree：** `7eb7361f97aa5caa792bd27a4a460c97d5f2a13c`
- **有效 RED：** 测试先要求 verifier 导出共享 marker 与普通文件验证 API，并要求 runner 对 sidecar/host 使用该 API且不再出现 `fileContains`。修正前 Node 在加载测试文件时以 `ERR_TEST_FAILURE` 明确报告缺少 `desktopNativeControlMarkers` export，证明合同不存在，而非产品场景失败。
- **单一合同：** 不可变 marker 集覆盖 `/__desktop/test-control`、`native-e2e`、旧 Vite 开关、Desktop E2E environment、Go feature tag，以及通配全部测试文案和 fixture 的 `E2E ` / `E2E-`。目录 verifier 先完成空树、符号链接和普通文件约束，再委托同一 file verifier；sidecar/host restore 删除 19 行自定义 chunk scanner，直接验证两份生产二进制。
- **功能回归：** 定向 Desktop runner 单元/静态合同 `14/14` 通过；测试逐一向真实临时资产注入七项 marker并全部被拒绝，clean WebView 文件列表通过，遗漏于 D19 的 `E2E permissions on` 由通配 marker 阻断。Desktop typecheck、全 workspace Oxlint `--deny-warnings`、递归 package 与十个 test-project typecheck通过；Vitest `37` files / `192` tests、Node unit/static `39/39` 通过。
- **真实生产制品：** Desktop Vite production build转换 `116` modules 并报告 `DESKTOP_PRODUCTION_ASSETS_PASS`；当前 `aarch64-apple-darwin` production sidecar 与 Tauri host 通过同一 file verifier。完整 Desktop 构建重新生成 production sidecar、执行 Tauri `beforeBuildCommand` 并以 `custom-protocol --no-bundle` 链接 release host，耗时 `47.71s`；重建后三类制品再次统一扫描并报告 `DESKTOP_PRODUCTION_ARTIFACTS_PASS`。
- **根门禁：** governance、architecture、compatibility-zero、docs、quality `17/17` 与 release policy `16/16` 全部通过，diff check 通过。此次只修改三个 Desktop verifier/harness 文件，没有改变根命令、OpenAPI、后端或生成物，因此没有重复运行 D19 已通过的 Task/OpenAPI/Go 矩阵。
- **双轴与范围：** 标准轴确认 marker 集对所有现有 WebView 控件和 native build 身份形成多重覆盖，目录与二进制不再规则分叉；规范轴确认 production App、共享 UI/theme、Tauri/Rust/Go product runtime、schema、migration、依赖锁和发行 policy 均零变化。
- **E2E disposition：** 没有执行任何 `run.mjs`，完整 Desktop build 只编译 production sidecar/WebView/host，没有启用 `native-e2e` mode/feature，也没有启动宿主、browser、fault、安装、签名、公证或平台发行场景。G8 与 change 继续 pending/in progress。

## 22. T21-D21：把 Production 二进制扫描接入所有正式构建与发行路径

D20 已有统一文件 verifier，但完成性审计确认它只在暂停的 native runner 恢复制品时扫描 sidecar/host；常规本地 build/package、Desktop CI 和受保护 macOS/Windows 发行都没有自动调用。因此既有“生产二进制干净”证据依赖手工补跑，不能约束公开命令面。

- **Base/implementation/result：** `70afa0660968b5ec642057ac2a1ea1c36ac7b37c` / `15a54f45398c88261767e62ee476ecab17bf90b3` / `15a54f45398c88261767e62ee476ecab17bf90b3`
- **Result tree：** `22bedf78d64775989ab364526f6a717fb1de693b`
- **有效 RED：** 新 architecture 断言先精确报告 local Desktop package、Desktop build 和 Desktop CI 三条 verifier 接线缺失；macOS 与 Windows repository policy 各因缺少 `verify-production.mjs --files` 失败；Desktop Node 测试以 `ERR_MODULE_NOT_FOUND` 证明跨平台 `verify-build.mjs` 尚不存在。随后才实现接线与路径解析。
- **统一构建接缝：** 新 `<Path>go-admin-plus-ui/apps/admin-desktop/scripts/verify-build.mjs</Path>` 复用 `<Path>release/shared/sidecar/build.mjs</Path>` 的 `hostTriple`/`outputName`，按当前平台精确解析 WebView、sidecar 与 Tauri host；本地 `build desktop/all`、本地 Desktop package 和 Desktop CI 在 Tauri 成功链接后自动验证。macOS Universal App verifier 与 Windows NSIS builder 在签名/暂存前直接扫描最终 host 和 sidecar。
- **二进制误判收紧：** 删除可能在签名或压缩数据中随机出现的四字节 `E2E ` / `E2E-` 通配，改为 route、mode/env/feature 以及全部当前边界、授权、scope、session 和 fixture 的精确长 marker。功能测试逐项注入并拒绝所有 marker；darwin arm64、Windows x64 精确路径和不支持的 Linux host 均有回归。
- **真实 production build：** `sh scripts/go-admin-plus-ui/build.sh desktop` 从头生成 `aarch64-apple-darwin` sidecar，Desktop Vite 转换 `116` modules并报告 `DESKTOP_PRODUCTION_ASSETS_PASS`，Tauri 2 custom-protocol release host 链接耗时 `46.92s`，随后公开构建命令自身报告 `DESKTOP_PRODUCTION_BUILD_PASS`。
- **Frontend 与根 Gate：** Oxlint `--deny-warnings`、递归 24/25 workspace typecheck 和十个 test-project 编译通过；Vitest `37` files / `192` tests与 Node unit/static `40/40` 通过。governance、architecture、compatibility-zero、docs、quality `17/17`、release policy `16/16`、Shell/Node syntax、CRLF 与 diff check 全部通过。
- **平台限制：** 当前 macOS 环境没有 `pwsh`，所以 Windows builder 没有伪称解释器级执行通过；Windows 接线由 CRLF 保持、repository policy、静态 architecture 合同和 Windows workflow 的实际 runner职责覆盖。没有执行签名、安装或发布。
- **范围与 E2E disposition：** 12 个实现文件只改变 production verifier、公开构建/CI/发行接线和对应负向测试；产品运行时、UI/CSS、OpenAPI、后端、数据库、依赖锁均不变。没有执行任何 `run.mjs`、browser、fault、native host 场景或平台安装 E2E；G8 与 change 继续 pending/in progress。

## 23. T21-D22：完成双宿主 Platform Port 与 Desktop 原生能力

完成性审计把 CONTEXT/Spec 的 adapter、Platform Port 与 Desktop host capability 要求逐项反查源码，确认既有 `PlatformPort` 只有声明，Browser/Desktop adapter 与双 App 均未实现或注入；Files 页在 Desktop 仍直接创建浏览器文件输入与下载 anchor。该缺口不是 E2E 缺失，而是生产实现缺失，因此先登记并提交授权 checkpoint，再在精确批准路径中完成实现。

- **Audit base/authorization/implementation/result：** `5bac141948c118ed08283656bca555bc38d09fe8` / `9be09eccc38d26a5f62dd75629c88e2a8fbc689d` / `aa502221356f7bbd9c3d7619ea10272f4a5971cf` / `aa502221356f7bbd9c3d7619ea10272f4a5971cf`
- **Result tree：** `bb167a772d714b427f1503e6cf69a579a1097bb5`
- **有效 RED：** adapter 测试依次证明 Web/Desktop factory、文件选择/保存方法不存在；Rust 单测证明内容分类与 canonical save payload 解码不存在；静态架构合同精确报告双 App 未创建/注入 Platform adapter；最后一个边界测试证明注入的 Web host 可返回 `../notes.txt` 并绕过校验。每个 RED 均只因对应能力缺失而失败，随后实现并转绿。
- **Port 与组合：** `PlatformPort` 现在统一声明 runtime、不可变 capability 集、文件选择/保存、通知和剪贴板写入；Web/Desktop adapter 各自实现同一接口，生产双 App 与测试专用 Desktop 入口显式创建并向唯一 `ProductWorkspace` 注入，Shell 再只向 Files 页面透传。Files 的 Web 分支保留原 `<input type="file">`、下载 anchor 与全部现有 CSS；Desktop 分支只调用 Port，不直接导入 Tauri 或接触原生路径。
- **Desktop host 边界：** 四个 Tauri command 由 build manifest 与 main capability 精确 allowlist；未授予 generic filesystem、clipboard read 或 notification query 权限。文件只允许 PDF/JPEG/PNG/TXT、最大 10 MiB，Rust 按扩展名和 magic/UTF-8 内容共同识别，拒绝控制字符、路径、symlink、伪装内容、非 canonical base64、类型/大小不一致；选择结果只序列化名称、类型、大小和内容，不序列化路径。保存使用随机 `create_new` 临时文件、sync 与 rename，失败清理 staging；macOS 剪贴板写入显式调度到主线程，通知与剪贴板输入均有界。
- **依赖与供应链：** Cargo 精确锁定 `tauri-plugin-dialog 2.7.2`、`tauri-plugin-notification 2.3.3`、`tauri-plugin-clipboard-manager 2.3.2`，更新 locked transitive graph；Files Web Domain 新增直接 Platform workspace 依赖，pnpm lock 只增加该 workspace link。release policy 与 production marker verifier继续覆盖最终 host/sidecar/WebView。
- **GREEN：** adapter/Files 定向 `21/21`；完整 frontend `37` files / `195` Vitest 与 Node unit/static `41/41`；递归 24/25 workspace 和十个 test-project typecheck、Oxlint `--deny-warnings` 全部通过。Rust locked unit `23/23`（包含真实临时文件替换、扩展名 mismatch 与 Unix symlink 拒绝）、`cargo fmt --check`、all-target strict Clippy 全部通过。
- **真实 production build：** `sh scripts/go-admin-plus-ui/build.sh all` 构建 Web `113` modules、当前 `aarch64-apple-darwin` production sidecar、Desktop WebView `116` modules及 Tauri custom-protocol release host；Rust release link 为 `1m25s`，最终自动扫描报告 `DESKTOP_PRODUCTION_ASSETS_PASS` 与 `DESKTOP_PRODUCTION_BUILD_PASS`。
- **根全目标后置复验：** Evidence checkpoint 后从最新 `main` 执行 Taskfile `build` 的唯一受管委派 `node scripts/go-admin-plus/run-script.mjs scripts/go-admin-plus/build.sh all server-sqlite`，Server、Web `113` modules、production sidecar、Desktop WebView `116` modules和 Tauri release host 全部通过，最终链接 `53.73s`并再次报告 `DESKTOP_PRODUCTION_BUILD_PASS`。当前 shell 没有固定 Go Task `3.48.0` 启动器，因此未伪称通过 CLI wrapper；architecture/Task contract 已独立证明该委派合同。
- **根门禁：** `GOVERNANCE_CHECK_PASS`、`ARCHITECTURE_CHECK_PASS`、`COMPATIBILITY_ZERO_PASS`、`DOCS_CHECK_PASS`、quality `17/17`、release policy `16/16`、敏感信息扫描和 `git diff --check` 全部通过。后端/OpenAPI/schema/migration/Permission/已删能力未改变，因此没有用重复的 Go Generator 或合同测试替代本次 adapter/native 风险验证。
- **范围与 E2E disposition：** 可见 Web UI/CSS 未改变，Desktop 只以同一 Files 管理界面换用原生选择/保存接缝；没有执行任何场景 `run.mjs`、浏览器、fault、native host、安装、签名、公证或发布。G8、十个 pending Ticket 与 change 继续保持 pending/in progress。

## 24. T21-D23：闭合 Desktop Files 二进制下载桥接

D22 后按真实数据路径从 Go strict OpenAPI handler 追踪到 WebView，确认成功 Files content 响应固定为 `Content-Type: application/octet-stream`，而 Rust proxy 只接受 PNG/JPEG/PDF/TXT，导致所有 Desktop 下载在进入共享页面和原生保存能力前确定性失败。该缺口经独立 D23 授权后修复，不回写或美化 D22 的历史证据。

- **Audit base/authorization/implementation/result：** `b9573ac16011073c8f33ca38bb700827523bed85` / `63bb941e904a0c14a659f1af2f4c3afbde91bcc0` / `0554d320b74c3e3be7f2c8db8c52e38af81b42b1` / `0554d320b74c3e3be7f2c8db8c52e38af81b42b1`
- **Result tree：** `108d70c73529f18f9bbb116aa526aed43a331e59`
- **有效 RED：** Rust 定向测试因生产解析边界不存在而以 missing `parse_response_body` 编译失败；TypeScript 定向测试证明旧 bridge 会接受 `image/png` envelope 而不是拒绝。二者只针对已确认的下载合同缺口，未依赖 E2E。
- **精确桥接合同：** Rust 只对成功的 `/files/objects/*/content` 响应读取唯一 `content-type`，且只接受 `application/octet-stream`，随后生成仅含 `encoding/mediaType/data` 的 canonical base64 envelope；非成功响应继续按原 JSON 路径解析并保留既有 native diagnostic。Desktop adapter 只对同一成功 GET 路由要求该三字段精确信封，拒绝额外字段、业务 MIME、非 canonical base64、解码异常和超过 10 MiB 的数据，并向共享 Files controller 返回固定 octet-stream `Response`。
- **GREEN 与回归：** Rust locked tests `24/24`、all-target strict Clippy 与 fmt 通过；Desktop adapter 定向 `8/8`，完整 frontend `37` files / `196` Vitest、Node unit/static `41/41`、Oxlint 和递归 workspace/十个 test-project typecheck 全部通过。Go 全包扫描除 Generator 唯一用例首次因 Corepack 下载 pnpm 的 registry connect timeout 外全部通过；该精确失败用例随后在不改代码的重试中以 `345.659s` 通过。OpenAPI contract `28/28`、API Client `7/7`、quality `17/17`、release policy `16/16`、governance、architecture、compatibility-zero、docs、contract lint、diff check 全部通过。
- **真实 production build：** 根 Taskfile 的唯一受管等价委派 `node scripts/go-admin-plus/run-script.mjs scripts/go-admin-plus/build.sh all server-sqlite` 退出 0，完成 Server、Admin Web `113` modules、`aarch64-apple-darwin` SQLite sidecar、Desktop WebView `116` modules与 Tauri 2 custom-protocol release host；原生链接为 `54.26s`，最终报告 `DESKTOP_PRODUCTION_ASSETS_PASS` 与 `DESKTOP_PRODUCTION_BUILD_PASS`。
- **范围与 E2E disposition：** 3 个实现文件只修改 Desktop proxy、adapter 和聚焦测试；Files API/OpenAPI、Go handler、数据库、页面标记、可见 UI/CSS、权限、依赖和受保护发行零变化。没有执行场景 `run.mjs`、browser/fault/native/platform install E2E，也没有签名、公证或发布；G8、pending Ticket 与 change 继续保持 pending/in progress。

## 25. T21-D24：删除未编译的 Desktop Demo 旧合同

最终 Desktop source graph 审计确认 T-17 已让 `main.rs` 与 proxy 全面切换到 `product_contract.rs`，但 525 行 `demo_contract.rs` 仍作为未声明 module 留在源码树中。Cargo 从不编译其中 validator 和 tests，它还独占 Desktop 对 `time`/`uuid` 的直接依赖，因此既有绿色构建不能证明这部分旧实现有效或必要。

- **Audit base/authorization/implementation/result：** `cf864c28df223a7813ce1afba5b5b0d27e9bc47f` / `5f9878fa5de308d36f9118630c959a4ecd820ca4` / `a1f61f66b05a6beb4f68455f03f952909dfda269` / `a1f61f66b05a6beb4f68455f03f952909dfda269`
- **Result tree：** `cf38d7be04ec335d6f87936592ad6ef927805558`
- **有效 RED 与收缩：** 新 compatibility-zero 用例在临时仓库恢复该文件时原先返回空失败集合，明确证明旧门禁无法发现死源码；实现删除 525 行文件，并只从 Desktop 根 package 的 Cargo.toml/lock dependency projection 移除 `time` 和 `uuid`。`cargo tree --depth 1` 确认两者不再是直接依赖；传递图仍需要的 crate 记录未从 lock 中误删。
- **GREEN：** compatibility-zero 定向 `2/2`，全部 quality `18/18`；Rust locked `24/24`、fmt、all-target strict Clippy 和 custom-protocol check 通过。governance、architecture、compatibility-zero、docs 与 release policy `16/16` 全部通过，diff check 干净。
- **真实 production build：** 根受管等价委派 `node scripts/go-admin-plus/run-script.mjs scripts/go-admin-plus/build.sh all server-sqlite` 退出 0，完成 Server、Admin Web `113` modules、`aarch64-apple-darwin` SQLite sidecar、Desktop WebView `116` modules和 Tauri 2 custom-protocol release host；原生链接 `53.25s`，最终报告 `DESKTOP_PRODUCTION_ASSETS_PASS` 与 `DESKTOP_PRODUCTION_BUILD_PASS`。
- **范围与 E2E disposition：** 活跃 product contract、运行行为、API、数据库、权限、页面与 UI/CSS 零变化；删除的是 Cargo module graph 外的旧文件及其两个无用直接 dependency edges。没有执行场景 E2E、签名、公证、安装或发布；G8、pending Ticket 与 change 状态不变。
