# T-20 Evidence: Windows x64 NSIS 发行切片

## 1. 激活与基线

- **Base：** `af22c2cf3d721937a4c1d8236776b47a309d4a8c`
- **Source branch/worktree：** `speculo/2026-08-26-project-architecture-reconstruction/T-20` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-20`
- **Owner：** implementation/integration 均为 `codex-root`
- **E2E disposition：** 本票只执行本机可证明的静态、policy、交叉构建、供应链和失败阻断 Gate；正式 Authenticode、原生安装/启动/重启/卸载场景延后到 T-21 后唯一最终 Windows 候选，且仍需受保护环境与生产凭据授权。

只读基线确认现有 workflow 和 release 资产仍构建已删除的 Wails/Go Desktop 入口，identity 为 `unsigned-self-use`，验证器明确要求 `NotSigned`，并保留旧 WebView2 payload、自用安装和旧路径合同。T20 必须在 Windows 专属路径内原子替换为 Tauri 2 x64 NSIS、production-only Authenticode、明确的数据保留卸载边界，以及 checksums/SBOM/provenance 失败阻断合同。

### T20-D01（Lead 批准）

T19 的发布态 sidecar 环境只按 architecture 选择 `darwin-arm64` / `darwin-amd64`，并硬编码 Unix 的 Go、Node 与 pnpm 路径。Windows x64 与 macOS x64 同为 `x86_64`，因此仅修改 Windows 打包脚本会得到能够安装、但完整 Generator 必然无法启动的伪候选。Lead 以“功能完整且不保留旧模式”的已授权目标批准精确开放 `<Path>go-admin-plus-ui/apps/admin-desktop/src-tauri/src/main.rs</Path>`：只增加 OS + architecture 映射、Windows `.exe` / `.cmd` 路径、包内 MinGit 和 `SYSTEMROOT/WINDIR/COMSPEC/TEMP/TMP` 白名单，以及对应 Rust 测试；不修改产品 API、proxy、vault、sidecar 输入/输出或生命周期。现有 macOS offline runtime 测试必须继续通过。

## 2. Source Checkpoint

- **Source commit：** `faebfeec3bdaad8c7c644b1bd93f3bee4ebc7fa2`
- **Source tree：** `a2f9c568304619d9a1f08ab7fa6dab3ccaa5d6a6`
- **固定审查范围：** `af22c2cf3d721937a4c1d8236776b47a309d4a8c..faebfeec3bdaad8c7c644b1bd93f3bee4ebc7fa2`
- **路径结果：** 21 个路径，全部位于 Ticket writable/shared paths；source worktree clean。
- **双轴复审：** 标准轴 pass、规范轴 pass，0 findings。

该 checkpoint 已原子替换旧 Wails/self-use Windows 发行资产：Tauri 2 x64 NSIS 使用 `com.goadmin.plus` identity、current-user 安装与 offline WebView2；workflow 固定 Go 1.26.5、Node 22.22.3、Rust 1.96.0、pnpm 11.1.3 及供应链工具版本，要求受保护环境内精确证书 thumbprint 的 Authenticode 和时间戳，并生成 checksums、SPDX SBOM、provenance 与安装证据。安装 tracer 覆盖登录、Demo CRUD、重启持久化、删除，以及卸载后保留 `%APPDATA%\\com.goadmin.plus\\go-admin-plus.db` 和 Credential Manager target `desktop-session-vault.com.goadmin.plus.stronghold` 的数据边界。

Windows packaged Generator 使用 `windows-amd64` 的 Go/Node/pnpm/MinGit，只继承 Windows 进程启动所需系统环境；安装后 compile gate 固定 `GOTOOLCHAIN=local`、`GOPROXY=off`、`GOSUMDB=off`、`GOENV=off`，并拒绝 skeleton 工作树漂移。`tauri-driver` 版本按实际 WebView2 Runtime 匹配且验证 Microsoft 签名，不再错误依赖 Edge 浏览器版本。

### Source Gate

- `go test ./internal/modules/generator ./cmd/desktop-sidecar -count=1`：pass。
- `go vet ./...`、`go mod verify`：pass。
- Windows x64 `CGO_ENABLED=0` sidecar 与 shared sidecar builder：pass，均产生真实 PE。
- frozen pnpm install 后 lint、typecheck、26 files / 132 tests、7 workspace boundary、Web build、Desktop build：pass。
- API client typecheck 与 7/7 tests：pass。
- Rust locked all-features 20/20 tests、strict clippy、release custom-protocol check：pass。
- OpenAPI contract lint、28/28 tests、generate-check：pass。
- governance 与固定 Go Task v3.45.4 `task:contract`：pass；Task 仅安装于临时目录，未写系统或项目工具链。
- Windows policy 3/3、policy verifier、Node syntax、PowerShell AST（official pwsh 7.6.5）、YAML、JSON、path/diff/clean：pass。

### 明确未宣称通过的边界

- macOS 上的 Windows Rust target 交叉检查停在 host 缺少 `llvm-rc`；该结果不记为 Windows native compile pass。受保护 Windows workflow 包含真实 target compile/test。
- 正式 Authenticode、NSIS 原生安装/启动/重启/卸载和完整业务 E2E 尚未运行；它们按统一 G8 决策保留到 T-21 后唯一最终 Windows 候选，且生产凭据与受保护 runner 仍需独立授权。
- parent-candidate integration 尚未执行，本节只证明 immutable source checkpoint。

## 3. Parent Candidate 与实现提升

- **Parent before：** `1d2cfd0075de565cd584d7a090e4cf48db41a29a`
- **Candidate/result：** `10aecc19dab8cb2a80c0fc8e16af6ecab2208d10`（tree `4ed4cbb977188ca09331ca191beb324250265122`）
- **Method/attempt：** final source 合入 frozen parent candidate；merge-commit attempt 1；`main` 以 `--ff-only` 提升
- **Retained locators：** source `speculo/2026-08-26-project-architecture-reconstruction/T-20` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-20`；candidate `speculo-candidate/2026-08-26-project-architecture-reconstruction/T-20-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/T-20-attempt-1`

attempt 1 从 clean checkout 通过 Go normal、SQLite-tag 与 full race（Generator 真实编译分别 356.512 秒、360.091 秒、483.661 秒）、vet、CGO-disabled build、Windows x64 PE sidecar 和 module verify；25-workspace frozen install、lint、完整 typecheck、26 files / 132 tests、7 boundary 以及 Web/Desktop build；Rust native sidecar-before-cargo、20 tests、all-target/all-feature strict clippy 和 locked release/custom-protocol check；合同 28 / 28、generate-check、API client 7 / 7、governance 与 `TASK_CONTRACT_PASS`；Windows policy 3 / 3、policy verifier、PowerShell AST、workflow YAML、JSON、Node syntax、21-path/diff/clean 与 Goal Plan 0-error Gate。

首轮验证中的执行环境问题均未被计为通过：三份 Go 套件最初并发运行时，普通和 SQLite Generator 的内嵌 pnpm 子进程在约 5 分钟被系统杀死；释放并发负载后，两套均从零串行重跑并通过，race 独立通过。Rust 首轮在 Windows sidecar builder 清理本机资源后按设计拒绝缺失的 `aarch64-apple-darwin` sidecar；重新按 native sidecar-before-cargo 顺序准备资源后，Rust 三段 Gate 全部从零通过。这些结果未修改源码或 candidate commit。

`main` 已精确包含 candidate/result 和最终 source checkpoint。T-20 实现可解除 T-21 的 Windows 实现依赖，但 Ticket 与 Map 保持 `in_progress` / `implemented-pending-final-e2e`；正式 Authenticode、原生签名 NSIS 安装、登录、Demo CRUD、Generator、重启持久化、卸载数据/凭据边界和 Windows trust 验证均未运行或声明通过，继续等待 G8 受保护最终候选和独立生产授权。
