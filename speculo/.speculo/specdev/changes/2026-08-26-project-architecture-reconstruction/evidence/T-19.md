# T-19 Evidence: macOS Universal DMG 发行切片

## 1. 激活与基线

- **Base：** `45e37678f695ef9c3b6333738b7cc5eb3f480e86`
- **Source branch/worktree：** `speculo/2026-08-26-project-architecture-reconstruction/T-19` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-19`
- **Owner：** implementation/integration 均为 `codex-root`
- **E2E disposition：** 本票只执行本机 unsigned Universal build、静态、policy、供应链和失败阻断 Gate；正式 Developer ID 签名、公证、安装/启动/重启场景延后到 T-21 后唯一最终 macOS 候选，且仍需受保护环境与生产凭据授权。

只读基线确认现有 workflow 和脚本仍构建已删除的 Wails ARM64 桌面端，旧 identity 为 `com.namewta.go-admin-plus`，与 Tauri 2 的 `com.goadmin.plus` 不一致，并提供不应继续存在的 ad-hoc/self-use 发布模式。现有 Tauri 2 App 与 Universal Rust target 已具备，但 sidecar builder 只输出单架构二进制。

### T19-D01（ticket，已批准）

T17 后的完整 Desktop sidecar 在启动时必须找到 tracked repository skeleton，Generator 的 render/compile gate 还必须解析 git、Go、Node 与 pnpm；Tauri 当前 `env_clear()` 后未恢复这些工具，也没有把发布资源设为工作目录。因此源码开发态 Generator 无法可靠运行，安装后的 `.app` 更会在 HTTP 启动前退出。

可选方案是删减 Desktop Generator、依赖用户预装工具，或交付自包含离线 runtime。前两项违反功能完整和可复现发行合同。用户以 `Q1A/Q2A` 批准第三项，并精确开放 `<Path>go-admin-plus-ui/apps/admin-desktop/src-tauri/src/main.rs</Path>`：开发态只白名单传递本机工具链环境；发布态只使用 App Resources 内的 tracked skeleton、离线 dependency store 和与宿主架构匹配的 Go/Node/pnpm。

后续只读审计确认 `<Path>go-admin-plus/internal/modules/generator/compile_gate.go</Path>` 会丢弃发布环境已设置的 `GOENV=off`、`GOTOOLCHAIN=local`、`GOPROXY=off` 与 `GOSUMDB=off`，使“离线”候选仍可读取用户 Go 配置或尝试网络。`T19-D01` 因此同时精确开放该文件及其既有测试，只允许强制保留上述四个固定 policy 值；公共 API、数据库、sidecar 生命周期和 Generator 完整 compile gate 均不改变。

## 2. Source Checkpoint

- **Source：** `6c97064f78719b6a9ceaa91d8a6fed5d92b5695d`
- **Tree：** `5ebcd54cd7ff0aa33d1f7c225065ee135a6a0a21`
- **范围：** 31 个变更路径，全部位于 Ticket writable/shared 合同；source worktree clean，base ancestry 通过。
- **双轴审查：** 标准轴 pass / 0 findings；规范轴 pass / 0 findings。最终固定输入为 `45e37678f695ef9c3b6333738b7cc5eb3f480e86...6c97064f78719b6a9ceaa91d8a6fed5d92b5695d`。

实现以 `com.goadmin.plus`、macOS 13、Universal `x86_64 + arm64` 和 production-only Developer ID/notary/hardened-runtime policy 替换旧 Wails ARM64/self-use 模式。Tauri 发布态只从规范化 App Resources 选择当前架构 Go/Node/pnpm，Generator 使用无 remote 的 tracked skeleton、共享只读 Go module cache、pnpm offline store，并强制 Go 子进程 `GOENV=off`、`GOTOOLCHAIN=local`、`GOPROXY=off`、`GOSUMDB=off`。pnpm 通过官方 `supportedArchitectures` 只保留 macOS x64/arm64 原生依赖；本地 unsigned App 从约 3.5 GB 的错误全平台候选收缩为约 2.2 GB。

本地 source Gate 通过：Generator 真实 compile gate（最终聚焦 Go 测试 366.435 秒）、Desktop sidecar、Desktop typecheck、Rust 19 tests、all-target/all-feature clippy、Universal Tauri host/sidecar build、固定 SHA-256 Go/Node/pnpm 下载、双架构离线 runtime 组装、App policy/architecture/resource 检查、workflow/YAML/JSON/plist/Shell parse 和 3/3 policy tests。最终 App 使用正常用户 Keychain 首次启动成功，Tauri host、sidecar、SQLite 与 Stronghold 全链路成立；诊断数据和 Keychain 条目已清理，数据目录保留在 `/private/tmp/go-admin-t19-cleanup.WwHbPu/com.goadmin.plus` 可恢复。

未运行且未声明通过：Developer ID 签名、App/DMG 公证与 staple、Gatekeeper quarantine 安装、正式 DMG、登录/Demo CRUD/Generator/重启统一 E2E、SBOM/provenance 真实产出、远端发布。以上按 Goal Plan 延后 T-21 后唯一受保护 macOS 系统候选；当前 workflow 与脚本只证明实现和失败阻断合同。

首次 parent-candidate `fc48f749c418235464ce2b441c8fe72131ca21b8` 从 clean checkout 运行 Rust Gate 时按预期硬失败：Tauri build script 找不到尚未构建的 `binaries/go-admin-sidecar-aarch64-apple-darwin`。该候选未提升，父分支保持不变，剩余长时 Go Gate停止。source 随后增加 workflow 的 host sidecar build，并由 policy test 强制其先于 cargo test；目标复现的 Rust 19 tests 与 clippy 均通过。attempt 2 必须从最新父状态和新 source checkpoint 完整重建、重跑。
