# T-19 Evidence: macOS Universal DMG 发行切片

## 1. 激活与基线

- **Base：** `45e37678f695ef9c3b6333738b7cc5eb3f480e86`
- **Source branch/worktree：** `speculo/2026-08-26-project-architecture-reconstruction/T-19` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-19`
- **Owner：** implementation/integration 均为 `codex-root`
- **E2E disposition：** 本票只执行本机 unsigned Universal build、静态、policy、供应链和失败阻断 Gate；正式 Developer ID 签名、公证、安装/启动/重启场景延后到 T-21 后唯一最终 macOS 候选，且仍需受保护环境与生产凭据授权。

只读基线确认现有 workflow 和脚本仍构建已删除的 Wails ARM64 桌面端，旧 identity 为 `com.namewta.go-admin-plus`，与 Tauri 2 的 `com.goadmin.plus` 不一致，并提供不应继续存在的 ad-hoc/self-use 发布模式。现有 Tauri 2 App 与 Universal Rust target 已具备，但 sidecar builder 只输出单架构二进制。

### T19-D01（ticket，已批准）

T17 后的完整 Desktop sidecar 在启动时必须找到 tracked repository skeleton，Generator 的 render/compile gate 还必须解析 git、Go、Node 与 pnpm；Tauri 当前 `env_clear()` 后未恢复这些工具，也没有把发布资源设为工作目录。因此源码开发态 Generator 无法可靠运行，安装后的 `.app` 更会在 HTTP 启动前退出。

可选方案是删减 Desktop Generator、依赖用户预装工具，或交付自包含离线 runtime。前两项违反功能完整和可复现发行合同。用户以 `Q1A/Q2A` 批准第三项，并精确开放 `<Path>go-admin-plus-ui/apps/admin-desktop/src-tauri/src/main.rs</Path>`：开发态只白名单传递本机工具链环境；发布态只使用 App Resources 内的 tracked skeleton、离线 dependency store 和与宿主架构匹配的 Go/Node/pnpm。该修正不改变公共 API、数据库、sidecar 生命周期或 Generator compile gate。
