# Evidence: T-13 - Files 本地存储安全垂直切片（源码审查完成）

- **Ticket：** `T-13`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-13` / `speculo/2026-08-26-project-architecture-reconstruction/T-13`
- **Base/source/tree：** `9b086f025dd31909edea086727e45c7f301c5ccf` / `fd43839c296047ed2e78cc9edb1b106b5b4129ca` / `ed8e335f6efc723f3df91a4a8e39e25a64747318`
- **状态：** source review；parent-candidate pending，E2E 按用户最新决定延后到最终统一系统候选

## 1. Source 实现与审查

源码实现双方言 Files metadata、`pending/ready/deleting` 对账状态、opaque id API、授权前置的流式 multipart、anchored/no-follow 本地存储、临时写入/fsync/原子发布、下载重授权、Browser transfer adapter、headless/Web 页面和 env-gated E2E harness。

独立审查退回并闭合两项语义漂移：Upload 现在用唯一 `readyAt` 同时更新数据库和响应投影，推进时钟回归证明响应与 List/DB 完全一致；前端文件名验证用 Unicode `Cc` 类别，与 Go `unicode.IsControl` 对齐，覆盖 U+0085 并证明非 `Cc` 的 U+2028 不被误拒。T13-D02 的 Browser adapter 依赖边界保持精确 allowlist，并保留额外内部依赖拒绝测试。

## 2. Source Verification

| Check | 结果 |
|---|---|
| Files normal/SQLite/full Go、focused race、vet、双 build、module verify | pass |
| disposable PostgreSQL dialect tests | pass |
| frozen pnpm install、17 项 Files tests、完整 `pnpm verify` | pass |
| workspace boundary | pass；7/7 |
| Lead 聚焦返修复核 | pass；`go test ./internal/modules/files/... -count=1`，domain Files 3/3 |
| source path audit 与 clean tree | pass；全部变更位于 T-13/T13-D01/T13-D02 授权路径 |
| browser/filesystem E2E | deferred；全部 Ticket 实现集成后由 Lead 在唯一最终系统候选统一执行 |

## 3. Remaining Gate

尚未创建 parent-candidate。真实 SQLite/PostgreSQL Web 与 Desktop profile 的上传/下载/重启、traversal、symlink、TOCTOU、中断和授权失败联合场景已登记到最终统一系统 E2E；当前仍须先完成全部非 E2E candidate Gate 和实现集成。Ticket、Map 保持 `in_progress`，不得把本 Evidence 当作集成或完成证明。
