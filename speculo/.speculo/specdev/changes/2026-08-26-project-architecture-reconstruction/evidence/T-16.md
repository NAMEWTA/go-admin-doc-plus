# Evidence: T-16 - Tauri 2 Desktop 安全宿主闭环（进行中）

- **Ticket：** `T-16`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-16` / `speculo/2026-08-26-project-architecture-reconstruction/T-16`
- **Base/latest source：** `ffc53bda0691fa5c64a7400bd17634e9dc28ea4d` / `f2d611133a302b912f43cb1d648943e1909762f0`
- **状态：** blocked；第 4 次 parent-candidate 静态门禁通过，required native E2E 因 macOS 锁屏环境阻塞，未提升

## 1. Source 事实

最终 source `f2d611133a302b912f43cb1d648943e1909762f0`（tree `8e93354e88ae5b1ebbbbdc373252523f509fa4e8`）clean。它实现 Tauri 2/Stronghold/单实例、随机 loopback Go sidecar、SQLite 迁移前备份、ready 后开窗、宿主代理 Session、Demo 原生 tracer、有界 shutdown，以及父宿主 stdin EOF 后 sidecar 自停。required native E2E 未在 source worktree 声明通过。

## 2. Source Verification

| Check | 结果 |
|---|---|
| Go desktop/cmd/tests、native tag、focused race | pass |
| Rust default/native feature tests | pass；16 / 17 tests |
| Rust all-features clippy | pass |
| 根 `pnpm verify` 与 Desktop Node runner tests | pass；12 files / 83 tests；runner 5 tests |
| release native/default host compile | pass；真实 release profile，约 116 秒 |
| 生产制品扫描 | pass；default release host、sidecar、dist 均无 native test-control |
| 父 stdin EOF 子进程测试 | pass；sidecar 在 10 秒界限内正常退出 |
| 最终资源检查 | pass；无 sidecar/host 进程，production/native-e2e Keychain identity 均不存在 |

## 3. 失败候选

| Attempt | Candidate | 结果与恢复 |
|---|---|---|
| 1 | `bbd9b2253de732919c6e5a62b731017df9bbb35f` | native runner 以全量 `ps` 发现 sidecar，输出超过 16 KiB；父分支未移动，改为受限精确 process query |
| 2 | `7fba62194a39f87608a4083181faf22ef3c92226` | migration-failure host 在 Stronghold 前等待 sidecar 导致 runner timeout；父分支未移动，调整 startup ordering 并补诊断 |
| 3 | `a142911174d22aebe91bb7aa391fd9e2260e7f57`（tree `87c202a4cf9a88e61f619a58a078085c675bfd00`） | 完整静态门禁通过；debug native host 90 秒内未出现登录窗口，runner 终止宿主后 sidecar 一度成为孤儿。Lead 精确回收后确认零进程/Keychain 残留；父分支未移动 |

三个失败 candidate/worktree 均按未授权 cleanup 合同保留。最新 source 改用 release native host，并以 parent stdin EOF 和 finally 精确 PID 回收同时闭合启动时延与异常退出残留；任何 recovered leak 仍判测试失败。

## 4. 第四次候选授权与恢复条件

用户最新 `Q1A/Q2A` 已明确批准 `T16-D03` 和第 4 次 candidate；Speculo config 与 Goal Plan 的 `max_integration_attempts` / `integration_attempt_limit` 已从 3 同步提升为 4。Lead 必须从包含 T11-D04 的当前父提交创建全新 parent-candidate，合并最终 source `f2d611133a302b912f43cb1d648943e1909762f0`，从头重跑静态门禁与 release native E2E；不得复用前三次失败候选的 pass 片段。验证通过前父分支保持未包含 T-16 source，远端、生产、签名、公证和 source cleanup 均不在授权范围。

## 5. 第四次候选执行结果

- **Parent before：** `ce21e84a432e5d882634399c4e607c94866442f9`
- **Source：** `f2d611133a302b912f43cb1d648943e1909762f0`
- **Candidate：** `004bd0d5b936018ffd95c01324423e7feaa7fb0d`（tree `36c068722c898aa0e8ad2d31d65dcf58fccaa3d6`）
- **Retained locator：** `speculo/integration/2026-08-26-project-architecture-reconstruction/T-16-attempt-3` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-16-attempt-3`

候选 clean merge，69 个变更路径全部属于 T-16 已授权范围。frozen pnpm install、根 `pnpm verify`、Go 普通/SQLite/native-tag、focused race/vet/CGO=0 build、Rust default/native-feature tests、all-target/all-feature clippy、default/native release build、governance、Task contract 与 SpecDev validator 均通过；Task contract 精确返回 `TASK_CONTRACT_PASS`，validator 为 0 error / 0 warning。初次 Rust test 在 sidecar 尚未按既定构建顺序生成时报告 externalBin 缺失，执行正式 sidecar builder 后从头通过，不构成产品失败。

required native runner 已构建并启动真实 release Tauri host 与 SQLite sidecar，但在等待登录窗口时返回 `native login window timed out`。独立诊断确认宿主和 sidecar 可持续运行，macOS `System Events` 的 UI automation 已启用，但窗口计数始终为 0；同期截图证明交互式 macOS 会话停留在锁屏界面。因此该结果是图形会话环境阻塞，不是源码门禁通过，也不能声明 native E2E 通过。

父分支未移动且不包含 T-16 source。诊断 host/sidecar 已停止，native-e2e 测试钥匙串项已删除，生产钥匙串项不存在，default sidecar/dist/release host 已恢复构建；进程、临时 Keychain identity 与运行器临时目录均零残留。诊断目录、锁屏截图和空 runner root 已可恢复地移入 `/Users/wta/.Trash/`。

恢复条件唯一为解锁当前 macOS 交互会话。解锁后先把最新的纯 SpecDev 状态提交合入这个 retained attempt-4 candidate，再从 required native E2E 开始复跑并复核 clean/resource scan；这仍属于同一第 4 次候选，不创建第 5 次候选。通过前 `promotion_status=failed`、Ticket 保持 blocked。
