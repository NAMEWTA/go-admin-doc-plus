# Evidence: T-16 - Tauri 2 Desktop 安全宿主闭环（进行中）

- **Ticket：** `T-16`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-16` / `speculo/2026-08-26-project-architecture-reconstruction/T-16`
- **Base/latest source：** `ffc53bda0691fa5c64a7400bd17634e9dc28ea4d` / `fdfa4afbe342edbd5b6feb00a8699780dd04caf4`
- **状态：** implementation integrated；第 4 次 parent-candidate 全部非 E2E Gate 通过并提升，native E2E 延后到最终统一系统候选

## 1. Source 事实

最终 source `fdfa4afbe342edbd5b6feb00a8699780dd04caf4`（tree `792f1dc446648f32469054e974e75aa9bab28c1c`）clean。它实现 Tauri 2/Stronghold/单实例、随机 loopback Go sidecar、SQLite 迁移前备份、ready 后开窗、宿主代理 Session、Demo 原生 tracer、有界 shutdown，以及父宿主 stdin EOF 后 sidecar 自停。第四次候选诊断后，同一 source 修复链继续闭合 release `custom-protocol` 资产、阶段安全诊断、有界 Accessibility 批量查询、预期宿主退出竞态和 macOS 先显示窗口再激活应用的顺序。native E2E 不在 source worktree 声明通过，并按用户最新决定延后到最终统一系统候选。

## 2. Source Verification

| Check | 结果 |
|---|---|
| Go desktop/cmd/tests、native tag、focused race | pass |
| Rust default/native feature tests | pass；latest native all-targets 17 / 17 tests |
| Rust all-features clippy | pass |
| 根 `pnpm verify` 与 Desktop Node runner tests | pass；既有完整 Gate 通过，latest runner 6 / 6 tests |
| release native/default host compile | pass；真实 release profile，约 116 秒 |
| 生产制品扫描 | pass；default release host、sidecar、dist 均无 native test-control |
| 父 stdin EOF 子进程测试 | pass；sidecar 在 10 秒界限内正常退出 |
| 最终资源检查 | pass；无 sidecar/host 进程，production/native-e2e Keychain identity 均不存在 |

## 3. 失败候选

| Attempt | Candidate | 结果与恢复 |
|---|---|---|
| 1 | `bbd9b2253de732919c6e5a62b731017df9bbb35f` | native runner 以全量 `ps` 发现 sidecar，输出超过 16 KiB；父分支未移动，改为受限精确 process query |
| 2 | `7fba62194a39f87608a4083181faf22ef3c92226` | migration-failure host 在 Stronghold 前等待 sidecar 导致 runner timeout；父分支未移动，调整 startup ordering 并补诊断 |
| 3 | `a142911174d22aebe91bb7aa391fd9e2260e7f57`（tree `87c202a4cf9a88e61f619a58a078085c675bfd00`） | 完整静态门禁通过；debug native host 90 秒内未出现登录窗口，runner 终止宿主后 sidecar 一度成为孤儿。Lead 精确回收后确认零进程/Keychain 残留；父分支未移动 |

三个失败 candidate/worktree 均按未授权 cleanup 合同保留。最新 source 改用 release native host，并以 parent stdin EOF 和 finally 精确 PID 回收同时闭合启动时延与异常退出残留；任何 recovered leak 仍判测试失败。

## 4. 第四次候选授权与当前验收口径

用户 `Q1A/Q2A` 已明确批准 `T16-D03` 和第 4 次 candidate；Speculo config 与 Goal Plan 的 `max_integration_attempts` / `integration_attempt_limit` 已从 3 同步提升为 4。后续用户又明确决定全部未完成 Ticket 先实现集成、最后只运行一次统一 E2E。因此 Lead 继续使用同一 retained 第 4 次 candidate，合并最终 source `fdfa4afbe342edbd5b6feb00a8699780dd04caf4` 与最新父状态，从头重跑全部非 E2E 门禁；通过后可作为实现 result 提升并解除 T-17 实现依赖，但 T-16 保持 `implemented-pending-final-e2e`。不得复用前三次失败候选的 pass 片段；远端、生产、签名、公证和 source cleanup 均不在授权范围。

## 5. 第四次候选历史诊断

- **Parent before：** `ce21e84a432e5d882634399c4e607c94866442f9`
- **Source：** `f2d611133a302b912f43cb1d648943e1909762f0`
- **Candidate：** `004bd0d5b936018ffd95c01324423e7feaa7fb0d`（tree `36c068722c898aa0e8ad2d31d65dcf58fccaa3d6`）
- **Retained locator：** `speculo/integration/2026-08-26-project-architecture-reconstruction/T-16-attempt-3` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-16-attempt-3`

候选 clean merge，69 个变更路径全部属于 T-16 已授权范围。frozen pnpm install、根 `pnpm verify`、Go 普通/SQLite/native-tag、focused race/vet/CGO=0 build、Rust default/native-feature tests、all-target/all-feature clippy、default/native release build、governance、Task contract 与 SpecDev validator 均通过；Task contract 精确返回 `TASK_CONTRACT_PASS`，validator 为 0 error / 0 warning。初次 Rust test 在 sidecar 尚未按既定构建顺序生成时报告 externalBin 缺失，执行正式 sidecar builder 后从头通过，不构成产品失败。

历史 native runner 已构建并启动真实 release Tauri host 与 SQLite sidecar，但在等待登录窗口时返回 `native login window timed out`。独立诊断确认当时交互式 macOS 会话停留在锁屏界面；诊断过程还暴露并推动闭合 release asset、Accessibility 查询、退出竞态和窗口显示顺序问题。该历史运行不构成 native E2E 通过；按最新统一验收口径，锁屏不再阻塞当前实现集成。

父分支未移动且不包含 T-16 source。诊断 host/sidecar 已停止，native-e2e 测试钥匙串项已删除，生产钥匙串项不存在，default sidecar/dist/release host 已恢复构建；进程、临时 Keychain identity 与运行器临时目录均零残留。诊断目录、锁屏截图和空 runner root 已可恢复地移入 `/Users/wta/.Trash/`。

## 6. 最终实现候选与提升

- **Parent before：** `ce21e84a432e5d882634399c4e607c94866442f9`
- **Source：** `fdfa4afbe342edbd5b6feb00a8699780dd04caf4`（tree `792f1dc446648f32469054e974e75aa9bab28c1c`）
- **Candidate/result：** `cd50bfdbd24517c8b3e8f3565d75fae8c3f6a22a`（tree `901fdbb2d967164178da02faabba7e1811af6caf`）
- **Method/attempt：** fast-forward promotion / retained attempt 4；未创建第 5 次候选
- **Parent containment：** `main` 已快进到同一 result SHA

最终候选从头通过 Go 普通与 SQLite 全量、三个 Desktop focused race、`desktop_native_e2e` / `desktop_e2e` build-tag tests、vet、`CGO_ENABLED=0` build、module verify；frozen pnpm install、lint、typecheck、12 files / 83 tests、Desktop runner 6 / 6 unit tests、Workspace boundary 6 / 6 与 Web/Desktop production build；Rust fmt、default 16 / 16、native-feature 17 / 17、all-target/all-feature clippy、custom-protocol check 和 locked release build。无 `custom-protocol` 的 release check 按设计被 compile guard 拒绝。

OpenAPI lint、28 / 28 contract tests、generate check、API client typecheck 与 7 / 7 tests、governance、固定临时 Go Task v3.45.4 的 `TASK_CONTRACT_PASS`、SpecDev validator `0 error / 0 warning`、diff/path/clean-tree 均通过。正式 sidecar、WebView dist 和 release host 均不含 native test control；host/sidecar 进程、测试与生产 Keychain identity 零残留。临时 Task 二进制已可恢复地移入 `/Users/wta/.Trash/go-admin-task-tool.nShdHI`。

T-16 实现结果已集成并可解除 T-17 的实现依赖，但 Ticket 保持 `in_progress` / `implemented-pending-final-e2e`。真实 native tracer 不在本候选运行，将在全部 T-01 至 T-21 实现集成后的唯一最终系统候选统一执行。
