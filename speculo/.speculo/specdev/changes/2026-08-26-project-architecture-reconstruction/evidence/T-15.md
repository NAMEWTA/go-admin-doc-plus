# Evidence: T-15 - Generator 新架构代码生成闭环（进行中）

- **Ticket：** `T-15`
- **Lead / implementation owner：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-15` / `speculo/2026-08-26-project-architecture-reconstruction/T-15`
- **Base/source/tree：** `c832ca4a4c1b087aaee9fcc2f9e671d7d7e487d3` / `8458ceac6a5ce05a1eec811374f60b2f33a83729` / `b582cbedbb73260e119b11dc27b22d113c1f3a43`
- **状态：** implementation integrated；parent-candidate 全部非 E2E Gate 通过并提升，浏览器 E2E 按用户最新决定延后到最终统一系统候选

## 1. Source 实现与审查

源码实现当前 profile 授权 schema/table metadata、稳定配置模型、确定性 preview/render、受控原子 writer、生成后 compile gate、OpenAPI/Go/domain/web-domain 模板、Generator API、Web wizard 和 env-gated E2E harness。输出根、标识符、模板和覆盖目标均 fail closed，preview 与 write 复用同一规范化模型。

独立审查发现 `Config`/`Preview` 可在只有 preview permission 时直接读取 schema/table/column metadata。最终 source 要求两条入口同时具备 `generator.metadata.read` 与 `generator.preview`，并以权限矩阵回归证明任一缺失均拒绝；write 继续要求独立 write 权限和确认边界。

## 2. Source Verification

| Check | 结果 |
|---|---|
| Generator Go package 全量测试 | pass；包含真实 render/generate/compile gate，约 246 秒 |
| metadata + preview permission focused test | pass |
| gofmt、source path audit 与 clean tree | pass；全部变更位于 T-15/T15-D01 授权路径 |
| dual-profile generation/browser E2E | 真实 render/generate/compile 属于非 E2E Gate 并已在 source 通过；跨 profile Web wizard 延后到最终统一系统候选 |

## 3. Parent Candidate 与集成修正

- **Parent before：** `b01540bf21d42cdf49270f50b39ca7d253f7b198`
- **Source：** `8458ceac6a5ce05a1eec811374f60b2f33a83729`（tree `b582cbedbb73260e119b11dc27b22d113c1f3a43`）
- **Candidate/result：** `b60d1bc33d702fdaac0cef7a28dad27582e5b975`（tree `38fa0f96eefd74bcbd5ec4e71e7df979d923d7cd`）
- **Candidate locator：** `speculo/integration/2026-08-26-project-architecture-reconstruction/T-15` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-15`
- **Method/attempt：** merge candidate + fast-forward promotion / attempt 1
- **Parent containment：** `main` 已快进到同一 result SHA

Candidate preflight 证明两个 Generator package 虽已有真实依赖与 package-local checks，但根 `verify`、shell Vitest 与 lock importer 尚未接入。经 `T15-D02` 串行授权后，只增加 Generator 聚合 typecheck、两个 Generator spec include 和两个既有 importer 的 workspace/catalog 依赖。

根 typecheck 随即发现 Generator 与 Demo 重复声明 Vite raw module 类型；Generator 已通过 `vite/client` 获得规范类型，因此删除冗余声明。真实 compile gate 的首轮稳定运行又证明测试错误地用同一个 5 分钟 parent context 连续包裹 fixture 验证和第二次完整生产编译；测试现给生产 gate 独立 deadline，产品 compile gate 自身的 10 分钟上限保持不变。

## 4. Candidate Verification

| Check | 结果 |
|---|---|
| pnpm frozen/root verify/build | pass；15 个 test files / 90 tests，Desktop runner 6 / 6 unit、boundary 6 / 6，Web/Desktop production build |
| Generator Go/SQLite full | pass；真实 render/generate/compile 约 316 秒 |
| Generator focused race | pass；核心包约 275 秒 |
| Go vet、CGO=0 cmd build、module verify | pass |
| OpenAPI/API client | lint pass；contract 28 / 28；generate check pass；API client 7 / 7 |
| governance/Task contract | `GOVERNANCE_CHECK_PASS`；固定 Go Task v3.45.4 精确返回 `TASK_CONTRACT_PASS` |
| SpecDev/path/clean | Goal Plan validator 0 error / 1 serialization warning；45 个变更路径全部属于 T-15/T15-D01/T15-D02；clean tree |

唯一 SpecDev warning 是 T-15/T-16 的历史 frontmatter 仍共享已串行移交的根路径；Goal Plan、Map 与两个 deviation 已明确 T-16 先完成、再由 T15-D02 接管，实际候选不存在并发写入。临时 Task 工具与首次落到 Go 版本目录的新建二进制均已可恢复地移入 `/Users/wta/.Trash/go-admin-task-tool.QA10fH` 和 `/Users/wta/.Trash/go-admin-task-default.QA10fH`。

## 5. Remaining Gate

T-15 实现结果已集成并可解除 T-17 的 T-15 实现依赖，但 Ticket、Map 保持 `in_progress` / `implemented-pending-final-e2e`。Web wizard、SQLite/PostgreSQL metadata 联合路径和最终产品 composition 不在本候选运行，将在全部 T-01 至 T-21 实现集成后的唯一最终系统候选统一执行。

## 2. Lead Dispatch And Candidate Return

Lead 已接收 Generator source checkpoint、权限审查与 candidate/result，并在最终 G8 候选执行双 profile browser、generate、compile 和 runtime 验证。

## 3. 修改范围与路径所有权

修改限于 Generator 合同、模块、模板、Domain/Web Domain、测试及获批根验证接入；无越权修改其他业务模块。

## 4. 验收与合同映射

`AC-019`、`AC-023`、`AC-028` 与 `AC-035` 由 golden、真实生成编译、架构门禁和 G8 双 profile 结果闭合。

## 5. Workspace Verification

source-worktree 只执行并通过非 E2E golden、权限、真实生成编译、Go/pnpm/合同/构建门禁；E2E not-run 于 source-worktree，最终结果来自 G8 candidate。

## 6. 双轴审查

标准轴与规范轴闭合 metadata read permission、Vite raw type、deadline 和根验证覆盖问题；完成态零未关闭 finding。

## 7. Integration Verification

candidate/result `b60d1bc33d702fdaac0cef7a28dad27582e5b975` 及后续修复进入 `main`，最终 Generator 全链路通过。

## 8. 偏差与决策

T15-D02 等共享接入偏差均获批并串行；无未批准 deviation 或 blocker。

## 9. 残余风险与交付定位

个人自用完成范围内无 required 残余门禁；生成器耗时与工具链事实保留于详细 Evidence。
