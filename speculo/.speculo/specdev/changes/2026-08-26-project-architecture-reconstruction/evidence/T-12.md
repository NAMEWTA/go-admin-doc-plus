# Evidence: T-12 - Scheduler 受控任务垂直切片（进行中）

- **Ticket：** `T-12`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-12` / `speculo/2026-08-26-project-architecture-reconstruction/T-12`
- **Base/source/tree：** `9b086f025dd31909edea086727e45c7f301c5ccf` / `a9b8a568b17934db78ea2d89d258cf5dfa065fad` / `1c4f9613027324cfd7c216f77bbf3db78a4c5d1f`
- **状态：** implementation integrated；parent-candidate 全部非 E2E Gate 通过并提升，E2E 按用户最新决定延后到最终统一系统候选

## 1. Source 实现与审查

源码实现双方言 Scheduler schema、编译期 typed registry、结构化 UTC schedule、定义/执行记录 API、Web 页面和 env-gated E2E harness。executor 只消费 T-08 注入的全局 Lease，handler 数据库副作用位于 lease transaction/savepoint 内，外部效果通过同事务 Outbox 表达。

独立审查退回并闭合三项问题：timeout 后 handler 返回 `nil` 不再被误记成功；下一次触发从实际完成时间计算，避免长任务补跑过期 backlog；软删除名称唯一性改为仅约束 active row，删除后可重建同名定义。最终 timeout 回滚并记录稳定 `task_timeout` 失败，停止/失锁后不产生新副作用。Go context 无法强制终止完全忽略取消的任意函数，因此 registry 只允许受控、遵守 context 的内部 handler；不声称进程级强杀。

## 2. Source Verification

| Check | 结果 |
|---|---|
| Scheduler focused/full Go、race、vet | pass |
| SQLite 与 disposable PostgreSQL 17 dialect tests | pass |
| contract generate/check | pass |
| Scheduler TypeScript/Vitest、oxlint、workspace boundary、Vite build | pass |
| source path audit 与 clean tree | pass；全部变更位于 T-12/T12-D01 授权路径 |
| multi-instance/browser E2E | deferred；全部 Ticket 实现集成后由 Lead 在唯一最终系统候选统一执行 |

## 3. Parent Candidate 与根接入

- **Parent before：** `3d8bc072241c7e76b4f9baf6793f26fdd5d30896`
- **Source：** `a9b8a568b17934db78ea2d89d258cf5dfa065fad`（tree `1c4f9613027324cfd7c216f77bbf3db78a4c5d1f`）
- **Candidate/result：** `d9e7d5aa5fd973e76a6d3c5b0a8648db04df7b71`（tree `fe2415cac6f73942f4507ed50e24da8fb13907c8`）
- **Candidate locator：** `speculo/integration/2026-08-26-project-architecture-reconstruction/T-12` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-12`
- **Method/attempt：** merge candidate + fast-forward promotion / attempt 1
- **Parent containment：** `main` 已快进到同一 result SHA

`T12-D02` 仅把两个 Scheduler package 接入根 typecheck、shell Vitest 和两个既有 lock importer。候选 merge 无冲突，未修改 Outbox、coordination 或产品 composition，也未发现新的产品修正需求。

## 4. Candidate Verification

| Check | 结果 |
|---|---|
| pnpm frozen/root verify/build | pass；21 个 test files / 111 tests，Desktop runner 6 / 6 unit、boundary 6 / 6，Web/Desktop production build |
| Go normal/SQLite full | pass；Generator 真实闭环分别约 371 秒 / 363 秒 |
| Scheduler focused race | pass；module 与 browser harness package |
| Go vet、CGO=0 cmd build、module verify | pass |
| OpenAPI/API client | lint pass；contract 28 / 28；generate check pass；API client 7 / 7 |
| governance/Task contract | `GOVERNANCE_CHECK_PASS`；固定 Go Task v3.45.4 精确返回 `TASK_CONTRACT_PASS` |
| SpecDev/path/clean | Goal Plan validator 0 error / 6 serialization warnings；44 个路径全部属于 T-12/T12-D01/T12-D02；diff-check 与 clean tree pass |

六个 warning 来自 T-10/T-12/T-15/T-16 历史 frontmatter 的共享根路径两两组合；Goal Plan/Map 已记录实际串行顺序，不存在并发写入。临时 Task 工具已可恢复地移入 `/Users/wta/.Trash/go-admin-task-tool.JLQ5xc`。

## 5. Remaining Gate

T-12 实现结果已集成并可解除 T-17 的 T-12 实现依赖，但 Ticket、Map 保持 `in_progress` / `implemented-pending-final-e2e`。SQLite/PostgreSQL 多实例唯一执行、lease-loss rollback/takeover、停止线性化、Outbox coupling 与真实 browser records 将在全部 T-01 至 T-21 实现集成后的唯一最终系统候选统一执行。
