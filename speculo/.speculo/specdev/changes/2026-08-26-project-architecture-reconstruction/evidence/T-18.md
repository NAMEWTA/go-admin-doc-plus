# T-18 Evidence: Linux OCI 与 Compose 发行切片

## 1. 激活与基线

- **Base：** `479fcc9190d38d14a185847d132f41b972c830a0`
- **Source branch/worktree：** `speculo/2026-08-26-project-architecture-reconstruction/T-18` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-18`
- **Owner：** implementation/integration 均为 `codex-root`
- **E2E disposition：** 本票仅执行静态、构建、policy 与供应链非 E2E Gate；真实双架构 OCI/Compose 场景延后到 T-21 后的唯一最终 Linux 候选。

只读基线确认现有资产仍使用 Redis、JWT settings 模板、旧 `server` / `migrate` 命令、旧 `go-admin-ui-plus` build context 和固定 `linux/amd64`。本机没有 Docker/buildx/Compose，因此不能把真实容器结果记录为通过；source 必须提供可在 Docker-capable Linux runner 直接执行的确定性验证入口、失败阻断和供应链产物合同。

## 2. Source Checkpoint

- **Source：** `e815b16ae8ac7ca73a72f99200ef004ad9b187e4`（tree `32ea206047c01335be5c887c352f47fef661d685`）
- **Diff：** 25 个路径；全部位于 `deploy/compose/**`、`release/linux/**`、`scripts/release/linux/**` 或 `.github/workflows/release-linux.yml`
- **删除：** 明文 settings 模板和 release 目录下旧配置/验证脚本；脚本权威入口迁至 `scripts/release/linux/**`

新 Compose 以 `postgres` / `sqlite` 显式 profile 运行同一 product binary，只暴露 Web listener；PostgreSQL 密码与 DSN 只由文件 secret reference 注入，SQLite 保持单实例 named volume。Server/Web 均使用固定多架构 base digest、非 root 用户、只读 rootfs、drop-all capability 与内部 backend 网络。正式 Server 启动执行 forward-only migrations；失败 fixture 通过缺失只读发布骨架证明启动失败不能 ready。

Generator 的 transport render 先于 compile gate 调用 Node/Go 工具，因此 OCI Server 不能只携带单一二进制。发布镜像内置无 remote 的只读 tracked skeleton、frozen workspace install、预取 pnpm store 与 Go module cache；compile fixture 仍在有界 tmpfs 中执行，产品数据只写 profile volume。该闭合完全位于 T-18 release 资产，不修改 T-17 产品代码或模块合同。

本地通过：policy tests `3 / 3`、`GO_ADMIN_LINUX_RELEASE_POLICY_PASS`、YAML/JSON/Shell parse、secret fixture 两次运行幂等且文件 mode `0600` / stdout 无 64 位 secret、Server `linux/amd64` 与 `linux/arm64` CGO-disabled 交叉构建、聚焦 Go tests、Web production build。registry manifest 还确认固定 Go、Node、Nginx 与 PostgreSQL 四个 base digest 均同时包含 `linux/amd64` 和 `linux/arm64`。

本机没有 Docker/buildx/Compose/syft，故 source 未运行镜像 build、Compose migration/health/persistence/restart、SBOM 和 artifact checksum。Workflow 已提供两架构 x 两 profile 矩阵、隔离 failure project、SPDX JSON、image ID、provenance 与 SHA256SUMS 的失败阻断入口，以上真实平台场景只在最终统一 Linux candidate 执行。

## 3. Parent Candidate 与实现提升

- **Parent before：** `39f2efe4ce8701165e07a86adb3322c8673459c1`
- **Candidate/result：** `37f989c7c64afb17c05bdeb35818c34416e753ca`（tree `855de1252161a78f933d9df98787815d55008646`）
- **Method/attempt：** source cherry-pick 到 frozen parent candidate；attempt 1；`main` 以 `--ff-only` 提升
- **Retained locators：** `speculo/2026-08-26-project-architecture-reconstruction/T-18` / `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-18`；`speculo-candidate/2026-08-26-project-architecture-reconstruction/T-18-attempt-1` / `specdev-candidate/2026-08-26-project-architecture-reconstruction/T-18-attempt-1`

候选从 frozen parent 通过完整 Go normal（Generator 414.8 秒）、full race（Generator 350.9 秒）、vet、CGO-disabled build 与 module verify；25-workspace frozen install、lint、完整 typecheck、26 files / 132 tests、7 boundary 和 Web/Desktop build；Rust 14 tests、all-target/all-feature clippy 与 check；合同 28 / 28、generate-check、API client 7 / 7、governance、`TASK_CONTRACT_PASS`、Linux policy 3 / 3、YAML/JSON/Shell、25-path/clean-tree 与 SpecDev validator。validator 为 0 error；10 个 warning 均为既有历史 ownership 投影。

首轮把 Rust clippy 与前端 build 并行启动时，Tauri 宏在 Desktop dist 尚未生成的瞬间按设计失败。该并行编排结果作废；前端 build 完成后从 Rust test 起按正式顺序重跑 test -> clippy -> check，整套通过。没有修改 candidate，也没有把失败前后的片段拼成通过结果。

`main` 已精确包含 candidate/result。T-18 实现可解除 T-21 的 Linux 实现依赖，但 Ticket 与 Map 保持 `in_progress` / `implemented-pending-final-e2e`；真实双架构 OCI、PostgreSQL/SQLite migration/health/persistence/restart、SBOM 与 checksum 仍只能在 Docker-capable Linux 最终候选执行，当前没有远端发布行为。
