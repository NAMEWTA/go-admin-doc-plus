# Evidence: T-03 - 应用内核、类型化 Profile 与可观测启动链

- **Change:** `2026-08-26-project-architecture-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/ticket/03-runtime-profiles-observability.md</Path>`
- **Lead / implementation owner:** `codex-root` / `codex-t03-runtime`
- **Base/source/candidate/result:** `27a186cbda7e77c403b3c64074260a10e924ee92` / `3801d776001b00d9d2a63e7343ca2f319986848a` / `cfae1e71cec1d123bc6ce0fca1d8f92938db78f2` / `cfae1e71cec1d123bc6ce0fca1d8f92938db78f2`
- **Candidate tree:** `55a3d581d5498838bd49184b6dd910d11bb49df2`
- **Parent before:** `b9faa150433eb533d9c4db35b98fc54cc2272001`
- **Status:** done

## 1. 实现摘要

三种正式 profile 使用独立 JSON Schema 与不可变配置快照，按 `defaults < file < env < non-secret CLI` 合成；PostgreSQL secret 只接受 env/`_FILE`，Desktop 路径、随机端口和一次性 token 只接受 Tauri launch material。Kernel 提供确定的 start/ready/drain/stop/failed 状态机和逆序清理；observability 分离 live、ready、metrics、capabilities 与 status，并统一 `no-store`。

## 2. Lead Dispatch And Candidate Return

Lead 将 T-03 从固定 base 派发给 `codex-t03-runtime`，授权 source worktree 实现、非 E2E 验证和非空提交；required process E2E、candidate integration 与 parent update 由 Lead 保留。implementation owner 返回初始 checkpoint `48a97ff77f0f07501fe7492a6fa4aaee59255f0c`，标准轴 findings 修正后返回最终 clean checkpoint `3801d776001b00d9d2a63e7343ca2f319986848a`。

## 3. 修改范围与路径所有权

12 个 source 文件均在 T-03 writable paths：`<Path>go-admin-plus/internal/app/kernel/**</Path>`、`<Path>go-admin-plus/internal/platform/config/**</Path>`、`<Path>go-admin-plus/internal/platform/observability/**</Path>`、`<Path>go-admin-plus/config/schema/**</Path>` 与 `<Path>go-admin-plus/cmd/config-check/**</Path>`。`go.mod/go.sum`、Taskfile、公共合同及旧宿主均未修改；无未声明路径、生成物或 Ticket 偏差。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-005 | 未知字段、profile 冲突、缺失/冲突/不可读 secret、非法值负向测试；JSON、fmt、GoString 与 JSON slog 遍历敏感值；真实 `config-check` 进程负向探针 | pass |
| AC-026 | starting/ready/draining/failed/stopped 状态矩阵；五端点 method gate、`no-store`、dependency failure 脱敏；真实 TCP `httptest.Server` | pass |
| AC-027 | 三 schema、precedence、env/`_FILE`、Desktop Tauri material、不可变 snapshot | pass |
| Path/integration | base/source/merge parents/tree/ancestor、clean worktree、无冲突 | pass |

## 5. Workspace Verification

source-worktree 运行 Go 普通/SQLite/race/vet/build、mod verify、聚焦并发测试与路径审计并保持 clean；不得且未声明 required E2E 通过。Lead 在 parent-candidate/current-workspace 重新运行完整 Go 门禁、OpenAPI lint、23 个合同测试、generation clean check、governance、SpecDev validator 与 required process E2E。

| Check | Result |
|---|---|
| `go test ./...`、`go test -tags sqlite3 ./...` | pass |
| `go test -race ./...` | pass |
| `go vet ./...`、`go vet -tags sqlite3 ./...` | pass |
| `go build ./...`、`go build -tags sqlite3 ./...`、`go mod verify` | pass |
| OpenAPI lint、23 contract tests、deterministic `generate --check` | pass |
| governance check、SpecDev validator、`git diff --check`、clean status | pass |

Required process E2E 在 parent-candidate 执行。Lead 构建真实 `config-check` 进程，以含 sentinel DSN、非法 log level 和目标 `127.0.0.1:45991` 启动：进程非零退出，端口前后均未监听，诊断包含字段规则且 stdout/stderr 不含 DSN、用户或 sentinel；有效 Server SQLite profile 返回 `{"profile":"server-sqlite","status":"valid"}`。独立 race test 进程通过真实 TCP server 验证 lifecycle/status 转换、dependency failure 脱敏和并发生命周期终态。

## 6. 双轴审查

规范轴初审与修正后复审均为 pass。标准轴首轮对 `27a186c..48a97ff` 报告 1 high、2 medium：结构化 JSON/slog 可遍历公开输入中的 secret；非 GET 在 405 前执行 probe；并发安全合同没有并发测试。原 owner 修正全部 finding 后，标准轴对 `27a186c..3801d77` 复审 pass、0 findings。

修正加入安全 `MarshalJSON`/`slog.LogValuer`，在 snapshot/probe 前统一 method gate，并以 barrier/race 覆盖并发 Start、Drain、DependencyFailed、Snapshot、重复 Start、取消与 cleanup error；同时修复 Start callback 后 context 已取消仍进入 ready 的问题。

## 7. Integration Verification

Lead 从 `main@b9faa150433eb533d9c4db35b98fc54cc2272001` 创建 `speculo/integration/2026-08-26-project-architecture-reconstruction/T-03`，以 merge commit 集成 source，无冲突。候选 `cfae1e71cec1d123bc6ce0fca1d8f92938db78f2` 全门禁通过后，`main` 通过 `--ff-only` 提升到同一 result；source commit 是 result 的 ancestor。candidate 临时 worktree/branch 可清理，source worktree/branch按授权保留。

## 8. 偏差与决策

无 Ticket/Spec 偏差。用户 `Q1A/Q2A` 已授权 worktree、implementation commit、Lead-owned local candidate integration 与 parent update；未执行远端、生产、source cleanup 或 destructive schema 动作。

## 9. 残余风险与交付定位

T-03 不接入旧宿主；新 kernel/config/observability 的产品装配归 T-17，数据库连接归 T-04，Tauri 窗口与 sidecar 归 T-16。旧全局配置只读保留到 T-21 原子收缩，不构成兼容合同。source commit 为 result ancestor；candidate 临时 worktree/branch 在 Evidence 提交后清理，source worktree/branch按授权保留。

## 10. 验收投影修正

最终完成审计确认 T-03 frontmatter 已为 `done`，本 Evidence 第 4 至第 7 节也已用不可变 result `cfae1e71cec1d123bc6ce0fca1d8f92938db78f2` 证明 `AC-005`、`AC-026`、`AC-027`、路径、集成和验证矩阵全部通过，但 Ticket 第 10 节的六个验收框仍保留为未勾选的陈旧投影。本轮只把这些框同步为已完成，不修改产品源码、原验收含义、result SHA 或 E2E disposition。
