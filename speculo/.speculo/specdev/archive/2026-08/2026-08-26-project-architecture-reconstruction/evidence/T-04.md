# Evidence: T-04 - 双方言持久化与模块迁移基座

- **Change:** `2026-08-26-project-architecture-reconstruction`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/ticket/04-dual-dialect-persistence-migrations.md</Path>`
- **Lead / implementation owner:** `codex-root` / `codex-t04-database`
- **Base/source/candidate/result:** `fe56a1932d8b9cec8db5f5dd7e24df33ab10853f` / `5f359381b7a3db09a5bd64e147ce4b22b80b0cda` / `8757554047a828e12712a1faf2d547412bcdf05b` / `8757554047a828e12712a1faf2d547412bcdf05b`
- **Candidate tree:** `ada5ca6d19d02441b33dfcbc8e4c0535ee236ae9`
- **Parent before:** `00cbb1063cdd7ada8f54b798d1c2ec73240dced0`
- **Status:** done

## 1. 实现摘要

建立 Bun + `database/sql` 的唯一进程 Database、pgx PostgreSQL 与 CGo-free modernc SQLite adapter、事务 Port、Goose 双方言 Provider/Runner、全局版本唯一与 forward-only/transactional migration policy、PostgreSQL session advisory migration lock、Server/Desktop SQLite 文件锁，以及禁用/缺省缓存仍回源的最小合同。SQLite URI 对 POSIX、Windows drive 与空 authority UNC 路径进行确定编码，并通过 DSN `_pragma` 为每个物理连接设置外键、busy timeout 与 WAL。

## 2. Lead Dispatch And Candidate Return

Lead 从 `main@fe56a1932d8b9cec8db5f5dd7e24df33ab10853f` 派发 T-04 给 `codex-t04-database`，source owner 只执行非 E2E 检查。owner 初始返回 `a841a5b97b37236883a9e5f1311784b77ab4eab1`；两轮 findings 修正依次形成 `69b5ba1b1c091cf6ce887cb96f9abc9ffc10a15a` 与最终 clean source `5f359381b7a3db09a5bd64e147ce4b22b80b0cda`。真实 PostgreSQL、跨进程 SQLite、candidate integration 和父分支更新由 Lead 保留。

## 3. 修改范围与路径所有权

15 个最终 source 文件均在 `<Path>database/fixtures/previous-greenfield/**</Path>`、`<Path>go-admin-plus/internal/platform/{database,migrations,cache}/**</Path>` 与 T-04 独占的 `<Path>go-admin-plus/go.mod</Path>`/`<Path>go-admin-plus/go.sum</Path>`。固定依赖包含 Bun/dialects `1.2.18`、Goose `3.27.3`、pgx `5.10.0`、flock `0.13.0`、modernc SQLite `1.54.0`；Goose 的解析要求使 modernc 从旧间接版本升级，撤回的 modernc libc 版本未进入依赖图。未修改 kernel/config、Taskfile、业务模块或旧数据库实现。

## 4. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-003 | 隔离 PostgreSQL 18.3 空库、上一 greenfield、重复迁移；两个独立 pgx pool/Runner 并发且 Applied 总和为 1 | pass |
| AC-004 | Server SQLite 与 Desktop SQLite 真实文件空库/重复迁移；真实子进程第二实例拒绝，holder 退出后恢复 | pass |
| AC-024 | 双方言列集合与读写结果等价；上一 fixture 只应用 v2；Down、NO TRANSACTION、ENVSUB、Up 前 SQL、重复版本全部 fail-closed | pass |
| AC-034 | nil/Disabled cache 每次从 loader 取得同一事实值；无 Redis 依赖或连接 | pass |
| Security/runtime | DSN/path 的 String、GoString、JSON、slog 与错误脱敏；取消/超时保留 `errors.Is`；Windows/UNC/POSIX URI 特殊字符 | pass |

## 5. Workspace Verification

source-worktree 运行目标/full Go 普通与 SQLite tag、race、双 vet/build、CGo-free test/build、module verify、路径与禁用项扫描，保持 clean；真实数据库 E2E 保持 pending。Lead 在固定 candidate `8757554047a828e12712a1faf2d547412bcdf05b` 重新运行全部回归和 required E2E。

| Check | Result |
|---|---|
| `go test ./...`、`go test -tags sqlite3 ./...`、`go test -race ./...` | pass |
| `go vet`/`go build` 普通与 SQLite tag | pass |
| `CGO_ENABLED=0` platform tests/full build、`go mod verify` | pass；`all modules verified` |
| Windows/Linux amd64 CGo-free database+migrations test compile | pass |
| `pnpm install --frozen-lockfile && pnpm verify` | pass；lint/type、22 tests、6 boundary、build |
| governance、contract `generate --check`、SpecDev goal-plan/self-check | pass；validator 均 0 error / 0 warning |
| `git diff --check`、ancestor/tree/status | pass；source 与 parent-before 均为 candidate parents，tree clean |

本机仍无 Go Task 可执行文件，未伪造 `task` wrapper 通过；实际执行其底层受管脚本与 package commands。`implement` 终态 validator 会要求未来 T-06 至 T-21 records，当前中间态按合同使用 goal-plan stage。

## 6. 双轴审查

首轮 `fe56a193..a841a5b`：标准轴 3 high + 1 medium，规范轴重复一个 high并新增一个 medium，涉及 PostgreSQL migration lock、SQLite connection-local PRAGMA、Goose annotation parser 等价性、context sentinel 和无界 Memory。第二轮 `fe56a193..69b5ba1` 确认前述闭合，但发现 Windows drive/UNC URI high、Up 前 SQL与 ENVSUB 两项 medium。最终 `fe56a193..5f359381` 使用空 authority `file:///C:/...`/`file:////server/share/...`、保留 POSIX 反斜杠、实际 modernc Ping及 deterministic annotation policy 后，标准轴与规范轴均 pass、0 findings。

## 7. Integration Verification

merge commit `8757554047a828e12712a1faf2d547412bcdf05b` 有两个父提交：`00cbb1063cdd7ada8f54b798d1c2ec73240dced0` 与 source `5f359381b7a3db09a5bd64e147ce4b22b80b0cda`，无冲突。Lead 创建 candidate worktree 后错误地在根 workspace 执行了 merge，导致 `main` 先指向该 commit、candidate 仍停在 parent；在旧 parent 上启动的门禁立即终止且全部作废。Lead 冻结 `main`，将独立 candidate 快进到同一不可变 SHA/tree，再从零完成全量门禁、跨平台编译和 required E2E；未以提前推进状态替代 candidate 证据。

Required E2E 使用忽略目录中的一次性 Go module：所有 PostgreSQL 18.3 binary/runtime/data/cache 位于独立临时目录，完成后进程停止且目录删除；三 profile 空库/幂等/上一 greenfield、两个独立 PostgreSQL pool advisory lock、SQLite 子进程锁冲突/释放恢复和 cache-disabled 全部 pass。该 harness、依赖与跨编译产物不进入 result。

## 8. 偏差与决策

无产品、Spec、schema 或路径偏差。候选操作顺序错误是 Lead execution incident，不改变实现或验收合同；通过丢弃旧父门禁、冻结父分支并在独立 candidate 同 SHA/tree 全量重跑闭合，且在本 Evidence 明示。用户授权仅覆盖本地 implementation/candidate/parent update；未执行生产迁移、外部数据库访问、远端、发布或 source cleanup。

## 9. 残余风险与交付定位

T-04 交付平台接缝与 previous-greenfield fixture，不包含业务表；业务 migration 由 T-06 至 T-15 owner 提供，T-08 在独占 `localcache/outbox/coordination` 路径实现有界缓存与可靠执行，T-17 组合全产品迁移。source branch/worktree按未授权 cleanup 合同保留；candidate transient worktree/branch在 Evidence 提交后清理。
