# Evidence: T-09 - Organization 部门与岗位垂直切片

- **Ticket：** `T-09`
- **Lead：** `codex-root`
- **Source workspace/branch：** `specdev-worktree/2026-08-26-project-architecture-reconstruction/T-09` / `speculo/2026-08-26-project-architecture-reconstruction/T-09`
- **Parent before/source/candidate/result：** `5064f44e72cc44efa84fe6d5d3ec2768be3926c5` / `42df51eff0d21617ecf2cf8075fc06723cf29a08` / `b71fb04b3fa2f87b4d21c7529c589b8c79d9223a` / `b71fb04b3fa2f87b4d21c7529c589b8c79d9223a`
- **状态：** done

## 1. 实现摘要

新增 Organization 独占的部门树与岗位 schema、repository、service、严格 OpenAPI transport、IAM consumer projection Port、Module Capability 声明、headless TypeScript domain、Vue 管理页面及双方言 browser harness。部门与岗位支持查询、字面量搜索、确定性排序、创建、编辑和删除；循环父子关系、重复键、stale revision、被引用删除、权限撤销与 Session revoke 均稳定失败且不破坏状态。

授权 adapter 与业务写入共享同一数据库和事务语义；未知数据范围 fail closed，`self` scope 不显示全局组织投影。双方言统一使用 Unicode code-point 长度、编码后的字面量搜索键及显式 binary/C 排序，PostgreSQL 测试以 URL `search_path` 进入动态隔离 schema 并验证 `current_schema()`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex-t09-organization`
- **最终 source base：** `c832ca4a4c1b087aaee9fcc2f9e671d7d7e487d3`
- **最终返回：** clean source `42df51eff0d21617ecf2cf8075fc06723cf29a08`（tree `d835504067a91fe2598166a51dd45259e4eb247b`）。
- **Lead 核对：** 44 个实现路径全部位于 Ticket writable paths 或 `T09-D01/T09-D02` 精确开放范围；根 package 只增加 Organization checks，Vitest 只增加 Organization includes，lockfile 只增加两个 Organization importers。required E2E 只以 Lead-owned parent-candidate 结果记账。

## 3. 修改范围与路径所有权

| 路径 | 所有权与用途 |
|---|---|
| `<Path>contracts/openapi/modules/organization.yaml</Path>` | T-09；Organization OpenAPI 3.1 公共合同 |
| `<Path>go-admin-plus/internal/modules/organization/**</Path>` | T-09；双方言迁移、领域、持久化、授权 adapter 与 HTTP transport |
| `<Path>go-admin-plus/test/organization/**</Path>` | T-09；双方言、隔离与 browser host harness |
| `<Path>go-admin-plus-ui/packages/{domains,web-domains}/organization/**</Path>` | T-09；headless domain、Web client/controller 与 Vue 页面 |
| `<Path>go-admin-plus-ui/tests/e2e/organization/**</Path>` | T-09；SQLite/PostgreSQL 真实浏览器 runner |
| IAM consumer Port、Organization manifests/importers/root checks | `T09-D01/T09-D02`；最小 Port、直接依赖和标准门禁接入 |

未修改 IAM 私有 schema/capability registry、共享 UI、Database API、产品 composition、其他模块 importer 或外部依赖版本。

## 4. 验收与合同映射

| 合同 | 证据 | 结果 |
|---|---|---|
| AC-014 | 双方言部门/岗位 CRUD、循环与引用冲突、revision、最终授权 | pass |
| AC-035 | 搜索、表单、删除确认、reset、乱序响应与写后投影 | pass |

## 5. Workspace Verification

source-worktree 只运行非 E2E 检查，不得且未声明 required E2E 通过。完整门禁和真实浏览器/数据库证据均由 Lead 在固定 parent-candidate 执行。

| Check | 结果 |
|---|---|
| Go 普通、`-tags sqlite3`、全量 race、双 tag vet/build、`CGO_ENABLED=0` build、module verify | pass |
| PostgreSQL Organization dialect contract | pass；真实 PostgreSQL 17.11、动态隔离 schema、`current_schema()` 与零残留 |
| 25-package frozen install 与 `pnpm verify` | pass；11 files / 77 tests、6 workspace boundary、typecheck/lint/build |
| 合同 suite、lint、generate check | pass；28/28、`CONTRACT_LINT_PASS`、`CONTRACT_GENERATE_CHECK_PASS` |
| governance、Task contract、SpecDev、diff/clean-tree | pass；`GOVERNANCE_CHECK_PASS`、`TASK_CONTRACT_PASS`、0 error / 0 warning |
| Chromium 双 profile required E2E | pass；精确输出 `ORGANIZATION_E2E_PASS profiles=sqlite,postgres` |
| 资源与隔离 | pass；PostgreSQL `public` 表数 0、`t09_*` schema 数 0、E2E temp 零残留、端口与进程零残留 |

本机未预装 Go Task；Lead 将固定 `v3.45.4` 构建到一次性目录执行受跟踪 `task:contract`，通过后把临时二进制移入废纸篓。PostgreSQL 17.11 同样使用一次性 data/socket，停止后将 data 目录移入废纸篓；未把环境准备或空测试选择器计为产品通过证据。

## 6. 双轴审查

- **最终固定 source/tree：** `42df51eff0d21617ecf2cf8075fc06723cf29a08` / `d835504067a91fe2598166a51dd45259e4eb247b`。
- **标准轴：** pass，0 findings。
- **规范轴：** pass，0 findings。
- **修正历史：** stage-1 checkpoint 因 projection request fence、同 DB/dialect 授权、self visibility、Unicode 语义与 PostgreSQL schema 实证不足而作废。最终实现消费 T-14 的 Module Capability Registry，闭合同事务最终授权、fail-closed scope、Unicode 字面量搜索、确定性排序、动态 capability 投影与完整负向回归。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `5064f44e72cc44efa84fe6d5d3ec2768be3926c5` |
| Source SHA | `42df51eff0d21617ecf2cf8075fc06723cf29a08` |
| Candidate branch/workspace | `speculo/integration/2026-08-26-project-architecture-reconstruction/T-09` / `specdev-worktree/.integration/2026-08-26-project-architecture-reconstruction/T-09` |
| Method/conflicts/attempt | merge-commit；无冲突；attempt 1 |
| Candidate/result/tree | `b71fb04b3fa2f87b4d21c7529c589b8c79d9223a` / `b71fb04b3fa2f87b4d21c7529c589b8c79d9223a` / `3612509143c3d5bcdbf0dde1ff6d9148c8aec399` |
| E2E disposition/result | required / pass |
| Parent promotion | 从精确 frozen parent 以 `--ff-only` 提升；提升前核对 parent/source/candidate ancestry、clean tree 与隔离资源 |

候选建立时 Lead 首次把 merge 命令错误落到根 worktree；因操作前根目录已确认为 clean，立即把 `main` 精确恢复到 frozen parent，随后在独立 candidate 重新合并并从 frozen install 起重跑全部 Gate。误提交未作为 candidate/result/evidence，最终代码 tree 与正式候选不受影响。

## 8. 偏差与决策

`T09-D01` 只开放两个 Organization manifests 与 IAM consumer-defined projection Port；`T09-D02` 只开放 Organization 根 checks、Vitest includes 和两个 lock importers。其余实现位于原 writable paths，不改变 Spec/ADR 产品决策。用户已授权 worktree、implementation commit、Lead local candidate integration 与 parent update；未操作远端、生产、发布或 source cleanup。

## 9. 残余风险与交付定位

- T-17 负责把 Organization capability、Port adapter 与页面显式注入最终产品 composition；本 Ticket 不越权修改全局注册。
- source 与 candidate worktree 按未授权 cleanup 合同保留。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-26-project-architecture-reconstruction/evidence/T-09.md</Path>`
